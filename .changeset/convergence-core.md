---
"@internet-privacy/marmot-ts": minor
---

Add the deterministic convergence selection core (ConvergencePolicy + default, branch scoring, rollback-horizon eligibility, canonical branch selection, and the commit_digest same-epoch ordering key) ported from the darkmatter convergence spec.
