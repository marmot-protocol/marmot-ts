---
"@internet-privacy/marmots": minor
---

- Add `isLastResort` option to `generateKeyPackage()` for controlling last_resort extension and export `getKeyPackageRefFromKeyPackageEvent()` utility
- clean up relay handling, and code