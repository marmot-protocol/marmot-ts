---
phase: 07-account-identity-proof-component-0x8009-legacy-clean-cut
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/components/ids.ts
  - src/core/components/account-identity-proof.ts
  - src/core/components/index.ts
  - src/core/components/dictionary.ts
  - src/core/components/integrity.ts
  - src/core/components/__tests__/account-identity-proof.test.ts
  - src/core/components/__tests__/integrity.test.ts
  - src/__tests__/exports.test.ts
autonomous: true
requirements: [PROOF-02, PROOF-04, PROOF-05, PROOF-06, CUT-02]

must_haves:
  truths:
    - "produceAccountIdentityProof, fed the spec vector inputs (BIP-340 secret key 3, all-zero aux randomness, created_at 1700000000, ciphersuite 0x0001, mls_signature_key 000102..1f), returns exactly the 104-byte component f9308a01...bbdfb5d, and accountIdentityProofTemplate reproduces event id b7e9a15dd85990fb0f49c33db3cc9875f73986207b038404ceb6b7fec4e0af6b and the spec's canonical NIP-01 serialization byte-for-byte (PROOF-02, D-03)"
    - "accountIdentityProofTemplate emits kind 450, exactly five tags in the order d, component, ciphersuite, signature_scheme, mls_signature_key with 0x-prefixed four-digit lowercase hex values, and the fixed content string, and all hashing, signing, and verification go through the Phase 6 src/core/authorization-proof.ts primitive, never a re-implementation (PROOF-02, D-12)"
    - "validateLeafAccountIdentityProof(leaf, ciphersuite) throws AccountIdentityProofError with reason missing-support when 0x8009 data exists but the support list omits 0x8009, missing-data when the list names 0x8009 but no entry exists, duplicate-data for two 0x8009 entries, and legacy-extension-present for a leaf carrying 0xf2f1 alone or together with 0x8009 (PROOF-04, CUT-02, D-13, D-14)"
    - "The same validator rejects a non-basic or non-point credential (invalid-credential), a proof signer that differs from BasicCredential.identity (identity-mismatch), an unknown ciphersuite (ciphersuite-mismatch), a signature key whose length is invalid for the ciphersuite's scheme (signature-key-mismatch), and a bad signature, substituted signature key, substituted ciphersuite, or malformed envelope (invalid-proof, with the Phase 6 AuthorizationProofError reachable as cause) (PROOF-04, D-13, D-16)"
    - "Every validator takes the applicable ciphersuite explicitly: validateKeyPackageAccountIdentityProof uses keyPackage.cipherSuite, optionally asserts equality with a caller-supplied group ciphersuite, and validateGroupMemberAccountIdentityProofs takes the group ciphersuite as a parameter (D-16)"
    - "A 0x8009 dictionary entry in KeyPackage-level extensions is rejected with invalid-location (PROOF-05)"
    - "0x8009 data as GroupContext state is rejected by makeAppComponentsExtension (UsageError), classifyGroupAccountIdentityProofProfile (invalid-location), and validateAppComponentIntegrity (component-integrity violation for any AppDataUpdate op targeting 0x8009 or a resulting 0x8009 entry); a SafeAAD entry listing 0x8009 is invalid-location; a GroupInfo extension list carrying 0x8009 is invalid-location through assertNoAccountIdentityProofComponent (PROOF-06, D-08)"
    - "classifyGroupAccountIdentityProofProfile returns current, legacy, mixed, or neither; assertCurrentGroupAccountIdentityProofProfile throws legacy-group, mixed-profile, or missing-requirement for everything except current (CUT-02, D-07)"
    - "Documented gap, not silently dropped: ts-mls exposes no AppEphemeral proposal surface and no production path reads Marmot component data from GroupInfo, so ROADMAP success criterion 3's GroupInfo/AppEphemeral clauses are covered only by the pure GroupInfo guard and recorded as a gap in 07-01-SUMMARY.md (D-08)"
    - "The 13 new runtime symbols reach the package root through src/core/components/index.ts, the exports snapshot gains exactly those 13 lines, no name collides with a legacy export, and src/core/account-identity-proof.ts is byte-unchanged (D-12)"
  artifacts:
    - path: "src/core/components/account-identity-proof.ts"
      provides: "marmot.member.account-identity-proof.v2 proof class: template, producer, leaf/KeyPackage/tree validators, GroupContext profile classifier, container location guard, typed AccountIdentityProofError"
      exports:
        [
          "AccountIdentityProofError",
          "AccountIdentityProofRejectReason",
          "AccountIdentityProofProfile",
          "AccountIdentityProofLocation",
          "ProduceAccountIdentityProofParams",
          "mlsSignatureSchemeForCiphersuite",
          "accountIdentityProofTemplate",
          "produceAccountIdentityProof",
          "hasAccountIdentityProofMaterial",
          "validateLeafAccountIdentityProof",
          "validateKeyPackageAccountIdentityProof",
          "validateGroupMemberAccountIdentityProofs",
          "classifyGroupAccountIdentityProofProfile",
          "assertCurrentGroupAccountIdentityProofProfile",
          "assertNoAccountIdentityProofComponent",
        ]
      contains: "export class AccountIdentityProofError extends Error"
    - path: "src/core/components/ids.ts"
      provides: "ACCOUNT_IDENTITY_PROOF_COMPONENT_ID (0x8009) and ACCOUNT_IDENTITY_PROOF_COMPONENT name"
      contains: "ACCOUNT_IDENTITY_PROOF_COMPONENT_ID: AppComponentId = 0x8009"
    - path: "src/core/components/__tests__/account-identity-proof.test.ts"
      provides: "Spec-vector test through the real class builder plus one negative test per reject reason and container"
      contains: "b7e9a15dd85990fb0f49c33db3cc9875f73986207b038404ceb6b7fec4e0af6b"
    - path: "src/core/components/integrity.ts"
      provides: "validateAppComponentIntegrity rejects 0x8009 as GroupContext state"
      contains: "ACCOUNT_IDENTITY_PROOF_COMPONENT_ID"
  key_links:
    - from: "src/core/components/account-identity-proof.ts"
      to: "src/core/authorization-proof.ts"
      via: "produceAuthorizationProof, encodeAuthorizationProof, decodeAuthorizationProof, verifyAuthorizationProof over the class template"
      pattern: "(produce|verify|decode|encode)AuthorizationProof\\("
    - from: "src/core/components/account-identity-proof.ts"
      to: "src/core/binary.ts"
      via: "BinaryReader.vector/opaque raw dictionary parse that counts duplicate 0x8009 entries"
      pattern: "\\.vector\\("
    - from: "src/core/components/dictionary.ts"
      to: "src/core/components/ids.ts"
      via: "makeAppComponentsExtension rejects ACCOUNT_IDENTITY_PROOF_COMPONENT_ID entries"
      pattern: "ACCOUNT_IDENTITY_PROOF_COMPONENT_ID"
    - from: "src/core/components/index.ts"
      to: "src/core/components/account-identity-proof.ts"
      via: "barrel export star"
      pattern: "account-identity-proof\\.js"
---

<objective>
Build the `marmot.member.account-identity-proof.v2` proof class (component `0x8009`) as a new, purely additive
`src/core/components/account-identity-proof.ts` on top of the Phase 6 `MarmotAuthorizationProof` primitive (D-12). The
module supplies the fixed kind-450 template and producer (PROOF-02, D-03), throwing leaf / KeyPackage / whole-tree
validators with a typed `AccountIdentityProofError` (PROOF-04, PROOF-05, D-13, D-14, D-16), the GroupContext profile
classifier (CUT-02, D-07), and container guards for GroupContext, AppDataUpdate, SafeAAD, KeyPackage-level extensions,
and GroupInfo extension lists (PROOF-06, D-08).

Purpose: every later plan in this phase wires these exact entry points. Nothing in this plan changes what marmot-ts
emits on the wire or which seams validate. The legacy `0xf2f1` module stays in place and unedited until plan 07-07.

Output: one new module, one new test file, two component-id constants, one builder guard, one integrity rule, a barrel
line, and 13 new lines in the exports snapshot.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-account-identity-proof-component-0x8009-legacy-clean-cut/07-CONTEXT.md
@.planning/phases/07-account-identity-proof-component-0x8009-legacy-clean-cut/07-RESEARCH.md
@.planning/phases/07-account-identity-proof-component-0x8009-legacy-clean-cut/07-PATTERNS.md
@refs/marmot/app-components/account-identity-proof-v2.md
@refs/marmot/foundation/authorization-proofs.md
@src/core/authorization-proof.ts
@src/core/components/dictionary.ts
@src/core/components/ids.ts

Planning-time facts verified on 2026-09-14 (do not re-derive):

- Reference submodules: `refs/marmot` at `4a2bc65`, `refs/mdk` at `17496e98` (bumped in commit `86fdd2b`). No new upstream commit touches proof code.
- `ts-mls` `getAppDataDictionary(extensions)` throws `ValidationError` when entries are unsorted or repeat a componentId, so a leaf with two `0x8009` entries cannot be read through it. Count entries with a raw parse: `app_data_dictionary` extensionData is an MLS vector of `{ uint16 componentId; opaque data<V> }`, which `new BinaryReader(bytes).vector((r) => ({ componentId: r.uint16(), data: r.opaque() }))` followed by `end()` reads (`src/core/binary.ts` BinaryReader: `uint16`, `opaque`, `vector`, `end`). `BinaryWriter.vector` / `opaque` produce the same byte layout, which is how tests build a duplicate-entry fixture.
- `decodeComponentsList` (`src/core/components/app-components-list.ts`) throws a plain `Error` on duplicate ids and a `BinaryDecodeError` on truncation; `encodeComponentsList` sorts and de-duplicates.
- `ts-mls` `GroupInfoTBS` has `extensions: GroupInfoExtension[]`; no code in `src/` reads Marmot component data from a GroupInfo. `grep -rn "AppEphemeral" ts-mls/src src` returns nothing.
- `ts-mls` signature public keys: Ed25519 32 bytes, Ed448 57 bytes, and ECDSA keys from noble `getPublicKey` default to compressed SEC1 (P-256 33, P-384 49, P-521 67 bytes), while MLS/OpenMLS use uncompressed SEC1 (65, 97, 133). Accept both ECDSA lengths.
- MDK `validate_leaf_account_identity_proof` accepts a legacy-only leaf; marmot-ts must reject it (clean cut, Pitfall 10). MDK `protocol_profile_of_group_extensions` rejects `0x8009` dictionary data in a GroupContext before classifying.
- `applesauce-core` `getEventHash` computes the NIP-01 id; Phase 6 already reproduced the spec vector with `schnorr.sign(hexToBytes(id), sk3, zeroAux)`.
- Planner decision on Phase 6 review warnings WR-01..WR-03: deferred, not fixed in Phase 7. This class builds its template only through `accountIdentityProofTemplate` (integer kind 450, ASCII string tags), so WR-01/WR-02 are unreachable through it, and the class validators convert every envelope-primitive throw into `invalid-proof`, so WR-03's untyped escapes cannot leak through them. Phase 8, which maps `AuthorizationProofError.reason` directly, should re-evaluate them.
- `pnpm exec tsc -p tsconfig.json --noEmit` (includes tests) exits 0 on the current tree.
</context>

## Artifacts this phase produces

New file `src/core/components/account-identity-proof.ts` (module tag `@module @category Core - App Components`),
re-exported from `src/core/components/index.ts` and therefore from `src/core/index.ts` and the package root:

| Symbol | Kind | Contract |
| --- | --- | --- |
| `AccountIdentityProofRejectReason` | type | literal union of the 14 reasons below |
| `AccountIdentityProofError` | class (runtime) | `extends Error`; `name = "AccountIdentityProofError"`; `readonly reason`; constructor `(message, reason, options?: ErrorOptions)` forwards `options` to `super` so `cause` is set |
| `AccountIdentityProofProfile` | type | `"current" \| "legacy" \| "mixed" \| "neither"` |
| `AccountIdentityProofLocation` | type | `"group-context" \| "key-package" \| "group-info"` |
| `ProduceAccountIdentityProofParams` | interface | `{ signer: AuthorizationProofSigner; accountIdentity: Uint8Array; mlsSignatureKey: Uint8Array; ciphersuite: number; createdAt?: number }` |
| `mlsSignatureSchemeForCiphersuite(ciphersuite: number): number` | function (runtime) | ciphersuite 1..7 to MLS signature scheme code point; otherwise throws `ciphersuite-mismatch` |
| `accountIdentityProofTemplate(ciphersuite: number, mlsSignatureKey: Uint8Array): AuthorizationProofTemplate` | function (runtime) | the exact kind-450 template; fresh tag arrays per call |
| `produceAccountIdentityProof(params): Promise<Uint8Array>` | function (runtime) | 104-byte encoded component; `createdAt` defaults to wall clock via the primitive |
| `hasAccountIdentityProofMaterial(leaf: LeafNode): boolean` | function (runtime) | true iff the leaf carries a `0x8009` entry, a legacy `0xf2f1` extension, or an undecodable dictionary |
| `validateLeafAccountIdentityProof(leaf: LeafNode, ciphersuite: number): void` | function (runtime) | throws on any spec validation failure |
| `validateKeyPackageAccountIdentityProof(keyPackage: KeyPackage, expectedCiphersuite?: number): void` | function (runtime) | KeyPackage-level location checks, then leaf validation with `keyPackage.cipherSuite` |
| `validateGroupMemberAccountIdentityProofs(state: ClientState, ciphersuite: number): void` | function (runtime) | validates every member leaf; rethrows with the member position, same reason, original as `cause` |
| `classifyGroupAccountIdentityProofProfile(extensions: GroupContextExtension[]): AccountIdentityProofProfile` | function (runtime) | MDK-shaped four-way classification; throws only for `0x8009` data or undecodable dictionaries |
| `assertCurrentGroupAccountIdentityProofProfile(extensions: GroupContextExtension[]): void` | function (runtime) | throws `legacy-group` / `mixed-profile` / `missing-requirement` |
| `assertNoAccountIdentityProofComponent(extensions: readonly { extensionType: number }[], location: AccountIdentityProofLocation): void` | function (runtime) | throws `invalid-location` when any `app_data_dictionary` extension in the list carries `0x8009` |

Reject reasons (kebab-case): `invalid-credential`, `legacy-extension-present`, `invalid-dictionary`, `duplicate-data`,
`missing-support`, `missing-data`, `invalid-location`, `ciphersuite-mismatch`, `signature-key-mismatch`,
`identity-mismatch`, `invalid-proof`, `legacy-group`, `mixed-profile`, `missing-requirement`. The nine D-13 minimum
reasons are all present; `invalid-credential`, `invalid-dictionary`, and the three profile reasons are the additions
D-13 allows. No reason literal exists for GroupInfo or AppEphemeral specifically (RESEARCH Open Question 1).

New constants in `src/core/components/ids.ts`: `ACCOUNT_IDENTITY_PROOF_COMPONENT_ID = 0x8009`,
`ACCOUNT_IDENTITY_PROOF_COMPONENT = "marmot.member.account-identity-proof.v2"`. `SUPPORTED_APP_COMPONENT_IDS` and
`DEFAULT_GROUP_COMPONENT_IDS` are NOT changed here (plan 07-06, D-05/D-15).

Runtime symbols added to the exports snapshot (13): `ACCOUNT_IDENTITY_PROOF_COMPONENT`,
`ACCOUNT_IDENTITY_PROOF_COMPONENT_ID`, `AccountIdentityProofError`, `accountIdentityProofTemplate`,
`assertCurrentGroupAccountIdentityProofProfile`, `assertNoAccountIdentityProofComponent`,
`classifyGroupAccountIdentityProofProfile`, `hasAccountIdentityProofMaterial`, `mlsSignatureSchemeForCiphersuite`,
`produceAccountIdentityProof`, `validateGroupMemberAccountIdentityProofs`, `validateKeyPackageAccountIdentityProof`,
`validateLeafAccountIdentityProof`.

## Source Coverage Audit (this plan)

| Source | ID | Item | Status |
| --- | --- | --- | --- |
| REQ | PROOF-02 | Kind-450 event byte-exact to the spec signing vector through the real class builder | COVERED (Task 1) |
| REQ | PROOF-04 | Missing support/data, signer/ciphersuite/scheme/signature-key mismatch, bad signature rejected | COVERED (Task 2; wired at seams in 07-06) |
| REQ | PROOF-05 | `0x8009` in KeyPackage-level extensions rejected | COVERED (Task 2; wired in 07-06) |
| REQ | PROOF-06 | GroupContext / SafeAAD / GroupInfo rejection; AppEphemeral documented gap | COVERED with documented gap (Tasks 2, 3) |
| REQ | CUT-02 | Legacy and mixed leaves, legacy/mixed/neither groups rejected (pure helpers) | COVERED (Task 2; wired in 07-06) |
| CONTEXT | D-03, D-07, D-08, D-12, D-13, D-14, D-16 | Cited per task | COVERED |
| RESEARCH | Pattern 1, Pattern 2, Pitfalls 1, 3, 4 (research), Pitfalls 7, 8, 9, 10 (PITFALLS.md) | Template over primitive, explicit ciphersuite, raw duplicate count, leaf-only guard | COVERED |
| RESEARCH | Don't hand-roll | Reuse Phase 6 primitive; re-home the verified scheme table verbatim | COVERED (Task 1) |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Component constants, typed error, kind-450 template and producer reproducing the spec vector (PROOF-02)</name>
  <files>src/core/components/ids.ts, src/core/components/account-identity-proof.ts, src/core/components/__tests__/account-identity-proof.test.ts</files>
  <read_first>
    - refs/marmot/app-components/account-identity-proof-v2.md (Registry, Signing event, Production and reuse, Signing test vector)
    - src/core/authorization-proof.ts (AuthorizationProofTemplate, AuthorizationProofSigner, produceAuthorizationProof, encodeAuthorizationProof, authorizationProofEventId, AuthorizationProofError: the only envelope logic this module may use)
    - src/core/__tests__/authorization-proof.test.ts lines 1-110 (vector constants, signWith helper with ZERO_AUX, expectReason helper pattern)
    - src/core/account-identity-proof.ts lines 69-94 (the verified ciphersuite-to-scheme table to re-home; read only, do not edit or import this file)
    - src/core/components/ids.ts (constant row style and JSDoc)
    - refs/mdk/crates/cgka-engine/src/account_identity_proof.rs lines 82-170 (current-profile proof event) and 829-869 (adopted signing vector test)
    - .planning/research/PITFALLS.md lines 14-51 (Pitfall 1: decimal-tag regression)
  </read_first>
  <behavior>
    - ACCOUNT_IDENTITY_PROOF_COMPONENT_ID === 0x8009 and ACCOUNT_IDENTITY_PROOF_COMPONENT === "marmot.member.account-identity-proof.v2"
    - mlsSignatureSchemeForCiphersuite returns 0x0807, 0x0403, 0x0807, 0x0808, 0x0603, 0x0808, 0x0503 for ciphersuites 1..7; ciphersuite 0 and 0x00ff throw AccountIdentityProofError with reason ciphersuite-mismatch
    - accountIdentityProofTemplate(1, KEY_00_1F) deep-equals { kind: 450, tags: [["d","marmot.account-identity-proof.v2"],["component","0x8009"],["ciphersuite","0x0001"],["signature_scheme","0x0807"],["mls_signature_key","000102030405060708090a0b0c0d0e0f101112131415161718191a1b1c1d1e1f"]], content: "Authorize this MLS leaf key for my Marmot account" }, where KEY_00_1F is the 32 bytes 0x00..0x1f
    - accountIdentityProofTemplate(7, KEY_00_1F).tags[2] is ["ciphersuite","0x0007"] and tags[3] is ["signature_scheme","0x0503"]; mutating the tags of one returned template does not change a second call's result
    - JSON.stringify([0, VECTOR_SIGNER_PUBKEY_HEX, 1700000000, t.kind, t.tags, t.content]) for t = accountIdentityProofTemplate(1, KEY_00_1F) equals the spec's canonical serialization string exactly
    - authorizationProofEventId(accountIdentityProofTemplate(1, KEY_00_1F), hexToBytes(VECTOR_SIGNER_PUBKEY_HEX), 1700000000) === "b7e9a15dd85990fb0f49c33db3cc9875f73986207b038404ceb6b7fec4e0af6b"
    - produceAccountIdentityProof({ signer: vectorSigner, accountIdentity: hexToBytes(VECTOR_SIGNER_PUBKEY_HEX), mlsSignatureKey: KEY_00_1F, ciphersuite: 1, createdAt: 1700000000 }) resolves to a 104-byte Uint8Array whose hex is exactly f9308a019258c31049344f85f89d5229b531c845836f99b08601f113bce036f9000000006553f100c5315d3c85b9d4907cb03395a2a97b3ba2eab393f8e45b13a5d5233acedac60a51d2a295e1b1b5ee372d18a49bdb8041a7dba9dedce722c7c6f712f78bbdfb5d
    - With createdAt omitted and Date.now mocked to 1700000123456, decodeAuthorizationProof(result).createdAt === 1700000123 (D-03 wall-clock default)
    - produceAccountIdentityProof with ciphersuite 0x00ff rejects with AccountIdentityProofError reason ciphersuite-mismatch and the signEvent spy has 0 calls
    - produceAccountIdentityProof with a signer that signs under a different secret key rejects with the Phase 6 AuthorizationProofError reason returned-pubkey-mismatch, passed through unchanged
    - new AccountIdentityProofError("m", "invalid-proof", { cause: inner }) is instanceof Error, has name "AccountIdentityProofError", reason "invalid-proof", and cause === inner
  </behavior>
  <action>
    Per D-12, add to src/core/components/ids.ts, right after GROUP_ENCRYPTED_MEDIA_COMPONENT_ID, the constant ACCOUNT_IDENTITY_PROOF_COMPONENT_ID: AppComponentId = 0x8009. Add the name ACCOUNT_IDENTITY_PROOF_COMPONENT = "marmot.member.account-identity-proof.v2" beside the other names. Give it a JSDoc line citing refs/marmot/app-components/account-identity-proof-v2.md. Do not change SUPPORTED_APP_COMPONENT_IDS or DEFAULT_GROUP_COMPONENT_IDS; plan 07-06 owns that per D-05 and D-15.

    Create src/core/components/account-identity-proof.ts:
    - Top JSDoc with "@module @category Core - App Components". Describe the proof class and cite refs/marmot/app-components/account-identity-proof-v2.md, refs/marmot/foundation/authorization-proofs.md, and refs/mdk/crates/cgka-engine/src/account_identity_proof.rs. Cite spec paths only, with no deprecated numbered-proposal citations.
    - Named exports only, relative imports with the .js extension, Uint8Array for all binary, and no Node-only binary type.
    - Imports: bytesToHex from "@noble/hashes/utils.js"; type AuthorizationProofSigner, type AuthorizationProofTemplate, produceAuthorizationProof, and encodeAuthorizationProof from "../authorization-proof.js"; ACCOUNT_IDENTITY_PROOF_COMPONENT_ID from "./ids.js".
    - This is a fresh module. Do not copy function or type names from the legacy module (Pitfall 1: the legacy 0xf2f1 proof was also internally called v2).

    Task 1 contents:
    1. Private constants: the kind 450, the d-tag value "marmot.account-identity-proof.v2", and the content "Authorize this MLS leaf key for my Marmot account". The d-tag value is spec-mandated; do not "fix" it.
    2. A private scheme table keyed by ciphersuite id: 1 and 3 map to 0x0807, 2 to 0x0403, 4 and 6 to 0x0808, 5 to 0x0603, 7 to 0x0503. Re-home these values verbatim from the verified legacy table; RESEARCH "Don't Hand-Roll" forbids re-deriving them.
    3. Per D-13: export type AccountIdentityProofRejectReason as the 14-literal union in "Artifacts this phase produces", declared in full now. Export class AccountIdentityProofError extends Error with constructor(message: string, readonly reason: AccountIdentityProofRejectReason, options?: ErrorOptions); call super(message, options) and set this.name = "AccountIdentityProofError". Also export the types AccountIdentityProofProfile and AccountIdentityProofLocation from the artifacts table.
    4. Export mlsSignatureSchemeForCiphersuite(ciphersuite: number): number. It looks up the table and throws AccountIdentityProofError("...", "ciphersuite-mismatch") when the id is unknown.
    5. Export accountIdentityProofTemplate(ciphersuite: number, mlsSignatureKey: Uint8Array): AuthorizationProofTemplate. It builds a new object each call. Tags, in this order:
       - ["d", domain]
       - ["component", "0x" + ACCOUNT_IDENTITY_PROOF_COMPONENT_ID in lowercase hex padded to 4 digits]
       - ["ciphersuite", "0x" + ciphersuite padded to 4 digits]
       - ["signature_scheme", "0x" + mlsSignatureSchemeForCiphersuite(ciphersuite) padded to 4 digits]
       - ["mls_signature_key", bytesToHex(mlsSignatureKey)]
       All hex is lowercase with no decimal form (Pitfall 1). The content is the fixed string.
    6. Export interface ProduceAccountIdentityProofParams and async produceAccountIdentityProof(params): Promise<Uint8Array>.
       - Build the template via accountIdentityProofTemplate(params.ciphersuite, params.mlsSignatureKey). Build it before calling the signer, so an unknown ciphersuite never reaches signEvent.
       - Call produceAuthorizationProof({ template, signerPubkey: params.accountIdentity, signer: params.signer, createdAt: params.createdAt }). Per D-03, createdAt stays optional and the primitive's wall-clock default applies.
       - Return encodeAuthorizationProof(proof).
       - Let AuthorizationProofError from the primitive propagate unchanged. A produce failure is a signing failure, not a leaf rejection; document this in JSDoc.

    Create src/core/components/__tests__/account-identity-proof.test.ts with vitest.
    - Import schnorr from "@noble/curves/secp256k1.js", bytesToHex and hexToBytes from "@noble/hashes/utils.js", getEventHash and type UnsignedEvent from "applesauce-core/helpers/event", and vi from vitest.
    - Top-level fixtures: VECTOR_SIGNER_PUBKEY_HEX, VECTOR_CREATED_AT = 1700000000, VECTOR_EVENT_ID, VECTOR_SIGNATURE_HEX, VECTOR_COMPONENT_HEX, VECTOR_CANONICAL_JSON (the spec serialization string), KEY_00_1F, SECRET_KEY_3 (32 bytes, last byte 3), and ZERO_AUX (32 zero bytes).
    - A signWith(secretKey, draft) helper that sets pubkey from schnorr.getPublicKey, id from getEventHash, and sig from schnorr.sign(hexToBytes(id), secretKey, ZERO_AUX).
    - vectorSigner = { signEvent: async (draft) => signWith(SECRET_KEY_3, draft as UnsignedEvent) }.
    - An expectReason helper (sync and async variants) that asserts instanceof AccountIdentityProofError and returns reason.
    - One it() per behavior bullet. Write the tests first and confirm they fail, then implement. Restore mocks after each test.
  </action>
  <verify>
    <automated>pnpm vitest run src/core/components/__tests__/account-identity-proof.test.ts && pnpm compile</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/core/components/__tests__/account-identity-proof.test.ts` exits 0 and the file contains the literals `b7e9a15dd85990fb0f49c33db3cc9875f73986207b038404ceb6b7fec4e0af6b`, `000000006553f100`, and `"Authorize this MLS leaf key for my Marmot account"`
    - `pnpm compile` exits 0
    - `grep -c "ACCOUNT_IDENTITY_PROOF_COMPONENT_ID: AppComponentId = 0x8009" src/core/components/ids.ts` is 1
    - `grep -c "export class AccountIdentityProofError extends Error" src/core/components/account-identity-proof.ts` is 1 and `grep -c "super(message, options)" src/core/components/account-identity-proof.ts` is 1
    - `for r in invalid-credential legacy-extension-present invalid-dictionary duplicate-data missing-support missing-data invalid-location ciphersuite-mismatch signature-key-mismatch identity-mismatch invalid-proof legacy-group mixed-profile missing-requirement; do grep -q "\"$r\"" src/core/components/account-identity-proof.ts || echo "MISSING $r"; done` prints nothing
    - `grep -c "produceAuthorizationProof(" src/core/components/account-identity-proof.ts` is at least 1 and `grep -c "getEventHash" src/core/components/account-identity-proof.ts` is 0 (no re-implemented event hashing)
    - `sed -n '/^export const SUPPORTED_APP_COMPONENT_IDS/,/];/p' src/core/components/ids.ts | grep -c "ACCOUNT_IDENTITY_PROOF_COMPONENT_ID"` is 0 (support list unchanged in this plan)
    - `git diff --quiet HEAD -- src/core/account-identity-proof.ts` exits 0
  </acceptance_criteria>
  <done>The class template and producer reproduce the spec signing vector byte-for-byte through the Phase 6 primitive, the typed error and all 14 reasons exist, and the component id and name constants are defined without touching the support or default lists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Leaf, KeyPackage, and whole-tree validators, GroupContext profile classifier, and location guard (PROOF-04, PROOF-05, PROOF-06, CUT-02)</name>
  <files>src/core/components/account-identity-proof.ts, src/core/components/__tests__/account-identity-proof.test.ts</files>
  <read_first>
    - src/core/components/account-identity-proof.ts (as left by Task 1; extend it)
    - src/core/components/__tests__/account-identity-proof.test.ts (as left by Task 1; extend it)
    - refs/marmot/app-components/account-identity-proof-v2.md ("Negotiation and presence", "Validation", "Migration from v1")
    - refs/mdk/crates/cgka-engine/src/account_identity_proof.rs lines 317-535 (validate_leaf_account_identity_proof, validate_current_proof, validate_proof_bindings, protocol_profile_of_group_extensions)
    - src/core/binary.ts BinaryReader and BinaryWriter (uint16, opaque, vector, end) and BinaryDecodeError
    - src/core/components/app-components-list.ts (decodeComponentsList, encodeComponentsList)
    - src/core/components/dictionary.ts (buildAppDataDictionary, componentEntry, appComponentsEntry)
    - src/core/credential.ts lines 40-64 (getCredentialPubkey, createCredential)
    - src/core/capabilities.ts lines 83-117 (required_capabilities extension shape)
    - src/core/key-package.ts lines 128-150 (generateKeyPackageWithKey call shape for the real KeyPackage fixture)
    - src/core/components/__tests__/integrity.test.ts (minimal ClientState ratchetTree fixture shape using nodeTypes)
    - src/core/authorization-proof.ts lines 86-104 (lift_x point check to mirror for the credential identity)
    - .planning/research/PITFALLS.md lines 86-132 (Pitfall 3) and 231-356 (Pitfalls 7-10)
  </read_first>
  <behavior>
    - Fixture VECTOR_LEAF is { credential: createCredential(VECTOR_SIGNER_PUBKEY_HEX), signaturePublicKey: KEY_00_1F, extensions: [app_data_dictionary with entries 0x0001 listing [0x0001, 0x8001, 0x8003, 0x8009], 0x0002 encoding [], 0x8009 = VECTOR_COMPONENT bytes] }, cast to LeafNode; validateLeafAccountIdentityProof(VECTOR_LEAF, 1) does not throw
    - A real ts-mls KeyPackage from generateKeyPackageWithKey (ciphersuite 1) whose leafNodeExtensions carry a dictionary built with a proof from produceAccountIdentityProof for the generated signature public key and a PrivateKeyAccount signer passes validateKeyPackageAccountIdentityProof(publicPackage) and validateKeyPackageAccountIdentityProof(publicPackage, 1); createSimpleGroup from that package yields a state that passes validateGroupMemberAccountIdentityProofs(clientState, 1)
    - Exact reasons from validateLeafAccountIdentityProof (ciphersuite 1 unless stated):
      - data entry present but the support list lacks 0x8009 → missing-support
      - no app_data_dictionary extension → missing-support
      - support list names 0x8009 but there is no 0x8009 entry → missing-data
      - raw-encoded dictionary with two 0x8009 entries → duplicate-data
      - two separate valid app_data_dictionary extensions → duplicate-data
      - dictionary bytes with the last byte dropped → invalid-dictionary
      - leaf whose only proof material is a custom extension of type 0xf2f1 → legacy-extension-present
      - VECTOR_LEAF plus a 0xf2f1 extension → legacy-extension-present
      - SafeAAD entry encoding [0x8009] → invalid-location
    - More exact reasons (credential, ciphersuite, and proof bytes):
      - credential identity of 32 zero bytes → invalid-credential; a non-basic credential → invalid-credential
      - credential identity 79be667ef9dcbbac55a06295ce870b07029bfcdb2dce28d959f2815b16f81798 (secret key 1) with the vector proof → identity-mismatch
      - ciphersuite 0x00ff → ciphersuite-mismatch
      - ciphersuite 2 with the 32-byte key → signature-key-mismatch
      - ciphersuite 3 (same scheme and key length, different ciphersuite tag) → invalid-proof whose cause is AuthorizationProofError reason invalid-signature
      - signaturePublicKey replaced by 32 bytes of 0xcd → invalid-proof (cause invalid-signature)
      - one bit flipped in the proof's signature bytes → invalid-proof (cause invalid-signature)
      - a 103-byte 0x8009 entry → invalid-proof (cause invalid-length); created_at wire bytes 0000000000000000 → invalid-proof (cause created-at-out-of-range)
    - validateKeyPackageAccountIdentityProof on a KeyPackage-shaped object { cipherSuite: 1, leafNode: VECTOR_LEAF, extensions: [] }:
      - passes with no second argument, and with expectedCiphersuite 1
      - expectedCiphersuite 2 → ciphersuite-mismatch
      - KeyPackage-level extensions carrying a dictionary with a 0x8009 entry → invalid-location
      - a KeyPackage-level 0xf2f1 extension → legacy-extension-present
      - cipherSuite 3 on the KeyPackage → invalid-proof, because the KeyPackage's own ciphersuite is used (D-16)
    - validateGroupMemberAccountIdentityProofs on a minimal ClientState whose ratchetTree holds VECTOR_LEAF and a second leaf with a flipped signature throws AccountIdentityProofError with reason invalid-proof and cause an AccountIdentityProofError. Its message names the member position and contains no 64-hex pubkey.
    - classifyGroupAccountIdentityProofProfile on hand-built GroupContext extension arrays:
      - app_components list [0x0001, 0x8003, 0x8009] plus required_capabilities extensionTypes [0x0006] → "current"
      - required_capabilities [0x0006, 0xf2f1] with a list lacking 0x8009 → "legacy"
      - both → "mixed"; neither → "neither"; no extensions at all → "neither"
      - a GroupContext dictionary carrying a 0x8009 data entry (built with makeAppDataDictionaryExtension) → throws invalid-location
    - assertCurrentGroupAccountIdentityProofProfile: current passes; legacy → legacy-group; mixed → mixed-profile; neither → missing-requirement
    - hasAccountIdentityProofMaterial: true for VECTOR_LEAF, the legacy-only leaf, the mixed leaf, and a truncated dictionary; false for a dictionary without a 0x8009 entry and for an empty extension list
    - assertNoAccountIdentityProofComponent(extensions, "group-info") throws invalid-location with "group-info" in the message for a list containing a dictionary with a 0x8009 entry; it does not throw for a dictionary without 0x8009 or for a list with no dictionary extension
  </behavior>
  <action>
    Extend src/core/components/account-identity-proof.ts. Add imports:
    - schnorr from "@noble/curves/secp256k1.js" and bytesToNumberBE from "@noble/curves/utils.js"
    - appDataDictionaryExtensionType, defaultCredentialTypes, defaultExtensionTypes, getAppDataDictionary, getGroupMembers, and the types ClientState, GroupContextExtension, KeyPackage, LeafNode from "ts-mls"
    - BinaryReader from "../binary.js"
    - decodeAuthorizationProof, verifyAuthorizationProof, AuthorizationProofError from "../authorization-proof.js"
    - decodeComponentsList from "./app-components-list.js"
    - APP_COMPONENTS_COMPONENT_ID and SAFE_AAD_COMPONENT_ID from "./ids.js"

    Private helpers:
    - A legacy extension-type constant 0xf2f1. It is NOT exported: CUT-01 forbids any legacy export, and it exists only to reject.
    - A signature-key length table keyed by scheme: 0x0807 accepts [32]; 0x0808 [57]; 0x0403 [33, 65]; 0x0503 [49, 97]; 0x0603 [67, 133].
    - readDictionaryEntries(extensionData): raw-parse via BinaryReader.vector reading uint16 componentId and opaque data, then end(). Map any throw to AccountIdentityProofError invalid-dictionary with cause. Do not route leaf dictionaries through getAppDataDictionary, because it hides duplicates by throwing (Pitfall 8).
    - leafDictionaryExtensions(extensions): extensions whose extensionType === appDataDictionaryExtensionType and whose extensionData is a Uint8Array.
    - bytesEqual.
    - A wrapper that converts any AuthorizationProofError (or other throw) from decode/verify into AccountIdentityProofError("...", "invalid-proof", { cause }). No untyped error may escape the class validators.

    validateLeafAccountIdentityProof(leaf, ciphersuite). Per D-14 it throws; per D-16 the ciphersuite parameter is explicit and never read off the leaf. Checks, in exactly this order:
    1. The credential must be basic, its identity exactly 32 bytes, and schnorr.utils.lift_x(bytesToNumberBE(identity)) must not throw. Otherwise invalid-credential.
    2. Any leaf extension with the legacy type → legacy-extension-present. This covers both legacy-only and mixed leaves (CUT-02, Pitfall 10; a deliberate, narrower divergence from MDK's legacy acceptance).
    3. More than one dictionary extension → duplicate-data. Zero dictionary extensions → missing-support.
    4. Raw-parse the entries.
       - More than one 0x8009 entry → duplicate-data.
       - Otherwise, component ids that are not strictly ascending → invalid-dictionary.
    5. The 0x0001 entry must exist and decodeComponentsList(entry) must include 0x8009, else missing-support. A decode throw → invalid-dictionary. The support-list check and the data-entry check are separate failure modes (Pitfall 7).
    6. No 0x8009 entry → missing-data.
    7. If a 0x0002 SafeAAD entry exists: decode it (a throw → invalid-dictionary); if it lists 0x8009 → invalid-location (PROOF-06, D-08).
    8. The scheme comes from mlsSignatureSchemeForCiphersuite (ciphersuite-mismatch). leaf.signaturePublicKey length must be in that scheme's allowed lengths, else signature-key-mismatch. The spec requires the key to be valid for the ciphersuite before the event is reconstructed (D-16).
    9. decoded = decodeAuthorizationProof(entry.data); map failures to invalid-proof with cause.
    10. decoded.signerPubkey must be bytesEqual to the identity, else identity-mismatch.
    11. verifyAuthorizationProof(accountIdentityProofTemplate(ciphersuite, leaf.signaturePublicKey), decoded); map failures to invalid-proof with cause.
    Error messages name the failing check and never embed key, pubkey, or signature bytes.

    Export the remaining functions:
    - hasAccountIdentityProofMaterial(leaf): true if any extension has the legacy type, or any dictionary extension fails the raw parse (fail closed), or any parsed entry is 0x8009. Otherwise false.
    - assertNoAccountIdentityProofComponent(extensions, location): for each dictionary extension in the list, raw-parse it (invalid-dictionary on failure). Throw invalid-location naming location if any entry is 0x8009. The parameter type is readonly { extensionType: number }[]; narrow at runtime so ts-mls GroupContext, GroupInfo, and KeyPackage extension arrays all typecheck.
    - validateKeyPackageAccountIdentityProof(keyPackage, expectedCiphersuite?), in order:
      (a) expectedCiphersuite provided and !== keyPackage.cipherSuite → ciphersuite-mismatch
      (b) any keyPackage.extensions entry with the legacy type → legacy-extension-present
      (c) assertNoAccountIdentityProofComponent(keyPackage.extensions, "key-package") (PROOF-05)
      (d) validateLeafAccountIdentityProof(keyPackage.leafNode, keyPackage.cipherSuite), per D-16 (KeyPackage validated with its own ciphersuite)
    - validateGroupMemberAccountIdentityProofs(state, ciphersuite): iterate getGroupMembers(state) with an index. Catch AccountIdentityProofError and rethrow new AccountIdentityProofError("account identity proof invalid for group member " + index + ": " + err.message, err.reason, { cause: err }).
    - classifyGroupAccountIdentityProofProfile(extensions), modelled on MDK protocol_profile_of_group_extensions (D-07):
      (1) assertNoAccountIdentityProofComponent(extensions, "group-context"), which rejects 0x8009 group state
      (2) legacyRequired = some extension with extensionType === defaultExtensionTypes.required_capabilities whose extensionData.extensionTypes includes the legacy type
      (3) currentRequired = getAppDataDictionary(extensions) (a ValidationError → invalid-dictionary) has a 0x0001 entry whose decodeComponentsList result includes 0x8009 (a decode throw → invalid-dictionary)
      (4) Return current, legacy, mixed, or neither.
    - assertCurrentGroupAccountIdentityProofProfile(extensions): classify, then throw legacy-group, mixed-profile, or missing-requirement for the three non-current outcomes (D-07, D-13).

    Tests:
    - Add small fixture builders: leafDictionary(entries), rawDictionaryExtension(entries) using BinaryWriter.vector/uint16/opaque to allow duplicates, and a legacyExtension(bytes) using makeCustomExtension with extensionType 0xf2f1.
    - For the real KeyPackage fixture, mirror src/core/key-package.ts: call keygen via ciphersuiteImpl.signature.keygen(), then generateKeyPackageWithKey with defaultCapabilities() and createDefaultKeyPackageLifetime(). Use PrivateKeyAccount.generateNew() from "applesauce-accounts/accounts" as the signer and createdAt 1700000000.
    - Implement one it() per behavior bullet, asserting the exact reason and, for invalid-proof, the cause's reason. Write the tests first, confirm they fail, then implement.
  </action>
  <verify>
    <automated>pnpm vitest run src/core/components/__tests__/account-identity-proof.test.ts && pnpm compile && pnpm exec tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/core/components/__tests__/account-identity-proof.test.ts` exits 0
    - `pnpm compile` and `pnpm exec tsc -p tsconfig.json --noEmit` exit 0
    - `for r in invalid-credential legacy-extension-present invalid-dictionary duplicate-data missing-support missing-data invalid-location ciphersuite-mismatch signature-key-mismatch identity-mismatch invalid-proof legacy-group mixed-profile missing-requirement; do grep -q "\"$r\"" src/core/components/__tests__/account-identity-proof.test.ts || echo "UNTESTED $r"; done` prints nothing
    - `grep -c "export function validateLeafAccountIdentityProof(" src/core/components/account-identity-proof.ts` is 1, and the same count is 1 for `validateKeyPackageAccountIdentityProof(`, `validateGroupMemberAccountIdentityProofs(`, `classifyGroupAccountIdentityProofProfile(`, `assertCurrentGroupAccountIdentityProofProfile(`, `assertNoAccountIdentityProofComponent(`, and `hasAccountIdentityProofMaterial(`
    - `grep -cE "^export (const|function|class).*0xf2f1|^export const LEGACY" src/core/components/account-identity-proof.ts` is 0 (the legacy extension type is not exported)
    - `grep -c "lift_x" src/core/components/account-identity-proof.ts` is at least 1 and `grep -c "\.vector(" src/core/components/account-identity-proof.ts` is at least 1
    - The test file contains the literal `79be667ef9dcbbac55a06295ce870b07029bfcdb2dce28d959f2815b16f81798` and the string `"group-info"`
    - `git diff --quiet HEAD -- src/core/account-identity-proof.ts` exits 0
  </acceptance_criteria>
  <done>Every spec validation failure for a leaf, KeyPackage, or member tree throws AccountIdentityProofError with a precise reason, legacy and mixed leaves and groups are rejected, and the GroupInfo guard and SafeAAD check are unit-tested.</done>
</task>

<task type="auto">
  <name>Task 3: GroupContext and AppDataUpdate leaf-only guards, barrel export, exports snapshot, cross-runtime gates (PROOF-06, D-08)</name>
  <files>src/core/components/dictionary.ts, src/core/components/integrity.ts, src/core/components/index.ts, src/core/components/__tests__/account-identity-proof.test.ts, src/core/components/__tests__/integrity.test.ts, src/__tests__/exports.test.ts</files>
  <read_first>
    - src/core/components/dictionary.ts lines 120-150 (makeAppComponentsExtension SafeAAD guard: the exact pattern to extend)
    - src/core/components/integrity.ts lines 1-180 (CommitIntegrityViolation, AppDataUpdateOp, validateAppComponentIntegrity rules 1-3)
    - src/core/components/__tests__/integrity.test.ts (dict/updateOp fixture helpers and validateAppComponentIntegrity describe block)
    - src/core/components/index.ts (flat export-star list)
    - src/__tests__/exports.test.ts (root inline snapshot)
    - refs/mdk/crates/cgka-engine/src/app_components.rs lines 40-49 (CURRENT_PROFILE_LEAF_ONLY_APP_COMPONENTS)
    - .planning/research/PITFALLS.md lines 282-313 (Pitfall 9) and 509-526 (Pitfall 15)
  </read_first>
  <action>
    Per D-08, in src/core/components/dictionary.ts import ACCOUNT_IDENTITY_PROOF_COMPONENT_ID from "./ids.js". Immediately after the existing SafeAAD check in makeAppComponentsExtension, add a second check with the same shape: throw a UsageError when any entry's componentId is ACCOUNT_IDENTITY_PROOF_COMPONENT_ID. Message: "account identity proof (0x8009) is LeafNode-only data and is not supported as group-component state". This guard rejects a data entry keyed 0x8009 only. An app_components (0x0001) required-id list that names 0x8009 stays allowed, because plan 07-06 relies on it for D-05. Add one sentence to the function JSDoc about the guard.

    Per D-08 and Pitfall 9, in src/core/components/integrity.ts import ACCOUNT_IDENTITY_PROOF_COMPONENT_ID from "./ids.js". In validateAppComponentIntegrity, directly after Rule 1, add a leaf-only check. It returns { reason: "component-integrity", detail } when either:
    (a) any args.appDataUpdateOps entry has componentId === ACCOUNT_IDENTITY_PROOF_COMPONENT_ID (update or remove), with detail "AppDataUpdate targets leaf-only app component 0x8009"; or
    (b) the resulting dictionary contains a 0x8009 entry, with detail "resulting GroupContext carries leaf-only app component 0x8009".
    Document it in the rules JSDoc as a separate paragraph ("Additionally, per account-identity-proof-v2.md Lifecycle ... mirroring MDK CURRENT_PROFILE_LEAF_ONLY_APP_COMPONENTS"). Keep Rules 1-3 and their MDK numbering unchanged. Do not change the CommitIntegrityViolationReason union. Seam-parity tests for this rule belong to Phase 8.

    In src/core/components/index.ts add export * from "./account-identity-proof.js"; directly after the ids.js line.

    Tests:
    - In src/core/components/__tests__/account-identity-proof.test.ts add a describe "GroupContext builder guard (PROOF-06)":
      - makeAppComponentsExtension([componentEntry(0x8009, new Uint8Array(104))]) throws /0x8009.*LeafNode/i
      - makeAppComponentsExtension([appComponentsEntry([0x0001, 0x8003, 0x8009])]) succeeds
      These live here, not in dictionary.test.ts, to keep file ownership exclusive from plan 07-04.
    - In src/core/components/__tests__/integrity.test.ts add three cases:
      - an update op on 0x8009 returns a violation whose detail contains "0x8009"
      - a remove op on 0x8009 returns a violation
      - resulting extensions carrying a 0x8009 entry (built with makeAppDataDictionaryExtension from "ts-mls", since makeAppComponentsExtension now refuses it) with no op returns a violation mentioning leaf-only
      Existing cases must remain unchanged and passing.

    Regenerate the root inline snapshot with pnpm vitest run src/__tests__/exports.test.ts -u; do not hand-edit it. The diff must be exactly 13 inserted symbol lines, the ones listed in "Artifacts this phase produces", and zero removed lines. On a TypeScript duplicate-export error or any other diff shape, stop and investigate.

    Run prettier on the eight touched files and fix any it reports. Then run the Bun and Deno smoke for the new test file, and the full suite once.
  </action>
  <verify>
    <automated>pnpm vitest run src/core/components/__tests__/account-identity-proof.test.ts src/core/components/__tests__/integrity.test.ts src/__tests__/exports.test.ts && pnpm compile && pnpm exec tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/core/components/__tests__/account-identity-proof.test.ts src/core/components/__tests__/integrity.test.ts src/__tests__/exports.test.ts` exits 0
    - `git diff --numstat HEAD -- src/__tests__/exports.test.ts` prints `13` insertions and `0` deletions
    - `for s in ACCOUNT_IDENTITY_PROOF_COMPONENT ACCOUNT_IDENTITY_PROOF_COMPONENT_ID AccountIdentityProofError accountIdentityProofTemplate assertCurrentGroupAccountIdentityProofProfile assertNoAccountIdentityProofComponent classifyGroupAccountIdentityProofProfile hasAccountIdentityProofMaterial mlsSignatureSchemeForCiphersuite produceAccountIdentityProof validateGroupMemberAccountIdentityProofs validateKeyPackageAccountIdentityProof validateLeafAccountIdentityProof; do grep -q "\"$s\"" src/__tests__/exports.test.ts || echo "MISSING $s"; done` prints nothing
    - `grep -c 'export \* from "./account-identity-proof.js";' src/core/components/index.ts` is 1
    - `grep -c "ACCOUNT_IDENTITY_PROOF_COMPONENT_ID" src/core/components/dictionary.ts` is at least 2 and `grep -c "ACCOUNT_IDENTITY_PROOF_COMPONENT_ID" src/core/components/integrity.ts` is at least 2
    - `bun run vitest run src/core/components/__tests__/account-identity-proof.test.ts` exits 0 and `deno run -A --node-modules-dir=auto npm:vitest run src/core/components/__tests__/account-identity-proof.test.ts` exits 0
    - `pnpm exec prettier --check src/core/components/ids.ts src/core/components/account-identity-proof.ts src/core/components/index.ts src/core/components/dictionary.ts src/core/components/integrity.ts src/core/components/__tests__/account-identity-proof.test.ts src/core/components/__tests__/integrity.test.ts src/__tests__/exports.test.ts` exits 0
    - `pnpm vitest run` (full suite) exits 0
    - `git diff --quiet HEAD -- src/core/account-identity-proof.ts src/core/key-package.ts src/core/capabilities.ts package.json pnpm-lock.yaml` exits 0
  </acceptance_criteria>
  <done>0x8009 can never be built or committed as GroupContext state through the create-time builder or the shared integrity validator, all new symbols reach the package root, and the suite is green on Node with Bun and Deno smoke passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inbound LeafNode / KeyPackage bytes → leaf and KeyPackage validators | Credential, signature key, extensions, and the 104-byte proof are attacker-controlled |
| inbound GroupContext extensions → profile classifier and integrity rule | A committer or Welcome sender controls required lists, required_capabilities, and dictionary entries |
| caller → validator ciphersuite parameter | The applicable ciphersuite is trusted local context; it must never be inferred from attacker bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Spoofing | legacy `0xf2f1`-only or mixed leaf accepted as current (downgrade) | high | mitigate | Step 2 of validateLeafAccountIdentityProof and the KeyPackage-level check reject any legacy extension; classifier rejects legacy/mixed groups; tests for all four shapes (Task 2) |
| T-07-02 | Tampering | valid proof for signature key A attached to a leaf with key B | high | mitigate | Template is rebuilt from the leaf's own signaturePublicKey, so substitution fails BIP-340 verification as invalid-proof; test replaces the key with 0xcd bytes (Task 2) |
| T-07-03 | Tampering | ciphersuite/scheme substitution | high | mitigate | Explicit ciphersuite parameter (KeyPackage uses its own cipherSuite, optional group equality); signature-key length check per scheme before reconstruction; ciphersuite 2 and 3 tests (Task 2) |
| T-07-04 | Tampering | support-list/data confusion and duplicate 0x8009 entries | medium | mitigate | Separate missing-support and missing-data checks; raw BinaryReader parse counts duplicates instead of trusting a decoder that throws or collapses them (Task 2) |
| T-07-05 | Elevation of Privilege | 0x8009 smuggled into GroupContext state, AppDataUpdate, KeyPackage-level extensions, or SafeAAD | high | mitigate | makeAppComponentsExtension guard, integrity rule for ops and resulting entries, classifier invalid-location, KeyPackage-level and SafeAAD location checks, each unit-tested (Tasks 2-3) |
| T-07-06 | Denial of Service | untyped throws (BinaryDecodeError, ValidationError, AuthorizationProofError) escaping to callers that map on reason | medium | mitigate | Every internal decode and verify is wrapped into AccountIdentityProofError with cause; tests assert the typed class for malformed dictionaries and envelopes (Task 2) |
| T-07-07 | Tampering | 0x8009 in GroupInfo or AppEphemeral | low | accept | No ts-mls AppEphemeral surface exists and no production path consumes GroupInfo component data; pure GroupInfo guard is unit-tested and the gap is recorded in the SUMMARY for future ts-mls upgrades |
| T-07-08 | Information Disclosure | error messages leaking pubkeys or key material | low | mitigate | Messages name the failing check and member position only; tree-validator test asserts no 64-hex pubkey in the message (Task 2) |

No package installs occur in this plan, so no supply-chain threat applies.
</threat_model>

<verification>
- `pnpm vitest run src/core/components/__tests__/account-identity-proof.test.ts src/core/components/__tests__/integrity.test.ts src/__tests__/exports.test.ts` exits 0
- `pnpm compile` and `pnpm exec tsc -p tsconfig.json --noEmit` exit 0
- `bun run vitest run src/core/components/__tests__/account-identity-proof.test.ts` and `deno run -A --node-modules-dir=auto npm:vitest run src/core/components/__tests__/account-identity-proof.test.ts` exit 0 (local smoke; the CI matrix is release authority)
- `pnpm vitest run` (full suite) exits 0
- `git diff --quiet HEAD -- src/core/account-identity-proof.ts` exits 0 (legacy module untouched until plan 07-07)
- Commit per CLAUDE.md Git Workflow once build and tests pass
</verification>

<success_criteria>
- ROADMAP Phase 7 success criterion 1: the spec signing vector's event id, signature, and 104-byte component reproduce through accountIdentityProofTemplate + produceAccountIdentityProof (Task 1)
- Success criterion 3 (pure layer): missing support/data, signer/ciphersuite/scheme/signature-key mismatch, bad signature, KeyPackage-level, GroupContext, and SafeAAD placements are rejected with typed reasons; GroupInfo is covered by the pure guard and AppEphemeral is a recorded gap (Tasks 2-3)
- Success criterion 5 (pure layer): legacy and mixed leaves and legacy/mixed/neither groups are rejected by the helpers plan 07-06 wires (Task 2)
- D-12: the new module lives at src/core/components/account-identity-proof.ts with 13 new root exports and no legacy names
</success_criteria>

<output>
Create `.planning/phases/07-account-identity-proof-component-0x8009-legacy-clean-cut/07-01-SUMMARY.md` when done. Its
"Deferred / known gaps" section MUST record:
1. PROOF-06 GroupInfo/AppEphemeral: pure GroupInfo guard only, no production consumer; AppEphemeral has no ts-mls surface.
2. Phase 6 WR-01..WR-03 deferred to Phase 8 with the rationale in this plan's context.
</output>
