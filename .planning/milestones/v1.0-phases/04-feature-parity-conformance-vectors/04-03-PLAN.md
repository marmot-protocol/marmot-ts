---
phase: 04-feature-parity-conformance-vectors
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - src/engine/ingestion-pool.ts
  - src/engine/retained-store.ts
  - src/engine/fork-recovery.ts
  - src/engine/group-engine.ts
  - src/engine/__tests__/ingest-deferred.test.ts
  - src/engine/__tests__/convergence-parity.test.ts
autonomous: true
requirements: [CONF-01]
must_haves:
  truths:
    - "A missing-parent commit stays deferred while its authenticated source epoch is within the rollback horizon."
    - "Expiry uses canonicalTipEpoch - commitSourceEpoch > maxRewindCommits, including Infinity."
    - "Known-state and tree-resolution refusal never let a truncated prefix compete."
    - "Authenticated intermediate anchors remain available for descendants and retryable capacity refusal is not terminally consumed."
  artifacts:
    - "Ingestion pool entries carry authenticated source-epoch metadata."
    - "Fork recovery uses one parent-resolution result vocabulary."
  key_links:
    - "ingest classification -> pool retention -> parent arrival -> retry"
    - "active source epochs -> retained-store pruning pins"
  prohibitions:
    - "Do not expire by wall clock, arrival order, queue age, or Rust storage thresholds."
---

<objective>Make missing-parent recovery complete and horizon-correct across both candidate seams, preserving intermediate anchors and retry semantics.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@.planning/phases/04-feature-parity-conformance-vectors/04-CONTEXT.md
@.planning/phases/04-feature-parity-conformance-vectors/04-RESEARCH.md
@refs/marmot/protocol-core/convergence.md
@src/engine/ingestion-pool.ts
@src/engine/retained-store.ts
@src/engine/fork-recovery.ts</context>
<assumption_delta_decision>Primary noun: convergence evidence. Decision: no-change. Rationale: a versioned stamp is persisted alongside wire bytes but does not generalize or replace record identity; Plan 04-02 pins identity for stamped and legacy variants.</assumption_delta_decision>

## Artifacts this phase produces

- Authenticated source-epoch pool metadata and horizon predicate
- Unified missing-parent/refusal result contract
- Intermediate-anchor and retry-after-capacity regression tests

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Retain missing-parent input by authenticated epoch and preserve anchors</name>
  <read_first>src/engine/ingestion-pool.ts, src/engine/retained-store.ts, src/engine/__tests__/ingest-deferred.test.ts, refs/marmot/protocol-core/convergence.md</read_first>
  <files>src/engine/ingestion-pool.ts, src/engine/retained-store.ts, src/engine/__tests__/ingest-deferred.test.ts</files>
  <behavior>
    - Deferred commit retries when its parent/intermediate anchor arrives.
    - It expires only when the strict epoch-distance inequality becomes true.
    - Infinity never expires; capacity-refused entries remain redeliverable.
  </behavior>
  <action>Replace arrival-based retention decisions with authenticated source-epoch metadata once authentication establishes it, and pin every intermediate retained state named by active deferred descendants. Implement only correctness-related capacity refusal/retry from MDK 7851a840/6bf697f9/93ecfbca; do not port SQLite layout, byte thresholds, metrics, throttling, or performance accounting. Never terminal-dedup a refused entry.</action>
  <verify><automated>CI=true npx --yes pnpm@10.18.3 vitest run src/engine/__tests__/ingest-deferred.test.ts</automated></verify>
  <acceptance_criteria>Boundary, beyond-boundary, Infinity, intermediate-anchor, and refusal/redelivery tests all pass with real MLS messages.</acceptance_criteria>
  <done>Deferred input survives exactly as long as the convergence contract requires.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Unify known-state and tree parent-resolution refusal</name>
  <read_first>src/engine/fork-recovery.ts, src/engine/group-engine.ts, src/engine/__tests__/convergence-parity.test.ts, .planning/phases/04-feature-parity-conformance-vectors/04-REFERENCE-FINDINGS.md</read_first>
  <files>src/engine/fork-recovery.ts, src/engine/group-engine.ts, src/engine/__tests__/convergence-parity.test.ts</files>
  <behavior>
    - Authentication mismatch against one retained state continues parent search.
    - Once a parent authenticates, authorization/component failure rejects the edge all-or-nothing.
    - Unresolved parents defer the whole candidate; no prefix competes.
  </behavior>
  <action>Introduce or reuse one discriminated parent-resolution outcome across ForkRecovery and #treeResolution, including stamped own commits from 04-02. Apply MLS authentication first, then parent-relative authorization and component validation. A temporary refusal or missing parent returns the same deferred outcome in both seams and never `continue`s a shortened chain.</action>
  <verify><automated>CI=true npx --yes pnpm@10.18.3 vitest run src/engine/__tests__/convergence-parity.test.ts src/engine/__tests__/ingest-deferred.test.ts &amp;&amp; CI=true npx --yes pnpm@10.18.3 compile</automated></verify>
  <acceptance_criteria>Tests prove full-chain deferral, later recovery, parent-relative validation, no truncated competitor, and green compile.</acceptance_criteria>
  <done>Roadmap criterion 6 and the CR-11 seam agreement are satisfied.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| unauthenticated commit → retained pool | Input requests durable retry resources. |
| retained state → candidate edge | A state may authenticate and authorize a branch. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-07 | Denial of Service | deferred pool | high | mitigate | Horizon expiry plus retryable capacity refusal; never silently terminalize refusal. |
| T-04-08 | Elevation of Privilege | parent authorization | high | mitigate | Authenticate parent first, then run existing legality gate against that exact state. |
| T-04-09 | Tampering | chain truncation | high | mitigate | All-or-nothing candidate construction and cross-seam parity tests. |
</threat_model>
<verification>Focused deferred/convergence suites and compile must exit 0 in under 60 seconds each.</verification>
<success_criteria>Roadmap criterion 6 holds and refreshed MDK correctness deltas are covered without Rust storage/performance mechanics.</success_criteria>
<output>Create `.planning/phases/04-feature-parity-conformance-vectors/04-03-SUMMARY.md` when done.</output>
