---
phase: 03.1-phase-3-review-closure
plan: 05
type: execute
wave: 4
depends_on: [03.1-03, 03.1-04]
files_modified:
  - src/engine/state-notifications.ts
  - src/engine/delivered-payloads.ts
  - src/engine/history-tree.ts
  - src/engine/group-engine.ts
  - src/engine/__tests__/state-notifications.test.ts
  - src/engine/__tests__/delivered-payloads.test.ts
  - src/__tests__/integration/ingest-commit-race.test.ts
  - .planning/phases/03-commit-integrity-convergence-parity/deferred-items.md
autonomous: true
requirements: [CONV-03]
must_haves:
  truths:
    - "D-04/D-05: notification ledger records empty derivations and uses digest-keyed lookup without linear has/record scans."
    - "D-05/D-06/D-07: both ledgers prune only below min(retained anchor, oldest tree-node epoch), with truthful Infinity documentation."
    - "D-07: a locally sent application payload is recorded and later invalidated if its branch loses."
    - "WR-07: tree traversal continues when needed for already-delivered payload/notification withdrawal, while post-removal delivery is gated."
  artifacts:
    - path: src/engine/state-notifications.ts
      provides: digest-keyed notification ledger including empty records
    - path: src/engine/delivered-payloads.ts
      provides: keyed payload ledger and tree-aware pruning
    - path: .planning/phases/03-commit-integrity-convergence-parity/deferred-items.md
      provides: accepted Infinity consequence and tree-pruning dependency
  key_links:
    - from: src/engine/group-engine.ts
      to: src/engine/history-tree.ts
      via: oldest retained tree-node epoch supplied to both ledger prune calls
      pattern: "pruneBelow|oldest|epoch"
    - from: local application send confirmation
      to: DeliveredPayloadLedger
      via: record own delivered payload before a later rewind can invalidate it
      pattern: "applicationMessage|record"
---

<objective>
Close ROADMAP criteria 4 and 5 with keyed ledgers, tree-derived retention, truthful Infinity semantics, own-payload accounting, and delivery-gated traversal.

Purpose: Keep CONV-03 withdrawal complete without claiming a memory bound the full-history tree does not provide.
Output: Two keyed ledgers, engine wiring, accepted deferral note, and failing-first unit/integration coverage.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03.1-phase-3-review-closure/03.1-CONTEXT.md
@.planning/phases/03-commit-integrity-convergence-parity/03-REVIEW-round1.md
@src/engine/state-notifications.ts
@src/engine/delivered-payloads.ts
@src/engine/history-tree.ts
@src/engine/group-engine.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Withdraw a locally delivered payload and notification across one losing branch</name>
  <files>src/engine/state-notifications.ts, src/engine/delivered-payloads.ts, src/engine/history-tree.ts, src/engine/group-engine.ts, src/__tests__/integration/ingest-commit-race.test.ts</files>
  <read_first>src/engine/state-notifications.ts; src/engine/delivered-payloads.ts; src/engine/history-tree.ts; src/engine/group-engine.ts; src/engine/ingest.ts; src/__tests__/integration/ingest-commit-race.test.ts</read_first>
  <behavior>
    - "A local application message recorded on a branch is emitted as invalidated when a competing branch wins."
    - "A notification derivation failure still records an empty digest entry that is reachable by rewind."
    - "Traversal is not suppressed merely because canonical state is removed; delivery remains suppressed while withdrawals surface."
  </behavior>
  <action>D-04, D-07, D-12 and D-13/WR-07: add failing-first integration cases for the own-payload gap, empty-record reachability, and eviction-versus-delivery gating. Record the local applicationMessage at confirmation using the same branch identity as inbound payloads. At both local-confirm and tree-rewind derivation sites, catch/log derivation failure and record an empty notification list. Rework the sweep guard so tree traversal required for ledger invalidation proceeds, but no new payload is delivered after self-removal. Preserve deterministic withdrawal order.</action>
  <verify><automated>test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm vitest run src/__tests__/integration/ingest-commit-race.test.ts &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932"</automated></verify>
  <acceptance_criteria>All three regressions are observed failing pre-fix; own payloads and announced notifications retract correctly while removed-state delivery remains blocked.</acceptance_criteria>
  <done>D-04, D-07 own-message accounting, and WR-07 are closed end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Index both ledgers and prune at the tree-derived correctness horizon</name>
  <files>src/engine/state-notifications.ts, src/engine/delivered-payloads.ts, src/engine/history-tree.ts, src/engine/group-engine.ts, src/engine/__tests__/state-notifications.test.ts, src/engine/__tests__/delivered-payloads.test.ts, .planning/phases/03-commit-integrity-convergence-parity/deferred-items.md</files>
  <read_first>src/engine/state-notifications.ts; src/engine/delivered-payloads.ts; src/engine/history-tree.ts; src/engine/group-engine.ts; src/engine/__tests__/state-notifications.test.ts; src/engine/__tests__/delivered-payloads.test.ts; .planning/phases/03-commit-integrity-convergence-parity/deferred-items.md</read_first>
  <behavior>
    - "has and record use a digest/state-tag keyed Map and are idempotent for repeated identity."
    - "Pruning retains every entry at or above min(anchorEpoch, oldest tree node epoch)."
    - "Infinity with an unpruned tree retains root-reachable entries and is documented as unbounded memory retention."
  </behavior>
  <action>D-05, D-06, D-07, and D-12: start with unit tests that distinguish keyed identity, duplicate suppression, same digest at distinct epochs where applicable, and a fork node older than the retained anchor. Replace both arrays with Map-backed indices while preserving deterministic iteration. Add or expose a read-only history-tree oldest-node epoch query and feed min(anchorEpoch, oldestTreeEpoch) to both prune operations. Correct both class docs: finite tree retention can bound entries, while Infinity plus an unpruned full-history tree is a correctness horizon but not a memory bound. Append the exact accepted consequence and future tree-pruning dependency to deferred-items.md.</action>
  <verify><automated>test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm vitest run src/engine/__tests__/state-notifications.test.ts src/engine/__tests__/delivered-payloads.test.ts src/__tests__/integration/ingest-commit-race.test.ts &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm compile &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932"</automated></verify>
  <acceptance_criteria>Keyed-ledger tests fail pre-fix and pass; no candidate-namable entry is pruned; Infinity documentation and deferred-items.md explicitly state unbounded retention until tree pruning.</acceptance_criteria>
  <done>ROADMAP criterion 4 is truthful and D-05/D-06/D-07 are fully implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| fork history → application withdrawal | Branch selection retracts previously visible state and payloads. |
| retention policy → ledger pruning | Incorrect pruning can erase evidence needed for future retraction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-09 | Tampering | ledger pruning | high | mitigate | Tree-derived minimum horizon and old-fork regression. |
| T-03.1-10 | Repudiation | own payload accounting | high | mitigate | Record own delivery and prove exact invalidation on branch loss. |
| T-03.1-SC | Tampering | dependency state | high | mitigate | Frozen/offline pnpm with lock hash guards. |
</threat_model>

<verification>Run ledger units, convergence integration, and compile with lock guards.</verification>
<success_criteria>Both ledgers have keyed access and complete withdrawal behavior; pruning is correct and Infinity claims are honest.</success_criteria>

## Artifacts this phase produces

- Map-backed ledger implementations and a history-tree horizon query.
- Accepted Infinity-retention entry in deferred-items.md.

<output>Create `.planning/phases/03.1-phase-3-review-closure/03.1-05-SUMMARY.md` when done.</output>
