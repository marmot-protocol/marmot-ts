---
phase: 04-feature-parity-conformance-vectors
plan: 04
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - src/client/group/wrapper-ledger.ts
  - src/client/marmot-client.ts
  - src/client/groups-manager.ts
  - src/client/group-factory.ts
  - src/client/group-registry.ts
  - src/client/session/group-session.ts
  - src/client/group/marmot-group.ts
  - src/engine/state-notifications.ts
  - src/engine/types.ts
  - src/engine/__tests__/state-notification-withdrawal.test.ts
  - src/__tests__/integration/app-message-replay-restart.test.ts
  - src/client/__tests__/marmot-client.test.ts
autonomous: true
requirements: [CONF-01]
must_haves:
  truths:
    - "Every terminally handled Nostr wrapper has a durable stable identity, including malformed and unsupported MLS payloads."
    - "Temporary deferral or resource refusal does not consume wrapper identity and remains redeliverable."
    - "Branch-selection withdrawal is emitted once and re-adoption emits one explicit revalidation, including after restart."
    - "MarmotClient reports whether ingest-state persistence is durable; omission uses one client-owned in-memory store with explicitly non-restart-safe capability."
  artifacts:
    - "src/client/group/wrapper-ledger.ts provides versioned terminal-wrapper persistence."
    - "ingestStateStore is threaded through every public create, import, join, and load construction path."
    - "StateNotification/result unions expose stable revalidation identity."
  key_links:
    - "verified outer event ID -> session terminal result -> wrapper ledger"
    - "persisted branch verdict -> load reconciliation -> withdrawal/revalidation output"
  prohibitions:
    - "Do not move Nostr wrapper types into the transport-neutral engine."
    - "Do not mark unverified or retryable input terminal."
---

<objective>Make restart replay idempotent at the outer transport boundary and make branch withdrawal/re-adoption observable exactly once.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@.planning/phases/04-feature-parity-conformance-vectors/04-CONTEXT.md
@.planning/phases/04-feature-parity-conformance-vectors/04-RESEARCH.md
@src/client/session/group-session.ts
@src/engine/state-notifications.ts
@src/__tests__/integration/app-message-replay-restart.test.ts</context>
<assumption_delta_decision>Primary noun: convergence evidence. Decision: no-change. Rationale: a versioned stamp is persisted alongside wire bytes but does not generalize or replace record identity; this plan keeps wrapper and effect identities separately stable.</assumption_delta_decision>

## Artifacts this phase produces

- `TerminalWrapperLedger` and terminal-disposition codec
- Public `ingestStateStore` option and discriminated durable/ephemeral ingest-persistence capability
- `stateRevalidated` (or equally explicit discretionary discriminant) with stable effect identity
- Restart tests for wrapper replay and withdrawal/re-adoption reconciliation

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Persist outer-wrapper identity only with terminal disposition</name>
  <read_first>src/client/marmot-client.ts, src/client/groups-manager.ts, src/client/group-factory.ts, src/client/group-registry.ts, src/client/group/marmot-group.ts, src/client/session/group-session.ts, src/extra/in-memory-key-value-store.ts, src/engine/message-dedup.ts, src/engine/history-tree.ts, src/client/__tests__/marmot-client.test.ts, src/__tests__/integration/app-message-replay-restart.test.ts</read_first>
  <files>src/client/group/wrapper-ledger.ts, src/client/marmot-client.ts, src/client/groups-manager.ts, src/client/group-factory.ts, src/client/group-registry.ts, src/client/group/marmot-group.ts, src/client/session/group-session.ts, src/client/__tests__/marmot-client.test.ts, src/__tests__/integration/app-message-replay-restart.test.ts</files>
  <behavior>
    - Valid, malformed, rejected, and unsupported terminal results persist the verified outer event ID with their outcome.
    - Restart replay produces no duplicate processing, notification, or rejection effect.
    - Deferred/capacity-refused delivery is not persisted and succeeds on later redelivery.
    - A supplied ingestStateStore reaches groups created, imported, joined, and loaded through MarmotClient and reports `{ kind: "durable" }`.
    - When omitted, MarmotClient creates one shared InMemoryKeyValueStore for all of its groups and reports `{ kind: "ephemeral" }`; dedup works within that client process but restart durability is explicitly unavailable.
  </behavior>
  <action>Add `ingestStateStore?: GenericKeyValueStore&lt;Uint8Array&gt;` to MarmotClientOptions and thread the resolved store through GroupsManagerOptions into both GroupFactory and GroupRegistry, then through every MarmotGroup constructor/fromClientState path into GroupSession and the group-namespaced versioned wrapper ledger. MarmotClient must resolve omission once, using one `InMemoryKeyValueStore&lt;Uint8Array&gt;` shared by every group it constructs, rather than creating a store per group. Expose a named discriminated capability such as `{ kind: "durable" } | { kind: "ephemeral"; reason: "ingest_state_store_omitted" }` from MarmotClient/GroupsManager so conformance support detection never claims restart-safe dedup for the omitted path. Consult the ledger only after existing Nostr verification, persist identity only with a terminal outcome, and keep existing inner content dedup.</action>
  <verify><automated>npx --yes pnpm@10.18.3 vitest run src/client/__tests__/marmot-client.test.ts src/__tests__/integration/app-message-replay-restart.test.ts</automated></verify>
  <acceptance_criteria>Construction tests prove the exact caller-supplied store object reaches create/import/join/load groups and reports durable; omission shares one in-memory store, reports ephemeral with the stable reason, dedups within-process, and a newly constructed client does not claim restart persistence. Actual supplied-store reload suppresses every terminal wrapper class, retryable refusal remains deliverable, and an unverified same-ID event cannot censor a later verified event.</acceptance_criteria>
  <done>Durable wrapper dedup from MDK 44e43eb7/93ecfbca is implemented at the correct trust boundary.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Reconcile once-only withdrawal and explicit re-adoption</name>
  <read_first>src/engine/state-notifications.ts, src/engine/types.ts, src/engine/__tests__/state-notification-withdrawal.test.ts, refs/marmot/foundation/conformance.md</read_first>
  <files>src/engine/state-notifications.ts, src/engine/types.ts, src/engine/__tests__/state-notification-withdrawal.test.ts, src/client/session/group-session.ts, src/client/group/marmot-group.ts</files>
  <behavior>
    - Only branch-selection invalidation is revalidatable.
    - Withdrawal and later re-adoption each emit once with stable commit/effect identity.
    - Same-batch ordered verdicts reduce to the final ordered state and restart reconciliation repairs an interrupted delivery.
  </behavior>
  <action>Extend the existing discriminated notification/result model and persisted ledger evidence to distinguish branch-selection withdrawal from terminal invalidation. Reconcile persisted canonical state and effect state during normal load/activation, emitting an explicit revalidation only for the same re-adopted origin. Preserve deterministic output order and Uint8Array digests.</action>
  <verify><automated>npx --yes pnpm@10.18.3 vitest run src/engine/__tests__/state-notification-withdrawal.test.ts src/__tests__/integration/app-message-replay-restart.test.ts &amp;&amp; npx --yes pnpm@10.18.3 compile</automated></verify>
  <acceptance_criteria>Direct, same-batch, and persist/reload tests prove exactly-once withdrawal/revalidation and no revival of terminal effects.</acceptance_criteria>
  <done>MDK a653714f observable withdrawal/re-adoption parity is present in public library outcomes.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Nostr wrapper → durable dedup | Attacker-controlled transport identity reaches persistence. |
| durable verdict → application events | Restart repair can repeat or revive effects. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-10 | Spoofing | wrapper ledger | high | mitigate | Verify event before ledger lookup/write and bind terminal write to the classified result. |
| T-04-11 | Repudiation | effect ledger | high | mitigate | Stable identities and durable ordered reconciliation tests. |
| T-04-12 | Denial of Service | retryable refusal | medium | mitigate | Refused/deferred input never consumes terminal dedup identity. |
</threat_model>
<verification>Focused restart and notification suites plus compile pass.</verification>
<success_criteria>Locked durable-dedup and withdrawal/re-adoption decisions are restart-safe without transport leakage into engine.</success_criteria>
<output>Create `.planning/phases/04-feature-parity-conformance-vectors/04-04-SUMMARY.md` when done.</output>
