---
phase: 07-account-identity-proof-component-0x8009-legacy-clean-cut
plan: 07
type: execute
wave: 4
depends_on: [07-06]
files_modified:
  - src/engine/__tests__/group-engine.test.ts
  - src/client/group/__tests__/marmot-group.test.ts
  - src/core/components/__tests__/safe-aad-parity.test.ts
  - src/core/account-identity-proof.ts
  - src/core/index.ts
  - src/core/__tests__/account-identity-proof.test.ts
  - src/__tests__/conformance/proof-v2-parity.test.ts
  - src/__tests__/fixtures/proof-v2-rust.json
  - tools/quality-gate/proof-v2-probe/Cargo.toml
  - tools/quality-gate/proof-v2-probe/Cargo.lock
  - tools/quality-gate/proof-v2-probe/src/main.rs
  - src/__tests__/exports.test.ts
  - AGENTS.md
autonomous: true
requirements: [CUT-01, CUT-02]

must_haves:
  truths:
    - "src/core/account-identity-proof.ts is deleted outright (not renamed or ported), its core barrel line is gone, and any surviving import of it fails pnpm compile or the root test typecheck (D-12, CUT-01)"
    - "The 15 legacy runtime exports are absent from the package root: the exports snapshot loses exactly those 15 lines and gains none (CUT-01)"
    - "The legacy-profile Rust tooling is retired in this phase: tools/quality-gate/proof-v2-probe/, src/__tests__/fixtures/proof-v2-rust.json, the legacy parity test, and the legacy unit test are deleted, so no generator for the 0xf2f1 shape remains to be copied (CUT-01, RESEARCH Pitfall 2)"
    - "The admin-policy commit callback rejects an Add whose KeyPackage leaf carries a tampered 0x8009 proof and an Add whose leaf carries only legacy 0xf2f1 material; an Add whose leaf carries no proof material bypasses the proof gate, pinned by a test named as the D-06 known gap Phase 8 must close (CUT-02, D-06)"
    - "The grep audit shows no 0xf2f1 reference in production code under src/ or examples/opentui/src except the private legacy constant in src/core/components/account-identity-proof.ts, and no legacy proof symbol or legacy module import anywhere in src/ or examples/opentui/src (CUT-01, CUT-02)"
    - "safe-aad-parity confirms both the pinned MDK current-profile fixture and a marmot-ts KeyPackage advertise 0x8009 (interop evidence for the cut)"
  artifacts:
    - path: "src/__tests__/exports.test.ts"
      provides: "Root export snapshot without legacy proof exports"
      contains: "validateLeafAccountIdentityProof"
    - path: "src/engine/__tests__/group-engine.test.ts"
      provides: "Admin-policy callback tests for tampered 0x8009, legacy-only, and the D-06 proof-less gap"
      contains: "D-06 known gap"
    - path: "AGENTS.md"
      provides: "Naming example no longer cites the deleted helper"
      contains: "test-accounts.ts"
  key_links:
    - from: "src/core/index.ts"
      to: "src/core/components/index.ts"
      via: "the only account identity proof surface left is the components barrel"
      pattern: "components/index\\.js"
    - from: "src/engine/__tests__/group-engine.test.ts"
      to: "src/engine/admin-policy.ts"
      via: "createAdminCommitPolicyCallback proof gate"
      pattern: "createAdminCommitPolicyCallback\\("
---

<objective>
Finish the legacy clean cut. Delete the `0xf2f1` module, its unit and parity tests, the Rust fixture, and the
`proof-v2-probe` generator (D-12, CUT-01). Rewrite the two remaining admin-policy tests that hand-built legacy proofs so
they exercise `0x8009` and pin the D-06 gap (CUT-02). Regenerate the exports snapshot so the 15 legacy runtime exports
disappear. Run the phase grep audit over `src/` and `examples/`.

Purpose: plan 07-06 already stopped every production path from emitting or accepting `0xf2f1`. This plan removes the
dead surface so no downstream consumer can still call a legacy builder or verifier (PITFALLS 15), and no fixture keeps
exercising the legacy shape as a happy path (PITFALLS 14).

Output: deletions, three rewritten test areas, a regenerated snapshot, one AGENTS.md line, and an audit record in the
SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-account-identity-proof-component-0x8009-legacy-clean-cut/07-CONTEXT.md
@.planning/phases/07-account-identity-proof-component-0x8009-legacy-clean-cut/07-RESEARCH.md
@.planning/phases/07-account-identity-proof-component-0x8009-legacy-clean-cut/07-06-SUMMARY.md
@src/core/components/account-identity-proof.ts
@src/engine/admin-policy.ts

<!-- planner-discipline-allow: 0xf2f1 -->
<!-- planner-discipline-allow: account-identity-proof.js -->

Planning-time facts verified on 2026-09-14 (do not re-derive):

- After plan 07-06, the legacy module `src/core/account-identity-proof.ts` has no production importer. Its remaining importers are `src/core/index.ts` (barrel line 1), `src/core/__tests__/account-identity-proof.test.ts`, `src/__tests__/conformance/proof-v2-parity.test.ts`, `src/engine/__tests__/group-engine.test.ts` (forged-proof admin-policy test around lines 327-375), and `src/client/group/__tests__/marmot-group.test.ts` (forged-proof test around lines 598-645).
- `src/__tests__/fixtures/proof-v2-rust.json` is consumed only by `proof-v2-parity.test.ts` once plan 07-06 has rewritten `darkmatter-invite-compat.test.ts`.
- `tools/quality-gate/proof-v2-probe/` (Cargo.toml, Cargo.lock, src/main.rs) generates the legacy "legacy-version-byte-2" 0xf2f1 fixture. It is not referenced by CI, package scripts, or other tools; `tools/quality-gate/lifetime-probe` and `safe-aad-probe` stay.
- `createAdminCommitPolicyCallback` returns "reject" from its proof loop before examining the sender. With `onUnverifiableCommit: "retry"` and `ratchetTree: [] as never`, the sender lookup throws `Error("unverifiable commit sender")`. So a proof-less Add that the proof loop skips produces that throw, while a rejected proof produces the return value "reject". This distinguishes "skipped" from "rejected" without building a tree.
- `src/__tests__/fixtures/safe-aad-rust.json` `advertised_app_components` is `[1, 32769, 32771, 32777, 32780]`; 32777 is 0x8009.
- `CLAUDE.md` is a symlink to `AGENTS.md`; line 206 cites the deleted test helper as a naming example.
</context>

## Artifacts this phase produces

No new production symbols. Removals:

- Deleted files: `src/core/account-identity-proof.ts`, `src/core/__tests__/account-identity-proof.test.ts`,
  `src/__tests__/conformance/proof-v2-parity.test.ts`, `src/__tests__/fixtures/proof-v2-rust.json`,
  `tools/quality-gate/proof-v2-probe/Cargo.toml`, `tools/quality-gate/proof-v2-probe/Cargo.lock`,
  `tools/quality-gate/proof-v2-probe/src/main.rs`.
- Removed barrel line from `src/core/index.ts`: the export-star of `./account-identity-proof.js` (line 1).
- The 15 runtime exports removed from the root snapshot:
  - constants: `ACCOUNT_IDENTITY_PROOF_EVENT_KIND`, `ACCOUNT_IDENTITY_PROOF_EXTENSION_TYPE`
  - event and digest helpers: `accountIdentityProofEventId`, `accountIdentityProofEventJson`, `accountIdentityProofSignatureFromSignedEvent`, `accountIdentityProofSigningDigest`
  - builders and codec: `buildAccountIdentityProofEvent`, `buildAccountIdentityProofExtension`, `decodeAccountIdentityProof`, `encodeAccountIdentityProof`, `makeAccountIdentityProofExtension`
  - signing and verification: `mlsSignatureScheme`, `signAccountIdentityProof`, `verifyAllLeafAccountIdentityProofs`, `verifyLeafAccountIdentityProof`

  Type-only legacy exports disappear with the module.

## Source Coverage Audit (this plan)

| Source | ID | Item | Status |
| --- | --- | --- | --- |
| CONTEXT | D-12 | Delete the legacy module outright so surviving imports fail the build | COVERED (Task 2) |
| CONTEXT | D-06 | Known gap pinned by a named test for Phase 8 | COVERED (Task 1) |
| REQ | CUT-01 | Legacy proof exports removed; no 0xf2f1 emission surface left | COVERED (Task 2) |
| REQ | CUT-02 | Legacy-only leaf rejected at the admin-policy gate | COVERED (Task 1) |
| RESEARCH | Pitfall 2 | Retire proof-v2-probe and its fixture in Phase 7 (planner decision; Phase 11 builds a new 0x8009 fixture) | COVERED (Task 2) |
| RESEARCH | PITFALLS 14, 15 | Grep audit of every legacy file; exports snapshot as a checklist item | COVERED (Task 2) |
| ORCHESTRATOR | grep audit | No 0xf2f1 in src/ or examples/ except rejection tests and the private constant | COVERED (Task 2) |

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the legacy-built admin-policy tests to 0x8009 and pin the D-06 gap; add 0x8009 to SafeAAD parity (CUT-02, D-06)</name>
  <files>src/engine/__tests__/group-engine.test.ts, src/client/group/__tests__/marmot-group.test.ts, src/core/components/__tests__/safe-aad-parity.test.ts</files>
  <read_first>
    - src/engine/admin-policy.ts (proof loop as changed by plan 07-06: presence check, KeyPackage validator, "reject" on throw, sender check afterwards)
    - src/engine/__tests__/group-engine.test.ts lines 1-40 and 320-380 (imports and the forged-proof test to replace)
    - src/client/group/__tests__/marmot-group.test.ts lines 1-60 and 595-650 (imports and the forged-proof test to replace)
    - src/core/components/__tests__/safe-aad-parity.test.ts (commonComponent loop)
    - src/core/components/account-identity-proof.ts (produceAccountIdentityProof signature)
    - src/core/components/dictionary.ts makeLeafAppComponentsExtension (proof-taking signature from plan 07-06)
    - src/__tests__/helpers/test-accounts.ts
  </read_first>
  <action>
    Per D-06 and CUT-02, in src/engine/__tests__/group-engine.test.ts, replace the single test "rejects a commit that adds a leaf with a forged account identity proof" with a describe block "admin commit policy account identity proof gate" of three tests. They share a local async builder that creates a KeyPackage-shaped object:
    - account = testAccount(6); mlsKey = 32 bytes of 0xcd
    - proof = await produceAccountIdentityProof({ signer: account.signer, accountIdentity: hexToBytes(account.pubkey), mlsSignatureKey: mlsKey, ciphersuite: 1, createdAt: 1700000000 })
    - leafNode = { credential: createCredential(account.pubkey), signaturePublicKey: mlsKey, extensions }
    - keyPackage = { cipherSuite: 1, leafNode, extensions: [] }, cast as never where the callback typing requires it

    Each test builds an incoming commit { kind: "commit", senderLeafIndex: 0, proposals: [{ proposal: { proposalType: defaultProposalTypes.add, add: { keyPackage } }, senderLeafIndex: 0 }] }. The callback is createAdminCommitPolicyCallback({ ratchetTree: [] as never, adminPubkeys: [account.pubkey], ciphersuiteId: 1, onUnverifiableCommit: "retry" }).
    (a) "rejects an Add whose 0x8009 proof signature is tampered": flip the last byte of a copy of proof before makeLeafAppComponentsExtension(proof); expect the callback to return "reject".
    (b) "rejects an Add whose leaf carries only legacy 0xf2f1 material": set extensions to [makeCustomExtension({ extensionType: 0xf2f1, extensionData: new Uint8Array(8) })] from "ts-mls"; expect "reject".
    (c) "D-06 known gap (Phase 8 GRP-02/GRP-04): an Add with no proof material skips the proof gate": set extensions to []; expect the callback to throw "unverifiable commit sender". This shows the proof loop let it through to the sender check rather than rejecting it. The test comment says Phase 8 flips this expectation to a rejection.

    In src/client/group/__tests__/marmot-group.test.ts, replace "rejects a commit that adds a leaf with a forged account identity proof" with one test of the same name. It uses case (a) under the real ciphersuite impl from getCiphersuiteImpl, onUnverifiableCommit "reject", and adminPubkeys [account.pubkey], and expects "reject".

    In both files, delete the legacy-module import and any import left unused. Import produceAccountIdentityProof and makeLeafAppComponentsExtension from the core components barrel (relative .js path), hexToBytes from "@noble/hashes/utils.js" if not already imported, and testAccount from the shared helper.

    In src/core/components/__tests__/safe-aad-parity.test.ts, add 0x8009 to the commonComponent array in "matches the dictionary extracted from a genuine MDK KeyPackage", so both the MDK fixture and the marmot-ts KeyPackage must advertise it.

    Run prettier on the three files.
  </action>
  <verify>
    <automated>pnpm vitest run src/engine/__tests__/group-engine.test.ts src/client/group/__tests__/marmot-group.test.ts src/core/components/__tests__/safe-aad-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The vitest command in verify exits 0
    - `grep -c "D-06 known gap" src/engine/__tests__/group-engine.test.ts` is 1
    - `grep -c "unverifiable commit sender" src/engine/__tests__/group-engine.test.ts` is at least 1
    - `grep -c '/core/account-identity-proof.js"' src/engine/__tests__/group-engine.test.ts src/client/group/__tests__/marmot-group.test.ts` reports 0 for both files
    - `grep -c "0x8009" src/core/components/__tests__/safe-aad-parity.test.ts` is at least 1
    - `pnpm exec prettier --check src/engine/__tests__/group-engine.test.ts src/client/group/__tests__/marmot-group.test.ts src/core/components/__tests__/safe-aad-parity.test.ts` exits 0
  </acceptance_criteria>
  <done>No remaining test hand-builds a legacy proof as a valid input; the admin-policy gate is proven to reject tampered 0x8009 and legacy-only Adds, and the D-06 skip is pinned for Phase 8.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the legacy module, tests, fixture, and probe; regenerate exports; grep audit (D-12, CUT-01)</name>
  <files>src/core/account-identity-proof.ts, src/core/index.ts, src/core/__tests__/account-identity-proof.test.ts, src/__tests__/conformance/proof-v2-parity.test.ts, src/__tests__/fixtures/proof-v2-rust.json, tools/quality-gate/proof-v2-probe/Cargo.toml, tools/quality-gate/proof-v2-probe/Cargo.lock, tools/quality-gate/proof-v2-probe/src/main.rs, src/__tests__/exports.test.ts, AGENTS.md</files>
  <read_first>
    - src/core/index.ts (line 1 is the legacy export-star)
    - src/__tests__/exports.test.ts (inline snapshot; the 15 legacy symbols listed in "Artifacts this phase produces")
    - AGENTS.md lines 200-210 (helper naming example)
    - .planning/research/PITFALLS.md lines 469-526 (Pitfalls 14 and 15)
    - .planning/phases/07-account-identity-proof-component-0x8009-legacy-clean-cut/07-RESEARCH.md ("Pitfall 2")
  </read_first>
  <action>
    Per D-12, delete with git rm, without renaming or porting any function:
    - src/core/account-identity-proof.ts
    - src/core/__tests__/account-identity-proof.test.ts
    - src/__tests__/conformance/proof-v2-parity.test.ts
    - src/__tests__/fixtures/proof-v2-rust.json
    - the whole tools/quality-gate/proof-v2-probe/ directory (Cargo.toml, Cargo.lock, src/main.rs)
    Retiring the probe here, not in Phase 11, is this plan's recorded decision. Phase 11 QA-03 needs a new Rust-signed 0x8009 fixture, and leaving the legacy generator risks a copy-paste of the legacy shape (RESEARCH Pitfall 2).

    Remove the legacy export-star line from src/core/index.ts and change nothing else in that file. Run pnpm compile and the root test typecheck immediately. Any surviving importer of the deleted module now fails; fix it by moving it to the components module, never by restoring the file.

    Regenerate the root inline snapshot with pnpm vitest run src/__tests__/exports.test.ts -u; do not hand-edit it. The diff must be exactly the 15 removed symbol lines listed in "Artifacts this phase produces" and zero insertions. On any other diff shape, stop and investigate.

    In AGENTS.md line 206, change the helper naming example to cite mock-network.ts and test-accounts.ts.

    Run the phase grep audit and record its full output in the SUMMARY under "CUT-01/CUT-02 grep audit":
    (1) production files under src and examples/opentui/src mentioning f2f1 case-insensitively;
    (2) any import path ending in the legacy module path outside components;
    (3) any legacy proof symbol name from the removed-exports list, or the removed client option name, anywhere in src or examples/opentui/src;
    (4) every test file still mentioning f2f1, each with a one-line note confirming its occurrences assert rejection or absence.
    Then run prettier on src/core/index.ts, src/__tests__/exports.test.ts, and AGENTS.md, the full suite, the extended conformance suite, the library build with the example typecheck, and Bun and Deno smoke on the exports test.
  </action>
  <verify>
    <automated>pnpm compile && pnpm exec tsc -p tsconfig.json --noEmit && pnpm vitest run src/__tests__/exports.test.ts src/engine/__tests__/group-engine.test.ts src/client/group/__tests__/marmot-group.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e src/core/account-identity-proof.ts && test ! -e src/core/__tests__/account-identity-proof.test.ts && test ! -e src/__tests__/conformance/proof-v2-parity.test.ts && test ! -e src/__tests__/fixtures/proof-v2-rust.json && test ! -d tools/quality-gate/proof-v2-probe` exits 0
    - `git diff --numstat HEAD -- src/__tests__/exports.test.ts` prints `0` insertions and `15` deletions
    - `for s in ACCOUNT_IDENTITY_PROOF_EVENT_KIND ACCOUNT_IDENTITY_PROOF_EXTENSION_TYPE accountIdentityProofEventId accountIdentityProofEventJson accountIdentityProofSignatureFromSignedEvent accountIdentityProofSigningDigest buildAccountIdentityProofEvent buildAccountIdentityProofExtension decodeAccountIdentityProof encodeAccountIdentityProof makeAccountIdentityProofExtension mlsSignatureScheme signAccountIdentityProof verifyAllLeafAccountIdentityProofs verifyLeafAccountIdentityProof; do grep -q "\"$s\"" src/__tests__/exports.test.ts && echo "STILL EXPORTED $s"; done` prints nothing
    - `grep -rlni "f2f1" src examples/opentui/src | grep -v "__tests__/"` prints exactly `src/core/components/account-identity-proof.ts`
    - `grep -rn 'account-identity-proof.js"' src examples/opentui/src | grep -v 'components/account-identity-proof.js"'` prints nothing
    - `grep -rnE "\b(signAccountIdentityProof|verifyLeafAccountIdentityProof|verifyAllLeafAccountIdentityProofs|buildAccountIdentityProofExtension|makeAccountIdentityProofExtension|AccountIdentityProofSigner|AccountIdentityProofRequest|accountProofSigner)\b" src examples/opentui/src` prints nothing
    - `grep -c "test-accounts.ts" AGENTS.md` is at least 1 and `grep -c "account-proof.ts" AGENTS.md` is 0
    - `pnpm vitest run` (full suite) exits 0 and `pnpm conformance:extended` exits 0
    - `pnpm build && (cd examples/opentui && ./node_modules/.bin/tsc --noEmit)` exits 0
    - `bun run vitest run src/__tests__/exports.test.ts` and `deno run -A --node-modules-dir=auto npm:vitest run src/__tests__/exports.test.ts` exit 0
    - `pnpm exec prettier --check src/core/index.ts src/__tests__/exports.test.ts AGENTS.md` exits 0
    - The SUMMARY contains a "CUT-01/CUT-02 grep audit" section with the four audit results
  </acceptance_criteria>
  <done>The legacy proof module, its tests, fixture, and Rust generator are gone; the package root exports none of the 15 legacy symbols; the audit shows 0xf2f1 only in the private rejection constant and rejection tests; build, example typecheck, and the full suite are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package exports → downstream apps | Any still-exported legacy builder or verifier is callable by consumers |
| repository tooling → future contributors | A legacy fixture generator invites reproducing the 0xf2f1 shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-22 | Spoofing | downstream apps still producing or accepting legacy proofs through dead exports (PITFALLS 15) | high | mitigate | Delete the module outright; exports snapshot must lose exactly the 15 symbols; compile and root test typecheck fail on any surviving import (Task 2) |
| T-07-23 | Tampering | legacy fixture or generator reused as a template for new fixtures (Pitfall 2) | medium | mitigate | Delete proof-v2-probe and proof-v2-rust.json; grep audit recorded in SUMMARY (Task 2) |
| T-07-24 | Elevation of Privilege | admin-policy regression accepting legacy-only or tampered Adds | high | mitigate | Rewritten callback tests for tampered 0x8009 and legacy-only material (Task 1) |
| T-07-25 | Elevation of Privilege | proof-less Add skipped by the admin gate (D-06 locked decision) | medium | transfer | Pinned by a named test; transferred to Phase 8 GRP-02/GRP-04, which flips the expectation (Task 1) |

No package installs occur in this plan, so no supply-chain threat applies.
</threat_model>

<verification>
- `pnpm compile` and `pnpm exec tsc -p tsconfig.json --noEmit` exit 0
- `pnpm vitest run` and `pnpm conformance:extended` exit 0
- `pnpm build && (cd examples/opentui && ./node_modules/.bin/tsc --noEmit)` exits 0
- Exports snapshot numstat is 0 insertions, 15 deletions
- Grep audit clean as specified in Task 2
- Commit per CLAUDE.md once build and tests pass
</verification>

<success_criteria>
- ROADMAP Phase 7 success criterion 4: the legacy proof exports no longer exist in the package, and no emission surface for 0xf2f1 remains
- Success criterion 5 (admin-policy gate): a leaf carrying only 0xf2f1 is rejected
- D-12: legacy module deleted outright; D-06 gap pinned for Phase 8
</success_criteria>

<output>
Create `.planning/phases/07-account-identity-proof-component-0x8009-legacy-clean-cut/07-07-SUMMARY.md` when done. It
MUST include the "CUT-01/CUT-02 grep audit" section, the proof-v2-probe retirement decision, and a "Deferred / known
gaps" entry restating D-06 (admin-policy skips proof-less Adds; Phase 8 GRP-02/GRP-04 must close it).
</output>
