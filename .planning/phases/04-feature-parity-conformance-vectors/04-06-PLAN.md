---
phase: 04-feature-parity-conformance-vectors
plan: 06
type: execute
wave: 5
depends_on: [04-01, 04-02, 04-03, 04-04, 04-05]
files_modified:
  - ts-mls/src/groupContext.ts
  - ts-mls/src/index.ts
  - src/__tests__/conformance/manifest.ts
  - src/__tests__/conformance/snapshot.ts
  - src/__tests__/conformance/subject.ts
  - src/__tests__/conformance/runner.ts
  - src/__tests__/conformance/adapter.test.ts
autonomous: true
requirements: [CONF-01]
must_haves:
  truths:
    - "A reusable TypeScript subject reads canonical MDK JSON directly and reports unsupported capabilities explicitly."
    - "Fixture paths cannot escape the pinned vectors root."
    - "Canonical equality contains every foundation/conformance projection field, disposition, and application-visible output."
    - "The adapter drives production MarmotGroup/engine behavior with real ts-mls cryptography and deterministic time."
  artifacts:
    - "ts-mls publicly exports groupContextEncoder from its package root."
    - "src/__tests__/conformance/manifest.ts exports validated manifest/capability contracts."
    - "src/__tests__/conformance/snapshot.ts exports the canonical projection."
    - "src/__tests__/conformance/subject.ts and runner.ts provide the reusable scenario boundary."
  key_links:
    - "manifest artifact -> safe loader -> subject operation"
    - "live confirmed ClientState + engine outcomes -> canonical snapshot -> expected JSON"
  prohibitions:
    - "Do not implement fixture-specific winner rules or translate the Rust simulator/storage layer."
    - "Do not silently skip a portable manifest entry."
---

<objective>Build the reusable, deterministic MDK scenario adapter and canonical snapshot oracle used by every cross-implementation scenario.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@.planning/phases/04-feature-parity-conformance-vectors/04-CONTEXT.md
@.planning/phases/04-feature-parity-conformance-vectors/04-RESEARCH.md
@refs/marmot/foundation/conformance.md
@refs/mdk/crates/cgka-conformance-simulator/vectors/manifest.v1.json
@src/engine/__tests__/convergence-parity.test.ts</context>
<assumption_delta_decision>Primary noun: convergence evidence. Decision: no-change. Rationale: a versioned stamp is persisted alongside wire bytes but does not generalize or replace record identity; the snapshot projects protocol identity independently of local storage representation.</assumption_delta_decision>

## Artifacts this phase produces

- `ConformanceManifestEntry`, `ConformanceCapability`, and explicit support result
- Public `groupContextEncoder` export from `ts-mls/src/groupContext.ts` through `ts-mls/src/index.ts`
- `CanonicalConformanceSnapshot` and projection function
- `MarmotConformanceSubject` and `runConformanceScenario`

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Validate the manifest and canonical snapshot projection</name>
  <read_first>refs/mdk/crates/cgka-conformance-simulator/vectors/manifest.v1.json, refs/marmot/foundation/conformance.md, ts-mls/src/groupContext.ts, ts-mls/src/index.ts, ts-mls/src/codec/tlsEncoder.ts, src/core/client-state.ts, src/engine/types.ts</read_first>
  <files>ts-mls/src/groupContext.ts, ts-mls/src/index.ts, src/__tests__/conformance/manifest.ts, src/__tests__/conformance/snapshot.ts, src/__tests__/conformance/adapter.test.ts</files>
  <behavior>
    - Loader accepts only declared relative artifacts beneath the vectors root and rejects absolute/traversal/malformed input.
    - `groupContextEncoder` is importable from the public `ts-mls` package root and serializes the live GroupContext used by the snapshot.
    - Snapshot contains raw group id, epoch, SHA256 serialized GroupContext, exporter commitment, indexed leaves/capabilities, required capabilities, sorted exact dictionary bytes, lifecycle, convergence status, input dispositions, unresolved publications/gates where specified, and application outputs.
    - Local queue/storage identifiers are excluded.
  </behavior>
  <action>Unconditionally add `groupContextEncoder` to the existing `./groupContext.js` export clause in `ts-mls/src/index.ts`; it is already a named export in `ts-mls/src/groupContext.ts`, so do not duplicate or wrap the codec. Import it only from the public `ts-mls` package root in snapshot.ts and call `encode(groupContextEncoder, liveGroupContext)` before noble SHA-256. Define strict test-only JSON validators without new packages, preserve upstream IDs, and resolve paths beneath the pinned root. Sort exactly where the specification says and retain per-input order where order is semantic.</action>
  <verify><automated>npx --yes pnpm@10.18.3 --filter ts-mls build &amp;&amp; npx --yes pnpm@10.18.3 vitest run src/__tests__/conformance/adapter.test.ts &amp;&amp; rg -n "groupContextEncoder" ts-mls/src/index.ts src/__tests__/conformance/snapshot.ts</automated></verify>
  <acceptance_criteria>`import { groupContextEncoder } from "ts-mls"` typechecks, the snapshot hashes bytes produced by that public encoder from the live GroupContext, complete projection passes, omitted/ad-hoc fields fail, and traversal is rejected.</acceptance_criteria>
  <done>Roadmap criterion 8 has one authoritative executable projection.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Drive production clients through a capability-declared deterministic subject</name>
  <read_first>src/client/group/marmot-group.ts, src/__tests__/helpers/mock-network.ts, src/engine/__tests__/convergence-parity.test.ts, refs/mdk/crates/cgka-conformance-simulator/src/subject.rs</read_first>
  <files>src/__tests__/conformance/subject.ts, src/__tests__/conformance/runner.ts, src/__tests__/conformance/adapter.test.ts, src/__tests__/helpers/mock-network.ts</files>
  <behavior>
    - Supported actions use production group/client APIs, actual save/load, real crypto, and fully drained async generators.
    - Unsupported operations produce an explicit scenario result naming capability and reason.
    - Injected clock and scheduler hooks control delivery, pass deadlines, restart, and queue order reproducibly.
  </behavior>
  <action>Implement a test-only subject boundary over MarmotGroup/engine and MockNetwork, not a second protocol implementation. Declare support before execution; record dispositions and outputs by stable input name; compare only through the canonical snapshot. Extend shared helpers for deterministic delivery/fault controls and preserve upstream scenario names in diagnostics.</action>
  <verify><automated>npx --yes pnpm@10.18.3 vitest run src/__tests__/conformance/adapter.test.ts &amp;&amp; npx --yes pnpm@10.18.3 compile</automated></verify>
  <acceptance_criteria>A small production-backed scenario, restart action, deterministic deadline, and explicit unsupported action all pass; compile remains green.</acceptance_criteria>
  <done>The reusable CONF-01 adapter is ready for the entire portable corpus.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| fixture JSON/path → loader | Upstream files control test actions and filesystem reads. |
| live state → oracle | An incomplete projection could certify false parity. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-16 | Tampering | fixture loader | high | mitigate | Root containment, strict schema validation, and traversal tests. |
| T-04-17 | Spoofing | snapshot equality | high | mitigate | Real MLS exporter/GroupContext digest and exhaustive canonical-field tests. |
| T-04-18 | Repudiation | unsupported capability | medium | mitigate | Explicit typed result keyed by upstream scenario ID. |
</threat_model>
<verification>Adapter test and compile pass with no production Node-only imports.</verification>
<success_criteria>Canonical snapshot and subject adapter are reusable, secure, production-backed, and fixture-neutral.</success_criteria>
<output>Create `.planning/phases/04-feature-parity-conformance-vectors/04-06-SUMMARY.md` when done.</output>
