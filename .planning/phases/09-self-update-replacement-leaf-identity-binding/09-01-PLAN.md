---
phase: 09-self-update-replacement-leaf-identity-binding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/components/tree-diff.ts
  - src/core/components/__tests__/tree-diff.test.ts
  - src/core/inbound.ts
  - src/core/components/account-identity-proof.ts
  - src/core/components/leaf-replacement.ts
  - src/core/components/__tests__/leaf-replacement.test.ts
  - src/core/components/index.ts
  - src/__tests__/helpers/account-identity-proof-fixtures.ts
autonomous: true
requirements: [UPD-01]
tags: [mls, account-identity-proof, commit-legality, self-update, app-components]

must_haves:
  truths:
    - "D-01: a pure core classifier sorts every changed leaf into exactly one of add / update-proposal / committer-update-path / unattributable / undecidable"
    - "D-01: a changed leaf whose signature bytes equal an Add proposal's KeyPackage leaf signature classifies as add, so a Remove+Add into a freed slot is never treated as an identity change"
    - "D-02: with full classification input available, a changed leaf matching no bucket classifies as unattributable (fail closed)"
    - "D-03: with classification input absent, or with committerLeafIndex absent and no proposal match, a changed leaf classifies as undecidable (defer, not reject)"
    - "D-04: deferredReasons carries a new snake_case literal for an unjudgeable replacement leaf, and DeferredReason widens automatically"
    - "D-05: AccountIdentityProofRejectReason carries a new kebab-case literal for a replacement-leaf identity change, distinct from identity-mismatch"
    - "diffChangedLeaves surfaces the parent leaf at the same index so a caller can compare prior and replacement identity without re-reading the tree"
  artifacts:
    - path: src/core/components/leaf-replacement.ts
      provides: "ChangedLeafClassification, ChangedLeafClassificationInput, classifyChangedLeaf — the pure D-01 three-bucket classifier"
      contains: "export function classifyChangedLeaf"
    - path: src/core/components/tree-diff.ts
      provides: "ChangedLeaf.parentLeaf — the prior leaf at the same MLS index"
      contains: "parentLeaf"
    - path: src/core/inbound.ts
      provides: "deferredReasons.unjudgeableIdentity (D-04)"
      contains: "unjudgeable_identity"
    - path: src/core/components/account-identity-proof.ts
      provides: "member-identity-changed and unattributable-leaf reject reasons (D-05/D-02)"
      contains: "member-identity-changed"
    - path: src/__tests__/helpers/account-identity-proof-fixtures.ts
      provides: "spliceLeafAtIndex, stripLeafAccountIdentityProof, forgeKeyPackage stale mode — fixtures plans 09-02 and 09-04 consume"
      contains: "export function spliceLeafAtIndex"
  key_links:
    - from: src/core/components/leaf-replacement.ts
      to: src/core/components/tree-diff.ts
      via: "classifyChangedLeaf takes a ChangedLeaf produced by diffChangedLeaves"
      pattern: 'ChangedLeaf'
    - from: src/core/components/leaf-replacement.ts
      to: src/core/components/bytes.ts
      via: "Add-bucket matching compares leaf signature bytes with bytesEqual"
      pattern: 'bytesEqual\('
    - from: src/core/components/index.ts
      to: src/core/components/leaf-replacement.ts
      via: "barrel re-export"
      pattern: 'leaf-replacement.js'
---

<objective>
Build the additive foundations UPD-01 needs, with no signature changes to any existing validator: the D-01
three-bucket changed-leaf classifier as a pure `src/core` module, the prior-leaf field on `ChangedLeaf`, the three
new reason literals (D-04, D-05, plus the D-02 fail-closed literal), and the test fixtures plans 09-02 and 09-04
consume.

Purpose: every change here is purely additive, so this plan compiles and tests green on its own. Plan 09-02 then
changes `validateCommitLegality`'s return type in one atomic compile unit, which is only tractable once the
classifier and literals already exist.
Output: new `src/core/components/leaf-replacement.ts`, `ChangedLeaf.parentLeaf`, one new `DeferredReason`, two new
`AccountIdentityProofRejectReason` literals, and three new test fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-self-update-replacement-leaf-identity-binding/09-CONTEXT.md
@.planning/phases/09-self-update-replacement-leaf-identity-binding/09-PATTERNS.md
@CLAUDE.md
@src/core/components/tree-diff.ts
@src/core/components/integrity.ts
@src/core/credential.ts

<interfaces>
Existing signatures this plan consumes (unchanged by this plan):

```typescript
// src/core/components/tree-diff.ts
export interface ChangedLeaf { leafIndex: number; leaf: LeafNode }
export function diffChangedLeaves(
  parentTree: ClientState["ratchetTree"], resultingTree: ClientState["ratchetTree"],
): ChangedLeaf[];

// src/core/components/bytes.ts
export function bytesEqual(a: Uint8Array | undefined, b: Uint8Array | undefined): boolean;

// src/core/credential.ts
export function getCredentialPubkey(credential: Credential): string; // throws on non-basic / non-32-byte

// ts-mls package root (bare specifier only — subpath imports fail the vendor guard)
defaultProposalTypes.add, defaultProposalTypes.update
type Proposal, type ProposalWithSender  // ProposalWithSender = { proposal: Proposal; senderLeafIndex: number | undefined }
type LeafNode                            // .credential, .signature, .signaturePublicKey, .extensions
// An Add proposal carries proposal.add.keyPackage.leafNode; an Update carries proposal.update.leafNode.
```

New signatures this plan creates (exact names; plans 09-02/09-03/09-04 import them):

```typescript
// src/core/components/tree-diff.ts  (field added to the existing interface)
export interface ChangedLeaf { leafIndex: number; leaf: LeafNode; parentLeaf?: LeafNode }

// src/core/components/leaf-replacement.ts  (new module)
export type ChangedLeafClassification =
  | { kind: "add" }
  | { kind: "update-proposal"; senderLeafIndex: number }
  | { kind: "committer-update-path" }
  | { kind: "unattributable" }
  | { kind: "undecidable" };
export interface ChangedLeafClassificationInput {
  proposals: readonly ProposalWithSender[];
  committerLeafIndex: number | undefined;
}
export function classifyChangedLeaf(
  changed: ChangedLeaf,
  input: ChangedLeafClassificationInput | undefined,
): ChangedLeafClassification;

// src/core/inbound.ts  (one entry added to the existing const object)
deferredReasons.unjudgeableIdentity === "unjudgeable_identity"

// src/core/components/account-identity-proof.ts  (two literals added to the existing union)
"member-identity-changed" | "unattributable-leaf"

// src/__tests__/helpers/account-identity-proof-fixtures.ts
export function spliceLeafAtIndex(state: ClientState, leafIndex: number, leaf: LeafNode): ClientState;
export function stripLeafAccountIdentityProof(leaf: LeafNode): LeafNode;
export async function forgeKeyPackage(args: {
  account: ReturnType<typeof testAccount>; ciphersuiteImpl: CiphersuiteImpl;
  proof: "missing" | "tampered" | "stale";   // "stale" is new in this plan
}): Promise<{ publicPackage: KeyPackage; privatePackage: PrivateKeyPackage }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prior leaf on ChangedLeaf, plus the three new reason literals</name>
  <files>src/core/components/tree-diff.ts, src/core/components/__tests__/tree-diff.test.ts, src/core/inbound.ts, src/core/components/account-identity-proof.ts</files>
  <read_first>
    - src/core/components/tree-diff.ts (full file — `beforeLeaf` is already computed locally at the push site; the `leafIndex` doc warning about member-enumeration indexes)
    - src/core/components/__tests__/tree-diff.test.ts (full file — `twoPartyEpoch1Group`, `ctxFor`, the four existing `it` blocks whose assertions must keep passing)
    - src/core/inbound.ts lines 69-83 (the `deferredReasons` const object and the derived `DeferredReason` type)
    - src/core/components/account-identity-proof.ts lines 88-123 (the reject-reason union, its "one literal per spec validation step — never a coarse bucket" doc comment, and `AccountIdentityProofError`)
    - .planning/phases/09-self-update-replacement-leaf-identity-binding/09-CONTEXT.md decisions D-04, D-05, D-06
    - refs/marmot/app-components/account-identity-proof-v2.md section "Lifecycle, authorization, and removal" (lines 96-111)
  </read_first>
  <behavior>
    - Test 1: for a proposal-less self-update by leaf 0, the single ChangedLeaf carries a defined `parentLeaf` whose signature bytes equal the parent tree's leaf-0 signature and differ from the resulting leaf's signature
    - Test 2: for a commit adding a brand-new member into a brand-new slot, that member's ChangedLeaf has `parentLeaf` undefined
    - Test 3: the four existing `diffChangedLeaves` assertions (self-update count, Add index, removal skipped, empty/ragged trees) still pass unchanged
  </behavior>
  <action>
In `src/core/components/tree-diff.ts`, add an optional `parentLeaf?: LeafNode` field to the exported `ChangedLeaf`
interface. Populate it at the single existing `changed.push(...)` site from the `beforeLeaf` local that the function
already computes — do not re-read the tree and do not change the skip logic, the signature-inequality comparison, or
the `leafIndex` arithmetic. `parentLeaf` is `undefined` exactly when the parent node at that index was absent or not
a leaf (a brand-new or previously-blank slot). Extend the interface doc comment to state that `parentLeaf` is the
prior occupant of the same MLS leaf index and that its absence means the slot had no prior member, so there is no
prior identity to preserve. Keep the module's existing `@see` citation.

In `src/core/inbound.ts`, add one entry to the `deferredReasons` const object per D-04: key `unjudgeableIdentity`,
value `"unjudgeable_identity"`. Follow the file's existing convention exactly — camelCase key, snake_case string
value, one-line doc comment above the entry explaining that a commit's changed leaves could not be attributed to its
proposals or committer, so authorization cannot be evaluated against this candidate parent yet. Change nothing else:
`DeferredReason` is derived via `typeof deferredReasons[keyof typeof deferredReasons]` and widens automatically, and
`disposition.deferred` already accepts the full union. Cite `refs/marmot/foundation/errors.md` lines 63-68 (a Commit
whose authorization cannot be evaluated against a candidate parent remains deferred; only failed authorization is
terminal) in the doc comment.

In `src/core/components/account-identity-proof.ts`, add two literals to the `AccountIdentityProofRejectReason` union.
Follow that union's kebab-case convention (it differs from `deferredReasons`' snake_case — do not cross them):
- `"member-identity-changed"` (D-05): the replacement leaf at an existing member's index carries a different account
  identity than the leaf it replaced. Extend the union's doc comment to record that this is deliberately NOT
  `identity-mismatch`, which means the proof's signer does not match this leaf's own credential identity — a
  single-leaf check. Conflating the two would collapse a membership-model violation into a proof-binding error.
- `"unattributable-leaf"` (D-02): a changed leaf that, with the commit's full proposal list and committer index
  available, matches no Add proposal, no Update proposal sender, and is not the committer's update-path leaf.
This module gains no new throw site — both literals are emitted by the commit-legality code in plan 09-02. Only the
union and its doc comment change here. Do not modify `AccountIdentityProofError` or any validator.

Add the two new `parentLeaf` tests to `src/core/components/__tests__/tree-diff.test.ts` inside the existing
`describe("diffChangedLeaves")` block, reusing that file's `twoPartyEpoch1Group` and `ctxFor` helpers.
  </action>
  <verify>
    <automated>pnpm vitest run src/core/components/__tests__/tree-diff.test.ts && pnpm compile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "parentLeaf" src/core/components/tree-diff.ts` outputs at least 3 (interface field, push site, doc comment)
    - `grep -c "unjudgeable_identity" src/core/inbound.ts` outputs 1, and `grep -c "unjudgeableIdentity" src/core/inbound.ts` outputs 1
    - `grep -c '"member-identity-changed"' src/core/components/account-identity-proof.ts` outputs 1
    - `grep -c '"unattributable-leaf"' src/core/components/account-identity-proof.ts` outputs 1
    - `grep -c "AccountIdentityProofError" src/core/components/tree-diff.ts` outputs 0 (tree-diff stays a pure structural diff with no validator dependency)
    - `pnpm vitest run src/core/components/__tests__/tree-diff.test.ts` passes with at least 6 tests, including both new `parentLeaf` assertions and the four pre-existing ones
    - `pnpm compile` exits 0
  </acceptance_criteria>
  <done>`ChangedLeaf` carries the prior occupant of its MLS index; the deferred-reason and reject-reason unions each carry their new literal in their own casing convention, with no validator behavior changed yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The pure D-01 three-bucket changed-leaf classifier</name>
  <files>src/core/components/leaf-replacement.ts, src/core/components/__tests__/leaf-replacement.test.ts, src/core/components/index.ts</files>
  <read_first>
    - src/core/components/tree-diff.ts (as modified by Task 1 — the `ChangedLeaf` shape the classifier consumes)
    - src/core/components/integrity.ts lines 435-466 (`validateAddProposalAccountIdentityProofs` — the ProposalWithSender normalization idiom and the `proposalType` / `"add" in proposal` narrowing pattern to mirror)
    - src/engine/admin-policy.ts lines 340-356 (the existing `isSelfUpdateOnly` check — the established way this repo compares an Update proposal's `senderLeafIndex` against a leaf index, including the `Number(...)` coercion)
    - src/core/components/bytes.ts (`bytesEqual` treats two undefined values as equal)
    - src/core/components/index.ts (barrel export style)
    - refs/mdk/crates/cgka-engine/src/account_identity_proof.rs `validate_staged_commit_account_identity_proofs` (line 539) — the three-bucket structure this mirrors: Adds, update_proposals keyed by member_id_of_sender, and update_path_leaf_node keyed by the committer
    - .planning/phases/09-self-update-replacement-leaf-identity-binding/09-CONTEXT.md decisions D-01, D-02, D-03
  </read_first>
  <behavior>
    - Test 1: a changed leaf whose signature bytes equal an Add proposal's `add.keyPackage.leafNode.signature` classifies as `add`, even when its `leafIndex` is a slot freed by a Remove proposal in the same commit (the freed-slot regression)
    - Test 2: a changed leaf whose index equals an Update proposal's `senderLeafIndex` classifies as `update-proposal` carrying that index
    - Test 3: a changed leaf whose index equals `committerLeafIndex` and matches no proposal classifies as `committer-update-path`
    - Test 4: with `committerLeafIndex` defined and proposals supplied, a changed leaf matching nothing classifies as `unattributable`
    - Test 5: with `input` undefined, any changed leaf classifies as `undecidable`
    - Test 6: with `input` supplied but `committerLeafIndex` undefined, a changed leaf matching no proposal classifies as `undecidable` (it could be the committer's own update-path leaf)
    - Test 7: a changed leaf matching an Add proposal classifies as `add` even when its index also equals `committerLeafIndex` (Add is checked first)
  </behavior>
  <action>
Create `src/core/components/leaf-replacement.ts` with the module header `/** @module @category Core - App Components */`.
It is pure `src/core`: it may import only from `ts-mls` (bare specifier) and relative `./` or `../` core modules, and
must not import from `src/engine` or `src/client`. All relative imports carry the emitted `.js` extension. Named
exports only.

Export `type ChangedLeafClassification` as a discriminated union on `kind` with exactly these five members:
`{ kind: "add" }`, `{ kind: "update-proposal"; senderLeafIndex: number }`, `{ kind: "committer-update-path" }`,
`{ kind: "unattributable" }`, `{ kind: "undecidable" }`.

Export `interface ChangedLeafClassificationInput { proposals: readonly ProposalWithSender[]; committerLeafIndex: number | undefined }`.

Export `function classifyChangedLeaf(changed: ChangedLeaf, input: ChangedLeafClassificationInput | undefined): ChangedLeafClassification`,
evaluated in this exact order:
1. If `input` is `undefined`, return `{ kind: "undecidable" }` immediately. This is the D-03 structural-impossibility
   signal used by `validateLegalityWithoutProposals`. Do NOT infer "no information" from an empty `proposals` array —
   a legitimate proposal-less self-update commit has an empty list and a defined committer, and must stay decidable.
2. Add bucket: return `{ kind: "add" }` if any item in `input.proposals` normalizes to a proposal with
   `proposalType === defaultProposalTypes.add` and an `add` property whose `add.keyPackage.leafNode.signature` is
   `bytesEqual` to `changed.leaf.signature`. Match on signature bytes, not on leaf index: a Remove+Add commit reuses
   a freed slot, so the index is worthless for this bucket and the signature is exact. This bucket performs no
   prior-identity comparison downstream — a new member in a freed slot legitimately has a different identity.
3. Update-proposal bucket: return `{ kind: "update-proposal", senderLeafIndex }` if any item has
   `proposalType === defaultProposalTypes.update`, a defined `senderLeafIndex`, and
   `Number(senderLeafIndex) === changed.leafIndex`.
4. Committer bucket: return `{ kind: "committer-update-path" }` if `input.committerLeafIndex !== undefined` and
   `input.committerLeafIndex === changed.leafIndex`.
5. Nothing matched: return `{ kind: "undecidable" }` when `input.committerLeafIndex === undefined` (the leaf could be
   the committer's own update-path leaf and we cannot tell — D-03), otherwise `{ kind: "unattributable" }` (full
   information was available and the leaf is attributable to nobody — D-02 fail closed).

Normalize each `readonly ProposalWithSender[]` item defensively the same way `validateAddProposalAccountIdentityProofs`
does, so a caller passing bare proposals cannot crash the classifier. The function is total and never throws: it reads
only `proposalType`, the narrowed payload, `senderLeafIndex`, and signature bytes.

Document in the function's doc comment: the bucket order and why Add is first; that `undecidable` means defer and
`unattributable` means reject; and the `@see` citation to
`refs/mdk/crates/cgka-engine/src/account_identity_proof.rs` `validate_staged_commit_account_identity_proofs`.

Add `export * from "./leaf-replacement.js";` to `src/core/components/index.ts`.

Write `src/core/components/__tests__/leaf-replacement.test.ts` implementing the behavior list. These are pure unit
tests over hand-built `ChangedLeaf` and `ProposalWithSender` values — no MLS group construction and no ciphersuite
are needed, because the classifier reads only proposal types, sender indexes, and signature bytes. Build minimal
`LeafNode`-shaped fixtures with distinct `signature` byte arrays and cast them as needed.
  </action>
  <verify>
    <automated>pnpm vitest run src/core/components/__tests__/leaf-replacement.test.ts && pnpm compile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function classifyChangedLeaf" src/core/components/leaf-replacement.ts` outputs 1
    - `grep -cE '"(add|update-proposal|committer-update-path|unattributable|undecidable)"' src/core/components/leaf-replacement.ts` outputs at least 5
    - `grep -c 'export \* from "./leaf-replacement.js";' src/core/components/index.ts` outputs 1
    - `grep -cE "from \"(\.\./)*(engine|client)/" src/core/components/leaf-replacement.ts` outputs 0 (purity: no engine or client import)
    - `grep -c "bytesEqual" src/core/components/leaf-replacement.ts` outputs at least 1
    - `pnpm vitest run src/core/components/__tests__/leaf-replacement.test.ts` passes with at least 7 tests, including the freed-slot Add case and both distinct `undecidable` paths (input absent, and committerLeafIndex absent)
    - `pnpm compile` exits 0
  </acceptance_criteria>
  <done>A pure, total, non-throwing core classifier sorts any changed leaf into one of five outcomes, with Add matched by signature bytes so a freed-slot reuse is never mistaken for an identity change.</done>
</task>

<task type="auto">
  <name>Task 3: Replacement-leaf test fixtures and their sanity assertions</name>
  <files>src/__tests__/helpers/account-identity-proof-fixtures.ts, src/core/components/__tests__/leaf-replacement.test.ts</files>
  <read_first>
    - src/__tests__/helpers/account-identity-proof-fixtures.ts (full file — `FORGE_CREATED_AT`, `tamperLastByte`, the `forgeKeyPackage` signature-keypair construction, `makeLeafAppComponentsExtension` usage, and the test-only module doc comment)
    - src/core/components/account-identity-proof.ts lines 148-212 (`accountIdentityProofTemplate`, `produceAccountIdentityProof` — the proof binds the MLS signature key as a signed input, so a proof produced for a different key cannot verify against this leaf)
    - src/core/components/dictionary.ts (`makeLeafAppComponentsExtension`, and how a leaf's `app_data_dictionary` extension is built)
    - src/engine/__tests__/account-identity-proof-seams.test.ts lines 879-896 (the existing fixture-sanity test — the precedent for proving a fixture fails for the intended reason and not by accident)
    - src/core/components/__tests__/tree-diff.test.ts (the `ClientState` / `ratchetTree` shape and `nodeTypes.leaf` narrowing idiom)
    - .planning/phases/09-self-update-replacement-leaf-identity-binding/09-CONTEXT.md decisions D-08, D-11
  </read_first>
  <action>
Extend `src/__tests__/helpers/account-identity-proof-fixtures.ts` with three additions. Keep the file test-only and
keep its existing exports byte-compatible — plans 09-02, 09-03, and 09-04 all consume it, as do the Phase 8 suites.

1. Widen `forgeKeyPackage`'s `proof` parameter to `"missing" | "tampered" | "stale"` and implement the `"stale"` mode.
   A stale proof is a genuinely, validly signed proof bound to a DIFFERENT MLS signature key — not a tampered one.
   Generate a second signature keypair via `ciphersuiteImpl.signature.keygen()`, call `produceAccountIdentityProof`
   with that second key as `mlsSignatureKey` (same account, same ciphersuite, same `FORGE_CREATED_AT`), and attach the
   resulting bytes unmodified to a leaf whose actual signature key is the FIRST keypair. Do not call `tamperLastByte`
   in this mode — the envelope must stay a structurally valid, correctly-signed proof. Document in the mode's doc
   comment that this models a proof carried across a signature-key rotation, and that per D-08 it is
   cryptographically indistinguishable from a corrupt proof because the 104-byte envelope stores no MLS signature key
   (the key is a signed input, recoverable only by reconstructing the event), so it correctly surfaces as
   `invalid-proof`. Leave the `"missing"` and `"tampered"` branches untouched.

2. Export `function spliceLeafAtIndex(state: ClientState, leafIndex: number, leaf: LeafNode): ClientState`. It returns
   a shallow-cloned `ClientState` whose `ratchetTree` is a copied array with the node at `leafIndex * 2` replaced by a
   leaf node carrying `leaf`. It must not mutate `state` or the input arrays. This is the D-11 fixture that makes
   UPD-01 testable at the pure-validator level: `validateCommitAccountIdentityProofs` takes two `ClientState` values,
   the tree diff compares signature bytes, and `validateLeafAccountIdentityProof` never checks the MLS leaf signature,
   so splicing another member's genuine, fully-valid leaf into a different index produces a changed leaf whose own
   proof is valid but whose account identity differs from the prior occupant's. Document that this fixture is
   deliberately NOT wire-valid and must never be fed through `processMessage`, which verifies leaf signatures and
   would reject it first — a seam test built on it would pass for the wrong reason.

3. Export `function stripLeafAccountIdentityProof(leaf: LeafNode): LeafNode`. It returns a copy of `leaf` whose
   `extensions` array has every `app_data_dictionary` entry removed, so the leaf carries no `0x8009` support or data.
   Do not mutate the input. This is the UPD-03 fixture plan 09-04 consumes.

Add a `describe("replacement-leaf fixtures (sanity)")` block to
`src/core/components/__tests__/leaf-replacement.test.ts` proving each fixture fails for its intended reason, following
the precedent at `account-identity-proof-seams.test.ts:880`:
- `forgeKeyPackage({ proof: "stale" })`: `validateLeafAccountIdentityProof` on its leaf throws an
  `AccountIdentityProofError` whose reason is `invalid-proof`, and the leaf's own `signaturePublicKey` is asserted to
  differ from the key the proof was produced for.
- `spliceLeafAtIndex`: the returned state is not the input state, the input's node at that index is unchanged, and the
  spliced node's leaf signature differs from the original occupant's — so `diffChangedLeaves` will actually report it.
- `stripLeafAccountIdentityProof`: `validateLeafAccountIdentityProof` on the stripped leaf throws, and the caught
  reason is recorded by reading `err.reason` rather than hardcoding, then asserted to be one of the two the validator
  can raise for absent support or absent data.
  </action>
  <verify>
    <automated>pnpm vitest run src/core/components/__tests__/leaf-replacement.test.ts && pnpm compile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function spliceLeafAtIndex" src/__tests__/helpers/account-identity-proof-fixtures.ts` outputs 1
    - `grep -c "export function stripLeafAccountIdentityProof" src/__tests__/helpers/account-identity-proof-fixtures.ts` outputs 1
    - `grep -c '"missing" | "tampered" | "stale"' src/__tests__/helpers/account-identity-proof-fixtures.ts` outputs 1
    - `grep -c "tamperLastByte" src/__tests__/helpers/account-identity-proof-fixtures.ts` outputs 2 (the helper definition and its single pre-existing use in the tampered branch — the stale branch must not call it)
    - `pnpm vitest run src/core/components/__tests__/leaf-replacement.test.ts` passes with at least 10 tests including the three fixture-sanity assertions
    - `pnpm vitest run src/engine/__tests__/account-identity-proof-seams.test.ts src/engine/__tests__/standalone-add-admission.test.ts` still passes (existing fixture consumers unaffected)
    - `pnpm compile` exits 0
  </acceptance_criteria>
  <done>A genuinely stale proof, a credential-substituted spliced leaf, and a dictionary-stripped leaf all exist as fixtures, each proven by a sanity test to fail for its intended reason.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Kind | Location |
|----------|------|----------|
| `ChangedLeaf.parentLeaf` | optional interface field | `src/core/components/tree-diff.ts` |
| `deferredReasons.unjudgeableIdentity` / `"unjudgeable_identity"` | const entry + derived union member | `src/core/inbound.ts` |
| `"member-identity-changed"` | `AccountIdentityProofRejectReason` literal | `src/core/components/account-identity-proof.ts` |
| `"unattributable-leaf"` | `AccountIdentityProofRejectReason` literal | `src/core/components/account-identity-proof.ts` |
| `ChangedLeafClassification` | discriminated union type | `src/core/components/leaf-replacement.ts` (new file) |
| `ChangedLeafClassificationInput` | interface | `src/core/components/leaf-replacement.ts` |
| `classifyChangedLeaf` | function | `src/core/components/leaf-replacement.ts` |
| `spliceLeafAtIndex` | test helper | `src/__tests__/helpers/account-identity-proof-fixtures.ts` |
| `stripLeafAccountIdentityProof` | test helper | `src/__tests__/helpers/account-identity-proof-fixtures.ts` |
| `forgeKeyPackage` `"stale"` mode | test helper parameter | `src/__tests__/helpers/account-identity-proof-fixtures.ts` |
| `leaf-replacement.test.ts` | test file | `src/core/components/__tests__/leaf-replacement.test.ts` (new file) |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote committer → local changed-leaf classification | A non-marmot-ts peer chooses the proposal list and update path that this classifier attributes changed leaves to |
| remote committer → freed tree slot | A Remove followed by an Add reuses a leaf index, making a legitimate new member byte-indistinguishable from an identity change at that index |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Spoofing | `classifyChangedLeaf` Add bucket | high | mitigate | The Add bucket matches on `add.keyPackage.leafNode.signature` byte equality, never on leaf index, so a freed-slot reuse is attributed to its Add proposal and an attacker cannot relabel a replacement leaf as an Add by choosing an index |
| T-09-02 | Elevation of Privilege | `classifyChangedLeaf` fallthrough | high | mitigate | With full classification input, an unmatched leaf returns `unattributable` (D-02 fail closed), removing the bypass where an attacker evades the identity check by making a leaf attributable to nobody |
| T-09-03 | Denial of Service | `undecidable` over-reporting | medium | mitigate | `undecidable` is returned only when `input` is absent, or when `committerLeafIndex` is absent and no proposal matched — never inferred from an empty proposal list, so a proposal-less self-update stays decidable and is not pooled forever |
| T-09-04 | Information Disclosure | classifier outputs | low | mitigate | `ChangedLeafClassification` carries only a `kind` and a numeric leaf index; no credential, pubkey, or signature bytes are returned or logged |
| T-09-05 | Tampering | test fixtures asserting the wrong thing | medium | mitigate | Each of the three new fixtures has a sanity test proving it fails for its intended reason; `spliceLeafAtIndex` is documented as not wire-valid so it is never fed through `processMessage` |
| T-09-06 | Tampering | npm/pip/cargo installs | high | accept | This plan adds no dependency; `package.json` is not in `files_modified`. No package-legitimacy checkpoint is required |
</threat_model>

<verification>
- `pnpm vitest run` is green and `pnpm compile` exits 0 after every task.
- No existing exported signature changed: `diffChangedLeaves`, `validateCommitAccountIdentityProofs`, and
  `validateCommitLegality` all keep their current parameter lists and return types in this plan. The tri-state is
  plan 09-02's scope.
- `src/core/components/leaf-replacement.ts` imports nothing from `src/engine` or `src/client` (dependency direction
  `utils <- core <- engine <- client`).
- The two new reject-reason literals use kebab-case and the new deferred reason uses snake_case, each matching its own
  union's existing convention.
</verification>

<success_criteria>
- The D-01 classifier exists, is pure and total, and distinguishes all five outcomes including both `undecidable` paths.
- `ChangedLeaf` exposes the prior occupant of its index.
- Three new reason literals exist in the correct casing, with `identity-mismatch` left untouched and unreused.
- Fixtures for a stale proof, a substituted-identity replacement leaf, and a stripped leaf exist and are sanity-tested.
</success_criteria>

<output>
Create `.planning/phases/09-self-update-replacement-leaf-identity-binding/09-01-SUMMARY.md` when done
</output>
