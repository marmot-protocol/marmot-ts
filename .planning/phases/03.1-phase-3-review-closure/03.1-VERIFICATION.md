---
phase: 03.1-phase-3-review-closure
verified: 2026-09-02T16:53:00Z
status: gaps_found
score: 6/7 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 5/7
  gaps_closed:
    - "Auto-commit publication now confirms or rolls back before the first cancellable yield."
    - "Failed registry activation removes and disposes the exact cached instance and permits a fresh retry."
    - "Removal realization completes internal obligations and contains throwing application listeners."
    - "Acknowledged proposal persistence failure returns a typed non-retryable outcome."
  gaps_remaining:
    - "A late GroupRegistry.get() cache hit can observe a group whose activation is pending and which may shortly be disposed."
  regressions: []
gaps:
  - truth: "Load-time removal and registry activation reach consumers only through a fully activated, durable group instance."
    status: failed
    reason: "GroupRegistry.track() inserts the group into #groups and emits updated before reconverge()/realizeRemovalIfNeeded() settle. A get() arriving in that interval returns the cache entry without awaiting #groupLoadPromises; if activation rejects, that caller retains the disposed partial instance."
    artifacts:
      - path: "src/client/group-registry.ts"
        issue: "track() exposes the instance at lines 259/271, while get() checks #groups first at line 335 and only consults #groupLoadPromises on a cache miss."
      - path: "src/__tests__/groups-manager.test.ts"
        issue: "Concurrent activation tests begin both get() calls before load installs the cache entry, so they do not exercise a late cache hit during pending activation."
    missing:
      - "Make pending activation part of the cache contract: await the in-flight activation before serving cache hits, or keep the group outside #groups until activation succeeds."
      - "Emit updated/loaded only after successful activation."
      - "Add a controlled-promise test that pauses activation after cache insertion, starts a second get(), rejects activation, and proves both callers reject without receiving the disposed instance."
---

# Phase 03.1: Phase 3 Review Closure Verification Report

**Phase Goal:** Close the findings that held Phase 3 completion so CONV-02/CONV-03 behavior is true in code rather than only claimed.
**Verified:** 2026-09-02T16:53:00Z
**Status:** gaps_found
**Re-verification:** Yes — after plans 03.1-12, 03.1-13, and 03.1-14

## Goal Achievement

### Observable Truths

| # | Roadmap truth | Status | Evidence |
|---|---|---|---|
| 1 | Load-time `removed` reaches the real consumer after registry forwarding is attached. | ✗ FAILED | The forwarder is attached before activation and normal-path tests pass, but `track()` publishes the group into `#groups` before awaited activation (`src/client/group-registry.ts:257-277`). A late `get()` returns that cache entry immediately (`:331-357`) and can retain it after activation rejects and disposes it (`:278-291`). The public contract therefore exposes a partially activated, soon-disposed group. |
| 2 | `commit` and `selfUpdate` authorize the exact by-reference proposal union using local identity. | ✓ VERIFIED | Shared authorization is wired through the outbound seams; authorization, legality-seam, and malformed-member tests pass in the independent full run. |
| 3 | Locally confirmed commits derive/record notifications and emit `historyChanged`. | ✓ VERIFIED | Local confirmation, notification attribution/withdrawal, and local-history tests pass. Plan 12 additionally moves auto-commit publication/terminal bookkeeping before the first yield (`src/client/group/marmot-group.ts:811-838`), and the cancellation/rollback integration tests pass. |
| 4 | Ledgers use digest-keyed lookup and correctness-safe tree-derived pruning, with Infinity documented honestly. | ✓ VERIFIED | Digest-keyed `Map` indexes and the `min(anchorEpoch, oldest tree epoch)` horizon remain substantive and wired; ledger/infinity suites pass. `.planning/phases/03-commit-integrity-convergence-parity/deferred-items.md` explicitly records unbounded retention pending tree pruning. |
| 5 | Tree sweeping is gated by delivery needs rather than self-eviction alone. | ✓ VERIFIED | Delivery/withdrawal wiring remains present; delivered-payload and state-notification-withdrawal suites pass. |
| 6 | The exact Phase-3-touched manifest contains no deprecated `MIP-NN` citation. | ✓ VERIFIED | Independent `rg` over the ten-file D-16 manifest returned no matches. |
| 7 | Remaining round-1 carry-forwards are fixed or explicitly accepted/deferred. | ✓ VERIFIED | Plans 07/09 and their tests close WR-03..WR-05 and WR-08..WR-13; the accepted correctness-first Infinity/tree-pruning and structural fork-recovery limitations are explicitly recorded in `deferred-items.md`. No item is silently represented as implemented. |

**Score:** 6/7 roadmap truths verified (0 present, behavior-unverified)

### Gap-Closure Plan Results

| Plan | Claimed closure | Verification |
|---|---|---|
| 03.1-12 | Cancellation-safe auto-commit publication | ✓ VERIFIED — publication and confirmation/rollback precede the `autoCommit` yield; integration tests pass. |
| 03.1-13 | Atomic activation cleanup and callback-safe removal | ✗ PARTIAL — exact-instance cleanup and throwable-listener containment work, but pending activation is externally visible to late cache hits. |
| 03.1-14 | Typed acknowledged-proposal persistence failure | ✓ VERIFIED — `#publishProposalResult` separates pre-ack rollback from post-ack typed persistence failure with `retryPublication: false`; runtime tests pass. |

### Required Artifacts and Key Links

| Artifact/link | Status | Details |
|---|---|---|
| `src/client/group-registry.ts` activation transaction | ✗ PARTIAL | Exists and is substantive, but cache visibility is earlier than activation completion; `get()` bypasses the in-flight promise on cache hit. |
| `src/client/group/marmot-group.ts` removal/cancellation flow | ✓ VERIFIED | Queued outbound is rejected before callbacks; callback exceptions are contained; auto-commit terminal work occurs before yield. |
| `src/client/runtime/group-runtime.ts` proposal outcome flow | ✓ VERIFIED | Relay ack, confirmation, typed persistence result, and no-republication flag are wired through `publishEffects`. |
| Gap-closure regression tests | ⚠ PARTIAL | Tests substantively cover the four prior blockers, but no test enters `get()` after cache insertion while activation remains pending. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Strict library compile | `PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm compile` | exit 0 | ✓ PASS |
| Complete one-shot suite | `PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm vitest run` | 80 files, 778 tests passed | ✓ PASS |
| Lockfile integrity | `sha256sum pnpm-lock.yaml` before/after commands | `0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932` | ✓ PASS |

### Requirements Coverage

| Requirement | Status | Evidence |
|---|---|---|
| CONV-02 | ✗ BLOCKED | Core self-removal and cancellation behaviors pass, but the public registry can return a pending-activation group that is later disposed, violating reliable removal/load semantics. |
| CONV-03 | ✓ SATISFIED for scoped implementation | Attribution, withdrawal, own-commit delivery, and typed post-confirm outcomes are implemented and covered; Phase 4 own-commit convergence-stamp work remains explicitly out of scope. |

### Anti-Patterns and Review Findings

| File | Line | Pattern | Severity | Impact |
|---|---:|---|---|---|
| `src/client/group-registry.ts` | 259-277, 335-357 | Cache hit bypasses pending activation | 🛑 BLOCKER | A caller may receive and retain a partially activated instance that cleanup soon disposes. |
| `src/client/group/marmot-group.ts` | 753-761 | Manual `listeners()` dispatch invokes bare functions | ⚠ WARNING | EventEmitter3 `on/once(event, fn, context)` context is lost for `removed`; `once`/context parity is untested. Internal durability remains intact, but public callback semantics differ from other events. |

No unreferenced `TBD`, `FIXME`, or `XXX` debt markers were found in the inspected production artifacts. The full green suite does not override the uncovered interleaving or listener-context analysis.

### Human Verification Required

None. The blocking race and listener-context warning are determinable from source control flow and API semantics; no visual, external-service, or experiential check is required.

### Gaps Summary

The four blockers from the prior verification are closed on their named paths. Phase 03.1 still does not achieve its closure goal because registry activation is exposed before it commits: a late caller can receive an instance that the activating caller subsequently disposes. This is a concrete failed behavior, not uncertainty, and no later roadmap phase specifically owns it. The manual safe removal dispatcher also changes EventEmitter3 context semantics and should be corrected or explicitly accepted with regression coverage.

---

_Verified: 2026-09-02T16:53:00Z_
_Verifier: the agent (gsd-verifier)_
