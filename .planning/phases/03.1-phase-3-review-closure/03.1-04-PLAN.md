---
phase: 03.1-phase-3-review-closure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/commit-authorization.ts
  - src/core/index.ts
  - src/core/group-members.ts
  - src/core/__tests__/commit-authorization.test.ts
  - src/core/__tests__/group-members.test.ts
  - src/engine/group-engine.ts
  - src/engine/__tests__/commit-authorization-seams.test.ts
autonomous: true
requirements: [CONV-02, CONV-03]
must_haves:
  truths:
    - "D-08/D-09/D-10: commit and selfUpdate reject the same unauthorized by-reference proposal union peers reject, using the local leaf identity."
    - "D-11: malformed credentials are skipped consistently by all three member enumeration helpers."
    - "Every authorization and malformed-leaf finding has a test observed failing before its production edit."
  artifacts:
    - path: src/core/commit-authorization.ts
      provides: pure shared outbound commit-authorization predicate
    - path: src/engine/__tests__/commit-authorization-seams.test.ts
      provides: both-send-seam parity regression
    - path: src/core/__tests__/group-members.test.ts
      provides: malformed-leaf coverage for all enumeration helpers
  key_links:
    - from: src/engine/group-engine.ts
      to: src/core/commit-authorization.ts
      via: thin adapters at commit and selfUpdate
      pattern: "commit|selfUpdate|authorize"
---

<objective>
Close ROADMAP criterion 2 and the malformed-member carry-forward by making outbound authorization a pure core rule shared by both authoring seams.

Purpose: Prevent marmot-ts from publishing a commit a conformant peer rejects.
Output: Core predicate, thin engine adapters, and failing-first seam/member regressions.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03.1-phase-3-review-closure/03.1-CONTEXT.md
@.planning/phases/03-commit-integrity-convergence-parity/03-REVIEW-round1.md
@refs/marmot/protocol-core/group-messaging.md
@src/engine/group-engine.ts
@src/core/group-members.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one unauthorized by-reference commit is refused at both send seams</name>
  <files>src/core/commit-authorization.ts, src/core/index.ts, src/core/__tests__/commit-authorization.test.ts, src/engine/group-engine.ts, src/engine/__tests__/commit-authorization-seams.test.ts</files>
  <read_first>src/engine/group-engine.ts; src/engine/admin-policy.ts; src/engine/types.ts; src/core/index.ts; src/engine/__tests__/commit-legality-seams.test.ts; refs/marmot/protocol-core/group-messaging.md</read_first>
  <behavior>
    - "A non-admin commit and selfUpdate carrying a foreign by-reference proposal both throw before PendingPublish."
    - "The actor is derived from state.privatePath.leafIndex and cannot be supplied falsely by the caller."
    - "The pure predicate accepts self-update-only and self_remove-only unions and rejects other non-admin unions."
  </behavior>
  <action>D-08, D-09, D-10, and D-12: first add regressions that fail against the private callback-based engine implementation. Extract a named pure predicate into src/core/commit-authorization.ts; give it only explicit actor/proposal/admin inputs and a deterministic typed decision. Make both send cases resolve the exact by-reference plus by-value union createCommit bundles, derive the actor from the local leaf, and use thin adapters that throw the documented recourse-bearing error before staging. Preserve the public SendIntent unless actorPubkey is proven unused elsewhere; do not refactor the inbound callback owned by Phase 4.</action>
  <verify><automated>test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm vitest run src/core/__tests__/commit-authorization.test.ts src/engine/__tests__/commit-authorization-seams.test.ts &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932"</automated></verify>
  <acceptance_criteria>Both seam regressions were failing-first, both reject the exact peer-visible union before staging, and the core predicate is directly unit tested.</acceptance_criteria>
  <done>ROADMAP criterion 2 and D-08/D-09/D-10 are executable and pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make malformed member credentials non-blocking everywhere</name>
  <files>src/core/group-members.ts, src/core/__tests__/group-members.test.ts, src/engine/__tests__/commit-authorization-seams.test.ts</files>
  <read_first>src/core/group-members.ts; src/core/__tests__/group-members.test.ts; src/engine/group-engine.ts</read_first>
  <behavior>
    - "getGroupMembers, getPubkeyLeafNodes, and getPubkeyLeafNodeIndexes each skip a malformed credential."
    - "A valid sibling leaf remains enumerable and outbound authorization does not brick."
  </behavior>
  <action>D-11 and D-12: add one failing-first malformed-tree fixture covering all three helpers, then wrap credential decoding consistently at the enumeration boundary. Extend the send-seam regression to show a malformed unrelated leaf cannot freeze a valid local commit. Preserve Uint8Array and named-export conventions.</action>
  <verify><automated>test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm vitest run src/core/__tests__/group-members.test.ts src/engine/__tests__/commit-authorization-seams.test.ts &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm compile &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932"</automated></verify>
  <acceptance_criteria>All three helper assertions fail pre-fix and pass after the uniform skip rule; focused tests and compile preserve the lock hash.</acceptance_criteria>
  <done>D-11 is closed without touching Phase-4 fork recovery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local API → MLS commit | Caller intent becomes peer-visible protocol state. |
| ratchet-tree credential → authorization | Potentially malformed group data informs membership and authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-07 | Elevation of privilege | outbound authorization | high | mitigate | Pure predicate uses local leaf identity and exact committed proposal union. |
| T-03.1-08 | Denial of service | member enumeration | medium | mitigate | Skip malformed credentials and retain valid siblings. |
| T-03.1-SC | Tampering | dependency state | high | mitigate | Frozen/offline pnpm with immediate lock hash guards. |
</threat_model>

<verification>Run focused core and seam tests plus compile with lock guards.</verification>
<success_criteria>Both outbound seams enforce the same pure authorization rule and malformed unrelated leaves cannot brick them.</success_criteria>

## Artifacts this phase produces

- New file: `src/core/commit-authorization.ts`.
- New tests: pure predicate and send-seam parity suites.

<output>Create `.planning/phases/03.1-phase-3-review-closure/03.1-04-SUMMARY.md` when done.</output>
