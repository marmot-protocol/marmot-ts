---
phase: 03-commit-integrity-convergence-parity
plan: 09
type: execute
wave: 2
depends_on: [03-08]
files_modified:
  - src/client/group/marmot-group.ts
  - src/client/group-registry.ts
  - src/client/runtime/group-runtime.ts
  - src/client/session/group-session.ts
  - src/engine/group-engine.ts
  - src/__tests__/groups-manager.test.ts
  - src/engine/__tests__/state-notification-withdrawal.test.ts
autonomous: true
gap_closure: true
requirements: [CONV-02, CONV-03]
must_haves:
  truths:
    - "A loaded removed group emits once through GroupsManager after its forwarding listener is attached."
    - "Confirmed local commit and selfUpdate operations expose digest-attributed notifications to consumers and record them for later rewind withdrawal."
    - "A rewind withdraws notifications from a locally confirmed commit and clears a withdrawn selfRemoved marker."
  artifacts:
    - path: "src/client/group-registry.ts"
      provides: "Listener-first load-time removal realization"
    - path: "src/engine/group-engine.ts"
      provides: "Local confirmation notification derivation and ledger recording"
    - path: "src/__tests__/groups-manager.test.ts"
      provides: "Public consumer load-time removal regression"
    - path: "src/engine/__tests__/state-notification-withdrawal.test.ts"
      provides: "Local commit/selfUpdate attribution and withdrawal regressions"
  key_links:
    - from: "src/client/group-registry.ts"
      to: "src/client/group/marmot-group.ts"
      via: "track listeners before explicit removal realization"
      pattern: "track|realize"
    - from: "src/client/runtime/group-runtime.ts"
      to: "src/engine/group-engine.ts"
      via: "confirmPublished notification return"
      pattern: "confirmPublished"
    - from: "src/engine/group-engine.ts"
      to: "src/engine/state-notifications.ts"
      via: "derive then ledger-record confirmed local changes"
      pattern: "deriveStateNotifications|record"
---

<objective>
Close the consumer-visible removal timing defect and make local commit notifications participate in delivery and rewind accounting.

Purpose: Fulfill CONV-02 and CONV-03 for restart/load and locally-authored commit paths, per D-10 through D-14.
Output: Listener-first realization, local confirmation notification propagation, and behavioral regressions.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-commit-integrity-convergence-parity/03-CONTEXT.md
@.planning/phases/03-commit-integrity-convergence-parity/03-VERIFICATION.md
@.planning/phases/03-commit-integrity-convergence-parity/03-07-SUMMARY.md
@src/engine/state-notifications.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Deliver load-time removal after public listeners attach (D-12, D-13, D-14)</name>
  <files>src/client/group/marmot-group.ts, src/client/group-registry.ts, src/__tests__/groups-manager.test.ts</files>
  <read_first>
    - src/client/group/marmot-group.ts — fromClientState and #realizeRemovalIfNeeded lifecycle
    - src/client/group-registry.ts — load/get/track ordering and removed forwarding listener
    - src/client/groups-manager.ts — registry-to-public removed event forwarding
    - src/__tests__/groups-manager.test.ts — real manager/store/network test construction
    - .planning/phases/03-commit-integrity-convergence-parity/03-CONTEXT.md — D-12 persisted-marker and exactly-once realization contract
  </read_first>
  <behavior>
    - A stored removedFromGroup state loaded through GroupsManager emits one public removed event after get/load completes.
    - Concurrent get calls for that group still produce one realization and one public event.
    - A persisted marker prevents duplicate emission on a later process load, while outbound remains blocked.
  </behavior>
  <action>
First add the failing GroupsManager-level regression using the real registry load path and removedMarkerStore. Make load construction side-effect free: remove the private realization call from `MarmotGroup.fromClientState`, expose a narrowly named idempotent method that delegates to the existing private funnel, and have `GroupRegistry` call it only after `track(loaded)` installs destroyed/removed forwarders. Apply the same listener-first ordering to any registry build/adopt path that can receive a pre-removed state. Preserve D-12 marker-before-emit semantics, D-13 classification, D-14 outbound block, concurrent-load deduplication, and exactly-once delivery across restarts.
  </action>
  <acceptance_criteria>
    - A GroupsManager-level test loads persisted removed state and observes exactly one `removed` event.
    - Listener attachment precedes explicit realization on every registry load path.
    - `MarmotGroup.fromClientState` does not emit removal before returning.
    - `pnpm vitest run src/__tests__/groups-manager.test.ts src/engine/__tests__/self-eviction.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/__tests__/groups-manager.test.ts src/engine/__tests__/self-eviction.test.ts</automated>
  </verify>
  <done>A real public consumer receives load-time removal exactly once, after forwarding listeners exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive, ledger-record, and surface local commit notifications (D-10, D-11)</name>
  <files>src/engine/group-engine.ts, src/client/session/group-session.ts, src/client/runtime/group-runtime.ts, src/client/group/marmot-group.ts, src/engine/__tests__/state-notification-withdrawal.test.ts</files>
  <read_first>
    - src/engine/group-engine.ts — PendingState, confirmPublished, #stateNotifications, #recordCommitNode, and rewind withdrawal
    - src/engine/state-notifications.ts — deriveStateNotifications and ledger API
    - src/client/session/group-session.ts — confirmPublished facade and notification result types
    - src/client/runtime/group-runtime.ts — all publish-confirmation call sites
    - src/client/group/marmot-group.ts — runtime composition and consumer event/result surface
    - src/engine/__tests__/state-notification-withdrawal.test.ts — existing inbound attribution and withdrawal construction
  </read_first>
  <behavior>
    - confirmPublished(commit) returns notifications derived from pending.parentState to pending.newState, all bearing the commit digest.
    - confirmPublished(selfUpdate) returns epochAdvanced and any membership/component differences with the same attribution rules.
    - Both kinds record their notification list in StateNotificationLedger before a future rewind can invalidate them.
    - Runtime/MarmotGroup exposes the confirmed notification list only after publish succeeds; publish failure exposes and records none.
  </behavior>
  <action>
Change the local confirmation contract to return the derived `StateNotification[]` for commit and selfUpdate. In `MarmotGroupEngine.confirmPublished`, encode/hash `pending.commitMessage` through the same commitDigest path used by inbound processing, derive against `pending.parentState` and `pending.newState`, record with the resulting epoch in `#stateNotifications`, and prune against the retained anchor alongside the existing ledger policy. Propagate the returned list through GroupSession and every GroupRuntime confirmation site to MarmotGroup's existing consumer-facing state-notification mechanism; do not fabricate an inbound `IngestResult` for a local action. Ensure publication failure never derives, records, or surfaces notifications. Add direct tests for both local kinds and consumer propagation.
  </action>
  <acceptance_criteria>
    - Local commit and selfUpdate tests assert digest-attributed notification arrays.
    - Runtime tests assert notifications surface after successful publish and not after publish failure.
    - `pnpm vitest run src/engine/__tests__/state-notification-withdrawal.test.ts src/client/runtime/__tests__/group-runtime.test.ts src/client/session/__tests__/group-session.test.ts` exits 0.
    - `pnpm compile` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/engine/__tests__/state-notification-withdrawal.test.ts src/client/runtime/__tests__/group-runtime.test.ts src/client/session/__tests__/group-session.test.ts &amp;&amp; pnpm compile</automated>
  </verify>
  <done>Successful local commit and selfUpdate confirmations produce consumer-visible, ledger-backed notifications; failed publishes do not.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove rewind withdrawal of locally confirmed changes and marker clearing</name>
  <files>src/engine/__tests__/state-notification-withdrawal.test.ts</files>
  <read_first>
    - src/engine/__tests__/state-notification-withdrawal.test.ts — real fork/rewind harness and existing marker-clear tests
    - src/engine/__tests__/convergence-parity.test.ts — local confirm plus competing-branch construction
    - src/engine/group-engine.ts — confirmPublished recording and #applyForkResolution withdrawal ordering
  </read_first>
  <behavior>
    - A real locally confirmed membership/component commit later superseded by a winning fork yields one stateInvalidated result containing that commit's notifications.
    - Winning-branch notifications are retained and stateInvalidated remains ordered before app-payload invalidation.
    - Withdrawing a locally confirmed selfRemoved notification clears the persisted marker when canonical state is live.
  </behavior>
  <action>
Extend the existing real-engine rewind suite so its abandoned branch is first advanced through `send` plus `confirmPublished`, not only inbound ingest. Assert exact digest identity and notification membership on withdrawal, winning-branch exclusion, deterministic result ordering, and D-12 marker clearing. Keep the test runtime-agnostic and use shared in-memory stores/test peelers; do not replace the real convergence path with a monkey-patched session for the local-commit withdrawal proof.
  </action>
  <acceptance_criteria>
    - At least one real rewind test withdraws a notification recorded by confirmPublished.
    - The test asserts exact commit digest and withdrawn notification kinds.
    - `pnpm vitest run src/engine/__tests__/state-notification-withdrawal.test.ts src/__tests__/groups-manager.test.ts` exits 0.
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/engine/__tests__/state-notification-withdrawal.test.ts src/__tests__/groups-manager.test.ts &amp;&amp; pnpm build</automated>
  </verify>
  <done>Locally confirmed state changes are withdrawn by a real rewind, and removal-marker state follows the canonical branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| persisted tombstone → public consumer | A durable removed state must be announced after listeners exist and only once. |
| publish confirmation → application view | Only successfully published local state changes may be announced and ledger-recorded. |
| rewind → application view | Superseded local announcements must be withdrawn by exact digest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03G-04 | Repudiation | GroupRegistry load | high | mitigate | Attach forwarding listeners before idempotent realization; test the public GroupsManager path. |
| T-03G-05 | Spoofing | local notification attribution | high | mitigate | Derive digest from the confirmed MLS commit bytes, never caller-supplied metadata. |
| T-03G-06 | Tampering | publish failure path | high | mitigate | Derive/record only inside successful confirmation; tests assert no failed-publish residue. |
| T-03G-07 | Denial of Service | notification ledger | medium | mitigate | Prune at the retained anchor after local recording, matching inbound behavior. |
</threat_model>

<verification>
Run the named GroupsManager, self-eviction, state-notification, runtime, and session suites followed by `pnpm build`.
</verification>

<success_criteria>
CONV-02 is observable through the public load path, and CONV-03 covers inbound, rewind-landed, local commit, and selfUpdate origins with exact withdrawal.
</success_criteria>

<output>
Create `.planning/phases/03-commit-integrity-convergence-parity/03-09-SUMMARY.md` when done.
</output>
