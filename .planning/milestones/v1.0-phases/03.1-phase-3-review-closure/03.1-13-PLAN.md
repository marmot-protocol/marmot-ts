---
phase: 03.1-phase-3-review-closure
plan: 13
type: execute
wave: 11
depends_on: [03.1-12]
files_modified: [src/client/group-registry.ts, src/client/group/marmot-group.ts, src/__tests__/integration/removed.test.ts, src/__tests__/groups-manager.test.ts]
autonomous: true
gap_closure: true
requirements: [CONV-02, CONV-03]
must_haves:
  truths:
    - "D-12/D-13: failed registry activation removes and disposes the exact partial instance, and a subsequent get retries hydration and activation."
    - "Both reconverge rejection and removed-marker realization rejection are covered through the public registry path."
    - "D-14: queued outbound is rejected before any user removed callback; a throwing callback cannot make the durable marker suppress later completion or observation."
  artifacts:
    - path: src/client/group-registry.ts
      provides: atomic cache activation cleanup and clean retry
    - path: src/client/group/marmot-group.ts
      provides: callback-safe durable removal realization ordering
    - path: src/__tests__/integration/removed.test.ts
      provides: marker failure and throwable-listener transaction regressions
    - path: src/__tests__/groups-manager.test.ts
      provides: public reconverge activation retry regression
  key_links:
    - from: src/client/group-registry.ts
      to: src/client/group/marmot-group.ts
      via: listener-first activation with exact-instance cleanup on rejection
      pattern: "track|reconverge|realizeRemovalIfNeeded|untrack"
    - from: src/client/group/marmot-group.ts
      to: removed marker store
      via: queued-outbound rejection and callback isolation around durable realization
      pattern: "removedMarker|rejectQueuedOutbound|emit"
---
<objective>Make registry activation and durable removal realization atomic and retry-safe across storage, convergence, and user-listener failures.</objective>
<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.planning/phases/03.1-phase-3-review-closure/03.1-CONTEXT.md
@.planning/phases/03.1-phase-3-review-closure/03.1-VERIFICATION.md
@.planning/phases/03.1-phase-3-review-closure/03.1-REVIEW.md
@.planning/phases/03.1-phase-3-review-closure/03.1-01-SUMMARY.md
@.planning/phases/03.1-phase-3-review-closure/03.1-06-SUMMARY.md
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Retry failed registry activation through the public get path</name>
  <files>src/client/group-registry.ts, src/__tests__/integration/removed.test.ts, src/__tests__/groups-manager.test.ts</files>
  <read_first>src/client/group-registry.ts; src/client/groups-manager.ts; src/__tests__/groups-manager.test.ts; src/__tests__/integration/removed.test.ts</read_first>
  <behavior>
    - "When activation-time reconverge rejects, get rejects, the exact instance is absent from the cache/listener map and disposed, and the next get loads a fresh instance and retries successfully."
    - "When removed-marker access rejects during activation, the same cleanup and fresh retry occur."
    - "Concurrent callers still share one load/activation attempt and observe the same failure or success."
  </behavior>
  <action>Per D-12, add separate failing-first public-path regressions for reconverge rejection and removed-marker realization rejection. Per D-13, make activation state atomic: if `track` activation fails after cache/listener installation, detach the listeners, remove only that exact instance from `#groups` and `#groupListeners`, dispose it, and rethrow so `#groupLoadPromises` clears and the next `get` performs a fresh load and activation. Guard cleanup by instance identity so a stale failing activation cannot evict a newer group. Preserve listener-before-reconverge-before-realize-before-loaded ordering and existing concurrent load deduplication.</action>
  <verify><automated>test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm vitest run src/__tests__/groups-manager.test.ts src/__tests__/integration/removed.test.ts &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932"</automated></verify>
  <acceptance_criteria>Both activation rejection regressions fail first, then prove exact-instance cleanup, disposal, fresh retry, listener ordering, and concurrent-load behavior.</acceptance_criteria>
  <done>A failed GroupRegistry activation never remains cached and every later get retries cleanly.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Make durable removal completion independent of throwable listeners</name>
  <files>src/client/group/marmot-group.ts, src/__tests__/integration/removed.test.ts</files>
  <read_first>src/client/group/marmot-group.ts; src/__tests__/integration/removed.test.ts; src/utils/key-value.ts</read_first>
  <behavior>
    - "Queued outbound is rejected before any removed listener runs."
    - "A listener that throws cannot turn an already-written durable marker into a rejected realization that suppresses retry semantics."
    - "A later non-throwing observer can complete or observe removal according to the chosen callback-isolation contract without duplicate durable writes."
  </behavior>
  <action>Per D-12, add a failing-first regression with a throwing `removed` listener followed by a second realization attempt and assert the complete durable/event behavior. Per D-13 and D-14, treat public listener exceptions as non-transactional: finish internal removal obligations, including queued-outbound rejection, before invoking callbacks; isolate/report listener failures so they do not reject the durable realization after the marker is committed. Preserve the existing group-scoped `${groupId}/removed` key, the instance-owned single-flight promise, restart idempotency, and marker-store failure retry. Do not replace typed ingest-result delivery with EventEmitter notifications.</action>
  <verify><automated>test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm vitest run src/__tests__/integration/removed.test.ts src/__tests__/groups-manager.test.ts &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm compile &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm vitest run &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932"</automated></verify>
  <acceptance_criteria>The throwable-listener regression fails first, then proves internal completion precedes callbacks, listener failure cannot poison durability/retry, focused and full tests pass, compile passes, and the lockfile is unchanged.</acceptance_criteria>
  <done>Durable removal realization remains complete and observable even when application listeners throw.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| persisted group → registry cache | Fallible activation must not expose or retain a partial instance. |
| library event → application callback | User callbacks are untrusted and may throw synchronously. |
| removal state → durable marker | Marker state must correspond to completed internal obligations. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-20 | Denial of service | GroupRegistry activation cache | critical | mitigate | Exact-instance cleanup/disposal and rejection/retry tests at both activation seams. |
| T-03.1-21 | Denial of service | MarmotGroup removal listeners | critical | mitigate | Complete internal obligations before callback and isolate callback exceptions. |
| T-03.1-22 | Tampering | removal marker transaction | high | mitigate | Preserve namespaced marker and prove storage failure remains retryable. |
| T-03.1-SC | Tampering | dependency state | high | mitigate | Frozen/offline pnpm commands with immediate lockfile hash guards. |
</threat_model>
<verification>Run focused registry/removal tests after each atomic task, then strict compile and the full Vitest suite with lockfile guards.</verification>
<success_criteria>Activation failure cannot poison the cache, listener failure cannot poison durable realization, and both paths have failing-first public regressions.</success_criteria>
## Source Coverage Audit
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Close all Phase 3 review blockers | 03.1-13 | COVERED | Closes both removal lifecycle blockers. |
| REQ | CONV-02 | Durable removed-inactive state and public signal | 03.1-13 | COVERED | Activation and callback exceptions are retry-safe. |
| REQ | CONV-03 | Marker consistency across convergence | 03.1-13 | COVERED | Group-scoped marker remains transactionally meaningful. |
| RESEARCH | — | Hydration then listener-first activation | 03.1-13 | COVERED | Adds atomic failure cleanup without changing ordering. |
| CONTEXT | D-12/D-13/D-14 | Failing tests, full closure, namespaced marker | 03.1-13 | COVERED | No marker migration or Phase 4 work. |
<output>Create `.planning/phases/03.1-phase-3-review-closure/03.1-13-SUMMARY.md` when done.</output>
