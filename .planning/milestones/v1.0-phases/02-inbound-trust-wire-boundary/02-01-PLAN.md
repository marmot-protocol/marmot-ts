---
phase: 02-inbound-trust-wire-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/verify.ts
  - src/client/__tests__/verify.test.ts
  - src/utils/tag-cardinality.ts
  - src/utils/__tests__/tag-cardinality.test.ts
  - src/utils/timestamp.ts
  - src/utils/__tests__/timestamp.test.ts
  - src/core/key-package.ts
  - src/core/__tests__/key-package.test.ts
autonomous: true
requirements: [SEC-01, WIRE-01, WIRE-02]
must_haves:
  truths:
    - "createDefaultKeyPackageLifetime() returns a Lifetime whose (notAfter - notBefore) is exactly 7257600n and whose notBefore is now-3600n (D-07)"
    - "getSingletonTagValue rejects (returns undefined) when a required tag is absent, repeated, empty, or carries more than one value (D-10/D-11)"
    - "getListTag rejects (returns undefined) when a required list tag is absent, repeated, empty, or contains duplicate values (D-10/D-11)"
    - "RejectReason is the string-literal union 'invalid-signature' | 'lifetime-cap' | 'tag-cardinality' (D-05)"
    - "isLifetimeWithinCap returns false when (notAfter - notBefore) > 7261200n (D-08 cap)"
    - "isLifetimeCurrentWithGrace returns false only when now < notBefore-3600n or now > notAfter+3600n (D-08 symmetric ~1h grace)"
    - "generateKeyPackage rejects (throws) when passed an explicit lifetime override whose (notAfter - notBefore) exceeds 7261200n, so the WIRE-01 cap holds regardless of how lifetime is supplied (D-09 produce path)"
  artifacts:
    - src/client/verify.ts
    - src/utils/tag-cardinality.ts
    - src/utils/timestamp.ts (modified)
  key_links:
    - "verify.ts re-exports applesauce VerifyEventMethod/verifyEvent/fakeVerifyEvent as the injectable default (D-02/D-03/D-04)"
    - "tag-cardinality.ts sits beside getTagValue in src/utils/nostr.ts without modifying it (D-10)"
  prohibitions:
    - "getTagValue in src/utils/nostr.ts is NOT modified (D-10)"
    - "VerifyEventMethod is NOT widened to Promise<boolean> or any async form (D-02)"
    - "No boolean skip-verification flag is introduced; trust-upstream is expressed only via fakeVerifyEvent (D-04)"
    - "The lifetime cap is NOT delegated to ts-mls LifetimeConfig.maximumTotalLifetime (dead code in rc.14)"
---

<objective>
Build the three shared trust-boundary primitives that every inbound entry point in this
phase consumes, plus fix the produce-side KeyPackage lifetime cap.

Purpose: Isolate the pure, dependency-free logic (verifier defaults + reject taxonomy,
table-driven tag-cardinality validator, lifetime cap/grace helpers) so the three
entry-point wiring plans (Wave 2/3) only wire, never reinvent. Closes the WIRE-01
produce side outright.
Output: `src/client/verify.ts` (NEW), `src/utils/tag-cardinality.ts` (NEW), a
capped/backdated/renamed `src/utils/timestamp.ts`, and a `src/core/key-package.ts`
guarded so the cap holds on the explicit-`lifetime`-override path too, each with
colocated unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-inbound-trust-wire-boundary/02-CONTEXT.md
@.planning/phases/02-inbound-trust-wire-boundary/02-RESEARCH.md
@.planning/phases/02-inbound-trust-wire-boundary/02-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols (source-grounding pass must exclude these from drift verification):

- `RejectReason` — string-literal union `'invalid-signature' | 'lifetime-cap' | 'tag-cardinality'` (`src/client/verify.ts`)
- `defaultVerifyEvent` — the injectable default `VerifyEventMethod` (re-export of applesauce `verifyEvent`) (`src/client/verify.ts`)
- Re-exports of `VerifyEventMethod`, `verifyEvent`, `fakeVerifyEvent`, `VerifiedEvent` from `src/client/verify.ts`
- `getSingletonTagValue(event, name): string | undefined` (`src/utils/tag-cardinality.ts`)
- `getListTag(event, name): string[] | undefined` (`src/utils/tag-cardinality.ts`)
- `TAG_CARDINALITY` — table-driven `(kind, tagName) → "singleton" | "list"` data map (`src/utils/tag-cardinality.ts`)
- `createDefaultKeyPackageLifetime()` — renamed from `createThreeMonthLifetime` (`src/utils/timestamp.ts`)
- `createThreeMonthLifetime` — retained as a `@deprecated` alias re-export of `createDefaultKeyPackageLifetime` (`src/utils/timestamp.ts`)
- `isLifetimeWithinCap(lifetime): boolean` (`src/utils/timestamp.ts`)
- `isLifetimeCurrentWithGrace(lifetime): boolean` (`src/utils/timestamp.ts`)
- New file paths: `src/client/verify.ts`, `src/utils/tag-cardinality.ts`
  </artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/client/verify.ts — RejectReason taxonomy + injectable verifier defaults</name>
  <files>src/client/verify.ts, src/client/__tests__/verify.test.ts</files>
  <read_first>
    - src/engine/types.ts (lines 100-113 — SkippedIngestResult.reason string-literal-union style to mirror)
    - .planning/phases/02-inbound-trust-wire-boundary/02-PATTERNS.md (section "src/client/verify.ts (NEW)" — import convention + reason-taxonomy shape)
    - .planning/phases/02-inbound-trust-wire-boundary/02-RESEARCH.md (Pattern 1 — applesauce VerifyEventMethod contract; Open Question 3 — why verify.ts is its own module)
  </read_first>
  <behavior>
    - RejectReason accepts exactly the three literals and no others (compile-time union).
    - defaultVerifyEvent is referentially the applesauce verifyEvent function.
    - fakeVerifyEvent is re-exported and, when applied, marks an event verified without a real check.
  </behavior>
  <action>
    Create `src/client/verify.ts` (named exports only, module doc comment `/** @module @category Client - Verify */`).
    Import type `VerifyEventMethod`, `VerifiedEvent` and values `verifyEvent`, `fakeVerifyEvent` from `applesauce-core/helpers` (NOT from `nostr-tools` directly — go through applesauce per D-02).
    Export a string-literal union type `RejectReason = "invalid-signature" | "lifetime-cap" | "tag-cardinality"` (D-05 taxonomy exactly; a bare union, no `kind` discriminant — it is a manager emit-payload field, not an IngestResult variant per D-06).
    Re-export the applesauce symbols so all three managers import the verifier surface from one home: `export type { VerifyEventMethod, VerifiedEvent }` and `export { verifyEvent, fakeVerifyEvent }`.
    Export `const defaultVerifyEvent: VerifyEventMethod = verifyEvent` as the pluggable default (D-03). Do NOT define any async wrapper and do NOT add a skip-verification boolean (D-02/D-04).
  </action>
  <verify>
    <automated>pnpm vitest run src/client/__tests__/verify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/client/__tests__/verify.test.ts` asserts `defaultVerifyEvent === verifyEvent` (imported from `applesauce-core/helpers`) and passes.
    - A type-level test (or a `satisfies`/assignment) confirms `RejectReason` accepts `"invalid-signature"`, `"lifetime-cap"`, `"tag-cardinality"` and rejects any other string.
    - `grep -v '^\s*\*' src/client/verify.ts | grep -c 'Promise<' ` returns 0 (verifier is never widened to async).
    - `pnpm compile` exits 0 (no default export; `.js` import extensions present).
  </acceptance_criteria>
  <done>verify.ts exports RejectReason + defaultVerifyEvent + the re-exported applesauce verifier surface; test asserts default identity and taxonomy; compiles clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/utils/tag-cardinality.ts — #236 table-driven validator + strict getters</name>
  <files>src/utils/tag-cardinality.ts, src/utils/__tests__/tag-cardinality.test.ts</files>
  <read_first>
    - src/utils/nostr.ts (lines 1-15 — getTagValue signature/style the strict getters sit beside; do NOT modify it)
    - src/core/protocol.ts (tag-name constants: KEY_PACKAGE_MLS_VERSION_TAG="mls_protocol_version", KEY_PACKAGE_CIPHER_SUITE_TAG, KEY_PACKAGE_EXTENSIONS_TAG, KEY_PACKAGE_PROPOSALS_TAG, KEY_PACKAGE_APP_COMPONENTS_TAG, KEY_PACKAGE_RELAYS_TAG, GROUP_EVENT_KIND=445, WELCOME_EVENT_KIND=444, ADDRESSABLE_KEY_PACKAGE_KIND=30443)
    - .planning/phases/02-inbound-trust-wire-boundary/02-RESEARCH.md (Pattern 2 — the verbatim #236 cardinality table cited from refs/marmot/transports/nostr.md lines 63-80)
    - .planning/phases/02-inbound-trust-wire-boundary/02-PATTERNS.md (section "src/utils/tag-cardinality.ts (NEW)")
  </read_first>
  <behavior>
    - getSingletonTagValue(event, "h") returns the value when exactly one `h` tag with exactly one value exists.
    - getSingletonTagValue returns undefined when the tag is absent, appears twice, has no value, or has extra values.
    - getListTag(event, "relays") returns all values when exactly one non-empty `relays` tag with distinct values exists.
    - getListTag returns undefined when the tag is absent, repeated, empty, or contains duplicate values.
    - TAG_CARDINALITY maps (445,"h")→singleton, (1059,"p")→singleton, (444,"e")→singleton, (444,"relays")→list, (30443,"d")→singleton, (30443,"i")→singleton, (30443,"mls_protocol_version")→singleton, (30443,"mls_ciphersuite"/"mls_extensions"/"mls_proposals"/"app_components")→list.
  </behavior>
  <action>
    Create `src/utils/tag-cardinality.ts` (named exports, module doc comment). Import `NostrEvent` from `applesauce-core/helpers/event` (same as nostr.ts).
    Encode the #236 cardinality table as an exported data map `TAG_CARDINALITY` keyed by kind then tag-name to the literal `"singleton" | "list"`, matching D-11 exactly (use the `src/core/protocol.ts` tag-name constants, kind 1059 via `kinds.GiftWrap` or the literal 1059). Model it as an exported enum-like object namespace consistent with `groupLifecycleStates`/`convergenceStatuses` project convention.
    Export `getSingletonTagValue(event: NostrEvent, name: string): string | undefined` — returns `tags` entry value ONLY when: exactly one tag has `t[0] === name`, that tag has exactly one value slot (`t.length === 2`), and the value is a non-empty string; otherwise return `undefined` (the reject signal for a required singleton).
    Export `getListTag(event: NostrEvent, name: string): string[] | undefined` — returns `t.slice(1)` ONLY when: exactly one tag has `t[0] === name`, it has at least one value, and there are no duplicate values (`new Set(values).size === values.length`, no empty values); otherwise return `undefined`.
    Never throw — return `undefined` on any violation (project rule: reject-via-typed-result for inbound flows). Leave `getTagValue` in `src/utils/nostr.ts` byte-for-byte untouched (D-10).
  </action>
  <verify>
    <automated>pnpm vitest run src/utils/__tests__/tag-cardinality.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts getSingletonTagValue returns the value for a single `["h","abc"]` tag and `undefined` for: absent, two `h` tags, `["h"]` (no value), `["h","a","b"]` (extra value), `["h",""]` (empty).
    - Test asserts getListTag returns `["r1","r2"]` for `["relays","r1","r2"]` and `undefined` for: absent, two `relays` tags, `["relays"]` (empty), `["relays","r1","r1"]` (duplicate).
    - Test asserts TAG_CARDINALITY entries for every (kind,tag) pair in D-11 resolve to the expected `"singleton"`/`"list"` value.
    - `pnpm vitest run src/utils/__tests__/tag-cardinality.test.ts` exits 0.
  </acceptance_criteria>
  <done>Strict getters and the #236 table exist, never throw, and reject every cardinality violation; getTagValue unchanged; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cap + backdate + rename the produce-side KeyPackage lifetime; guard explicit-override; add cap/grace check helpers</name>
  <files>src/utils/timestamp.ts, src/utils/__tests__/timestamp.test.ts, src/core/key-package.ts, src/core/__tests__/key-package.test.ts</files>
  <read_first>
    - src/utils/timestamp.ts (lines 37-64 — isLifetimeValid pattern + createThreeMonthLifetime being fixed)
    - src/core/key-package.ts (lines 61-111 — GenerateKeyPackageOptions.lifetime override + resolvedLifetime call site at ~L105 that must adopt the new name and gain the cap guard)
    - .planning/phases/02-inbound-trust-wire-boundary/02-CONTEXT.md (D-07 produce side, D-08 inbound side, D-09 produce path, Claude's Discretion on helper rename)
    - .planning/phases/02-inbound-trust-wire-boundary/02-RESEARCH.md (Pitfall 3 — ts-mls maximumTotalLifetime is dead code; the cap MUST live in marmot-ts; Open Question 2 — the explicit-override latent gap; Code Examples "Produce-side lifetime")
  </read_first>
  <behavior>
    - createDefaultKeyPackageLifetime() sets notBefore = now-3600n and notAfter = notBefore + 7257600n so (notAfter - notBefore) === 7257600n (D-07).
    - isLifetimeWithinCap returns true when (notAfter - notBefore) <= 7261200n, false when it exceeds the cap (D-08).
    - isLifetimeCurrentWithGrace returns true when now is within [notBefore-3600, notAfter+3600], false outside (D-08).
    - createThreeMonthLifetime still resolves (deprecated alias) and produces the identical capped lifetime.
    - generateKeyPackage with no lifetime argument still succeeds (default is within cap).
    - generateKeyPackage passed an explicit over-cap lifetime (notAfter - notBefore > 7261200n) throws before producing a KeyPackage; passed an at-cap or under-cap explicit lifetime it succeeds.
  </behavior>
  <action>
    In `src/utils/timestamp.ts`: rename `createThreeMonthLifetime` to `createDefaultKeyPackageLifetime()` returning `Lifetime` where `const now = BigInt(Math.floor(Date.now()/1000)); const notBefore = now - 3600n; const notAfter = notBefore + 7257600n;` (84 days; D-07). Keep `createThreeMonthLifetime` as a `/** @deprecated Use {@link createDefaultKeyPackageLifetime} instead. */` alias that returns `createDefaultKeyPackageLifetime()` so downstream callers do not break (Claude's Discretion in CONTEXT).
    Add `isLifetimeWithinCap(lifetime: Lifetime): boolean` returning `(lifetime.notAfter - lifetime.notBefore) <= 7261200n` (the 84-day-plus-1h cap; D-08). Reference the cap as a named `const MAX_KEY_PACKAGE_LIFETIME_SECONDS = 7261200n` and the grace as `const LIFETIME_GRACE_SECONDS = 3600n`.
    Add `isLifetimeCurrentWithGrace(lifetime: Lifetime): boolean` returning `now >= lifetime.notBefore - LIFETIME_GRACE_SECONDS && now <= lifetime.notAfter + LIFETIME_GRACE_SECONDS` (symmetric ~1h grace; D-08). Follow the existing `isLifetimeValid` BigInt-Unix-seconds style; leave `isLifetimeValid` and `formatMlsTimestamp` in place.
    In `src/core/key-package.ts`: make the 84-day cap a true produce-side invariant regardless of how lifetime is supplied (D-09 produce path; RESEARCH Open Question 2). Update the `resolvedLifetime` call site (~L105) to call the renamed `createDefaultKeyPackageLifetime()` (the deprecated alias also resolves, but the produce path should use the accurate name), and immediately after resolving, guard the explicit-override path: import `isLifetimeWithinCap` from `../utils/timestamp.js` and, when the caller passed an explicit `lifetime`, reject an over-cap value with `throw new Error(...)` before any KeyPackage is built (domain-validation-failure convention; D-08 strict cap). Checking `resolvedLifetime` unconditionally is acceptable since the default is always within cap.
    D-09 loop-closure: D-09 also names `defaultLifetimeConfig` as a produce touchpoint, but it is intentionally NOT modified — its `maximumTotalLifetime` is dead code in ts-mls rc.14 (Pitfall 3, and `prohibitions` below), so the marmot-ts cap is enforced in `createDefaultKeyPackageLifetime()` + this `generateKeyPackage` guard rather than delegated to ts-mls config. This sub-item was assessed and deliberately skipped.
    Do NOT touch or rely on ts-mls `LifetimeConfig.maximumTotalLifetime` (Pitfall 3).
  </action>
  <verify>
    <automated>pnpm vitest run src/utils/__tests__/timestamp.test.ts src/core/__tests__/key-package.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts `createDefaultKeyPackageLifetime()` yields `notAfter - notBefore === 7257600n` and `notBefore === BigInt(Math.floor(Date.now()/1000)) - 3600n` (within a ±2s tolerance on the now-read).
    - Test asserts `isLifetimeWithinCap` returns true at range 7261200n, true at 7257600n, and false at 7261201n.
    - Test asserts `isLifetimeCurrentWithGrace` returns false for a lifetime whose notAfter is now-7200 (expired beyond grace) and true for one whose notBefore is now-1800 (inside grace).
    - Test asserts the deprecated `createThreeMonthLifetime()` returns a lifetime with `notAfter - notBefore === 7257600n` (alias parity).
    - `src/core/__tests__/key-package.test.ts` asserts `generateKeyPackage` passed an explicit `lifetime` with `notAfter - notBefore === 7261201n` rejects (`await expect(...).rejects.toThrow(...)`), and that an explicit at-cap `lifetime` (7261200n) and the no-lifetime default path both resolve to a `CompleteKeyPackage`.
    - `grep -v '^\s*\*' src/utils/timestamp.ts | grep -c '90n \* 24n' ` returns 0 (the old 90-day value is gone).
    - `pnpm vitest run src/utils/__tests__/timestamp.test.ts src/core/__tests__/key-package.test.ts` exits 0.
  </acceptance_criteria>
  <done>Produce-side lifetime is 84 days with a backdated notBefore, renamed with a deprecated alias; the explicit-`lifetime` override is capped in `generateKeyPackage`; and the inbound cap/grace check helpers exist and are tested. WIRE-01 produce side closed on both the default and explicit-override paths.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                                          |
| -------------------------------- | ---------------------------------------------------------------------------------------------------- |
| relay/caller → marmot-ts inbound | Untrusted Nostr events (445/1059/30443) and their tags/lifetime fields cross here before any is read |
| marmot-ts → peer (produce side)  | KeyPackages this client publishes must conform to the ≤84-day cap or a conformant peer rejects them  |

## STRIDE Threat Register

| Threat ID        | Category               | Component                                                                                                  | Severity | Disposition | Mitigation Plan                                                                                                                                                                                                                                                               |
| ---------------- | ---------------------- | ---------------------------------------------------------------------------------------------------------- | -------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-EoP         | Elevation of Privilege | createDefaultKeyPackageLifetime + generateKeyPackage (`src/utils/timestamp.ts`, `src/core/key-package.ts`) | high     | mitigate    | Cap produced lifetime range at 7257600s (≤ 7261200s cap) so a leaked KeyPackage key has a bounded exposure window; remove the 90-day value; also reject an over-cap explicit `lifetime` override in `generateKeyPackage` so the cap holds regardless of caller path (WIRE-01) |
| T-02-TAMPER-PRIM | Tampering              | getSingletonTagValue/getListTag (`src/utils/tag-cardinality.ts`)                                           | high     | mitigate    | Strict getters return undefined (reject) on repeated/empty/duplicate required tags, so no first-match smuggling primitive reaches consumers (WIRE-02)                                                                                                                         |
| T-02-SPOOF-PRIM  | Spoofing               | defaultVerifyEvent (`src/client/verify.ts`)                                                                | high     | mitigate    | Provide the applesauce verifier as the injectable default so entry-point plans can gate before trust; trust-upstream only via fakeVerifyEvent, never a skip flag (SEC-01)                                                                                                     |
| T-02-SC          | Tampering              | npm/pip/cargo installs                                                                                     | low      | accept      | No new external packages introduced this phase (RESEARCH Package Legitimacy Audit: not applicable); nothing to install                                                                                                                                                        |
| </threat_model>  |

<verification>
- `pnpm vitest run src/client/__tests__/verify.test.ts src/utils/__tests__/tag-cardinality.test.ts src/utils/__tests__/timestamp.test.ts` exits 0.
- `pnpm compile` exits 0 (strict TS: no unused locals, `.js` extensions, named exports).
- `pnpm lint` (prettier --check) passes on the new/modified files.
- `grep -c 'export default' src/client/verify.ts src/utils/tag-cardinality.ts` returns 0 for each (named exports only).
</verification>

<success_criteria>

- RejectReason, the strict tag getters + #236 table, and the capped/backdated lifetime helpers all exist with passing colocated unit tests.
- WIRE-01 produce side is closed: no code path emits a >84-day KeyPackage lifetime — the default path is capped and an over-cap explicit `lifetime` override to `generateKeyPackage` is rejected.
- getTagValue is unchanged; no async verifier; no skip-verification flag.
  </success_criteria>

<output>
Create `.planning/phases/02-inbound-trust-wire-boundary/02-01-SUMMARY.md` when done.
</output>
