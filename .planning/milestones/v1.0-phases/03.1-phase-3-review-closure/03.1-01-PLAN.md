---
phase: 03.1-phase-3-review-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/group-registry.ts
  - src/client/group/marmot-group.ts
  - src/__tests__/integration/removed.test.ts
autonomous: true
requirements: [CONV-02]
must_haves:
  truths:
    - "CR-01: loading a persisted multi-tip group that reconverges to removal attaches registry listeners before removal realization and emits exactly one public GroupsManager removed event."
    - "WR-01: concurrent removal-realization calls share one in-flight operation, write one durable marker, and emit one removed event."
    - "The already-updated refs remain pinned at Marmot 4a2bc65 and MDK 908780b3 from isolated commit 73ab46a; this plan does not update them again."
  artifacts:
    - path: "src/client/group-registry.ts"
      provides: "listener-first hydrate/track/reconverge/load ordering"
    - path: "src/client/group/marmot-group.ts"
      provides: "same-instance single-flight removal realization"
    - path: "src/__tests__/integration/removed.test.ts"
      provides: "persisted multi-tip public-event and concurrent-realization regressions"
  key_links:
    - from: "src/client/group-registry.ts"
      to: "src/client/group/marmot-group.ts"
      via: "track installs removed forwarder before reconverge can realize removal"
      pattern: "track|reconverge|realizeRemovalIfNeeded"
    - from: "src/client/group/marmot-group.ts"
      to: "removedMarkerStore"
      via: "one cached Promise spans marker read, marker write, queue rejection, and event emission"
      pattern: "realizeRemovalIfNeeded"
  prohibitions:
    - "Do not port OwnCommitConvergenceStamp or modify fork-recovery behavior; that defect class belongs to Phase 4."
    - "Do not replace the real GroupsManager event assertion with an EventEmitter prototype spy."
    - "Do not mutate pnpm-lock.yaml or run an unfrozen dependency install."
---

<objective>
Close CR-01 and WR-01 through the real persisted removal path.

Purpose: A removal reached during load must be publicly observable exactly once, including when load-time reconvergence and concurrent realization race.
Output: Reference-aligned listener-first registry orchestration, single-flight realization, and failing-first integration coverage.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.1-phase-3-review-closure/03.1-CONTEXT.md
@.planning/phases/03.1-phase-3-review-closure/03.1-RESEARCH.md
@.planning/phases/03.1-phase-3-review-closure/03.1-PATTERNS.md
@.planning/phases/03-commit-integrity-convergence-parity/03-REVIEW.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reference-align and prove listener-first persisted multi-tip removal end to end</name>
  <files>src/client/group-registry.ts, src/__tests__/integration/removed.test.ts</files>
  <read_first>AGENTS.md; .planning/phases/03.1-phase-3-review-closure/03.1-RESEARCH.md; .planning/phases/03-commit-integrity-convergence-parity/03-REVIEW.md; src/client/group-registry.ts; src/client/group/marmot-group.ts; src/__tests__/integration/removed.test.ts; src/__tests__/integration/rewind-persistence.test.ts</read_first>
  <behavior>
    - "A saved history with two tips reconverges to removedFromGroup only after the GroupsManager/GroupRegistry removed listener is attached."
    - "The public manager observes exactly one removed event and the durable removal marker is set."
    - "The loaded event occurs after reconvergence/removal realization completes."
  </behavior>
  <action>D-12: first assert `git -C refs/marmot rev-parse HEAD` is `4a2bc65f8db5866cec3b2a127dedb37818eaf207` and `git -C refs/mdk rev-parse HEAD` is `908780b383046ac56b84b9e74b2e8c33389c1e9b`, and verify commit `73ab46a` is the isolated pointer bump; do not fetch, fast-forward, or recommit either ref. Write the CR-01 regression so it fails against current source using a real persisted competing-tip fixture and the public GroupsManager removed event. Per the resolved upstream contract, keep `GroupRegistry.load()` hydration-only: it reads stores and builds the group but performs no reconvergence or removal realization. Registry activation must install cache/listeners first, then await reconvergence when hydrated history has multiple tips, await realization, emit `loaded`, and resolve `get()`. Keep adopt/create behind the same listener-first tracking boundary. Remove the prototype-wide spy from this scenario. Never alter pnpm-lock.yaml.</action>
  <verify>
    <automated>test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm vitest run src/__tests__/integration/removed.test.ts &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932"</automated>
  </verify>
  <acceptance_criteria>Failing-first evidence is recorded; hydration-only load has no state-derived effects; activation reaches removed state only after listeners attach; the manager receives exactly one event; the marker exists; loaded follows realization; pinned refs and lockfile are unchanged.</acceptance_criteria>
  <done>CR-01 is pinned through the persisted public load path and the bounded reference inspection is recorded without lockfile drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make public removal realization single-flight</name>
  <files>src/client/group/marmot-group.ts, src/__tests__/integration/removed.test.ts</files>
  <read_first>src/client/group/marmot-group.ts; src/client/group/group-media-service.ts; src/client/group/__tests__/group-media-service.test.ts; src/__tests__/integration/removed.test.ts</read_first>
  <behavior>
    - "Two overlapping realizeRemovalIfNeeded calls return the same in-progress outcome."
    - "An overlapping marker read results in one getItem, one setItem, and one removed emission."
    - "A failed realization clears the in-flight slot so a later call can retry."
  </behavior>
  <action>D-12 and the still-current WR-09 portion of D-13: add the failing WR-01 concurrency regression using a deferred marker-store read before production changes. Add an instance-owned in-flight Promise around the complete private realization transaction, installing it before the first await and returning it to every concurrent caller. Clear the cached Promise after completion; on rejection it must also clear so retry is possible. Keep durable-marker restart idempotency and the state guard intact, and keep marker write, queued-outbound rejection, and removed emission inside the single-flight transaction.</action>
  <verify>
    <automated>test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm vitest run src/__tests__/integration/removed.test.ts &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm compile &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932"</automated>
  </verify>
  <acceptance_criteria>The concurrency regression was observed failing before the fix and now proves one marker transaction and one event; retry after injected failure succeeds; focused tests and strict compile pass without lockfile drift.</acceptance_criteria>
  <done>WR-01 is pinned by overlapping calls that produce one durable transaction and one public event, with failure retry preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted stores → live registry | Untrusted/stale serialized state and fork tips can cause lifecycle transitions while public listeners are being installed. |
| concurrent callers → removal realization | Multiple asynchronous callers can race the durable marker transaction and duplicate externally visible effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-01 | Tampering | GroupRegistry load ordering | high | mitigate | Track the group and attach forwarding listeners before reconvergence consumes durable removal state; assert with a persisted multi-tip fixture. |
| T-03.1-02 | Denial of Service | MarmotGroup removal realization | medium | mitigate | Serialize same-instance realization with one in-flight Promise and permit retry after failure. |
| T-03.1-SC | Tampering | pnpm dependency state | high | mitigate | No package install is planned; frozen/offline pnpm commands are surrounded by the authoritative lockfile hash guard. |
</threat_model>

<verification>
Run the focused removed integration suite, then strict compile, with the authoritative lockfile hash checked before and after every pnpm command.
</verification>

<success_criteria>
CR-01 and WR-01 each have failing-first regression evidence and pass through the real public path; references are inspected/updated only as warranted; no Phase 4 convergence work is pulled in.
</success_criteria>

## Artifacts this phase produces

- New symbols: one private in-flight removal-realization field (exact identifier chosen during implementation).
- New files: None.
- Modified public contracts: None.

<output>
Create `.planning/phases/03.1-phase-3-review-closure/03.1-01-SUMMARY.md` when done.
</output>
