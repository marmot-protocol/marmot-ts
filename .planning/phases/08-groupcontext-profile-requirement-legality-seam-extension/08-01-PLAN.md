---
phase: 08-groupcontext-profile-requirement-legality-seam-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/components/tree-diff.ts
  - src/core/components/__tests__/tree-diff.test.ts
  - src/core/components/index.ts
  - src/__tests__/helpers/account-identity-proof-fixtures.ts
  - src/core/components/account-identity-proof.ts
  - src/core/components/__tests__/account-identity-proof.test.ts
  - src/core/components/integrity.ts
  - src/core/components/__tests__/integrity.test.ts
  - src/engine/types.ts
  - src/__tests__/exports.test.ts
  - src/core/__tests__/group.test.ts
  - src/client/__tests__/join-account-identity-proof.test.ts
autonomous: true
requirements: [GRP-01, GRP-02, GRP-03]
tags: [mls, account-identity-proof, commit-legality, app-components]

must_haves:
  truths:
    - "D-01: validateCommitLegality returns reason account-identity-proof when the parent or resulting GroupContext does not classify as the current 0x8009 profile (a commit that drops the 0x8009 requirement or adds 0xf2f1 fails)"
    - "D-01/D-02: validateCommitLegality validates the 0x8009 proof of every non-blank leaf that is new or re-signed between the parent and resulting ratchet trees; unchanged leaves are not re-verified"
    - "D-02: blanked (removed) leaves are never validated"
    - "D-03: a changed Update or update-path leaf is checked for proof validity only (support, data, signer = credential identity, ciphersuite/scheme, signature key, signature) with no comparison against the member's prior leaf"
    - "D-05: CommitIntegrityViolationReason and the engine RejectedIngestResult.reason union both include account-identity-proof"
    - "D-06: an account-identity-proof violation carries proofReason (and leafIndex, the true MLS leaf index, for leaf failures) with a pubkey-free detail, and validateCommitLegality never throws"
    - "D-07: inside validateCommitLegality the order is component-integrity, then profile/proof, then disband-legality, then admin-leaf coupling; 0x8009 GroupContext data still reports component-integrity"
    - "A pure helper validates every Add proposal's KeyPackage proof pre-apply and returns the same structured account-identity-proof violation (consumed by plan 08-02 per D-08/D-09)"
    - "GRP-01: a newly created group requires 0x8009 in its app_components list, not in required_capabilities, and carries no 0x8009 GroupContext data"
    - "GRP-03: joining via Welcome fails and persists nothing when the group does not require 0x8009 or when any current member's proof is invalid"
  artifacts:
    - path: src/core/components/tree-diff.ts
      provides: "ChangedLeaf, diffChangedLeaves: pure changed-leaf detection over two ratchet trees"
      contains: "export function diffChangedLeaves"
    - path: src/core/components/integrity.ts
      provides: "validateCommitAccountIdentityProofs, validateAddProposalAccountIdentityProofs, extended validateCommitLegality"
      contains: "account-identity-proof"
    - path: src/core/components/account-identity-proof.ts
      provides: "GroupProfileSupport, getGroupProfileSupport (non-throwing profile classification)"
      contains: "export function getGroupProfileSupport"
    - path: src/__tests__/helpers/account-identity-proof-fixtures.ts
      provides: "forgeKeyPackage, dropAccountIdentityProofRequirement test fixtures reused by plans 08-02..08-04"
      contains: "export async function forgeKeyPackage"
    - path: src/engine/types.ts
      provides: "RejectedIngestResult reason literal account-identity-proof plus optional proofReason/leafIndex"
      contains: "account-identity-proof"
  key_links:
    - from: src/core/components/integrity.ts
      to: src/core/components/tree-diff.ts
      via: "validateCommitAccountIdentityProofs calls diffChangedLeaves on parent/resulting ratchet trees"
      pattern: 'diffChangedLeaves\('
    - from: src/core/components/integrity.ts
      to: src/core/components/account-identity-proof.ts
      via: "getGroupProfileSupport for D-01a, validateLeafAccountIdentityProof for D-01b, validateKeyPackageAccountIdentityProof for Add proposals"
      pattern: 'getGroupProfileSupport\(|validateLeafAccountIdentityProof\(|validateKeyPackageAccountIdentityProof\('
    - from: "validateCommitLegality"
      to: "validateCommitAccountIdentityProofs"
      via: "called between validateAppComponentIntegrity and classifyDisbandCommit (D-07)"
      pattern: 'validateCommitAccountIdentityProofs\('
---

<objective>
Extend the single shared commit-legality adapter so every commit-processing seam enforces the `0x8009`
GroupContext profile and valid `0x8009` proofs on changed leaves (D-01, D-02, D-03, D-05, D-06, D-07). Add the
pure Add-proposal proof helper that plan 08-02 wires into the pre-apply seams (D-08/D-09), plus shared
forged-fixture helpers. Pin GRP-01 (creation) and GRP-03 (Welcome join) with explicit tests (both were
implemented in Phase 7; Phase 8 owns their tests).

Purpose: send, inbound ingest, pool replay, and tree-fed convergence all reach `validateCommitLegality`.
Putting the profile and changed-leaf proof checks there once closes the mdk#707 seam-asymmetry class, instead of
adding a check at each seam.
Output: new `src/core/components/tree-diff.ts`, extended `src/core/components/integrity.ts`,
`getGroupProfileSupport`, the reason/field propagation in `src/engine/types.ts`, shared test fixtures, and
GRP-01/GRP-03 tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-groupcontext-profile-requirement-legality-seam-extension/08-CONTEXT.md
@.planning/phases/08-groupcontext-profile-requirement-legality-seam-extension/08-RESEARCH.md
@.planning/phases/08-groupcontext-profile-requirement-legality-seam-extension/08-PATTERNS.md
@CLAUDE.md
@src/core/components/integrity.ts
@src/core/components/account-identity-proof.ts
@src/core/group-members.ts
@src/core/components/bytes.ts

<interfaces>
Existing signatures this plan consumes (from src/core/components/account-identity-proof.ts, integrity.ts, bytes.ts):

```typescript
export type AccountIdentityProofRejectReason =
  | "invalid-credential" | "legacy-extension-present" | "invalid-dictionary" | "duplicate-data"
  | "missing-support" | "missing-data" | "invalid-location" | "ciphersuite-mismatch"
  | "signature-key-mismatch" | "identity-mismatch" | "invalid-proof"
  | "legacy-group" | "mixed-profile" | "missing-requirement";
export class AccountIdentityProofError extends Error { readonly reason: AccountIdentityProofRejectReason }
export function validateLeafAccountIdentityProof(leaf: LeafNode, ciphersuite: number): void; // throws AccountIdentityProofError
export function validateKeyPackageAccountIdentityProof(keyPackage: KeyPackage, ciphersuite: number): void; // throws
export function classifyGroupAccountIdentityProofProfile(extensions: GroupContextExtension[]): AccountIdentityProofProfile; // throws invalid-location / invalid-dictionary
export function assertCurrentGroupAccountIdentityProofProfile(extensions: GroupContextExtension[]): void; // legacy-group | mixed-profile | missing-requirement
export function bytesEqual(a: Uint8Array | undefined, b: Uint8Array | undefined): boolean;
export function validateCommitLegality(args: {
  parentState: ClientState; resultingState: ClientState;
  proposals: readonly (Proposal | ProposalWithSender)[]; committerLeafIndex?: number;
}): CommitIntegrityViolation | undefined;
// ClientState.groupContext.cipherSuite is a number; ClientState.ratchetTree is (Node | undefined)[] with leaves at even node indices.
```

New signatures this plan creates (exact names; downstream plans 08-02..08-04 import them):

```typescript
// src/core/components/tree-diff.ts
export interface ChangedLeaf { leafIndex: number; leaf: LeafNode }
export function diffChangedLeaves(parentTree: ClientState["ratchetTree"], resultingTree: ClientState["ratchetTree"]): ChangedLeaf[];

// src/core/components/account-identity-proof.ts
export type GroupProfileSupport =
  | { kind: "supported" }
  | { kind: "unsupported"; proofReason: AccountIdentityProofRejectReason };
export function getGroupProfileSupport(extensions: GroupContextExtension[]): GroupProfileSupport; // never throws

// src/core/components/integrity.ts
export type CommitIntegrityViolationReason =
  "component-integrity" | "admin-leaf-coupling" | "disband-legality" | "account-identity-proof";
export interface CommitIntegrityViolation {
  reason: CommitIntegrityViolationReason; detail: string;
  proofReason?: AccountIdentityProofRejectReason; leafIndex?: number;
}
export function validateCommitAccountIdentityProofs(args: { parentState: ClientState; resultingState: ClientState }): CommitIntegrityViolation | undefined;
export function validateAddProposalAccountIdentityProofs(
  proposals: readonly (Proposal | ProposalWithSender)[], ciphersuite: number,
): CommitIntegrityViolation | undefined;

// src/__tests__/helpers/account-identity-proof-fixtures.ts
export async function forgeKeyPackage(args: {
  account: ReturnType<typeof testAccount>; ciphersuiteImpl: CiphersuiteImpl; proof: "missing" | "tampered";
}): Promise<{ publicPackage: KeyPackage; privatePackage: PrivateKeyPackage }>;
export function dropAccountIdentityProofRequirement(state: ClientState): Proposal;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure changed-leaf tree diff and shared forged-fixture helpers</name>
  <files>src/core/components/tree-diff.ts, src/core/components/__tests__/tree-diff.test.ts, src/core/components/index.ts, src/__tests__/helpers/account-identity-proof-fixtures.ts</files>
  <read_first>
    - src/core/group-members.ts (nodeToLeafIndex helper, nodeTypes import, pure ClientState tree-read style)
    - src/core/components/bytes.ts (bytesEqual handles undefined)
    - src/core/components/index.ts (barrel export style)
    - ts-mls/src/ratchetTree.ts (Node / RatchetTree shape, lines 1-80) and ts-mls/src/leafNode.ts (LeafNode.signature)
    - ts-mls/src/updatePath.ts lines 71-138 (update-path leaf is always re-signed; extensions carried forward)
    - .planning/phases/08-groupcontext-profile-requirement-legality-seam-extension/08-RESEARCH.md sections "Pattern 2", "Pattern 3", "Pattern 5", "Assumptions Log" (A1)
    - src/engine/__tests__/commit-legality-seams.test.ts lines 79-245 (raw createCommit/joinGroup group construction, buildComponentIntegrityViolation shape)
    - src/client/__tests__/join-account-identity-proof.test.ts lines 1-60 and 219-280 (generateKeyPackageWithKey + makeLeafAppComponentsExtension forged-leaf construction)
    - src/core/components/app-components-list.ts (the components-list encoder beside decodeComponentsList)
    - src/__tests__/helpers/test-accounts.ts (testAccount return type)
  </read_first>
  <behavior>
    - Test 1: a proposal-less self-update commit by leaf 0 in a 2-party group yields exactly one ChangedLeaf with leafIndex 0; leaf 1 is absent and its signature bytes are byte-identical before and after (locks Assumption A1)
    - Test 2: a commit adding a new member yields a ChangedLeaf whose leafIndex is the new member's MLS leaf index (nodeIndex / 2), plus the committer's update-path leaf
    - Test 3: a commit removing leaf 1 never reports leaf 1 (blanked leaves skipped per D-02)
    - Test 4: identical parent and resulting trees yield an empty array; trees of different lengths do not throw
  </behavior>
  <action>
Create `src/core/components/tree-diff.ts` (module header `/** @module @category Core - App Components */`). It exports `interface ChangedLeaf { leafIndex: number; leaf: LeafNode }` and `function diffChangedLeaves(parentTree: ClientState["ratchetTree"], resultingTree: ClientState["ratchetTree"]): ChangedLeaf[]`, per D-02. The function walks even node indices from 0 up to the max length of both arrays. It skips a resulting node that is undefined or not `nodeTypes.leaf` (blank or removed, so never validated). It skips a leaf whose parent node at the same index is a leaf with `bytesEqual(parentLeaf.signature, resultingLeaf.signature)` (unchanged, trusted per D-01). It pushes `{ leafIndex: nodeIndex / 2, leaf }` for every other leaf (new, or re-signed by an Add, Update, or committer update path). Compare by `leaf.signature` only; do not use ts-mls leaf encoders/equality helpers (not exported from the package root, and the vendor guard fails subpath imports). Import only from `ts-mls` and relative `src/core` modules, never from src/engine or src/client. The docstring cites D-02 and Assumption A1: a LeafNode signature covers its full TBS content, so any content change forces a new signature. Add `export * from "./tree-diff.js";` to `src/core/components/index.ts`.

Create `src/__tests__/helpers/account-identity-proof-fixtures.ts` (test-only; `__tests__` is excluded from the build) with two named exports reused by plans 08-02, 08-03, and 08-04:
- `forgeKeyPackage({ account, ciphersuiteImpl, proof })`: builds a cryptographically valid MLS KeyPackage through ts-mls's public `generateKeyPackageWithKey`, with `createCredential(account.pubkey)` and `defaultCapabilities()`. It bypasses `src/core/key-package.ts`, which always attaches a real proof. With `proof: "missing"`, the leaf carries no `0x8009` app_data_dictionary entry. With `proof: "tampered"`, the leaf carries `makeLeafAppComponentsExtension(bytes)`, where `bytes` is a real `produceAccountIdentityProof` output for that exact MLS signature key (same account, ciphersuite, and `createdAt: 1700000000`) with its last byte XOR `0xff`. Follow the signature-key-pair construction the join test uses around line 219.
- `dropAccountIdentityProofRequirement(state)`: returns an `AppDataUpdate` proposal (`appDataUpdateProposalType`, `operation: "update"`) targeting `APP_COMPONENTS_COMPONENT_ID` whose `update` bytes are the state's current `getAppComponents` id list minus `ACCOUNT_IDENTITY_PROOF_COMPONENT_ID`, encoded with the components-list encoder from `src/core/components/app-components-list.ts`. This is the wire-valid "commit that drops the 0x8009 requirement" fixture from D-04.

Write `src/core/components/__tests__/tree-diff.test.ts` implementing the behavior list. Use real groups built with core `createSimpleGroup`/`generateKeyPackage` and raw ts-mls `createCommit`/`joinGroup`, following the `fourPartyEpoch1Group` pattern.
  </action>
  <verify>
    <automated>pnpm vitest run src/core/components/__tests__/tree-diff.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function diffChangedLeaves" src/core/components/tree-diff.ts` outputs 1
    - `grep -c 'export \* from "./tree-diff.js";' src/core/components/index.ts` outputs 1
    - `grep -c "bytesEqual" src/core/components/tree-diff.ts` outputs at least 1
    - `grep -cE "export (async )?function (forgeKeyPackage|dropAccountIdentityProofRequirement)" src/__tests__/helpers/account-identity-proof-fixtures.ts` outputs 2
    - `pnpm vitest run src/core/components/__tests__/tree-diff.test.ts` passes with at least 4 tests, including the sibling-signature-unchanged assertion (A1) and the removed-leaf-skipped assertion
  </acceptance_criteria>
  <done>diffChangedLeaves returns exactly the new or re-signed non-blank leaves with true MLS leaf indices; shared forged-KeyPackage and requirement-drop fixtures exist for later plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Profile/proof check inside validateCommitLegality plus the Add-proposal helper</name>
  <files>src/core/components/account-identity-proof.ts, src/core/components/__tests__/account-identity-proof.test.ts, src/core/components/integrity.ts, src/core/components/__tests__/integrity.test.ts, src/engine/types.ts</files>
  <read_first>
    - src/core/components/integrity.ts (full file: reason union lines 37-52, validateCommitLegality lines 297-373, the getAppComponents try/catch non-throwing precedent)
    - src/core/components/account-identity-proof.ts lines 90-135 (reject reasons, error class, profile type) and 540-690 (validateKeyPackageAccountIdentityProof, validateGroupMemberAccountIdentityProofs, classify/assert)
    - src/core/components/__tests__/integrity.test.ts (existing validateCommitLegality test construction)
    - src/core/components/__tests__/account-identity-proof.test.ts (existing profile-classification fixtures)
    - src/engine/types.ts lines 166-185 (RejectedIngestResult)
    - src/engine/ingest.ts lines 725-752 (inbound yields `reason: violation.reason`, so the engine union must accept the new literal)
    - src/__tests__/helpers/account-identity-proof-fixtures.ts (from Task 1)
    - refs/mdk/crates/cgka-engine/src/account_identity_proof.rs (validate_staged_commit_account_identity_proofs, protocol_profile_of_group_extensions)
    - refs/marmot/app-components/account-identity-proof-v2.md ("Validation", "Lifecycle, authorization, and removal", "Migration from v1")
    - refs/marmot/foundation/errors.md (diagnostics-privacy rule)
  </read_first>
  <behavior>
    - getGroupProfileSupport: current group returns kind "supported"; mixed returns unsupported "mixed-profile"; a group whose app_components omits 0x8009 returns "missing-requirement"; a GroupContext dictionary carrying a 0x8009 entry returns "invalid-location"; undecodable app_components bytes return "invalid-dictionary"; it never throws
    - validateCommitAccountIdentityProofs: an honest Add of a core-generated KeyPackage returns undefined; an honest self-update returns undefined (D-03: no prior-leaf identity comparison); applying dropAccountIdentityProofRequirement returns reason "account-identity-proof", proofReason "missing-requirement", no leafIndex; a raw commit adding forgeKeyPackage({proof:"tampered"}) returns proofReason "invalid-proof" with leafIndex equal to the added leaf's MLS index; a raw commit adding forgeKeyPackage({proof:"missing"}) returns the same proofReason that validateLeafAccountIdentityProof throws for that leaf; a removal-only commit returns undefined
    - validateAddProposalAccountIdentityProofs: accepts bare Proposal and ProposalWithSender; ignores non-Add proposals; returns undefined for a valid Add; returns reason "account-identity-proof" with proofReason equal to validateKeyPackageAccountIdentityProof's thrown reason for missing/tampered KeyPackages; omits leafIndex
    - validateCommitLegality ordering (D-07): a resulting GroupContext carrying 0x8009 data still reports "component-integrity"; a commit that both adds a tampered-proof member and de-leafs an admin reports "account-identity-proof" (not "admin-leaf-coupling"); a resulting state whose app_components bytes do not decode yields a violation instead of throwing
    - Diagnostics privacy: no account-identity-proof violation's detail contains any member pubkey hex
  </behavior>
  <action>
In `src/core/components/account-identity-proof.ts`, add `type GroupProfileSupport` (discriminated by `kind`: `{ kind: "supported" }` or `{ kind: "unsupported"; proofReason: AccountIdentityProofRejectReason }`). Add `function getGroupProfileSupport(extensions: GroupContextExtension[]): GroupProfileSupport`. It calls `assertCurrentGroupAccountIdentityProofProfile(extensions)` inside try/catch. It returns supported on success, `{ kind: "unsupported", proofReason: err.reason }` for an `AccountIdentityProofError`, and `{ kind: "unsupported", proofReason: "invalid-dictionary" }` for any other thrown value. It must never throw, because it is the D-01a check and the D-11 load-time classifier in plan 08-03. Docstring cites D-01, D-11, and refs/mdk `protocol_profile_of_group_extensions`.

In `src/core/components/integrity.ts`:
1. D-05: add `"account-identity-proof"` to `CommitIntegrityViolationReason`.
2. D-06: add optional `proofReason?: AccountIdentityProofRejectReason` and `leafIndex?: number` to `CommitIntegrityViolation`. Extend its docstring: both fields are pubkey-free, and `leafIndex` is the true MLS leaf index (never the member enumeration index `validateGroupMemberAccountIdentityProofs` uses).
3. Add `validateCommitAccountIdentityProofs({ parentState, resultingState })` (D-01, D-02, D-03), pure and non-throwing:
   - (a) `getGroupProfileSupport(parentState.groupContext.extensions)`: if unsupported, return `{ reason: "account-identity-proof", detail: "parent GroupContext is outside the current account identity proof profile (<proofReason>)", proofReason }`. Repeat for `resultingState` with detail prefix "resulting GroupContext". Both must be current, so the profile cannot change.
   - (b) For each entry of `diffChangedLeaves(parentState.ratchetTree, resultingState.ratchetTree)`, call `validateLeafAccountIdentityProof(entry.leaf, resultingState.groupContext.cipherSuite)` in try/catch. On `AccountIdentityProofError`, return `{ reason: "account-identity-proof", detail: "member leaf <leafIndex> account identity proof invalid (<err.reason>)", proofReason: err.reason, leafIndex: entry.leafIndex }`. On any other thrown value, return the same without `proofReason`, with detail "member leaf <leafIndex> account identity proof validation failed".
   - Never interpolate `err.message` or any credential bytes into `detail`. Per D-03, do not compare a changed leaf against the member's prior leaf identity (that is Phase 9).
4. Add `validateAddProposalAccountIdentityProofs(proposals, ciphersuite)`, pure and non-throwing, used pre-apply by plan 08-02 (D-08/D-09). Normalize each item to a `Proposal`. For each proposal with `proposalType === defaultProposalTypes.add` and an `add` field, call `validateKeyPackageAccountIdentityProof(proposal.add.keyPackage, ciphersuite)` in try/catch, and return on the first failure `{ reason: "account-identity-proof", detail: "Add proposal <position> KeyPackage account identity proof invalid (<err.reason>)", proofReason: err.reason }`. Omit `leafIndex`, which is unknown pre-apply. A non-AccountIdentityProofError throw maps to the same shape without `proofReason`.
5. D-07: in `validateCommitLegality`, call `validateCommitAccountIdentityProofs({ parentState: args.parentState, resultingState: args.resultingState })` immediately after the `integrityViolation` early return and before `classifyDisbandCommit`, returning its violation if defined. Update the adapter's docstring to list the four checks in order and cite refs/mdk `validate_staged_commit_account_identity_proofs` plus refs/marmot/app-components/account-identity-proof-v2.md.

Verify there is no circular import: account-identity-proof.ts must not import integrity.ts (it currently does not).

In `src/engine/types.ts`, per D-05/D-06: add `| "account-identity-proof"` to `RejectedIngestResult.reason`. Add optional `proofReason?: AccountIdentityProofRejectReason` and `leafIndex?: number` to `RejectedIngestResult` (type-only import from `../core/components/account-identity-proof.js`), each with a one-line pubkey-free docstring. Session ingest types derive from this union automatically; do not edit `src/client/session/group-session.ts`.

Write the behavior-list tests into `src/core/components/__tests__/account-identity-proof.test.ts` (getGroupProfileSupport) and `src/core/components/__tests__/integrity.test.ts` (the other three groups), using the Task 1 fixtures and real ts-mls `createCommit` states. Run the tests RED first, then implement GREEN.
  </action>
  <verify>
    <automated>pnpm vitest run src/core/components/__tests__/integrity.test.ts src/core/components/__tests__/account-identity-proof.test.ts && pnpm compile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"account-identity-proof"' src/core/components/integrity.ts` outputs at least 3
    - `grep -c "proofReason?: AccountIdentityProofRejectReason" src/core/components/integrity.ts` outputs 1 and `grep -c "leafIndex?: number" src/core/components/integrity.ts` outputs 1
    - `grep -cE "export function (validateCommitAccountIdentityProofs|validateAddProposalAccountIdentityProofs)" src/core/components/integrity.ts` outputs 2
    - `grep -c "export function getGroupProfileSupport" src/core/components/account-identity-proof.ts` outputs 1
    - `awk '/export function validateCommitLegality/,0' src/core/components/integrity.ts | grep -oE "validateAppComponentIntegrity\(|validateCommitAccountIdentityProofs\(|classifyDisbandCommit\(|validateAdminLeafCoupling\(" | tr '\n' ' '` prints the four names in exactly that order (D-07)
    - `grep -c '"account-identity-proof"' src/engine/types.ts` outputs 1 and `grep -c "proofReason?: AccountIdentityProofRejectReason" src/engine/types.ts` outputs 1
    - `grep -c "integrity.js" src/core/components/account-identity-proof.ts` outputs 0
    - The two test files pass, including the D-07 ordering test, the non-throwing undecodable-app_components test, and the pubkey-free detail assertion
    - `pnpm compile` exits 0
  </acceptance_criteria>
  <done>validateCommitLegality rejects profile drift and invalid changed-leaf proofs with a structured, pubkey-free account-identity-proof violation in D-07 order, never throws, and the engine reject union carries the new literal and optional fields.</done>
</task>

<task type="auto">
  <name>Task 3: Exports snapshot, GRP-01 creation test, GRP-03 join tests, full-suite regression</name>
  <files>src/__tests__/exports.test.ts, src/core/__tests__/group.test.ts, src/client/__tests__/join-account-identity-proof.test.ts</files>
  <read_first>
    - src/__tests__/exports.test.ts (root inline snapshot)
    - src/core/__tests__/group.test.ts lines 29-100 (existing app_components / required_capabilities assertions)
    - src/client/__tests__/join-account-identity-proof.test.ts (full file: commitInviteeAdd, joinAsInvitee, the mixed-profile and creator-leaf tests, persistence assertions)
    - src/client/groups-manager.ts lines 700-730 (join-time assertCurrentGroupAccountIdentityProofProfile + validateGroupMemberAccountIdentityProofs before adoptClientState)
    - src/__tests__/helpers/account-identity-proof-fixtures.ts (Task 1)
    - refs/marmot/protocol-core/group-setup.md and refs/marmot/protocol-core/joining.md
  </read_first>
  <action>
1. Run `pnpm vitest run src/__tests__/exports.test.ts -u` so the root inline snapshot picks up the four new runtime exports: `diffChangedLeaves`, `getGroupProfileSupport`, `validateAddProposalAccountIdentityProofs`, `validateCommitAccountIdentityProofs`. Review the diff: it must add exactly those four names and remove nothing.
2. GRP-01, in `src/core/__tests__/group.test.ts`: add `it("GRP-01: a created group requires 0x8009 in app_components, not required_capabilities, with no GroupContext state", ...)`. Build a group with core `createSimpleGroup` and assert:
   - `getAppComponents(extensions)` contains `ACCOUNT_IDENTITY_PROOF_COMPONENT_ID`;
   - `getComponentData(extensions, ACCOUNT_IDENTITY_PROOF_COMPONENT_ID)` is undefined;
   - the `required_capabilities` extension's `extensionTypes` includes neither `0x8009` nor `0xf2f1`;
   - `getGroupProfileSupport(extensions)` equals `{ kind: "supported" }`.
3. GRP-03, in `src/client/__tests__/join-account-identity-proof.test.ts`: add two tests; each asserts `inviteeGroupStateStore.keys()` resolves to `[]` after the rejection.
   - `it("GRP-03: rejects a Welcome into a group that does not require 0x8009 without persisting it", ...)`: start from a `createSimpleGroup` state and apply a raw `createCommit` with `extraProposals: [dropAccountIdentityProofRequirement(state)]` (ts-mls accepts it; only the Marmot layer refuses). Sanity-assert the resulting state classifies `"neither"`. Then `commitInviteeAdd` the invitee from that state and assert `joinAsInvitee` rejects with `{ name: "AccountIdentityProofError", reason: "missing-requirement" }`.
   - `it("GRP-03: rejects a Welcome when a current non-creator member's proof is invalid without persisting it", ...)`: add a second member built with `forgeKeyPackage({ proof: "tampered" })` via raw `createCommit`, then invite the invitee. Assert the join rejects with `name: "AccountIdentityProofError"`.
   - Prefix the titles of the existing mixed-profile and creator-leaf tests with `GRP-03: ` so all four join-rejection cases trace to the requirement.
4. Run the full suite. If an existing test fails because it relied on a proof-less or proof-invalid leaf being committed, or on a group outside the current profile, fix the fixture to use real proofs (core `generateKeyPackage` with a real signer). Never weaken or bypass the new check. Record any such fixture fix in the SUMMARY.
  </action>
  <verify>
    <automated>pnpm vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '"(diffChangedLeaves|getGroupProfileSupport|validateAddProposalAccountIdentityProofs|validateCommitAccountIdentityProofs)"' src/__tests__/exports.test.ts` outputs 4
    - `grep -c "GRP-01" src/core/__tests__/group.test.ts` outputs at least 1
    - `grep -c "GRP-03" src/client/__tests__/join-account-identity-proof.test.ts` outputs at least 4
    - `pnpm vitest run src/client/__tests__/join-account-identity-proof.test.ts src/core/__tests__/group.test.ts src/__tests__/exports.test.ts` passes
    - `pnpm vitest run` reports 0 failed test files (pre-phase baseline: 98 files / 1099 tests all passing)
    - `pnpm exec prettier --check src/core/components src/__tests__/helpers src/engine/types.ts src/core/__tests__/group.test.ts src/client/__tests__/join-account-identity-proof.test.ts` exits 0
  </acceptance_criteria>
  <done>Root exports reflect the new core symbols; GRP-01 and GRP-03 are pinned by named tests that assert nothing is persisted; the whole suite is green with the extended adapter on every seam.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Kind | Location |
|----------|------|----------|
| `ChangedLeaf` | interface | `src/core/components/tree-diff.ts` (new file) |
| `diffChangedLeaves` | function | `src/core/components/tree-diff.ts` |
| `GroupProfileSupport` | type (discriminated union) | `src/core/components/account-identity-proof.ts` |
| `getGroupProfileSupport` | function | `src/core/components/account-identity-proof.ts` |
| `"account-identity-proof"` | `CommitIntegrityViolationReason` literal | `src/core/components/integrity.ts` |
| `CommitIntegrityViolation.proofReason`, `.leafIndex` | optional fields | `src/core/components/integrity.ts` |
| `validateCommitAccountIdentityProofs` | function | `src/core/components/integrity.ts` |
| `validateAddProposalAccountIdentityProofs` | function | `src/core/components/integrity.ts` |
| `RejectedIngestResult.reason` `"account-identity-proof"`, `.proofReason`, `.leafIndex` | union literal + optional fields | `src/engine/types.ts` |
| `forgeKeyPackage`, `dropAccountIdentityProofRequirement` | test helpers | `src/__tests__/helpers/account-identity-proof-fixtures.ts` (new file) |
| `tree-diff.test.ts` | test file | `src/core/components/__tests__/tree-diff.test.ts` (new file) |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote committer → local ClientState | Commit bytes from any member (admin or not) produce a resulting ratchet tree and GroupContext the local client would adopt |
| persisted tree edge → canonical state | Edges written before this gate existed are re-validated by tree-fed convergence through the same adapter |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Spoofing | `validateCommitLegality` changed leaves | high | mitigate | `validateCommitAccountIdentityProofs` validates every new or re-signed leaf (Add, Update, update path) via `diffChangedLeaves` + `validateLeafAccountIdentityProof` on all three call sites |
| T-08-02 | Tampering | GroupContext profile drift | high | mitigate | D-01a: parent and resulting GroupContext must both be `getGroupProfileSupport` → supported; a requirement-drop commit returns `account-identity-proof` / `missing-requirement` |
| T-08-03 | Denial of Service | fork-recovery and tree-fed callers that do not wrap the adapter | medium | mitigate | Every validator throw inside the new checks is caught and mapped to a violation; tested with undecodable app_components bytes |
| T-08-04 | Tampering | tree diff under-inclusion | medium | mitigate | Changed-leaf test uses signature inequality (signature covers full LeafNode TBS); Assumption A1 locked by a test asserting an untouched sibling keeps identical signature bytes |
| T-08-05 | Information Disclosure | violation `detail` strings | low | mitigate | `detail` built only from leaf index and reason literal; never `err.message` or credential bytes; test asserts no member pubkey hex appears |
| T-08-06 | Elevation of Privilege | check ordering vs admin reasoning | medium | mitigate | D-07 order enforced and tested: an invalid identity reports before admin-leaf coupling |
</threat_model>

<verification>
- `pnpm vitest run` green (0 failed files) and `pnpm compile` exits 0.
- `validateCommitLegality` source order: component-integrity → account-identity proofs → disband → admin-leaf coupling.
- There are exactly three production callers of `validateCommitLegality` (`ingest.ts`, `fork-recovery.ts`, `group-engine.ts` `#assertStagedCommitLegal`). This plan changes none of them, yet all three now enforce D-01..D-03.
- This phase carries a planned review-fix cycle (ROADMAP note). Code review should focus on this adapter's non-throwing contract and diff correctness.
</verification>

<success_criteria>
- GRP-02 (core): the shared adapter rejects profile drift and invalid changed-leaf proofs with a structured violation.
- GRP-01 and GRP-03 have named, passing tests.
- No test fixture weakened; full suite green.
</success_criteria>

<output>
Create `.planning/phases/08-groupcontext-profile-requirement-legality-seam-extension/08-01-SUMMARY.md` when done
</output>
