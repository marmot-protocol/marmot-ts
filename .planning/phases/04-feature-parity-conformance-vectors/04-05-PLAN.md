---
phase: 04-feature-parity-conformance-vectors
plan: 05
type: execute
wave: 4
depends_on: [04-03, 04-04]
files_modified:
  - src/core/convergence-policy.ts
  - src/engine/group-engine.ts
  - src/client/group/marmot-group.ts
  - src/engine/types.ts
  - src/engine/__tests__/convergence-scheduling.test.ts
  - src/__tests__/helpers/mock-network.ts
autonomous: true
requirements: [CONF-01]
must_haves:
  truths:
    - "Convergence policy v1 defaults maxConvergencePassMs to 5000 and a pass deadline never moves after opening."
    - "PendingPublish and Merging retain inbound work without admitting a new pass."
    - "A bounded pass preserves unfinished work and resumes deterministically."
    - "After Stable settlement, one already-queued authorized local intent gets one preparation attempt before inbound-only work opens another pass."
  artifacts:
    - "Version-compatible convergence-policy field and immutable pass state."
    - "Injectable monotonic clock and scheduler-driving test hooks."
  key_links:
    - "policy -> engine pass open/deadline -> retained continuation"
    - "Stable settlement -> MarmotGroup local queue -> next inbound pass"
  prohibitions:
    - "Do not use wall-clock Date for pass elapsed time or extend the deadline on input."
    - "Do not implement terminal disbanding or Phase 5 runtime matrix execution."
---

<objective>Implement the bounded convergence-pass contract and deterministic local-intent fairness required by the updated specification.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@.planning/phases/04-feature-parity-conformance-vectors/04-CONTEXT.md
@refs/marmot/protocol-core/convergence.md
@src/core/convergence-policy.ts
@src/engine/group-engine.ts
@src/client/group/marmot-group.ts</context>
<assumption_delta_decision>Primary noun: convergence evidence. Decision: no-change. Rationale: a versioned stamp is persisted alongside wire bytes but does not generalize or replace record identity; scheduler state consumes evidence without redefining identity.</assumption_delta_decision>

## Artifacts this phase produces

- `maxConvergencePassMs` policy field (serialized snake-case counterpart `max_convergence_pass_ms` where required)
- Immutable convergence-pass state and deterministic drive hooks
- Fixed-deadline, continuation, lifecycle-gate, and anti-starvation tests

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Bound passes with one immutable monotonic deadline and continuation</name>
  <read_first>src/core/convergence-policy.ts, src/engine/group-engine.ts, src/engine/types.ts, src/engine/__tests__/convergence-parity.test.ts, refs/marmot/protocol-core/convergence.md</read_first>
  <files>src/core/convergence-policy.ts, src/engine/group-engine.ts, src/engine/types.ts, src/engine/__tests__/convergence-scheduling.test.ts</files>
  <behavior>
    - Default policy is 5000 ms and compatibility decode supplies it for older policy input.
    - Later input cannot alter openedAt/deadline.
    - Deadline expiry retains remaining work; deterministic drive resumes it.
    - PendingPublish/Merging queue inbound input without a pass transition.
  </behavior>
  <action>Add the updated policy field and explicit pass object sampled once from an injected monotonic clock. Separate collection deadline from quiescence timing, preserve unfinished authenticated input after expiry, and expose narrow scheduler hooks usable in production and tests without Node timer types. Gate new-pass admission on lifecycle while retaining the input.</action>
  <verify><automated>npx --yes pnpm@10.18.3 vitest run src/engine/__tests__/convergence-scheduling.test.ts</automated></verify>
  <acceptance_criteria>Fake-clock tests prove immutable deadline, exact boundary behavior, continuation, compatibility default, and lifecycle gating.</acceptance_criteria>
  <done>MDK 6aba6ef9 bounded continuation and roadmap criterion 9 timing/gating are implemented.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Give one queued authorized local intent priority after settlement</name>
  <read_first>src/client/group/marmot-group.ts, src/__tests__/helpers/mock-network.ts, src/engine/__tests__/convergence-scheduling.test.ts, src/core/commit-authorization.ts</read_first>
  <files>src/client/group/marmot-group.ts, src/__tests__/helpers/mock-network.ts, src/engine/__tests__/convergence-scheduling.test.ts</files>
  <behavior>
    - On Stable settlement, exactly one pre-existing authorized state intent is attempted before an inbound-only next pass.
    - Unauthorized or newly queued intents do not steal the slot.
    - Failed preparation consumes the one attempt according to the contract and processing continues deterministically.
  </behavior>
  <action>At the existing outbound queue drain seam, snapshot which state intents were already queued when the pass settles, reuse the current authorization path, and allow exactly one preparation attempt before admitting an inbound-only pass. Extend MockNetwork only with deterministic ordering/failure controls used by production-path tests.</action>
  <verify><automated>npx --yes pnpm@10.18.3 vitest run src/engine/__tests__/convergence-scheduling.test.ts &amp;&amp; npx --yes pnpm@10.18.3 compile</automated></verify>
  <acceptance_criteria>Queue-pressure tests prove authorized fairness, no bypass during unsafe lifecycle states, one-attempt semantics, and green compile.</acceptance_criteria>
  <done>Roadmap criterion 9 anti-starvation behavior is observable and deterministic.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| inbound queue → scheduler | Adversarial volume can influence pass timing and local progress. |
| local intent → commit preparation | Authorization and lifecycle gate outbound mutation. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-13 | Denial of Service | pass deadline | high | mitigate | Immutable deadline and deterministic continuation/fairness tests. |
| T-04-14 | Elevation of Privilege | local fairness slot | high | mitigate | Reuse exact existing authorization gate before preparation. |
| T-04-15 | Tampering | lifecycle admission | medium | mitigate | Explicit PendingPublish/Merging tests prevent pass opening. |
</threat_model>
<verification>Scheduling suite and compile pass using pnpm 10.</verification>
<success_criteria>All three roadmap criterion 9 clauses hold under deterministic pressure without Phase 04.1 or Phase 5 scope.</success_criteria>
<output>Create `.planning/phases/04-feature-parity-conformance-vectors/04-05-SUMMARY.md` when done.</output>
