---
phase: 03.1-phase-3-review-closure
plan: 06
type: execute
wave: 2
depends_on: [03.1-01]
files_modified: [src/client/group/marmot-group.ts, src/client/groups-manager.ts, src/__tests__/groups-manager.test.ts, src/__tests__/integration/removed.test.ts]
autonomous: true
requirements: [CONV-02]
must_haves:
  truths:
    - "D-14: removal markers use the group-scoped /removed namespace and destroy clears only that key."
    - "D-15: accepted and rejected ids are bounded by LRU capacity, same-id genuine events remain processable, and h must equal the subscribed group id before caching."
    - "D-15/IN-02: rejection assertions are exactly 2 for backfill+subscribe redelivery and exactly 1 elsewhere."
  artifacts:
    - path: src/client/groups-manager.ts
      provides: bounded trust-boundary caches and exact h identity gate
    - path: src/__tests__/groups-manager.test.ts
      provides: capacity, censorship, h mismatch, and exact-count regressions
  key_links:
    - from: Nostr h tag
      to: subscribed group identity
      via: equality check before either LRU insert
      pattern: "getSingletonTagValue|seen|rejected"
---
<objective>Close the marker collision and Nostr subscription amplification defects in D-14/D-15.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@.planning/phases/03.1-phase-3-review-closure/03.1-CONTEXT.md
@src/client/group/marmot-group.ts
@src/client/groups-manager.ts
@src/__tests__/groups-manager.test.ts</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Reject a wrong-group event before bounded identity caching</name>
  <files>src/client/groups-manager.ts, src/__tests__/groups-manager.test.ts</files>
  <read_first>src/client/groups-manager.ts; src/__tests__/groups-manager.test.ts; src/core/transport.ts; src/utils/nostr.ts</read_first>
  <behavior>
    - "A validly signed event whose singleton h differs from the subscribed group is rejected before ingest or seen insertion."
    - "Accepted-id and rejected-id caches never exceed the selected approximately-10k capacity."
    - "A malformed forgery cannot censor a later genuine event sharing its id."
  </behavior>
  <action>D-15 and D-12: add failing-first wrong-h, capacity, eviction, repeated-rejection, and same-id genuine-event tests. Replace the Set with deterministic bounded id LRUs for accepted and rejected identities; cache ids only, and order verification/cardinality/value checks so a rejected object never poisons accepted dedup. Restore exact rejection counts: 2 only for backfill plus subscription redelivery, 1 for single-delivery cases.</action>
  <verify><automated>test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm vitest run src/__tests__/groups-manager.test.ts &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932"</automated></verify>
  <acceptance_criteria>Every D-15 test fails before edits, caches are bounded, h identity is checked before caching, genuine same-id delivery survives, and exact counts pass.</acceptance_criteria>
  <done>D-15, WR-08, and IN-02 are closed.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Namespace and clear the durable removal marker</name>
  <files>src/client/group/marmot-group.ts, src/__tests__/integration/removed.test.ts</files>
  <read_first>src/client/group/marmot-group.ts; src/__tests__/integration/removed.test.ts; src/engine/history-tree.ts</read_first>
  <behavior>
    - "Removal realization writes groupId/removed and never overwrites a bare group-state key."
    - "Destroy and supersession remove groupId/removed."
  </behavior>
  <action>D-14 and D-12: add a failing-first shared-backend collision regression, centralize the namespaced marker key, use it for get/set/remove, and correct the option documentation to require a separate namespace on the same durable backend. Do not add a legacy bare-key migration.</action>
  <verify><automated>test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm vitest run src/__tests__/integration/removed.test.ts &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm compile &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932"</automated></verify>
  <acceptance_criteria>The collision test is failing-first; only the namespaced marker is written and cleared; focused tests and compile pass.</acceptance_criteria>
  <done>D-14 is closed.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| relay events → group ingest | Attacker-controlled ids and tags enter a long-lived subscription. |
| logical stores → shared backend | Erased generic types can collide at runtime. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-11 | Denial of service | subscription caches | high | mitigate | Bounded id-only LRUs and wrong-h rejection. |
| T-03.1-12 | Tampering | removed marker | high | mitigate | Namespaced key and collision regression. |
| T-03.1-SC | Tampering | dependency state | high | mitigate | Frozen/offline pnpm and lock guards. |
</threat_model>
<verification>Run manager and removal suites plus compile with lock guards.</verification>
<success_criteria>Trust-boundary memory is bounded and durable marker writes cannot overwrite group state.</success_criteria>
## Artifacts this phase produces
- Bounded accepted/rejected id caches and marker-key helper.
<output>Create `.planning/phases/03.1-phase-3-review-closure/03.1-06-SUMMARY.md` when done.</output>
