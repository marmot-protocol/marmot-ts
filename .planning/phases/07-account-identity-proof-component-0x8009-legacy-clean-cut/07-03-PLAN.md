---
phase: 07-account-identity-proof-component-0x8009-legacy-clean-cut
plan: 03
type: execute
wave: 2
depends_on: [07-02]
files_modified:
  - src/engine/__tests__/commit-legality-seams.test.ts
  - src/engine/__tests__/commit-authorization-seams.test.ts
  - src/engine/__tests__/send-commit-legality.test.ts
  - src/engine/__tests__/group-engine.test.ts
  - src/engine/__tests__/disband-request.test.ts
  - src/engine/__tests__/disband-convergence.test.ts
  - src/engine/__tests__/ingest-deferred.test.ts
  - src/engine/__tests__/message-dedup.test.ts
  - src/engine/__tests__/convergence-status.test.ts
  - src/engine/__tests__/history-tree.test.ts
  - src/engine/__tests__/history-tree-ingest.test.ts
  - src/engine/__tests__/ingestion-pool.test.ts
  - src/engine/__tests__/retained-store.test.ts
  - src/engine/__tests__/self-eviction.test.ts
  - src/engine/__tests__/state-notification-withdrawal.test.ts
  - src/engine/__tests__/convergence-parity.test.ts
  - src/engine/__tests__/convergence-scheduling.test.ts
autonomous: true
requirements: [PROOF-03]

must_haves:
  truths:
    - "Every generateKeyPackage call in the 17 engine test files passes a real signer whose pubkey equals the KeyPackage credential identity, so no engine test builds a proof-less KeyPackage (D-02)"
    - "Signers are PrivateKeyAccount.signer instances passed directly, drawn from existing accounts in scope or testAccount(slot); no proof-signer adapter is introduced (D-04)"
    - "Literal test identities replaced by derived pubkeys keep their original relative lexicographic order, and no test assertion is weakened or re-baselined to hide an ordering change (D-02)"
    - "All 17 engine test files pass unchanged in intent, and production code is untouched (PROOF-03 groundwork for the 07-06 required-signer flip)"
  artifacts:
    - path: "src/engine/__tests__/commit-legality-seams.test.ts"
      provides: "KeyPackages built with real signers"
      contains: "signer:"
    - path: "src/engine/__tests__/convergence-parity.test.ts"
      provides: "KeyPackages built with real signers, identity order preserved"
      contains: "signer:"
  key_links:
    - from: "src/engine/__tests__/*.test.ts"
      to: "src/__tests__/helpers/test-accounts.ts"
      via: "testAccount(slot) deterministic identities"
      pattern: "testAccount\\("
    - from: "src/engine/__tests__/*.test.ts"
      to: "src/core/key-package.ts"
      via: "generateKeyPackage({ credential, signer, ciphersuiteImpl })"
      pattern: "signer:"
---

<objective>
Make every engine test build KeyPackages with a real signer matching the credential identity (D-02), passing
`PrivateKeyAccount.signer` directly (D-04). This lets plan 07-06 make `generateKeyPackage`'s `signer` required and remove
the proof-less path without breaking the engine suite.

Purpose: D-02 is locked ("Tests pass a real signer"). This plan is test-only and wire-neutral. The emitted leaf format
does not change until plan 07-06.

Output: 17 engine test files updated; no production file, export, or fixture changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-account-identity-proof-component-0x8009-legacy-clean-cut/07-CONTEXT.md
@.planning/phases/07-account-identity-proof-component-0x8009-legacy-clean-cut/07-02-SUMMARY.md
@src/__tests__/helpers/test-accounts.ts
@src/core/key-package.ts

Migration rules (identical in plans 07-03, 07-04, 07-05; each plan owns a disjoint file set):

1. Every `generateKeyPackage({ ... })` call in an owned file passes `signer: X.signer`, where `X` is a
   `PrivateKeyAccount` whose `pubkey` equals the credential identity passed as `credential`.
2. Obtain `X` from an existing `PrivateKeyAccount` already in scope when there is one. Otherwise use
   `testAccount(slot)` imported from the shared helper, with a relative path ending in `/__tests__/helpers/test-accounts.js`.
3. When a credential used a literal test pubkey (for example `"a".repeat(64)`), replace that literal with the derived
   `pubkey` of one account held in a variable, and use that same variable everywhere the literal meant the same identity
   (admin lists, actorPubkey, expectations, lookups).
4. Preserve relative order. Tie-breaks and admin ordering can depend on lexicographic pubkey order, so within a file
   assign slots in the same ascending order as the literals sort. Use this default mapping, which preserves the order of
   the common literals and keeps each derived pubkey's leading hex digit close to the original:
   - `"2".repeat(64)` → slot 0
   - `"3".repeat(64)` → slot 1
   - `"4".repeat(64)` → slot 2
   - `"884704bd…c3a6"` → slot 5
   - `"a".repeat(64)` → slot 6
   - `"d".repeat(64)` → slot 9
   - `"e".repeat(64)` → slot 11
   Pick any other free slots in ascending order.
5. A literal pubkey that never backs a KeyPackage (for example a non-member admin entry) may stay literal. If a test then
   fails on ordering between it and a migrated identity, migrate that literal too with an order-preserving slot.
6. A local setup helper that takes a pubkey string and generates a KeyPackage inside (for example
   `createTestGroupState(adminPubkey, ciphersuiteImpl)`) changes minimally. It takes the `PrivateKeyAccount`, or the
   pubkey plus signer, and passes the signer through.
7. Never weaken, delete, or re-baseline an assertion to make a test pass. If an assertion encoded a literal pubkey value
   rather than identity equality, update it to the derived value and list it in the SUMMARY.
8. No production file changes. Hand-built legacy-proof leaves that do not call `generateKeyPackage` are out of scope and
   stay as they are; for example the forged-proof admin-policy test in group-engine.test.ts is rewritten by plan 07-07.

Signer gate (used in acceptance criteria). This Node one-liner prints every `generateKeyPackage({...})` call whose
options object lacks `signer` and exits 1 if any exist:

`node -e 'const fs=require("fs");let bad=0;for(const f of process.argv.slice(1)){const s=fs.readFileSync(f,"utf8");for(const m of s.matchAll(/generateKeyPackage\(\s*\{[\s\S]*?\}\s*\)/g)){if(!/\bsigner\b/.test(m[0])){bad++;console.log(f+": "+m[0].replace(/\s+/g," ").slice(0,100))}}}process.exit(bad?1:0)' FILES...`

Plan 07-06 makes `signer` required, so `pnpm exec tsc -p tsconfig.json --noEmit` becomes the definitive gate there.
</context>

## Artifacts this phase produces

None. This plan is a test-only migration: it creates no production symbol, file, export, or fixture. It modifies the 17
engine test files listed in `files_modified`.

## Source Coverage Audit (this plan)

| Source | ID | Item | Status |
| --- | --- | --- | --- |
| CONTEXT | D-02 | Tests pass a real signer (engine tests) | COVERED (Tasks 1-2) |
| CONTEXT | D-04 | Callers pass PrivateKeyAccount.signer directly | COVERED (Tasks 1-2) |
| REQ | PROOF-03 | Prerequisite for required-signer KeyPackage generation | COVERED (groundwork; flip in 07-06) |
| RESEARCH | PITFALLS 14 | No test fixture silently keeps a proof-less happy path | COVERED (signer gate) |

<tasks>

<task type="auto">
  <name>Task 1: Real signers in the commit-legality, authorization, engine, disband, and dedup engine tests (D-02, D-04)</name>
  <files>src/engine/__tests__/commit-legality-seams.test.ts, src/engine/__tests__/commit-authorization-seams.test.ts, src/engine/__tests__/send-commit-legality.test.ts, src/engine/__tests__/group-engine.test.ts, src/engine/__tests__/disband-request.test.ts, src/engine/__tests__/disband-convergence.test.ts, src/engine/__tests__/ingest-deferred.test.ts, src/engine/__tests__/message-dedup.test.ts, src/engine/__tests__/convergence-status.test.ts</files>
  <read_first>
    - src/__tests__/helpers/test-accounts.ts (testAccount contract and slot order)
    - src/core/key-package.ts (GenerateKeyPackageOptions.signer as left by plan 07-02)
    - Each file in this task: run `grep -n "generateKeyPackage\|createCredential\|repeat(64)\|Pubkey" <file>` first, then read each call site and its local setup helper
    - .planning/phases/07-account-identity-proof-component-0x8009-legacy-clean-cut/07-CONTEXT.md (D-02, D-04)
  </read_first>
  <action>
    Apply migration rules 1-8 from this plan's context, per D-02 and D-04, to the nine files. Import testAccount from "../../__tests__/helpers/test-accounts.js" only where a file needs it. Import PrivateKeyAccount (type) from "applesauce-accounts/accounts" only if a changed helper signature needs the type.

    For group-engine.test.ts, migrate createTestGroupState and every direct generateKeyPackage call ("a"→slot 6, "d"→slot 9 per rule 4). Leave the hand-built forged-proof test (the one constructing a leaf by hand with a tampered signature) unchanged.

    Remove any import that becomes unused, since noUnusedLocals applies to tests through tsconfig.json. Run the listed tests after each file or two so an identity-order regression is caught close to its cause. Run prettier on the changed files.
  </action>
  <verify>
    <automated>pnpm vitest run src/engine/__tests__/commit-legality-seams.test.ts src/engine/__tests__/commit-authorization-seams.test.ts src/engine/__tests__/send-commit-legality.test.ts src/engine/__tests__/group-engine.test.ts src/engine/__tests__/disband-request.test.ts src/engine/__tests__/disband-convergence.test.ts src/engine/__tests__/ingest-deferred.test.ts src/engine/__tests__/message-dedup.test.ts src/engine/__tests__/convergence-status.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The signer gate from this plan's context, run on the nine files of this task, prints nothing and exits 0
    - The vitest command in verify exits 0
    - `git diff --stat HEAD -- src ':(exclude)src/**/__tests__/**' ':(exclude)src/__tests__/**'` prints nothing (no production file touched)
    - `pnpm exec prettier --check` on the nine files exits 0
  </acceptance_criteria>
  <done>Every KeyPackage built in these nine engine test files is signed by an account matching its credential, and all nine files pass.</done>
</task>

<task type="auto">
  <name>Task 2: Real signers in the history-tree, ingestion-pool, retained-store, eviction, withdrawal, and convergence engine tests (D-02, D-04)</name>
  <files>src/engine/__tests__/history-tree.test.ts, src/engine/__tests__/history-tree-ingest.test.ts, src/engine/__tests__/ingestion-pool.test.ts, src/engine/__tests__/retained-store.test.ts, src/engine/__tests__/self-eviction.test.ts, src/engine/__tests__/state-notification-withdrawal.test.ts, src/engine/__tests__/convergence-parity.test.ts, src/engine/__tests__/convergence-scheduling.test.ts</files>
  <read_first>
    - src/__tests__/helpers/test-accounts.ts
    - Each file in this task: run `grep -n "generateKeyPackage\|createCredential\|repeat(64)\|Pubkey" <file>` first, then read each call site and its local setup helper
    - src/engine/__tests__/convergence-parity.test.ts in full around its fixture builders (lines 150-260, 370-390): committer identity ordering is part of branch tie-breaking here, so rule 4 matters most
  </read_first>
  <action>
    Apply migration rules 1-8 from this plan's context, per D-02 and D-04, to the eight files.

    In convergence-parity.test.ts, self-eviction.test.ts, and state-notification-withdrawal.test.ts, assign slots so that every pair of identities that was ordered (admin versus members, member1 versus member2) keeps the same order. These tests exercise committer-identity tie-breaks. If a test still changes outcome, stop and investigate. Do not reassign expectations to fit; record the finding in the SUMMARY.

    Remove any import that becomes unused. Run prettier on the changed files. Then run the root test typecheck and the full suite once.
  </action>
  <verify>
    <automated>pnpm vitest run src/engine/__tests__/history-tree.test.ts src/engine/__tests__/history-tree-ingest.test.ts src/engine/__tests__/ingestion-pool.test.ts src/engine/__tests__/retained-store.test.ts src/engine/__tests__/self-eviction.test.ts src/engine/__tests__/state-notification-withdrawal.test.ts src/engine/__tests__/convergence-parity.test.ts src/engine/__tests__/convergence-scheduling.test.ts && pnpm exec tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The signer gate from this plan's context, run on all 17 files in `files_modified`, prints nothing and exits 0
    - The vitest command in verify exits 0 and `pnpm exec tsc -p tsconfig.json --noEmit` exits 0
    - `pnpm vitest run src/engine` exits 0
    - `pnpm exec prettier --check src/engine/__tests__` exits 0
    - `git diff --stat HEAD -- src ':(exclude)src/**/__tests__/**' ':(exclude)src/__tests__/**'` prints nothing
    - `pnpm vitest run` (full suite) exits 0
  </acceptance_criteria>
  <done>All 17 engine test files build KeyPackages only with real, identity-matching signers, keep their original identity order and assertions, and the engine suite plus the full suite pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → verification confidence | Tests are the evidence that seams validate proofs; stale proof-less fixtures would give false confidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-13 | Tampering | engine fixtures silently keeping proof-less KeyPackages (PITFALLS 14) | medium | mitigate | Signer gate over all 17 files; plan 07-06 makes the signer required so tsc rejects any straggler |
| T-07-14 | Repudiation | identity reassignment silently changing tie-break outcomes, then re-baselined | medium | mitigate | Order-preserving slot mapping (rule 4); rule 7 forbids weakening assertions; ordering-sensitive files named explicitly in Task 2 |
| T-07-15 | Information Disclosure | deterministic secret keys used in tests | low | accept | Test-only material from src/__tests__/helpers, excluded from the published build |

No package installs occur in this plan, so no supply-chain threat applies.
</threat_model>

<verification>
- The signer gate over all 17 files exits 0
- `pnpm exec tsc -p tsconfig.json --noEmit` exits 0
- `pnpm vitest run` (full suite) exits 0
- No production file changed (`git diff --stat HEAD -- src ':(exclude)src/**/__tests__/**' ':(exclude)src/__tests__/**'` prints nothing)
- Commit per CLAUDE.md once tests pass
</verification>

<success_criteria>
- D-02 for the engine suite: no engine test generates a KeyPackage without a real signer
- D-04: signers are PrivateKeyAccount.signer instances passed directly
- Identity order and assertion intent preserved
</success_criteria>

<output>
Create `.planning/phases/07-account-identity-proof-component-0x8009-legacy-clean-cut/07-03-SUMMARY.md` when done
(list any assertion updated because it encoded a literal pubkey value).
</output>
