---
phase: 03-commit-integrity-convergence-parity
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/__tests__/convergence-parity.test.ts
  - src/engine/fork-recovery.ts
  - src/engine/tree-convergence.ts
  - src/core/convergence.ts
  - src/engine/ingest.ts
autonomous: true
requirements: [CONV-04]

must_haves:
  truths:
    - "A device's own published+confirmed commit at epoch N is still the live tip after ingesting a same-epoch sibling that loses on the deterministic ordering rule (D-16 property 1)"
    - "The own commit is materializable as a convergence candidate: when a WINNING same-epoch sibling arrives, the engine produces a real branch decision (a rewind) rather than skipping for lack of a replayable own-commit branch"
    - "Two engine instances built from the same pre-fork state and fed the same competing commits in opposite array order select the same branch, asserted on the resulting confirmationTag (D-16 property 2)"
    - "No MDK-style PrevalidatedOwnCommits / own-commit-stamping shim is introduced unless a written test demonstrably fails first (D-15 verify-first)"
    - "No scenario-vector driver and no new vector fixtures are authored in this phase (D-15)"
  artifacts:
    - src/engine/__tests__/convergence-parity.test.ts
  key_links:
    - "RetainedHistoryStore.record is reached identically by own commits (via MarmotGroupEngine.confirmPublished → #recordCommitNode) and inbound commits (via ctx.recordCommit) — one recording path, no own-commit special case"
    - "ForkRecovery#buildBranches replays retained.appliedCommitsBetween plus the incoming pool uniformly through processMessage, so an own commit's branch is an ordinary candidate"
    - "sortPeeledCommits in ingest.ts canonicalizes commit order before forkPool classification, which is what makes the dual-ordering property hold"
---

<objective>
Close CONV-04 verify-first (D-15): read the MDK Rust convergence machinery against marmot-ts's
`fork-recovery.ts` / `tree-convergence.ts` / `retained-store.ts`, then write native Vitest tests
for the two properties D-16 names. A clean pass requires NO production change — the research
hypothesis (Assumption A1) is that marmot-ts's pure-function `processMessage` architecture makes
MDK's `PrevalidatedOwnCommits` stamping shim unnecessary. Only if a written test actually fails
does a minimal, targeted fix land, and it is scoped to whatever the failure proves broken.

Purpose: criterion 5 is the one item in this phase that must NOT be assumed. This plan makes the
assumption falsifiable and records the answer.
Output: `src/engine/__tests__/convergence-parity.test.ts` plus, conditionally, a minimal fix.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-commit-integrity-convergence-parity/03-CONTEXT.md
@.planning/phases/03-commit-integrity-convergence-parity/03-RESEARCH.md
@.planning/phases/03-commit-integrity-convergence-parity/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read the MDK own-commit machinery against marmot-ts and record the divergence analysis</name>
  <files>src/engine/__tests__/convergence-parity.test.ts</files>
  <read_first>
    - refs/mdk/crates/cgka-engine/src/openmls_projection.rs — the `PrevalidatedOwnCommits` type and its doc comment, `own_commit_stamp`, `stamp_processed_own_commit_record`, `already_applied_commit_prefix`. The doc comment is the load-bearing evidence: it states OpenMLS cannot re-process a commit the local device authored
    - refs/mdk/crates/cgka-engine/src/fork_recovery.rs — `ForkRecoveryManager::resolve`, the same-epoch ordering-key comparator, and the snapshot lifecycle
    - refs/mdk/crates/cgka-engine/src/message_processor/ingest.rs — the `WrongEpoch` branch
    - refs/marmot/protocol-core/convergence.md — "Candidate branches", "Branch selection", "Same-epoch races", "Applying the selected branch". Confirm the branch-selection rule list contains no "prefer own commit" clause
    - src/engine/fork-recovery.ts — `#buildBranches` / `explore()` / `candidatesAt`, and `resolveFork` where `ours = retained.appliedCommitsBetween(forkEpoch, tipEpoch)` is merged with the incoming pool
    - src/engine/retained-store.ts — `RetainedHistoryStore.record` / `appliedCommitsBetween` / `hasState` / `anchorEpoch`
    - src/engine/group-engine.ts lines 601-660 (`confirmPublished` → `#recordCommitNode`) and lines 1330-1447 (`#resolveFork`, `#applyForkResolution`)
    - src/engine/ingest.ts lines 240-266 (`sortPeeledCommits`) and lines 535-570 (where sorted commits are classified into `forkPool`)
    - src/engine/tree-convergence.ts — `buildTreeBranchSet`
    - src/core/convergence.ts lines 119-320 — `BranchCandidate`, `scoreBranch`, `compareBranchScores`, `selectCanonicalBranch`, `commitDigest`, `CommitOrderingKey`, `compareCommitOrderingKeys`
    - .planning/phases/03-commit-integrity-convergence-parity/03-RESEARCH.md → "Convergence / CONV-04 findings (verify-first)", "Pitfall 5", and Assumptions A1/A2/A3
  </read_first>
  <action>
This task is analysis plus the test-file skeleton; it produces no production change.

Confirm the three structural claims the research makes, by reading (not by editing):

1. There is exactly one recording path for applied commits: own commits reach `RetainedHistoryStore.record` via `MarmotGroupEngine.confirmPublished` → `#recordCommitNode`, and inbound commits reach the same `#recordCommitNode` via `IngestContext.recordCommit`. Grep-confirm that `retained.record(` has no call site that branches on commit authorship.
2. `ForkRecovery#buildBranches` calls `processMessage` uniformly over `candidatesAt(state)` with no authorship distinction, and `resolveFork` feeds it `[...ours, ...incoming]` where `ours` comes from `retained.appliedCommitsBetween`.
3. Assumption A3: `sortPeeledCommits` is applied to every commit that can reach `forkPool` in `ingestEnvelopes`. Grep-confirm there is no path that pushes into `forkPool` before the sort, and record what the tree sweep / `#reconvergeFromTree` path does instead (it sources candidates from the history tree, not from the caller's array).

Create `src/engine/__tests__/convergence-parity.test.ts` with the imports and harness it needs, modeled on `src/engine/__tests__/group-engine.test.ts`: `testPeeler(ciphersuite)` over `decryptGroupMessages` / `createGroupEvent`, `createTestGroupState`-style setup, `createAdminCommitPolicyCallback`, `RetainedHistoryStore`, `DEFAULT_CONVERGENCE_POLICY`, `MarmotGroupEngine`. Prefer the shared doubles in `src/__tests__/helpers` over inline mocks where they fit.

Add a file-level doc comment that states: these are native Vitest tests for CONV-04 per D-15/D-16; the scenario-vector parity harness is Phase 4 / CONF-01; MDK's own-commit stamping exists to work around an OpenMLS constraint that ts-mls's pure-function `processMessage` does not share.

Do NOT author any vector fixture, vector manifest, or step interpreter in this file or anywhere else in this phase (D-15).

Record the analysis conclusion (hypothesis holds / does not hold, and which of A1/A2/A3 the reading challenges) in the plan SUMMARY, not in a separate report file.
</action>
<acceptance_criteria> - `src/engine/__tests__/convergence-parity.test.ts` exists and its file-level doc comment names D-15, D-16, and Phase 4 / CONF-01 as the owner of the vector harness - `pnpm vitest run src/engine/__tests__/convergence-parity.test.ts` exits 0 (the harness compiles and runs, even with no assertions yet) - No file matching `**/vectors/**` is created anywhere in the repo by this task - `grep -rn "retained.record(" src/engine/ | wc -l` output is recorded in the SUMMARY together with the confirmation that no call site branches on commit authorship
</acceptance_criteria>
<verify>
<automated>pnpm vitest run src/engine/__tests__/convergence-parity.test.ts</automated>
</verify>
<done>The MDK-vs-marmot-ts analysis is done and recorded, and a runnable test file with the engine harness exists.</done>
</task>

<task type="auto">
  <name>Task 2: Assert the two D-16 properties</name>
  <files>src/engine/__tests__/convergence-parity.test.ts</files>
  <read_first>
    - src/engine/__tests__/convergence-parity.test.ts — the harness from Task 1
    - src/engine/__tests__/group-engine.test.ts — the existing patterns for driving `send` → `confirmPublished` → `ingest` against a real ciphersuite, and for building a competing peer commit
    - src/engine/group-engine.ts lines 387-600 (`send` / `#sendInner`) and 601-660 (`confirmPublished`) — the publish-before-apply contract these tests must respect
    - src/core/convergence.ts lines 191-320 — `scoreBranch`, `compareBranchScores`, `selectCanonicalBranch`, `commitDigest`, `compareCommitOrderingKeys`; the tie-break chain ending on `tipDigest` bytes is what makes a same-epoch winner deterministic
    - .planning/phases/03-commit-integrity-convergence-parity/03-RESEARCH.md → "Property 1" and "Property 2" paragraphs, which state exactly what each test should assert
  </read_first>
  <action>
Add three `it()` blocks.

`it("keeps a device's own published+confirmed commit as the live tip when a losing same-epoch sibling arrives")` — D-16 property 1. Build a group at epoch N. Have the local engine `send({ kind: "commit", ... })` and `confirmPublished(pending)` so the commit is applied and recorded in retained history. Independently build a peer commit at the SAME source epoch N from the same parent state, chosen so it LOSES the deterministic comparison (make the loss deterministic by construction — compute both `commitDigest` values and, if the peer's would win, regenerate the peer commit until it loses, or assert on `compareCommitOrderingKeys` / `compareBranchScores` up front so the test's premise is explicit rather than incidental). Ingest the sibling envelope. Assert the engine's `state.confirmationTag` still equals our own commit's `newState.confirmationTag`, and that `state.groupContext.epoch` is unchanged. Assert the sibling's ingest result was NOT `processed` on the canonical path.

`it("materializes its own confirmed commit as a convergence candidate when a winning same-epoch sibling arrives")` — the plumbing half of criterion 5. Same setup, but construct the sibling so it WINS the deterministic comparison. Assert the ingest produced an actual branch decision — the engine rewound (`state.confirmationTag` now equals the sibling branch's tip and `lifecycle` returned to `stable`) rather than skipping for lack of a replayable own-commit branch. State in a comment that this is the property MDK's `PrevalidatedOwnCommits` exists to guarantee in OpenMLS, and that spec `convergence.md` "Branch selection" lists no "prefer own commit" rule, so winning-sibling adoption is correct, not a bug.

`it("selects the same branch when two engines receive the same competing commits in opposite delivery order")` — D-16 property 2. Build two `MarmotGroupEngine` instances from the SAME pre-fork `ClientState` (deserialize the same serialized state twice, or construct twice from the same creation inputs — do not share a mutable `ClientState` object between engines; ts-mls zeroes consumed secrets in place). Produce at least two competing same-epoch commits. Feed engine A the envelope array `[commitA, commitB]` and engine B the array `[commitB, commitA]`. Drain both generators fully (the ingest generator must be drained before the next batch). Assert `bytesToHex(engineA.state.confirmationTag)` equals `bytesToHex(engineB.state.confirmationTag)` and that both landed on the same `groupContext.epoch`.

Every test must fully drain each `ingest()` async generator before asserting or issuing the next batch.
</action>
<acceptance_criteria> - `pnpm vitest run src/engine/__tests__/convergence-parity.test.ts` exits 0 - `grep -c "it(" src/engine/__tests__/convergence-parity.test.ts` is at least 3 - The file contains an assertion comparing `bytesToHex(...confirmationTag)` between two engine instances - The losing-sibling test asserts on the ordering premise (a `compareCommitOrderingKeys` or `compareBranchScores` assertion) so the outcome cannot pass by accident
</acceptance_criteria>
<verify>
<automated>pnpm vitest run src/engine/__tests__/convergence-parity.test.ts</automated>
</verify>
<done>Both D-16 properties plus the own-commit-materializability property have passing (or demonstrably failing) assertions.</done>
</task>

<task type="auto">
  <name>Task 3: Conditional minimal remediation — only if Task 2 proved a divergence</name>
  <files>src/engine/fork-recovery.ts, src/engine/tree-convergence.ts, src/core/convergence.ts, src/engine/ingest.ts</files>
  <read_first>
    - src/engine/__tests__/convergence-parity.test.ts — the failing assertion and its exact message
    - src/engine/ingest.ts lines 240-266 and 535-570 — `sortPeeledCommits` and its coverage of every path into `forkPool` (research's first suspected gap)
    - src/engine/tree-convergence.ts — `buildTreeBranchSet` and its structural (witness-free) tie-break basis when two persisted fork branches score identically on load (research's second suspected gap)
    - src/core/convergence.ts lines 254-292 — `isBranchEligible` and `selectCanonicalBranch`, specifically the `compareBranchScores(score, bestScore) >= 0` loop tie-break named in Assumption A2
    - refs/mdk/crates/cgka-engine/src/openmls_projection.rs — re-read the `PrevalidatedOwnCommits` doc comment before considering any stamping-shaped fix
    - .planning/phases/03-commit-integrity-convergence-parity/03-RESEARCH.md → "If either test fails" and "Pitfall 5"
  </read_first>
  <action>
GATE: if every assertion in Task 2 passed, this task is a NO-OP. Make no production change, and
record in the SUMMARY that CONV-04 passed clean and Assumption A1 held — that is the expected
outcome and it satisfies criterion 5's "a clean pass requires no code change".

Only if an assertion failed:

1. Diagnose which of the three research-named suspects it is: (a) `sortPeeledCommits` not covering every path that reaches `forkPool` (Assumption A3); (b) `buildTreeBranchSet`'s tie-break basis when two persisted branches score identically on load; (c) the `>= 0` tie-break in `selectCanonicalBranch`'s loop being order-observable (Assumption A2).
2. Fix the narrowest thing the failing assertion proves broken. Prefer a deterministic tie-break correction or extending the existing canonical sort over any new mechanism.
3. Do NOT port MDK's `PrevalidatedOwnCommits` / `own_commit_stamp` / roll-forward-to-retained-anchor machinery. That exists solely because OpenMLS cannot re-process a locally authored commit; ts-mls's `processMessage` is a pure function over an explicit `ClientState` with no such constraint (Pitfall 5). If the failure genuinely appears to require own-commit stamping, stop and record that finding in the SUMMARY rather than building the shim — it would contradict the phase's verify-first premise and needs a decision, not an improvisation.
4. Do NOT add a "prefer own commit" clause to branch scoring. `convergence.md` "Branch selection" lists no such rule; adding one would make marmot-ts diverge from every conformant peer, which is the exact failure mode this phase exists to prevent.
5. Re-run the full suite; a fix here touches shared convergence code and must not regress `src/engine/__tests__/group-engine.test.ts`, `history-tree.test.ts`, `history-tree-ingest.test.ts`, or `convergence-status.test.ts`.

Record the outcome (clean pass, or the exact fix and which assumption it invalidated) in the SUMMARY.
</action>
<acceptance_criteria> - `pnpm vitest run src/engine/__tests__/convergence-parity.test.ts` exits 0 - `pnpm vitest run src/engine` exits 0 - `pnpm compile` exits 0 - `grep -rn "PrevalidatedOwnCommits\|ownCommitStamp\|own_commit_stamp" src/` returns no matches (no stamping shim was introduced) - The SUMMARY records either "CONV-04 clean pass, no code change, A1 held" or the exact minimal fix, the failing assertion, and which assumption it invalidated
</acceptance_criteria>
<verify>
<automated>pnpm vitest run src/engine && pnpm compile</automated>
</verify>
<done>CONV-04's two properties pass on the engine, and the SUMMARY states whether that required any production change.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols and files created by THIS plan (new — not pre-existing in the codebase):

| Artifact                     | Kind          | Location                                          |
| ---------------------------- | ------------- | ------------------------------------------------- |
| `convergence-parity.test.ts` | new test file | `src/engine/__tests__/convergence-parity.test.ts` |

Conditionally modified (only on a proven divergence): `src/engine/fork-recovery.ts`, `src/engine/tree-convergence.ts`, `src/core/convergence.ts`, `src/engine/ingest.ts`. This plan introduces no new exported symbol on the expected (clean-pass) path.

Created by sibling plans in this phase (for cross-plan reference): `validateAppComponentIntegrity`, `validateAdminLeafCoupling`, `collectAppDataUpdateOps`, `StateNotification`, `StateNotificationLedger`, `StateInvalidatedIngestResult`, the `"self-evicted"` skip reason, `AdminDepletionError`, `withCapturedProposals`, `removedMarkerStore`.

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                                                                            |
| ------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| peer commit → convergence decision          | A same-epoch sibling from an untrusted peer competes with the local device's own confirmed commit for canonical status |
| transport delivery order → branch selection | Arrival order is transport-controlled and must not influence which branch wins                                         |

## STRIDE Threat Register

| Threat ID       | Category          | Component                                                                                                                    | Severity | Disposition | Mitigation Plan                                                                                                                                                                                                        |
| --------------- | ----------------- | ---------------------------------------------------------------------------------------------------------------------------- | -------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-09         | Tampering         | Branch selection leaking arrival order into the comparator, letting a relay pick the winner by controlling delivery sequence | high     | mitigate    | The dual-ordering test (D-16 property 2) feeds two engines the same commits in opposite order and asserts an identical resulting `confirmationTag`; `sortPeeledCommits` canonicalizes before `forkPool` classification |
| T-03-10         | Denial of Service | An own confirmed commit that cannot be materialized as a convergence candidate, stranding the device on an unresolvable fork | high     | mitigate    | The materializability test asserts a real branch decision (rewind) occurs when a winning sibling arrives, proving the own commit replays as an ordinary candidate                                                      |
| T-03-11         | Tampering         | Introducing a "prefer own commit" scoring clause as a fix, silently diverging marmot-ts from every conformant peer           | high     | mitigate    | Explicitly forbidden in Task 3; `convergence.md` "Branch selection" is the authority and lists no such rule                                                                                                            |
| T-03-12         | Repudiation       | Assuming A1 holds without evidence and shipping an unverified criterion 5                                                    | medium   | mitigate    | D-15 verify-first: the tests are written and run before any conclusion is recorded; the SUMMARY must state the outcome explicitly                                                                                      |
| </threat_model> |

<verification>
- `pnpm vitest run src/engine/__tests__/convergence-parity.test.ts` exits 0
- `pnpm vitest run src/engine` exits 0
- `pnpm compile` exits 0 and `pnpm lint` exits 0
- No vector fixture, vector manifest, or step-interpreter file was added anywhere in the repo (D-15)
- `refs/mdk/` and `refs/marmot/` are unmodified (`git status --porcelain refs/` is empty)
</verification>

<success_criteria>

- Both D-16 properties, plus own-commit materializability, have passing native Vitest assertions
- The SUMMARY states unambiguously whether CONV-04 passed clean or required a fix, and names the fix if any
- No `PrevalidatedOwnCommits`-shaped machinery and no vector harness exists in `src/`
  </success_criteria>

<output>
Create `.planning/phases/03-commit-integrity-convergence-parity/03-03-SUMMARY.md` when done
</output>
