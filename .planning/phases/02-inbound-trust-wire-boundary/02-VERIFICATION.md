---
phase: 02-inbound-trust-wire-boundary
verified: 2026-07-22T14:30:00Z
status: passed
score: 4/4 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 2/4
  gaps_closed:
    - "Required-tag cardinality is enforced for all four listed entry points (445 h; 1059 p; 444 e/relays; 30443 d/i/mls_protocol_version)"
    - "The inbound Nostr path only trusts events after verifying their id and signature — no unverified field is used to gate future trust decisions"
    - "createWelcomeRumor (the 444 producer) conforms to the same #236 cardinality its own consumer (getWelcome) enforces"
  gaps_remaining: []
  regressions: []
deferred: []
human_verification: []
---

# Phase 2: Inbound Trust & Wire Boundary Verification Report

**Phase Goal:** The inbound Nostr path only trusts events after verifying their id and signature, and published/consumed KeyPackages plus required tags conform to the tightened #236 wire boundary — a conformant peer no longer silently accepts malformed input or forges trust in unverified fields.
**Verified:** 2026-07-22T14:30:00Z
**Status:** passed
**Re-verification:** Yes — after gap-closure plan 02-04

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                                                    | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | SC1: An inbound event with an invalid Nostr event id or Schnorr signature is rejected before any `h`/`p` routing tag is trusted or any decryption is attempted                                           | ✓ VERIFIED | `src/client/invite-manager.ts` (1059) and `src/client/key-package-manager.ts`/`src/client/group/invite.ts` (30443) verify signature first, correctly, unchanged from prior verification. `src/client/groups-manager.ts`'s 445 `#connectGroup` drain (lines 495-521) now records an event id into the `seen` dedup Set **only inside the branch that has already passed both `safeVerifyEvent` and the `h`-cardinality gate** (`seen.add(event.id)` at line 518, immediately before `trusted.push(event)`) — the previously-confirmed WR-01 poisoning path (an unverified event's id occupying the dedup slot before verification) no longer exists. A dedicated regression test (`groups-manager.test.ts:270` — "does not let a corrupted same-id forgery censor the genuine event that arrives later (WR-01)") delivers a corrupted-signature same-id forgery first, confirms it is rejected and does NOT occupy `seen`, then delivers the genuine event and confirms it reaches `group.ingest()`. Test passes. |
| 2   | SC2: Published KeyPackages cap their MLS Lifetime at ≤ 7,261,200s (84 days); an inbound KeyPackage with an over-long or expired Lifetime is rejected rather than accepted for eligibility                | ✓ VERIFIED | Unchanged from prior verification (plan 02-04 explicitly touched no lifetime code, confirmed by re-read). `src/utils/timestamp.ts` (`MAX_KEY_PACKAGE_LIFETIME_SECONDS = 7_261_200n`, `createDefaultKeyPackageLifetime`, `isLifetimeWithinCap`, `isLifetimeCurrentWithGrace`); `src/core/key-package.ts` guards the explicit-override path; `src/client/key-package-manager.ts#track()` (lines 481-489) and `src/client/group/invite.ts#createInviteIntent` both reject over-cap/expired inbound Lifetimes; `src/core/key-package-eligibility.ts` pushes a soft-reject reason. All re-confirmed present and passing (`timestamp.test.ts` 7/7, `key-package.test.ts` 13/13, `key-package-manager.test.ts`, `invite.test.ts` all green in the full suite run).                                                                                                                                                                                                                                                      |
| 3   | SC3: An event with a repeated, empty, or duplicate required tag (445 `h`; 1059 `p`; 444 `e`/`relays`; 30443 `d`/`i`/`mls_protocol_version`) is rejected, not silently resolved by taking the first match | ✓ VERIFIED | 445 `h` (`groups-manager.ts:513`), 444 `e`/`relays` (`welcome-event.ts`), and 30443 `d`/`i`/`mls_protocol_version` (`key-package-manager.ts:471-479`, `invite.ts`) unchanged and still correctly gated. **1059 `p` is now implemented**: `InviteManager.ingestEvent` (invite-manager.ts:224-227) calls `getSingletonTagValue(event, "p")` after the signature gate and before storage, emitting `rejected(event, "tag-cardinality")` on absent/repeated/empty `p`. Five new tests in `invite-manager.test.ts` (lines 577-650+) cover two-`p`, absent-`p`, empty-`p`, valid single-`p` happy path, and invalid-signature-runs-first ordering — all pass.                                                                                                                                                                                                                                                                                                                                                          |
| 4   | `createWelcomeRumor` (the 444 producer) conforms to the same #236 cardinality its own consumer (`getWelcome`) enforces — a well-formed produced rumor is always readable by this library's own consumer  | ✓ VERIFIED | `src/core/welcome-event.ts` (lines 61-64): `createWelcomeRumor` now throws when `new Set(groupRelays).size !== groupRelays.length`, mirroring the consumer's `getListTag` duplicate-rejection rule exactly. `welcome.test.ts`'s new `createWelcomeRumor` describe (lines 327+) asserts both the duplicate-URL throw and a distinct-relay round-trip through `getWelcome`/`getWelcomeGroupRelays` — both pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

**Score:** 4/4 truths verified (0 present-behavior-unverified)

### Required Artifacts

| Artifact                                           | Expected                                                                                         | Status     | Details                                                                                                                                                                                        |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------------ | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/client/verify.ts`                             | `RejectReason`, `defaultVerifyEvent`, `safeVerifyEvent`, re-exported applesauce verifier surface | ✓ VERIFIED | Unchanged, re-read and confirmed correct (try/catch wraps verify call, defaults to applesauce `verifyEvent`)                                                                                   |
| `src/utils/tag-cardinality.ts`                     | `TAG_CARDINALITY` table + `getSingletonTagValue`/`getListTag`                                    | ✓ VERIFIED | Unchanged (plan 02-04 explicitly prohibited modifying these — confirmed no diff)                                                                                                               |
| `src/utils/timestamp.ts`                           | Capped/backdated lifetime + cap/grace helpers                                                    | ✓ VERIFIED | Unchanged, confirmed present                                                                                                                                                                   |
| `src/core/key-package.ts`                          | Explicit-override lifetime guard                                                                 | ✓ VERIFIED | Unchanged, confirmed present                                                                                                                                                                   |
| `src/client/marmot-client.ts`                      | `verifyEvent` option threaded to all three managers                                              | ✓ VERIFIED | Unchanged, confirmed present                                                                                                                                                                   |
| `src/client/groups-manager.ts` (modified by 02-04) | 445 verify + `h` cardinality + dedup-after-verify reordering                                     | ✓ VERIFIED | `seen.add(event.id)` now happens only after both gates pass (line 518); a `rejectedEvents` object-identity Set was added alongside (see Anti-Patterns below — non-blocking, tracked follow-up) |
| `src/client/invite-manager.ts` (modified by 02-04) | 1059 outer verify + `p`-tag cardinality gate + rejected emit                                     | ✓ VERIFIED | Both gates present in correct order (216-227)                                                                                                                                                  |
| `src/core/welcome-event.ts` (modified by 02-04)    | 444 `e`/`relays` strict getters + producer duplicate-relay rejection                             | ✓ VERIFIED | Producer (createWelcomeRumor, 61-64) and consumer (getListTag) now agree                                                                                                                       |
| `src/client/key-package-manager.ts`                | `track()` verify + cardinality + lifetime + rejected emit                                        | ✓ VERIFIED | Unchanged, confirmed present (441-495)                                                                                                                                                         |
| `src/client/group/invite.ts`                       | `createInviteIntent` verify + cardinality + lifetime                                             | ✓ VERIFIED | Unchanged, confirmed present                                                                                                                                                                   |
| `src/core/key-package-eligibility.ts`              | Lifetime `reasons.push` check                                                                    | ✓ VERIFIED | Unchanged, confirmed present                                                                                                                                                                   |
| `src/core/key-package-event-decode.ts`             | `getKeyPackageLifetime` read helper                                                              | ✓ VERIFIED | Unchanged, confirmed present (one pre-existing, unrelated `TODO` at line 121 predates this phase)                                                                                              |

### Key Link Verification

| From                                                       | To                                                                                  | Via                                              | Status  | Details                                                        |
| ---------------------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------ | ------- | -------------------------------------------------------------- |
| `MarmotClientOptions.verifyEvent`                          | `GroupsManagerOptions.verifyEvent`                                                  | constructor threading                            | ✓ WIRED | Unchanged                                                      |
| `MarmotClientOptions.verifyEvent`                          | `InviteManagerOptions.verifyEvent`                                                  | constructor threading                            | ✓ WIRED | Unchanged                                                      |
| `MarmotClientOptions.verifyEvent`                          | `KeyPackageManagerOptions.verifyEvent`                                              | constructor threading                            | ✓ WIRED | Unchanged                                                      |
| `getSingletonTagValue` (tag-cardinality.ts)                | `InviteManager.ingestEvent` `p`-tag gate                                            | direct import + call                             | ✓ WIRED | New in 02-04; confirmed at invite-manager.ts:224               |
| `safeVerifyEvent` + `getSingletonTagValue(event,'h')` gate | `seen.add(event.id)` (445 drain)                                                    | reordered to occur only after both gates pass    | ✓ WIRED | Confirmed at groups-manager.ts:507-519; regression test passes |
| `createWelcomeRumor` duplicate-rejection                   | `getListTag`/`getWelcomeGroupRelays` duplicate-rejection (producer/consumer parity) | `new Set(...)` check mirrors `getListTag`'s rule | ✓ WIRED | Confirmed at welcome-event.ts:61-64; round-trip test passes    |

### Behavioral Spot-Checks / Test Runs

| Behavior                                     | Command                                                                                                                               | Result                                                | Status |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ------ |
| Targeted 02-04 test files                    | `pnpm vitest run src/client/__tests__/invite-manager.test.ts src/__tests__/groups-manager.test.ts src/core/__tests__/welcome.test.ts` | 3 files / 49 tests passed                             | ✓ PASS |
| Full workspace suite (run once)              | `pnpm vitest run`                                                                                                                     | 68 files / 647 tests passed                           | ✓ PASS |
| Library typecheck/build                      | `pnpm compile` (`tsc -b tsconfig.build.json`)                                                                                         | exit 0, no output (clean)                             | ✓ PASS |
| 1059 `p`-tag cardinality gate present        | `grep -n 'getSingletonTagValue(event, "p")' src/client/invite-manager.ts`                                                             | match at line 224                                     | ✓ PASS |
| 445 dedup-after-verify ordering              | manual read of groups-manager.ts:495-521 + WR-01 regression test                                                                      | `seen.add` occurs only in trusted branch; test passes | ✓ PASS |
| createWelcomeRumor duplicate-relay rejection | `grep -n 'new Set(groupRelays)' src/core/welcome-event.ts`                                                                            | match at line 61                                      | ✓ PASS |
| Debt markers in phase-modified files         | `grep -n -E "TBD                                                                                                                      | FIXME                                                 | XXX    | TODO | HACK | PLACEHOLDER"` across all 11 phase-touched source files | 1 pre-existing `TODO` in `key-package-event-decode.ts:121` (predates this phase, unrelated function) | ℹ️ INFO — not new, not blocking |

### Requirements Coverage

| Requirement | Source Plan                | Description                                                                                       | Status      | Evidence                                                                                                                                                                                               |
| ----------- | -------------------------- | ------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| SEC-01      | 02-01, 02-02, 02-03, 02-04 | Inbound Nostr path verifies id+signature before trusting routing tags/decryption                  | ✓ SATISFIED | Verify-before-trust gates on 445/1059/30443 all confirmed; the 445 drain's dedup-before-verify defect (WR-01) is closed — dedup slot is now populated only after verification passes                   |
| WIRE-01     | 02-01, 02-03               | KeyPackage Lifetime capped ≤84d on produce; inbound over-long/expired rejected                    | ✓ SATISFIED | Produce and inbound paths both correctly capped/checked, confirmed by passing tests; untouched by plan 02-04 as required                                                                               |
| WIRE-02     | 02-01, 02-02, 02-03, 02-04 | Required-tag cardinality enforced for 445 h, 1059 p, 444 e/relays, 30443 d/i/mls_protocol_version | ✓ SATISFIED | All four entry points now implemented and tested, including the previously-missing 1059 `p` gate; the 444 producer/consumer parity gap (WR-02, a self-consistency corollary of WIRE-02) is also closed |

No orphaned requirements found for Phase 2 in `.planning/REQUIREMENTS.md` — SEC-01, WIRE-01, WIRE-02 are the only IDs mapped to this phase and all three are claimed across the four plans' `requirements` frontmatter fields.

### Anti-Patterns Found

| File                                                             | Line               | Pattern                                                                                                                                                                                       | Severity   | Impact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ---------------------------------------------------------------- | ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/client/groups-manager.ts`                                   | 495, 498, 509, 514 | New `rejectedEvents: Set<NostrEvent>` (object-identity keyed) added alongside the fixed `seen` Set, to suppress duplicate `rejected` emits for exact-object relay redelivery                  | ⚠️ Warning | Code review (02-REVIEW.md WR-01/WR-02) flags this as (a) relying on an unguaranteed network-layer object-identity invariant (works only because `MockNetwork` shares object instances; a real relay pool deserializing per-message would defeat the suppression) and (b) unbounded — an adversarial relay can stream unlimited distinct invalid events, each retained in memory for the connection's lifetime. This does NOT reintroduce the WR-01 trust defect (a genuine same-id event is still verified and reaches `group.ingest()` regardless of `rejectedEvents`' behavior — confirmed by the passing regression test) and does not block the phase's success criteria. Per task instructions, accepted as a tracked follow-up (`groupsmanager-rejectedevents-dos`), not a phase-goal blocker. |
| `src/client/invite-manager.ts` vs `src/client/groups-manager.ts` | —                  | Rejection-suppression behavior now differs between the 1059 and 445 inbound boundaries (445 suppresses redelivered-object rejections via `rejectedEvents`; 1059 re-emits on every redelivery) | ℹ️ Info    | Cosmetic/consistency inconsistency only (02-REVIEW.md IN-01); not a security or correctness defect                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `src/utils/timestamp.ts`                                         | ~95-99             | `isLifetimeWithinCap` doesn't reject an inverted (`notAfter < notBefore`) range                                                                                                               | ℹ️ Info    | Pre-existing, unchanged since prior verification; defense-in-depth gap only — ts-mls's own lifetime validation on add/join still catches it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `src/core/welcome-event.ts`                                      | ~20-22             | `isEventId` accepts uppercase hex                                                                                                                                                             | ℹ️ Info    | Pre-existing, unchanged since prior verification; lenient not incorrect                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `src/core/key-package-eligibility.ts`                            | ~85-176            | `evaluateKeyPackageForGroup` performs neither signature verification nor credential-vs-author identity matching                                                                               | ℹ️ Info    | Pre-existing, unchanged; not itself a trust boundary — the real invite path re-verifies independently                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `src/core/key-package-event-decode.ts`                           | 121                | Pre-existing `// TODO: parse the rest of the client tag`                                                                                                                                      | ℹ️ Info    | Predates this phase (2026-06-18), unrelated to phase scope                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

No `TBD`, `FIXME`, or `XXX` markers found in any phase-modified file. The one `TODO` found is pre-existing, unrelated to phase scope, and does not gate this verification (debt-marker gate applies to markers introduced/left by phase work; this one predates it and was already noted as non-blocking in the prior verification pass).

### Human Verification Required

None — all findings are programmatically confirmed via code reading, grep, and passing/failing test evidence, not matters of visual/UX judgment. All three previously-identified defects have dedicated regression tests that exercise the exact failure mode (censorship-by-poisoned-dedup for WR-01, missing-p-tag rejection for GAP 1, producer/consumer round-trip parity for WR-02) rather than relying on symbol presence alone.

### Gaps Summary

No gaps remain. This is a re-verification following gap-closure plan 02-04, which closed all three defects the prior verification (`gaps_found`, 2/4) identified:

1. **1059 `p`-tag cardinality** — now enforced in `InviteManager.ingestEvent` (invite-manager.ts:224-227), with 5 passing tests covering the malformed and happy-path cases.
2. **445 dedup-before-verify ordering (WR-01)** — fixed; `seen.add(event.id)` now runs only after both the signature and `h`-cardinality gates pass, closing the same-id-forgery censorship path, confirmed by a dedicated regression test that reproduces the exact forge-then-genuine-arrives-later scenario.
3. **`createWelcomeRumor`/`getWelcome` relay-cardinality parity (WR-02)** — the producer now rejects duplicate relay URLs at produce time, matching the consumer's strict rule, confirmed by a round-trip test.

A code review of the gap-closure diff (02-REVIEW.md) confirmed all three fixes are correct, and flagged two non-blocking Warnings (an auxiliary `rejectedEvents` Set in `groups-manager.ts` that is unbounded and relies on an untested object-identity assumption) plus one Info (behavioral inconsistency between the 445 and 1059 boundaries' rejection-suppression). These do not undermine the fix to the core WR-01 defect — the genuine event still reaches `group.ingest()` regardless of `rejectedEvents`' behavior, confirmed by the passing regression test — and per the task's explicit framing are accepted as a tracked follow-up (`groupsmanager-rejectedevents-dos`), not phase-goal blockers.

Full test suite (68 files / 647 tests) passes; `pnpm compile` is clean. All three roadmap success criteria (SC1, SC2, SC3) and all three requirement IDs (SEC-01, WIRE-01, WIRE-02) are satisfied. Phase goal achieved — a conformant peer accessing this library's inbound Nostr paths (445, 1059, 444, 30443) no longer silently accepts malformed input or forges trust in unverified fields.

---

_Verified: 2026-07-22T14:30:00Z_
_Verifier: Claude (gsd-verifier)_
