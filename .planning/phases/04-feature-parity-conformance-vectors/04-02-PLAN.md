---
phase: 04-feature-parity-conformance-vectors
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/own-commit-stamp.ts
  - src/engine/types.ts
  - src/engine/group-engine.ts
  - src/engine/retained-store.ts
  - src/engine/fork-recovery.ts
  - src/engine/__tests__/own-commit-stamp.test.ts
  - src/engine/__tests__/convergence-parity.test.ts
autonomous: true
requirements: [CONF-01]
must_haves:
  truths:
    - "Confirmed own commits persist authenticated committer, authorization-aware priority, sorted consumed proposal references, and exact wire bytes before staged evidence is cleared."
    - "Restart recovery admits a stamped own commit without replaying it or reconstructing proposals from its parent."
    - "Legacy unstamped records remain readable but cannot enter candidacy through unsafe reconstruction."
    - "Record identity is unchanged by whether convergence evidence is stamped or legacy."
  artifacts:
    - "src/engine/own-commit-stamp.ts contains versioned codec/result contracts."
    - "Retained history persists and restores stamped own-commit wire records."
  key_links:
    - "PendingState preparation -> confirmPublished -> durable retained record"
    - "Stored stamped record -> ForkRecovery candidate ordering"
  prohibitions:
    - "Do not call framedCommitProposalsWithSender on the own-commit recovery path."
    - "Do not change record identity to the convergence stamp."
---

<objective>
Port MDK's OwnCommitConvergenceStamp structurally so restart recovery uses evidence captured at confirmation rather than reconstructing consumed proposals.

Purpose: Close the CR-08/CR-11 defect class and roadmap criterion 5.
Output: Versioned stamp codec, confirm-time persistence, legacy compatibility, and real-MLS restart tests.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-feature-parity-conformance-vectors/04-CONTEXT.md
@.planning/phases/04-feature-parity-conformance-vectors/04-REFERENCE-FINDINGS.md
@refs/mdk/crates/traits/src/message.rs
@src/engine/types.ts
@src/engine/retained-store.ts
@src/engine/fork-recovery.ts
</context>

<assumption_delta_decision>
Primary noun: convergence evidence. Decision: no-change. Rationale: a versioned stamp is persisted alongside wire bytes but does not generalize or replace record identity. Require an invariant test that record identity round-trips across stamped and legacy variants.
</assumption_delta_decision>

## Artifacts this phase produces

- `OwnCommitConvergenceStamp` and its versioned encode/decode result types
- Stamped own-commit retained record variant
- Restart/convergence tests for stamped and legacy records

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define versioned own-commit evidence without changing record identity</name>
  <reversibility rating="costly">The persisted variant is a shared on-disk contract, but explicit versioning and legacy decode avoid a migration or published-wire break.</reversibility>
  <read_first>src/engine/types.ts, src/engine/history-tree.ts, src/engine/retained-store.ts, refs/mdk/crates/traits/src/message.rs</read_first>
  <files>src/engine/own-commit-stamp.ts, src/engine/types.ts, src/engine/retained-store.ts, src/engine/__tests__/own-commit-stamp.test.ts</files>
  <behavior>
    - Codec round-trips exact wire bytes, authenticated committer, priority, and lexicographically sorted proposal refs.
    - Decoder distinguishes stamped and legacy-unstamped records and rejects corrupt/trailing bytes.
    - Stable record identity is identical before and after stamped and legacy decode/re-encode.
  </behavior>
  <action>Create a named-export, Uint8Array-based versioned codec following the history-tree BinaryReader/BinaryWriter pattern. Persist the stamp beside the existing wire bytes under the existing record identity; sort references before encoding. Make legacy absence explicit with a discriminated result, with no inferred fields. The CONTEXT decision already selects this format direction, so the costly rating records the contract without re-asking it.</action>
  <verify><automated>corepack pnpm vitest run src/engine/__tests__/own-commit-stamp.test.ts</automated></verify>
  <acceptance_criteria>Stamped and legacy fixtures decode, corrupt data fails closed, sorted encoding is deterministic, and the required identity invariant passes across both variants.</acceptance_criteria>
  <done>Durable convergence evidence has an explicit backward-compatible representation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture and consume own-commit evidence at the confirmation boundary</name>
  <reversibility rating="costly">Changing recovery to depend on persisted evidence affects restart semantics across engine and runtime seams, but the legacy branch preserves readable stores.</reversibility>
  <read_first>src/engine/group-engine.ts, src/engine/fork-recovery.ts, src/engine/wire-format.ts, src/engine/__tests__/convergence-parity.test.ts, src/client/runtime/group-runtime.ts</read_first>
  <files>src/engine/group-engine.ts, src/engine/retained-store.ts, src/engine/fork-recovery.ts, src/engine/__tests__/convergence-parity.test.ts</files>
  <behavior>
    - Preparation captures committer, authorization priority, and consumed references while staged proposals still exist.
    - Confirmation persists stamp and wire bytes before clearing PendingState evidence.
    - After true serialize/load restart, a self-authored by-reference chain remains a complete candidate.
    - Legacy unstamped own records defer or report unavailable while within horizon rather than reconstructing or truncating the branch.
  </behavior>
  <action>Thread the stamp through PendingState and confirmPublished into retained persistence, then teach ForkRecovery to build the own candidate directly from the stamped record. Preserve publish-before-apply rollback: abandoned publication must clear transient stamp data and must not persist it. Delete the own-path dependency on framedCommitProposalsWithSender while leaving peer replay validation intact. Use real ts-mls commits and actual store hydration in tests.</action>
  <verify><automated>corepack pnpm vitest run src/engine/__tests__/own-commit-stamp.test.ts src/engine/__tests__/convergence-parity.test.ts &amp;&amp; corepack pnpm compile</automated></verify>
  <acceptance_criteria>A persist→new engine→recover test keeps the complete own branch and deterministic ordering; legacy evidence cannot be guessed; publish failure persists no stamp; compile passes.</acceptance_criteria>
  <done>Roadmap criterion 5 is structurally satisfied at the live confirmation/recovery seams.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| staged commit → durable store | Authenticated transient evidence becomes restart authority. |
| durable bytes → recovery | Corrupt or legacy records influence branch candidacy. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-04 | Tampering | stamp codec | high | mitigate | Version, strict decode/end check, deterministic sorting, and real-crypto candidate tests. |
| T-04-05 | Elevation of Privilege | stamped priority | high | mitigate | Derive priority only through the existing outbound authorization decision at preparation. |
| T-04-06 | Repudiation | legacy record | medium | mitigate | Surface explicit legacy result and forbid proposal reconstruction. |
</threat_model>

<verification>Run both focused suites, `corepack pnpm compile`, and confirm `rg "framedCommitProposalsWithSender" src/engine/fork-recovery.ts` only finds the peer-replay branch.</verification>
<success_criteria>Confirmed own commits survive restart with exact captured evidence; old records remain readable without unsafe candidacy or identity drift.</success_criteria>
<output>Create `.planning/phases/04-feature-parity-conformance-vectors/04-02-SUMMARY.md` when done.</output>
