---
phase: 03-commit-integrity-convergence-parity
plan: 10
type: execute
wave: 2
depends_on: [03-08]
files_modified:
  - src/engine/fork-recovery.ts
  - src/engine/retained-store.ts
  - src/engine/__tests__/convergence-parity.test.ts
autonomous: true
gap_closure: true
requirements: [CONV-04]
must_haves:
  truths:
    - "Every locally confirmed commit in a chained branch remains reconstructable when later commits reference earlier proposals."
    - "A same-epoch sibling cannot displace a deeper valid own-confirmed branch because replay could not decrypt its own commit."
    - "Native Vitest regressions derived by direct MDK source inspection cover own-commit convergence, including chained by-reference proposals, per D-15 and D-16."
  artifacts:
    - path: "src/engine/fork-recovery.ts"
      provides: "Structural own-commit convergence metadata and complete branch reconstruction"
    - path: "src/engine/retained-store.ts"
      provides: "Retained per-link state/proposal evidence needed to reconstruct an own branch"
    - path: "src/engine/__tests__/convergence-parity.test.ts"
      provides: "Native MDK-derived convergence regressions, including the chained proposal case"
  key_links:
    - from: "src/engine/retained-store.ts"
      to: "src/engine/fork-recovery.ts"
      via: "own-confirmed link stamp/proposal provenance consumed during DFS"
      pattern: "OwnCommit|knownNext|proposal"
    - from: "refs/mdk/crates/traits/src/message.rs"
      to: "src/engine/__tests__/convergence-parity.test.ts"
      via: "OwnCommitConvergenceStamp and StoredMessagePayload::OwnCommitWire semantics translated into native focused regressions"
      pattern: "OwnCommitConvergenceStamp|OwnCommitWire"
---

<objective>
Replace the narrow own-state shortcut with a structurally complete convergence solution and add native regressions derived from MDK source inspection.

Purpose: Close CONV-04 for deep branches whose later commits consume by-reference proposals, not only shallow synthetic forks.
Output: Reconstructable own-commit chain metadata and focused native regressions, including a chained by-reference case.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-commit-integrity-convergence-parity/03-VERIFICATION.md
@.planning/phases/03-commit-integrity-convergence-parity/03-03-SUMMARY.md
@refs/marmot/protocol-core/convergence.md
@refs/mdk/crates/traits/src/message.rs
@refs/mdk/crates/cgka-engine/src/openmls_projection.rs
@refs/mdk/crates/cgka-engine/src/fork_recovery.rs
@refs/mdk/crates/cgka-engine/src/group_state_changes.rs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Preserve one chained own branch with a by-reference proposal end to end</name>
  <files>src/engine/fork-recovery.ts, src/engine/retained-store.ts, src/engine/__tests__/convergence-parity.test.ts</files>
  <read_first>
    - src/engine/__tests__/convergence-parity.test.ts — existing shallow own-commit cases and documented depth-two failure
    - src/engine/fork-recovery.ts — KnownNextState, buildBranches DFS, proposal reconstruction, and resolveFork
    - src/engine/retained-store.ts — applied commit/state storage and retrieval contract
    - refs/mdk/crates/traits/src/message.rs — OwnCommitConvergenceStamp definition and StoredMessagePayload::OwnCommitWire persistence payload
    - refs/mdk/crates/cgka-engine/src/openmls_projection.rs — own_commit_stamp capture, stamp_processed_own_commit_record persistence handoff, and already_applied_commit_prefix consumption during recovery
    - refs/mdk/crates/cgka-engine/src/fork_recovery.rs — canonical branch selection behavior after stamped own links are materialized
    - refs/marmot/protocol-core/convergence.md — authoritative own-confirmed convergence rules
  </read_first>
  <behavior>
    - An own confirmed proposal followed by an own confirmed commit that consumes it by reference remains a complete candidate chain.
    - Adding a same-epoch competing sibling does not truncate the own branch or select the shallower competitor.
    - Proposal references are resolved from the exact parent-link evidence, not unrelated current-state unapplied proposals.
  </behavior>
  <action>
Write the failing depth-two regression first. Port MDK's structural `OwnCommitConvergenceStamp` idea from `traits/src/message.rs` into the existing retained-history/ForkRecovery architecture: retain enough per-applied-link authorship, parent identity, resulting state, and consumed proposal-reference evidence for DFS to advance every own-confirmed link without replaying a commit to its author. Follow the pinned MDK lifecycle explicitly: `openmls_projection.rs` captures the stamp with `own_commit_stamp`, hands it to persisted processing through `stamp_processed_own_commit_record`, and consumes the retained own-commit prefix through `already_applied_commit_prefix`; `StoredMessagePayload::OwnCommitWire` in `traits/src/message.rs` is the persisted wire-plus-stamp representation. Replace digest-only opportunism with parent-bound structural matching. A missing reference must reject only the malformed candidate link, never silently truncate an already-proven own chain. Keep `validateCommitLegality` on reconstructed links and retain cloning before exploratory mutation. Cite the current topic-organized spec path, not deprecated MIP numbering.
  </action>
  <acceptance_criteria>
    - The new regression constructs at least two own confirmed commits with a by-reference proposal and a same-epoch competitor.
    - The selected canonical tip is the complete own branch and no valid own link is omitted from the winner chain.
    - `pnpm vitest run src/engine/__tests__/convergence-parity.test.ts` exits 0.
    - `pnpm compile` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/engine/__tests__/convergence-parity.test.ts &amp;&amp; pnpm compile</automated>
  </verify>
  <done>A chained own-confirmed branch consuming proposals by reference is reconstructable and wins according to the canonical convergence policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add native MDK-derived own-confirmed-commit regressions (D-15, D-16)</name>
  <files>src/engine/__tests__/convergence-parity.test.ts</files>
  <read_first>
    - refs/mdk/crates/traits/src/message.rs — authoritative OwnCommitConvergenceStamp and OwnCommitWire persistence structure
    - refs/mdk/crates/cgka-engine/src/openmls_projection.rs — authoritative own_commit_stamp capture, stamp_processed_own_commit_record persistence handoff, and already_applied_commit_prefix recovery consumption
    - refs/mdk/crates/cgka-engine/src/fork_recovery.rs — canonical selection behavior to inspect after own-commit materialization
    - refs/mdk/crates/cgka-engine/src/group_state_changes.rs — authoritative state-change handling relevant to chained proposals
    - src/engine/__tests__/convergence-parity.test.ts — native harness and ciphersuite setup
    - .planning/phases/03-commit-integrity-convergence-parity/03-CONTEXT.md — D-15 prohibition on fixtures/scenario drivers and D-16 required properties
  </read_first>
  <behavior>
    - Focused native Vitest cases derived from inspected MDK behavior cover same-epoch sibling, longer competing branch, and chained by-reference proposal cases.
    - Both D-16 properties remain asserted: no own-commit rollback for a same-epoch sibling, and two independent in-memory engines given identical competing commits in opposite delivery orders select the same canonical branch.
  </behavior>
  <action>
After Plan 08 materializes refs/mdk, inspect the authoritative stamp definition/payload in `traits/src/message.rs`, its capture/persistence/consumption lifecycle in `openmls_projection.rs`, and canonical selection in `fork_recovery.rs`, then add focused native `it(...)` regressions to the existing Vitest file using its real MarmotGroupEngine/ForkRecovery harness. Per D-15, create no vector fixture module, serialized vector corpus, scenario interpreter, or table-driven scenario driver; those remain Phase 4/CONF-01 scope. For D-16, construct two independent in-memory MarmotGroupEngine instances from the same pre-fork state, deliver the identical same-epoch competing commits as `[commitA, commitB]` to one and `[commitB, commitA]` to the other, drain both ingest generators, and assert equality of their selected canonical branch identity (including the resulting confirmation tag). Also preserve the same-epoch own-commit non-rollback property and include the chained by-reference proposal regression required by verification. Cite the inspected MDK source paths and named functions/types in test comments without copying executable vector infrastructure.
  </action>
  <acceptance_criteria>
    - Native regression comments identify the inspected MDK source path and analogous function or behavior.
    - The D-16 dual-ordering regression uses two independent in-memory engines, opposite delivery order for identical commits, and asserts equal canonical confirmation tags.
    - No new fixture module, vector corpus, scenario interpreter, or scenario-driver infrastructure exists per D-15.
    - `pnpm vitest run src/engine/__tests__/convergence-parity.test.ts` exits 0.
    - `pnpm vitest run src/engine/__tests__/commit-legality-seams.test.ts` exits 0.
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/engine/__tests__/convergence-parity.test.ts src/engine/__tests__/commit-legality-seams.test.ts &amp;&amp; pnpm build</automated>
  </verify>
  <done>Focused native MDK-derived regressions, including chained proposal references, pass against the TypeScript engine without adding Phase 4 vector infrastructure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| retained history → candidate DFS | Persisted metadata influences which branch is considered valid and canonical. |
| remote fork material → canonical state | Malformed references must not truncate a valid local branch or bypass legality checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03G-08 | Tampering | own-commit stamp matching | high | mitigate | Bind evidence to commit digest and exact parent identity; validate reconstructed proposals and resulting state. |
| T-03G-09 | Denial of Service | candidate DFS | medium | mitigate | Reuse existing dedup/horizon bounds and avoid unbounded proposal searches. |
| T-03G-10 | Repudiation | MDK parity claim | high | mitigate | Annotate each native regression with the inspected upstream path/function and execute it through the real engine. |
| T-03G-11 | Elevation of Privilege | reconstructed commit | high | mitigate | Preserve shared validateCommitLegality on every adopted candidate edge. |
</threat_model>

<verification>
Run convergence-parity and commit-legality-seams together, then `pnpm build`.
</verification>

<success_criteria>
CONV-04 holds for shallow and chained own-confirmed branches and is demonstrated with focused native regressions derived from direct MDK inspection per D-15/D-16.
</success_criteria>

<output>
Create `.planning/phases/03-commit-integrity-convergence-parity/03-10-SUMMARY.md` when done.
</output>
