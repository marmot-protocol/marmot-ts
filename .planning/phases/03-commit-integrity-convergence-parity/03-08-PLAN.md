---
phase: 03-commit-integrity-convergence-parity
plan: 08
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
gap_closure: true
requirements: [WIRE-03, CONV-01]
must_haves:
  truths:
    - "The checked-in ts-mls, marmot, and MDK gitlinks are materialized without changing pnpm-lock.yaml or advancing a gitlink."
    - "WIRE-03 and CONV-01 rejection behavior executes at the pure validator, inbound/replay, and outbound seams."
  artifacts:
    - path: "ts-mls/package.json"
      provides: "Materialized local MLS workspace dependency"
    - path: "src/core/components/__tests__/integrity.test.ts"
      provides: "WIRE-03 and CONV-01 validator coverage"
    - path: "src/engine/__tests__/commit-legality-seams.test.ts"
      provides: "Inbound and replay legality coverage"
    - path: "src/engine/__tests__/send-commit-legality.test.ts"
      provides: "Outbound legality coverage"
  key_links:
    - from: "package.json"
      to: "ts-mls/package.json"
      via: "pnpm workspace dependency resolution"
      pattern: "ts-mls"
    - from: "src/engine/group-engine.ts"
      to: "src/core/components/integrity.ts"
      via: "shared validateCommitLegality send gate"
      pattern: "validateCommitLegality"
---

<objective>
Restore the executable local MLS prerequisite and run the existing Phase 03 legality slices end to end.

Purpose: Separate the empty-checkout environment blocker from production defects and turn WIRE-03/CONV-01 static evidence into behavioral evidence.
Output: A materialized, buildable ts-mls checkout and recorded green targeted commands; no production or lockfile edits.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-commit-integrity-convergence-parity/03-VERIFICATION.md
@.gitmodules
@package.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Materialize the pinned MLS dependency and prove one legality path end to end</name>
  <precondition>The repository is on a non-master branch and all three recorded gitlinks are available from their configured remotes.</precondition>
  <files>None — environment restoration only; preserve pnpm-lock.yaml and all gitlinks</files>
  <read_first>
    - .gitmodules — authoritative submodule locations
    - package.json — workspace declaration and local ts-mls dependency
    - .planning/phases/03-commit-integrity-convergence-parity/03-VERIFICATION.md — empty-checkout blocker and targeted behavior predicates
    - src/engine/__tests__/commit-legality-seams.test.ts — one vertical inbound/replay legality path
  </read_first>
  <action>
Record `git status --short --branch` and `git submodule status ts-mls refs/marmot refs/mdk`. Before any environment-changing command, run `sha256sum pnpm-lock.yaml > /tmp/marmot-ts-phase-03-pnpm-lock.sha256`; this non-repository baseline intentionally persists for later tasks and plans. Run `git submodule update --init --recursive ts-mls refs/marmot refs/mdk` to materialize exactly the three pinned gitlinks, then verify `ts-mls/package.json`, `refs/marmot/foundation`, and `refs/mdk/crates/cgka-engine` exist and every submodule HEAD equals its superproject gitlink. Run `pnpm --filter ts-mls build`, `pnpm compile`, and `pnpm vitest run src/engine/__tests__/commit-legality-seams.test.ts`. Run `sha256sum -c /tmp/marmot-ts-phase-03-pnpm-lock.sha256` and fail if it reports any change; do not run dependency installation, update a gitlink, or edit production files. This is the production-quality tracer because it proves the real core validator through the MLS-backed engine seam, not a mocked substitute, while also restoring the authoritative sources Plan 03-10 consumes.
  </action>
  <acceptance_criteria>
    - `test -s ts-mls/package.json` exits 0
    - Each of `ts-mls`, `refs/marmot`, and `refs/mdk` has HEAD equal to its superproject gitlink
    - `pnpm --filter ts-mls build` exits 0
    - `pnpm compile` exits 0
    - `pnpm vitest run src/engine/__tests__/commit-legality-seams.test.ts` exits 0
    - `/tmp/marmot-ts-phase-03-pnpm-lock.sha256` contains the pre-task lockfile hash and `sha256sum -c /tmp/marmot-ts-phase-03-pnpm-lock.sha256` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test -s /tmp/marmot-ts-phase-03-pnpm-lock.sha256 &amp;&amp; test -s ts-mls/package.json &amp;&amp; test -d refs/marmot/foundation &amp;&amp; test -d refs/mdk/crates/cgka-engine &amp;&amp; for p in ts-mls refs/marmot refs/mdk; do test "$(git -C "$p" rev-parse HEAD)" = "$(git ls-tree HEAD "$p" | awk '{print $3}')" || exit 1; done &amp;&amp; pnpm --filter ts-mls build &amp;&amp; pnpm compile &amp;&amp; pnpm vitest run src/engine/__tests__/commit-legality-seams.test.ts &amp;&amp; sha256sum -c /tmp/marmot-ts-phase-03-pnpm-lock.sha256</automated>
  </verify>
  <done>The pinned MLS workspace is present and one real WIRE-03/CONV-01 engine path executes successfully without repository-content drift.</done>
</task>

<task type="auto">
  <name>Task 2: Execute all existing WIRE-03 and CONV-01 seam regressions</name>
  <precondition>ts-mls/package.json exists and `pnpm --filter ts-mls build` succeeds.</precondition>
  <files>None — behavioral verification only</files>
  <read_first>
    - src/core/components/__tests__/integrity.test.ts — pure integrity/admin-leaf predicates
    - src/engine/__tests__/commit-legality-seams.test.ts — inbound and convergence/replay parity
    - src/engine/__tests__/send-commit-legality.test.ts — send and tree-fed parity
    - .planning/phases/03-commit-integrity-convergence-parity/03-CONTEXT.md — D-01 through D-09 locked seam behavior
  </read_first>
  <action>
Run the three verification-owned suites together after the MLS build. Treat every failure as a production gap to report rather than weakening or skipping an assertion. Then run the focused library build. Capture command results in the plan summary so Phase 03 re-verification has executable evidence for WIRE-03 and CONV-01.
  </action>
  <acceptance_criteria>
    - `pnpm vitest run src/core/components/__tests__/integrity.test.ts src/engine/__tests__/commit-legality-seams.test.ts src/engine/__tests__/send-commit-legality.test.ts` exits 0
    - `pnpm build` exits 0
    - `sha256sum -c /tmp/marmot-ts-phase-03-pnpm-lock.sha256` exits 0 against Task 1's pre-change baseline
  </acceptance_criteria>
  <verify>
    <automated>test -s /tmp/marmot-ts-phase-03-pnpm-lock.sha256 &amp;&amp; pnpm vitest run src/core/components/__tests__/integrity.test.ts src/engine/__tests__/commit-legality-seams.test.ts src/engine/__tests__/send-commit-legality.test.ts &amp;&amp; pnpm build &amp;&amp; sha256sum -c /tmp/marmot-ts-phase-03-pnpm-lock.sha256</automated>
  </verify>
  <done>WIRE-03 and CONV-01 are behaviorally rerun across the pure, inbound, replay, tree-fed, and send seams.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| superproject → submodule checkout | The build consumes code identified by a pinned gitlink. |
| untrusted commit → canonical MLS state | Legality gates must reject before state adoption. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03G-01 | Tampering | ts-mls checkout | high | mitigate | Initialize only the recorded gitlink and assert its exact HEAD before executing tests. |
| T-03G-02 | Tampering | pnpm-lock.yaml | high | mitigate | Check the existing lockfile remains byte-unchanged; no install/update command is authorized. |
| T-03G-03 | Repudiation | WIRE-03/CONV-01 verification | medium | mitigate | Record exact targeted command outcomes in 03-08-SUMMARY.md. |
</threat_model>

<verification>
`pnpm --filter ts-mls build`, `pnpm build`, and the three named legality suites all exit 0; pnpm-lock.yaml is unchanged.
</verification>

<success_criteria>
The environment blocker is removed independently of production fixes, and WIRE-03/CONV-01 have executable seam-level evidence.
</success_criteria>

<output>
Create `.planning/phases/03-commit-integrity-convergence-parity/03-08-SUMMARY.md` when done.
</output>
