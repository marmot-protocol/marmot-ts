---
phase: 03.1-phase-3-review-closure
plan: 06
subsystem: client-trust-boundary
tags: [nostr, lru, removal, persistence, deduplication]
requires:
  - phase: 03.1-phase-3-review-closure
    provides: listener-first single-flight removal realization from plan 01
provides:
  - bounded accepted and rejected identity caches for group subscriptions
  - exact subscribed-group routing validation before accepted deduplication
  - collision-safe durable removal markers under the group removed namespace
affects: [groups-manager, removal-realization, subscription-security]
tech-stack:
  added: []
  patterns: [bounded-id-lru, validation-before-dedup, group-scoped-store-keys]
key-files:
  created: []
  modified:
    - src/client/groups-manager.ts
    - src/client/group/marmot-group.ts
    - src/__tests__/groups-manager.test.ts
    - src/__tests__/integration/removed.test.ts
key-decisions:
  - "Validate each delivery before consulting its rejected-id history, so malformed identities stay bounded without censoring a later valid same-id event."
  - "Store removal realization only at `${groupId}/removed`, including shared-backend and teardown paths."
patterns-established:
  - "Long-lived subscription identity memory uses deterministic bounded LRUs rather than unbounded Sets."
  - "Logical stores sharing one backend isolate auxiliary state with group-scoped suffix keys."
requirements-completed: [CONV-02]
coverage:
  - id: D1
    description: "Group subscriptions bound accepted and rejected identities while rejecting wrong-group events before accepted deduplication."
    requirement: CONV-02
    verification:
      - kind: integration
        ref: "src/__tests__/groups-manager.test.ts#GroupsManager connectGroup drain trust boundary"
        status: pass
    human_judgment: false
  - id: D2
    description: "Durable removal realization cannot overwrite serialized group state on a shared backend."
    requirement: CONV-02
    verification:
      - kind: integration
        ref: "src/__tests__/integration/removed.test.ts#keeps removal markers in a group-scoped namespace on a shared backend"
        status: pass
    human_judgment: false
duration: 7min
completed: 2026-09-02
status: complete
---

# Phase 03.1 Plan 06: Trust-Boundary Cache and Marker Isolation Summary

**Bounded Nostr subscription identity LRUs and group-scoped removal markers close memory-amplification and shared-backend collision paths without same-id censorship.**

## Performance

- **Duration:** 7 min
- **Started:** 2026-09-02T14:29:00Z
- **Completed:** 2026-09-02T14:36:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Replaced the unbounded accepted-id set with a deterministic 10,000-entry LRU and added an equally bounded rejected-id LRU.
- Enforced exact singleton `h` equality with the subscribed group before accepted caching, while preserving valid delivery after a same-id malformed forgery.
- Moved durable removal realization to `${groupId}/removed` for reads, writes, destroy, and supersession clearing, with a shared-backend collision regression.

## Task Commits

1. **Task 1: Reject a wrong-group event before bounded identity caching**
   - `fd7116d` (test): failing cache, routing, exact-count, and censorship regressions
   - `48e3272` (fix): bounded accepted/rejected LRUs and exact group routing
   - `3e5fd55` (fix): exact per-delivery rejection assertions and scoped marker assertion update
2. **Task 2: Namespace and clear the durable removal marker**
   - `ba49111` (test): failing shared-backend namespace regression
   - `66731a9` (fix): namespaced marker access and option documentation

## Files Created/Modified

- `src/client/groups-manager.ts` - Bounded identity LRUs and validation-before-dedup group routing gate.
- `src/client/group/marmot-group.ts` - Central group-scoped removal marker key for all marker operations.
- `src/__tests__/groups-manager.test.ts` - LRU capacity, exact count, wrong-group, and same-id censorship regressions.
- `src/__tests__/integration/removed.test.ts` - Shared-backend collision and namespaced lifecycle assertions.

## Decisions Made

- Rejected-id history is populated only after the current event fails verification or routing validation; it never short-circuits validation of a later same-id delivery.
- Rejection events remain one-for-one with actual relay deliveries: two for the mock backfill-plus-subscription replay and one for a single delivery.
- The existing typed `tag-cardinality` rejection reason covers a singleton `h` whose value does not match the subscribed group, avoiding an unrelated public union expansion.

## Deviations from Plan

None - plan executed exactly as written.

## Verification

- Both task regressions failed before their respective implementation changes.
- `PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm vitest run src/__tests__/groups-manager.test.ts src/__tests__/integration/removed.test.ts` — 22 tests passed.
- `PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm compile` — passed.
- `pnpm-lock.yaml` SHA256 remained `0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932` before and after every pnpm command.

## Known Stubs

None.

## Issues Encountered

- The first plan-wide acceptance pass exposed two stale bare-marker assertions and clarified that exact rejection counts describe deliveries, not unique rejected ids; both were corrected and the full verification rerun.
- The unrelated `src/__tests__/exports.test.ts` snapshot remains assigned to plan 03.1-08 and was not touched.

## Next Phase Readiness

- D-14/D-15, WR-08, WR-19, and IN-02 are pinned by focused regressions.
- CONV-02 removal realization now remains collision-safe even when applications intentionally share a durable backend across typed logical stores.

## Self-Check: PASSED

- All four modified implementation/test files exist.
- All five RED/GREEN/correction commits are present in git history.
- Focused tests, strict compile, exact rejection counts, and the lockfile invariant passed.
