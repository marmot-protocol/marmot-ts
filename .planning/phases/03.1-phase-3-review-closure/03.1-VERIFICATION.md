---
phase: 03.1-phase-3-review-closure
verified: 2026-09-02T15:47:30Z
status: gaps_found
score: 5/7 must-haves verified
behavior_unverified: 0
overrides_applied: 0
gaps:
  - truth: "Removal activation is durable, retryable, and reaches the public consumer exactly once."
    status: failed
    reason: "GroupRegistry caches a group before activation completes and does not untrack it if reconvergence or removal realization rejects; a subsequent get() returns the partially activated cached instance without retrying."
    artifacts:
      - path: "src/client/group-registry.ts"
        issue: "track() inserts and attaches listeners before awaited activation, but has no rejection cleanup or cached activation state."
    missing:
      - "On activation failure, detach listeners, remove/dispose the exact cached instance, and permit a later load to retry, or make cache hits await a retryable activation promise."
      - "Add rejection-and-retry tests for reconvergence and removal-marker storage failures."
  - truth: "Removal realization remains complete and observable when application listeners throw."
    status: failed
    reason: "The durable marker is written before synchronous EventEmitter delivery. A throwing removed listener rejects realization after the marker is committed; retries then return early and cannot complete or re-emit the transaction."
    artifacts:
      - path: "src/client/group/marmot-group.ts"
        issue: "#performRemovalRealization writes the marker at line 738 and calls emit at line 748 without isolating listener exceptions or rolling back the marker."
    missing:
      - "Define and implement non-transactional listener-failure semantics, or roll back the marker when the remaining realization work fails."
      - "Add a throwing-listener retry regression that asserts marker, queued-outbound, and public-event semantics."
  - truth: "An elected self-remove auto-commit cannot leave the group lifecycle stranded if the consumer stops iteration."
    status: failed
    reason: "MarmotGroup.ingest yields autoCommit before calling runtime.publishEffects. Async-generator cancellation after the first result skips both confirmation and rollback, leaving PendingPublish staged indefinitely."
    artifacts:
      - path: "src/client/group/marmot-group.ts"
        issue: "The first yield is at line 798; publication/confirmation begins only after generator resumption at lines 799-825."
    missing:
      - "Complete the publish attempt before the first externally cancellable yield while preserving deterministic result ordering."
      - "Add a test that calls next() once, then return(), and proves lifecycle is not PendingPublish."
  - truth: "Every relay-acknowledged publish returns a typed, non-retryable post-confirm outcome even when persistence fails."
    status: failed
    reason: "publishProposal confirms local state after relay acknowledgement and then directly awaits save(); a save rejection escapes as an apparent publication failure, unlike the typed commit/selfUpdate post-confirm contract."
    artifacts:
      - path: "src/client/runtime/group-runtime.ts"
        issue: "publishProposal at lines 167-177 returns only after an unguarded #save; publishEffects' successful proposal result is therefore unreachable on post-ack save failure."
    missing:
      - "Return an inspectable proposal persistence failure with retryPublication: false after relay acknowledgement."
      - "Add a proposal save-rejection regression proving state remains applied and republication is forbidden."
---

# Phase 03.1: Phase 3 Review Closure Verification Report

**Phase Goal:** Close Phase 3 review findings and complete CONV-02/CONV-03 scope with durable, observable notification/convergence behavior and exact closure criteria.
**Verified:** 2026-09-02T15:47:30Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Roadmap truth | Status | Evidence |
|---|---|---|---|
| 1 | Load-time removal reaches the real consumer after registry forwarding is attached. | ✗ FAILED | The successful path and public manager test exist, but `GroupRegistry.track()` caches before awaited activation and leaves the instance cached on rejection (`group-registry.ts:257-275,318-340`). `MarmotGroup` also commits the marker before a synchronous, throwable public callback (`marmot-group.ts:733-748`). Durability and retryability therefore do not hold on observable failure paths. |
| 2 | `commit` and `selfUpdate` authorize the exact by-reference proposal union using local identity. | ✓ VERIFIED | Shared `src/core/commit-authorization.ts` is wired into both engine seams; pure and seam tests passed in the independent 774-test run, including malformed sibling credentials and referenced proposals. |
| 3 | Locally confirmed commits derive/record consumer-visible notifications and emit `historyChanged`. | ✓ VERIFIED | `confirmPublished` notification flow, ledger attribution/withdrawal, self-remove delivery, and local-history tests pass. However, an auto-commit can remain unconfirmed and stranded if iteration stops after its first yield; that separate lifecycle blocker is recorded below. |
| 4 | Notification and payload ledgers use digest-keyed lookup and correctness-safe tree-derived pruning, with Infinity documented honestly. | ✓ VERIFIED | Both ledgers use nested `Map` indexes; engine pruning supplies the retained/tree horizon; ledger tests cover empty records, digest+epoch identity, invalidation, and `min(anchor, oldestTreeEpoch)`. `deferred-items.md` explicitly records unbounded retention under Infinity pending tree pruning. |
| 5 | Tree sweeping is gated by delivery needs, not self-eviction alone. | ✓ VERIFIED | Production sweep/withdrawal wiring is present and the state-notification withdrawal suite passes, including ordered notification and payload invalidations. |
| 6 | The exact Phase-3-touched manifest has no deprecated `MIP-NN` citations. | ✓ VERIFIED | An independent `rg 'MIP-[0-9]+'` over the ten-file D-16 manifest returned no matches; `CLAUDE.md` points to `refs/marmot/mip-coverage.md`. |
| 7 | Every carried review concern is fixed or explicitly accepted/deferred, with exact closure behavior. | ✗ FAILED | Current `03.1-REVIEW.md` identifies four production-path blockers not covered by the green suite: auto-commit generator cancellation, activation rejection caching, throwing removal listeners, and proposal post-ack persistence failure. None is assigned to a later roadmap phase. |

**Score:** 5/7 roadmap truths verified (0 present, behavior-unverified)

### Every PLAN Must-Have

| Plan | Truths | Artifacts | Key links | Verdict |
|---|---|---|---|---|
| 03.1-01 | Listener-first public removal, single-flight realization, pinned references | All 3 exist and are substantive | Forwarder-before-realization and marker-store links exist | ✗ PARTIAL — successful/concurrent paths pass, but activation rejection remains cached and a throwing listener permanently consumes the marker. |
| 03.1-02 | Commit/selfUpdate post-confirm persistence and Welcome outcomes remain observable; pre-confirm relay failure rolls back | All 3 exist and are substantive | Runtime confirmation maps to typed ancillary outcomes | ✓ VERIFIED for its literal commit/selfUpdate scope. A homologous acknowledged-proposal gap remains and blocks the broader closure goal. |
| 03.1-03 | Applied notification stream for auto-commit/tree adoption; safe envelope-less audit; exact withdrawal; retry classification | All 6 exist and are substantive | Manual trace confirms group→session forwarding and engine ledger→withdrawal wiring | ✗ PARTIAL — drained success paths pass, but yielding `autoCommit` before publication allows consumer cancellation to strand PendingPublish. |
| 03.1-04 | Shared outbound authorization and malformed-member tolerance | All 3 exist and are substantive | Both engine send seams call the core predicate | ✓ VERIFIED; authorization and group-member suites pass. |
| 03.1-05 | Digest-keyed ledgers, safe horizon, own-payload invalidation, delivery-gated sweeping | All 3 exist and are substantive | Engine supplies tree horizon and records local application payloads | ✓ VERIFIED; notification, delivered-payload, and withdrawal suites pass. |
| 03.1-06 | Namespaced marker; bounded accepted/rejected IDs; pre-cache `h` check; exact rejection behavior | Both artifacts exist and are substantive | `h` is checked before LRU insertion | ✓ VERIFIED; groups-manager tests pass. |
| 03.1-07 | Exact legality error/audit/decode diagnostics | Both artifacts exist and are substantive | Structured integrity reason reaches send error and audit | ✓ VERIFIED; legality suites pass. |
| 03.1-08 | Root-nameable public signature types and runtime export snapshot | Both artifacts exist and are substantive | Explicit root exports are exercised by compile imports | ✓ VERIFIED; export snapshot and compile pass. |
| 03.1-09 | Engine-derived session union, shared byte equality, exhaustive disposition, removed guard, canonical member helper | All 3 exist and are substantive | Session result mapping is derived from engine union | ✓ VERIFIED; compile and focused tests pass. |
| 03.1-10 | Send-seam rollback, local `historyChanged`, namespaced local teardown | All 4 artifacts exist and are substantive | Engine lifecycle rollback and session marker teardown are wired | ✓ VERIFIED for the named commit/selfUpdate construction failures. The distinct auto-commit generator-cancellation path remains open. |
| 03.1-11 | Exact manifest citation migration and maintainer mapping pointer | Both artifacts exist and are substantive | Manifest citations use current `refs/marmot/...` paths | ✓ VERIFIED by independent negative scan. |

All artifact declarations passed existence/substance checks. Automated key-link queries reported false negatives only for descriptive/non-path `from` values in 03.1-03 and 03.1-11; manual code and manifest tracing verified those intended links. All prohibition clauses were checked: no OwnCommitConvergenceStamp/fork-selection expansion, EventEmitter was not substituted for result delivery, `SendResult` was unchanged, the citation scan stayed manifest-scoped, and the lockfile was not changed.

### Required Artifacts and Data Flow

| Artifact group | Exists / substantive | Wiring and real data | Status |
|---|---|---|---|
| Removal (`group-registry.ts`, `marmot-group.ts`) | Yes | Persisted tombstone/marker flows to a real manager event on the success path; exception recovery is incomplete | ✗ PARTIAL |
| Notifications (`engine/types.ts`, `group-engine.ts`, `group-session.ts`) | Yes | Exact commit digest and derived notifications flow through confirmation and withdrawal ledgers | ✓ VERIFIED |
| Ledgers (`state-notifications.ts`, `delivered-payloads.ts`, `history-tree.ts`) | Yes | Real commit/payload identities and tree epochs populate pruning/invalidation paths | ✓ VERIFIED |
| Authorization (`commit-authorization.ts`, `group-engine.ts`) | Yes | Actual by-value/by-reference proposals and local leaf identity feed one predicate | ✓ VERIFIED |
| Runtime effects (`group-runtime.ts`, `group-effects.ts`) | Yes | Commits/selfUpdates expose typed ancillary failure; proposal save failure escapes instead | ✗ PARTIAL |
| Trust caches (`groups-manager.ts`) | Yes | Verified event/group IDs flow through bounded caches after `h` validation | ✓ VERIFIED |
| Public exports and citations | Yes | Root imports/snapshot and exact manifest scan are executable | ✓ VERIFIED |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| Registry cache | removal realization | listener-first `track()` activation | ✗ PARTIAL | Correct ordering on success; no atomic cleanup/activation state on rejection. |
| Removal marker | public `removed` event | single-flight realization | ✗ PARTIAL | Marker precedes throwable callback, so failure is durably suppressive. |
| Auto-commit ingest result | runtime publication | async-generator continuation | ✗ NOT SAFE | Publication only begins after the consumer resumes beyond the first yield. |
| Commit/selfUpdate confirmation | applied notification stream | typed `GroupPublishResult` and mapped ingest variant | ✓ WIRED | Tests prove digest attribution, delivery, and later withdrawal. |
| Proposal acknowledgement | typed ancillary outcome | confirm then persist | ✗ PARTIAL | Confirmation occurs, but persistence rejection bypasses the typed outcome. |
| Engine ledgers | history tree | `oldestEpoch` plus retained anchor | ✓ WIRED | Correctness horizon is supplied to both ledgers. |
| Nostr `h` tag | bounded identity caches | equality gate before insertion | ✓ WIRED | Production and same-id-censorship tests pass. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full runnable workspace behavior | `PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm vitest run` | 80 files, 774 tests passed | ✓ PASS |
| Strict library compile | `PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm compile` | `tsc -b tsconfig.build.json`, exit 0 | ✓ PASS |
| Four uncovered exception/cancellation paths | Source trace plus test-name enumeration | No named regression exercises iterator cancellation, activation retry, throwing-listener recovery, or proposal save rejection | ✗ FAIL |
| Lockfile integrity | `sha256sum pnpm-lock.yaml` after checks | `0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932` | ✓ PASS |

### Probe Execution

No phase probe script is declared. The independent full Vitest run and compile are the executable evidence. Passing existing tests do not override the uncovered source-level failure paths.

### Requirements Coverage

| Requirement | Source plans | Status | Evidence |
|---|---|---|---|
| CONV-02 | 01, 03, 04, 06, 07, 08, 09, 10, 11 | ✗ BLOCKED | Normal SelfEvicted/removal classification, namespaced durability, and public delivery pass, but activation rejection and throwing-listener recovery can permanently suppress reliable removal completion. |
| CONV-03 | 02, 03, 04, 05, 07, 08, 09, 10, 11 | ✗ BLOCKED | Notification attribution/withdrawal works on exercised paths, but auto-commit cancellation can strand the locally staged commit, and acknowledged proposal persistence failure remains reported with unsafe retry semantics. |

Both declared IDs exist in `.planning/REQUIREMENTS.md` and are mapped to Phase 3. No additional requirement is mapped specifically to Phase 03.1, so there is no orphaned ID.

### Anti-Patterns Found

No unreferenced `TBD`, `FIXME`, or `XXX` marker, placeholder implementation, or empty handler was found in the phase-modified source scope. The two repository TODOs found are in unrelated pre-existing key-package/protocol files and are not Phase 03.1 modifications.

### Human Verification Required

None. This is a non-visual TypeScript library phase. The four gaps are observable from deterministic control flow and missing regression coverage; they are failures, not matters of human judgment.

### Deferred Items

The accepted `maxRewindCommits: Infinity` memory consequence is explicitly recorded in Phase 3 `deferred-items.md` and depends on future history-tree pruning. Phase 4 explicitly owns OwnCommitConvergenceStamp/fork-recovery restructuring and conformance vectors. None of the four gaps above matches a later phase goal or success criterion, so none is deferred.

### Disconfirmation Pass

- Partially met requirement: CONV-02 works on ordinary load/removal but is not retry-safe when activation or a public listener throws.
- Misleading green coverage: the self-remove tests fully drain the async generator, so they cannot detect cancellation after the `autoCommit` yield.
- Untested error path: proposal publication has no post-ack `save()` rejection test; the only proposal tests cover successful save and pre-ack relay failure.

### Gaps Summary

Phase 03.1 implemented and tested most of its planned closure surface, but the exact closure goal is not achieved. Four deterministic gaps remain at cancellation/exception boundaries. They can strand lifecycle state, permanently bypass removal activation, suppress a durable removal signal, or encourage retry of an already-acknowledged proposal. These are BLOCKER findings and must be closed before the phase proceeds.

---

_Verified: 2026-09-02T15:47:30Z_
_Verifier: Codex (gsd-verifier)_
