---
phase: 07-account-identity-proof-component-0x8009-legacy-clean-cut
plan: 05
type: execute
wave: 2
depends_on: [07-02]
files_modified:
  - src/__tests__/integration/ingest-commit-race.test.ts
  - src/__tests__/integration/removed.test.ts
  - src/__tests__/integration/self-remove.test.ts
  - src/__tests__/integration/convergence-status.test.ts
  - src/__tests__/integration/own-proposal-snapshot.test.ts
  - src/__tests__/integration/history-tree-persistence.test.ts
  - src/__tests__/integration/app-message-replay-restart.test.ts
  - src/__tests__/integration/rewind-persistence.test.ts
  - src/__tests__/integration/self-update-persistence.test.ts
  - src/__tests__/conformance/adapter.test.ts
  - src/__tests__/conformance/smoke.test.ts
  - src/__tests__/conformance/extended.spec.ts
autonomous: true
requirements: [PROOF-03]

must_haves:
  truths:
    - "Every generateKeyPackage call in the nine integration test files and three conformance files passes a real signer whose pubkey equals the credential identity (D-02)"
    - "Signers are PrivateKeyAccount.signer instances passed directly, drawn from accounts already in scope (for example the conformance accounts map) or testAccount(slot) (D-04)"
    - "Identity order used by race, removal, and convergence scenarios is preserved, and no assertion is weakened or re-baselined (D-02)"
    - "The root Vitest suite and the extended conformance suite (pnpm conformance:extended) both pass with production code untouched (PROOF-03 groundwork)"
  artifacts:
    - path: "src/__tests__/integration/ingest-commit-race.test.ts"
      provides: "Race scenarios built with real signers, identity order preserved"
      contains: "signer:"
    - path: "src/__tests__/conformance/extended.spec.ts"
      provides: "Extended conformance KeyPackages signed by the scenario accounts"
      contains: "signer:"
  key_links:
    - from: "src/__tests__/conformance/extended.spec.ts"
      to: "src/core/key-package.ts"
      via: "generateKeyPackage({ credential, signer: accounts.get(client)!.signer, ciphersuiteImpl })"
      pattern: "signer: accounts\\.get"
    - from: "src/__tests__/integration/*.test.ts"
      to: "src/__tests__/helpers/test-accounts.ts"
      via: "testAccount(slot) deterministic identities"
      pattern: "testAccount\\("
---

<objective>
Make every integration and conformance test build KeyPackages with a real signer matching the credential identity
(D-02), passing `PrivateKeyAccount.signer` directly (D-04). Plan 07-06 can then make `generateKeyPackage`'s `signer`
required without breaking the multi-member scenarios.

Purpose: this plan is test-only and wire-neutral. It covers the heaviest scenario files (commit races, removal,
convergence, restart persistence) and the extended conformance runner that CI executes separately.

Output: nine integration test files and three conformance files updated; no production file, export, or fixture changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-account-identity-proof-component-0x8009-legacy-clean-cut/07-CONTEXT.md
@.planning/phases/07-account-identity-proof-component-0x8009-legacy-clean-cut/07-02-SUMMARY.md
@src/__tests__/helpers/test-accounts.ts
@src/core/key-package.ts

Migration rules (identical in plans 07-03, 07-04, 07-05; each plan owns a disjoint file set):

1. Every `generateKeyPackage({ ... })` call in an owned file passes `signer: X.signer`, where `X` is a
   `PrivateKeyAccount` whose `pubkey` equals the credential identity passed as `credential`.
2. Obtain `X` from an existing `PrivateKeyAccount` already in scope when there is one. Otherwise use
   `testAccount(slot)` imported from `../helpers/test-accounts.js`.
3. When a credential used a literal test pubkey (for example `"a".repeat(64)`), replace that literal with the derived
   `pubkey` of one account held in a variable, and use that same variable everywhere the literal meant the same identity.
4. Preserve relative order. Within a file, assign slots in the same ascending order as the literals sort. Use this
   default mapping:
   - `"2".repeat(64)` → slot 0
   - `"3".repeat(64)` → slot 1
   - `"4".repeat(64)` → slot 2
   - `"884704bd…c3a6"` → slot 5
   - `"a".repeat(64)` → slot 6
   - `"d".repeat(64)` → slot 9
   - `"e".repeat(64)` → slot 11
   Pick any other free slots in ascending order.
5. A literal pubkey that never backs a KeyPackage may stay literal. If ordering then breaks, migrate it too.
6. A local setup helper that takes a pubkey string and generates a KeyPackage inside changes minimally. It takes the
   account, or the pubkey plus signer.
7. Never weaken, delete, or re-baseline an assertion to make a test pass. If an assertion encoded a literal pubkey value
   rather than identity equality, update it to the derived value and list it in the SUMMARY.
8. No production file changes.

Signer gate (used in acceptance criteria). This Node one-liner prints every `generateKeyPackage({...})` call whose
options object lacks `signer` and exits 1 if any exist:

`node -e 'const fs=require("fs");let bad=0;for(const f of process.argv.slice(1)){const s=fs.readFileSync(f,"utf8");for(const m of s.matchAll(/generateKeyPackage\(\s*\{[\s\S]*?\}\s*\)/g)){if(!/\bsigner\b/.test(m[0])){bad++;console.log(f+": "+m[0].replace(/\s+/g," ").slice(0,100))}}}process.exit(bad?1:0)' FILES...`

Plan-specific facts verified on 2026-09-14:

- ingest-commit-race.test.ts has 11 generateKeyPackage sites. Each scenario redeclares `adminPubkey = "a".repeat(64)` and `memberPubkey = "e".repeat(64)`, and one scenario uses `"d".repeat(64)` for a second member.
- extended.spec.ts already derives identities from an `accounts` map (`accounts.get(client)!.signer.getPublicKey()`), so its fix is adding `signer: accounts.get(client)!.signer`. It runs only under `pnpm conformance:extended` (`vitest.extended.config.ts`), which CI executes on Node and Deno.
- CI runs `pnpm vitest run` and `pnpm conformance:extended` as separate steps.
</context>

## Artifacts this phase produces

None. This plan is a test-only migration: it creates no production symbol, file, export, or fixture. It modifies the 12
test files listed in `files_modified`.

## Source Coverage Audit (this plan)

| Source | ID | Item | Status |
| --- | --- | --- | --- |
| CONTEXT | D-02 | Tests pass a real signer (integration and conformance) | COVERED (Tasks 1-2) |
| CONTEXT | D-04 | Callers pass PrivateKeyAccount.signer directly | COVERED (Tasks 1-2) |
| REQ | PROOF-03 | Prerequisite for required-signer KeyPackage generation | COVERED (groundwork; flip in 07-06) |
| RESEARCH | PITFALLS 14 | No fixture silently keeps a proof-less happy path | COVERED (signer gate) |

<tasks>

<task type="auto">
  <name>Task 1: Real signers in commit-race, removal, self-remove, and convergence-status integration scenarios (D-02, D-04)</name>
  <files>src/__tests__/integration/ingest-commit-race.test.ts, src/__tests__/integration/removed.test.ts, src/__tests__/integration/self-remove.test.ts, src/__tests__/integration/convergence-status.test.ts</files>
  <read_first>
    - src/__tests__/helpers/test-accounts.ts
    - src/core/key-package.ts (GenerateKeyPackageOptions.signer as left by plan 07-02)
    - src/__tests__/integration/ingest-commit-race.test.ts: `grep -n "generateKeyPackage\|repeat(64)\|Pubkey =" src/__tests__/integration/ingest-commit-race.test.ts`, then read each scenario's setup block (around lines 40-75, 140-170, 305-330, 435-460, 610-640, 745-780, 925-950, 1060-1150)
    - src/__tests__/integration/removed.test.ts lines 90-130 and 280-300 (key packages and GroupsManager construction)
    - src/__tests__/integration/self-remove.test.ts lines 95-130 and src/__tests__/integration/convergence-status.test.ts lines 110-240
  </read_first>
  <action>
    Apply migration rules 1-8 from this plan's context, per D-02 and D-04, to the four files.

    In ingest-commit-race.test.ts, give each scenario its own account variables using rule 4 ("a"→slot 6, "d"→slot 9, "e"→slot 11). This keeps admin < second member < member in every scenario. Replace every in-scenario use of the replaced literals with the derived pubkey variable.

    In removed.test.ts and self-remove.test.ts, keep the admin/member/evicted identity ordering intact. These scenarios attribute removals to authenticated committer identities.

    Remove any import that becomes unused. Run each file's tests right after migrating it. Run prettier on the changed files.
  </action>
  <verify>
    <automated>pnpm vitest run src/__tests__/integration/ingest-commit-race.test.ts src/__tests__/integration/removed.test.ts src/__tests__/integration/self-remove.test.ts src/__tests__/integration/convergence-status.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The signer gate from this plan's context, run on the four files of this task, prints nothing and exits 0
    - The vitest command in verify exits 0
    - `git diff --stat HEAD -- src ':(exclude)src/**/__tests__/**' ':(exclude)src/__tests__/**'` prints nothing
    - `pnpm exec prettier --check` on the four files exits 0
  </acceptance_criteria>
  <done>The race, removal, self-remove, and convergence-status scenarios build every KeyPackage with an identity-matching account signer and keep their identity ordering and assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Real signers in persistence and replay integration tests and the conformance runners (D-02, D-04)</name>
  <files>src/__tests__/integration/own-proposal-snapshot.test.ts, src/__tests__/integration/history-tree-persistence.test.ts, src/__tests__/integration/app-message-replay-restart.test.ts, src/__tests__/integration/rewind-persistence.test.ts, src/__tests__/integration/self-update-persistence.test.ts, src/__tests__/conformance/adapter.test.ts, src/__tests__/conformance/smoke.test.ts, src/__tests__/conformance/extended.spec.ts</files>
  <read_first>
    - src/__tests__/helpers/test-accounts.ts
    - src/__tests__/conformance/extended.spec.ts lines 1-70 (accounts and identities maps)
    - src/__tests__/conformance/adapter.test.ts lines 60-175 and src/__tests__/conformance/smoke.test.ts lines 95-125
    - For the five integration files: `grep -n "generateKeyPackage\|createCredential\|repeat(64)\|Pubkey" <file>` first, then read the call sites (app-message-replay-restart has alice/bob/carol plus admin/member scenarios)
  </read_first>
  <action>
    Apply migration rules 1-8 from this plan's context, per D-02 and D-04, to the eight files.

    - extended.spec.ts: add signer: accounts.get(client)!.signer to the generateKeyPackage options. The credential already comes from the same account's pubkey.
    - adapter.test.ts and smoke.test.ts: reuse any existing account; otherwise use testAccount(slot) per rule 2.
    - app-message-replay-restart.test.ts: if alice, bob, and carol are literal pubkeys, keep alice < bob < carol ordering with ascending slots.

    Remove any import that becomes unused. Run prettier on the changed files. Then run the root test typecheck, the full suite, and the extended conformance suite.
  </action>
  <verify>
    <automated>pnpm vitest run src/__tests__/integration/own-proposal-snapshot.test.ts src/__tests__/integration/history-tree-persistence.test.ts src/__tests__/integration/app-message-replay-restart.test.ts src/__tests__/integration/rewind-persistence.test.ts src/__tests__/integration/self-update-persistence.test.ts src/__tests__/conformance/adapter.test.ts src/__tests__/conformance/smoke.test.ts && pnpm exec tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The signer gate from this plan's context, run on all 12 files in `files_modified`, prints nothing and exits 0
    - The vitest command in verify exits 0 and `pnpm exec tsc -p tsconfig.json --noEmit` exits 0
    - `pnpm conformance:extended` exits 0
    - `grep -c "signer: accounts.get(client)!.signer" src/__tests__/conformance/extended.spec.ts` is at least 1
    - `pnpm exec prettier --check src/__tests__/integration src/__tests__/conformance` exits 0
    - `git diff --stat HEAD -- src ':(exclude)src/**/__tests__/**' ':(exclude)src/__tests__/**'` prints nothing
    - `pnpm vitest run` (full suite) exits 0
  </acceptance_criteria>
  <done>All integration and conformance KeyPackages are built with identity-matching account signers, and the root suite, the test typecheck, and the extended conformance suite pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scenario fixtures → verification confidence | Multi-member scenarios are the evidence for join, removal, and convergence behavior under real proofs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-19 | Tampering | scenario fixtures silently keeping proof-less KeyPackages (PITFALLS 14) | medium | mitigate | Signer gate over all 12 files; plan 07-06's required signer makes tsc the definitive gate |
| T-07-20 | Repudiation | identity reassignment silently changing race or removal attribution, then re-baselined | medium | mitigate | Per-scenario order-preserving slots (rule 4), rule 7 forbids weakening assertions, per-file test runs |
| T-07-21 | Information Disclosure | deterministic test secret keys | low | accept | Test-only material, excluded from the published build |

No package installs occur in this plan, so no supply-chain threat applies.
</threat_model>

<verification>
- Signer gate over all 12 files exits 0
- `pnpm exec tsc -p tsconfig.json --noEmit` exits 0
- `pnpm vitest run` and `pnpm conformance:extended` exit 0
- No production file changed
- Commit per CLAUDE.md once tests pass
</verification>

<success_criteria>
- D-02 for integration and conformance suites
- D-04: signers passed directly
- Race, removal, and convergence identity ordering preserved
</success_criteria>

<output>
Create `.planning/phases/07-account-identity-proof-component-0x8009-legacy-clean-cut/07-05-SUMMARY.md` when done
(list any assertion updated because it encoded a literal pubkey value).
</output>
