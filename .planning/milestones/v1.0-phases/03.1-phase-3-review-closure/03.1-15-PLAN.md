---
phase: 03.1-phase-3-review-closure
plan: 15
type: execute
wave: 12
depends_on: [03.1-13]
files_modified: [src/client/group-registry.ts, src/client/group/marmot-group.ts, src/__tests__/groups-manager.test.ts, src/__tests__/integration/removed.test.ts]
autonomous: true
gap_closure: true
requirements: [CONV-02]
must_haves:
  truths:
    - "D-12/D-13: every GroupRegistry.get() for an id with pending activation awaits the same activation outcome, including a call that begins after the group is provisionally cached."
    - "A rejected activation reaches every concurrent caller as a rejection; no caller receives the disposed partial instance, and updated/loaded expose only successfully activated groups."
    - "D-14: durable removal still completes internal cancellation and marker obligations before public callbacks, while EventEmitter3 on/once listeners retain their registered context and one-shot semantics."
  artifacts:
    - path: src/client/group-registry.ts
      provides: activation-aware cache visibility and post-success registry events
    - path: src/__tests__/groups-manager.test.ts
      provides: controlled late-cache-hit activation rejection regression
    - path: src/client/group/marmot-group.ts
      provides: failure-isolated removed dispatch with EventEmitter3 context parity
    - path: src/__tests__/integration/removed.test.ts
      provides: on/once context and exception-isolation regression coverage
  key_links:
    - from: src/client/group-registry.ts
      to: GroupRegistry.get
      via: in-flight activation is consulted before a provisional cache entry can be returned
      pattern: "groupLoadPromises|groups|get|track"
    - from: src/client/group/marmot-group.ts
      to: EventEmitter3 removed listener records
      via: safe dispatch preserves fn, context, and once metadata after internal completion
      pattern: "removed|context|once|emitRemovedSafely"
---

<objective>Close the final registry-activation visibility race and preserve EventEmitter3 removed-listener semantics without weakening durable removal ordering.

Purpose: A registry caller must never receive an instance that is still activating and may be disposed, and callback isolation must not silently change the emitter contract.
Output: Activation-aware cache behavior plus controlled concurrency and listener-context regressions.</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03.1-phase-3-review-closure/03.1-CONTEXT.md
@.planning/phases/03.1-phase-3-review-closure/03.1-RESEARCH.md
@.planning/phases/03.1-phase-3-review-closure/03.1-PATTERNS.md
@.planning/phases/03.1-phase-3-review-closure/03.1-VERIFICATION.md
@.planning/phases/03.1-phase-3-review-closure/03.1-REVIEW.md
@.planning/phases/03.1-phase-3-review-closure/03.1-13-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make pending activation invisible to late registry cache hits</name>
  <files>src/client/group-registry.ts, src/__tests__/groups-manager.test.ts</files>
  <read_first>src/client/group-registry.ts; src/__tests__/groups-manager.test.ts; .planning/phases/03.1-phase-3-review-closure/03.1-VERIFICATION.md; .planning/phases/03.1-phase-3-review-closure/03.1-13-SUMMARY.md</read_first>
  <behavior>
    - "A controlled activation promise pauses after track has provisionally installed the group; a second get begun in that interval remains pending instead of resolving the cached instance."
    - "When the paused activation rejects, both the activating get and the late cache-hit get reject with the activation error, neither returns the disposed group, and a later get hydrates and activates a fresh instance."
    - "updated and loaded are emitted only for successful activation; failed provisional activation is not observable through the registry's public loaded collection or events."
    - "The same contract holds whether reconverge or realizeRemovalIfNeeded is the paused/rejected activation step."
  </behavior>
  <action>Per D-12, first commit a failing controlled-interleaving regression that deliberately starts the second `GroupRegistry.get()` only after `track()` has inserted the first instance and activation is paused; do not reuse the existing simultaneous-get setup, because it never enters the late cache-hit window. In a separate implementation commit, make `#groupLoadPromises` (or an equivalently explicit activation registry) authoritative before returning any `#groups` hit: a caller observing pending activation must await that exact promise and share its rejection. Move public `updated`/`loaded` visibility to the successful activation boundary, while retaining plan 13's exact-instance listener cleanup, disposal, and retry with a freshly hydrated instance on failure. Keep `load()` hydration-only and preserve the listener-first `track -> reconverge -> realize -> loaded` ordering established by the locked CR-12 decision. Per D-13, do not change fork selection, OwnCommitConvergenceStamp, proposal reconstruction, `fork-recovery.ts`, or any Phase 4-owned behavior. Commit RED and GREEN atomically as separate commits.</action>
  <verify>
    <automated>test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm vitest run src/__tests__/groups-manager.test.ts &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm compile &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932"</automated>
  </verify>
  <acceptance_criteria>The late-hit regression demonstrably fails before production edits, then proves both callers share the rejection, no disposed instance escapes, public registry events occur only after success, retry returns a fresh group, focused tests and compile pass, and the lockfile hash remains exact.</acceptance_criteria>
  <done>Registry cache hits cannot bypass in-flight activation or retain a group that activation cleanup disposes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve removed-listener context and once semantics under safe dispatch</name>
  <files>src/client/group/marmot-group.ts, src/__tests__/integration/removed.test.ts</files>
  <read_first>src/client/group/marmot-group.ts; src/__tests__/integration/removed.test.ts; node_modules/eventemitter3/index.js; node_modules/eventemitter3/index.d.ts; .planning/phases/03.1-phase-3-review-closure/03.1-REVIEW.md</read_first>
  <behavior>
    - "on(removed, listener, context) invokes listener with the registered context as this."
    - "once(removed, listener, context) invokes once with that context and removes it before callback execution so re-entrant or later removal cannot invoke it again."
    - "One throwing removed listener does not suppress later listeners and cannot undo the already-completed marker/outbound-cancellation obligations."
    - "Listener order remains registration order and ordinary no-context callbacks continue receiving the MarmotGroup argument."
  </behavior>
  <action>Per D-12, first commit failing public regressions for EventEmitter3 `on(event, fn, context)` and `once(event, fn, context)` on `removed`, including a throwing listener followed by a contextual observer. Then implement a narrowly owned safe-dispatch mechanism that preserves EventEmitter3 listener function, context, once, order, and re-entrancy semantics while containing each application exception. Use the installed EventEmitter3 implementation/types as the behavioral reference; do not depend on undocumented private storage without a stable project-owned adapter, and do not route CONV-03 state notifications through EventEmitter per D-11. Preserve plan 13's D-14 transaction order: durable marker and queued-outbound cancellation complete before any application callback. Commit RED and GREEN atomically as separate commits.</action>
  <verify>
    <automated>test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm vitest run src/__tests__/integration/removed.test.ts src/__tests__/groups-manager.test.ts &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm compile &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932" &amp;&amp; PNPM_CONFIG_FROZEN_LOCKFILE=true PNPM_CONFIG_OFFLINE=true pnpm vitest run &amp;&amp; test "$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)" = "0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932"</automated>
  </verify>
  <acceptance_criteria>The context/once regressions fail first, then context, one-shot removal, registration order, exception isolation, and durable-before-callback ordering pass in focused and full suites; compile passes and every pnpm command is bracketed by the exact lockfile guard.</acceptance_criteria>
  <done>Safe removed dispatch matches EventEmitter3's public listener contract while application failures remain non-transactional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| registry activation -> public cache caller | Fallible hydration-derived activation must commit before a group instance becomes usable. |
| library lifecycle event -> application callback | User callbacks may throw, rely on registered context, or re-enter event handling. |
| dependency state -> verification | Test/build commands must not mutate or resolve a different dependency graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-25 | Tampering | GroupRegistry activation/cache boundary | critical | mitigate | Gate every cache hit on the exact in-flight activation and expose updated/loaded only after success. |
| T-03.1-26 | Denial of service | failed registry activation | high | mitigate | Share rejection, dispose only the exact failed instance, and prove a clean fresh retry. |
| T-03.1-27 | Repudiation | removed listener dispatch | medium | mitigate | Regression-pin context, once, order, and per-listener exception isolation against EventEmitter3 semantics. |
| T-03.1-SC | Tampering | pnpm dependency state | high | mitigate | Run frozen/offline pnpm commands with the exact SHA-256 guard before and after every command. |
</threat_model>

<verification>Run the controlled registry interleaving first, then the removal/registry focused suites, strict compile, and the full one-shot Vitest suite under frozen/offline pnpm with lock SHA `0f516945e45e257735c4c89a5e9e08b4bb2f839b7ce48121a71b4fb0b03a0932` checked around every command.</verification>

<success_criteria>The activation transaction is invisible until successful, all concurrent callers share its outcome, failure cannot leak a disposed instance, and removed listeners retain EventEmitter3 context/once behavior without compromising durable removal completion.</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Close findings that hold Phase 3 completion | 03.1-15 | COVERED | Closes the sole remaining blocking activation race and its related callback-contract warning. |
| REQ | CONV-02 | Durable removed-inactive state and reliable public removal/load semantics | 03.1-15 | COVERED | Activation cannot leak a soon-disposed instance; removed callback delivery preserves its public contract. |
| REQ | CONV-03 | Digest-attributed notification/withdrawal behavior | existing 03.1 plans | COVERED | Latest verification marks this scoped requirement satisfied; D-11 keeps it out of EventEmitter dispatch. |
| RESEARCH | — | Hydration-only load followed by listener-first activation | 03.1-15 | COVERED | Adds commit visibility to the established activation transaction. |
| RESEARCH | — | Single-flight activation and removal realization | 03.1-15 | COVERED | Controlled late-hit regression covers the missed interleaving; existing removal single-flight remains intact. |
| CONTEXT | D-12 | Failing-first regression per finding | 03.1-15 | COVERED | Both tasks require separate RED and GREEN commits. |
| CONTEXT | D-13 | Close all non-Phase-4-entangled findings | 03.1-15 | COVERED | Covers blocker and warning while fencing transferred fork work. |
| CONTEXT | D-14 | Namespaced durable marker with no migration | 03.1-15 | COVERED | Marker contract and durable-before-callback order are preserved unchanged. |
| CONTEXT | D-11 | Results plus ledger, not EventEmitter, for state notifications | 03.1-15 | COVERED | Listener repair is limited to the existing removed lifecycle event. |
| CONTEXT | Deferred Ideas / Phase 4 | OwnCommitConvergenceStamp, WR-11, IN-07, fork-recovery rewrite | — | EXCLUDED | Explicitly transferred; no Phase 4 artifact is modified. |

<output>Create `.planning/phases/03.1-phase-3-review-closure/03.1-15-SUMMARY.md` when done.</output>
