---
phase: 03-commit-integrity-convergence-parity
plan: 11
type: execute
wave: 3
depends_on: [03-09]
files_modified:
  - src/engine/group-engine.ts
  - src/engine/__tests__/send-commit-legality.test.ts
autonomous: true
gap_closure: true
requirements: [CONV-01, WIRE-03]
must_haves:
  truths:
    - "commit and selfUpdate use one outbound actor-authorization decision over the exact proposal union that createCommit will commit."
    - "A non-admin cannot locally author a commit containing an admin-only by-value or by-reference proposal."
    - "The proposal union used for authorization and post-create legality cannot drift."
  artifacts:
    - path: "src/engine/group-engine.ts"
      provides: "Shared exact-union outbound authorization helper"
    - path: "src/engine/__tests__/send-commit-legality.test.ts"
      provides: "commit/selfUpdate and by-value/by-reference authorization matrix"
  key_links:
    - from: "src/engine/group-engine.ts"
      to: "ts-mls createCommit"
      via: "same normalized proposal union used for authorization and commit construction"
      pattern: "unappliedProposals|extraProposals|createCommit"
    - from: "src/engine/group-engine.ts"
      to: "src/core/components/integrity.ts"
      via: "same union passed to post-create validateCommitLegality"
      pattern: "validateCommitLegality"
---

<objective>
Unify outbound authorization for commit and selfUpdate over the exact proposal set that reaches the MLS commit.

Purpose: Prevent marmot-ts from authoring a local commit peers reject, while preserving WIRE-03/CONV-01 legality parity.
Output: One shared authorization/proposal-normalization seam and a complete behavior matrix.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-commit-integrity-convergence-parity/03-CONTEXT.md
@.planning/phases/03-commit-integrity-convergence-parity/03-VERIFICATION.md
@.planning/phases/03-commit-integrity-convergence-parity/03-09-SUMMARY.md
@src/core/components/integrity.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject a non-admin selfUpdate carrying a referenced admin proposal</name>
  <files>src/engine/group-engine.ts, src/engine/__tests__/send-commit-legality.test.ts</files>
  <read_first>
    - src/engine/group-engine.ts — commit/selfUpdate send cases, proposal selection, admin authorization, #assertStagedCommitLegal
    - src/engine/__tests__/send-commit-legality.test.ts — actor/admin fixtures and proposal staging helpers
    - src/core/admin-policy.ts — authoritative admin proposal authorization callback
    - refs/mdk/crates/cgka-engine/src/message_processor/send.rs — outbound authorization and proposal-union functions used as direct analogs
    - .planning/phases/03-commit-integrity-convergence-parity/03-CONTEXT.md — D-02, D-05 through D-09 locked behavior
  </read_first>
  <behavior>
    - A non-admin selfUpdate that would consume an unapplied admin-only proposal by reference throws before wrapping/publish.
    - The same proposal committed by an admin succeeds.
    - Rejection leaves canonical state, lifecycle, and unapplied proposals unchanged.
  </behavior>
  <action>
Write the failing selfUpdate regression first. Extract one internal helper that resolves the exact commit proposal union from selected references, all MLS `state.unappliedProposals` that createCommit consumes implicitly, caller by-value proposals, and any D-05 auto-coupled admin-policy proposal. Preserve source/proposal identity long enough to avoid double-counting a selected reference that is also present in unapplied state. Run the existing admin callback/authorization rule over that normalized union before createCommit for both `commit` and `selfUpdate`. Use the identical normalized union for post-create `#assertStagedCommitLegal` so authorization and legality cannot inspect different commits.
  </action>
  <acceptance_criteria>
    - The non-admin selfUpdate by-reference regression throws before publish staging.
    - The admin control case succeeds.
    - Tests assert state/lifecycle/proposal preservation after rejection.
    - `pnpm vitest run src/engine/__tests__/send-commit-legality.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/engine/__tests__/send-commit-legality.test.ts</automated>
  </verify>
  <done>The selfUpdate seam cannot bypass actor authorization for implicitly committed referenced proposals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete the commit/selfUpdate exact-union authorization matrix</name>
  <files>src/engine/group-engine.ts, src/engine/__tests__/send-commit-legality.test.ts</files>
  <read_first>
    - src/engine/group-engine.ts — shared helper created by Task 1 and both createCommit call sites
    - src/engine/__tests__/send-commit-legality.test.ts — existing component-integrity, coupling, and depletion cases
    - src/core/components/integrity.ts — resulting-state legality adapter
    - refs/mdk/crates/cgka-engine/src/message_processor/send.rs — exact committed proposal-set construction and authorization functions used as direct analogs
  </read_first>
  <behavior>
    - commit and selfUpdate reject non-admin admin-only proposals whether supplied by value, selected by reference, or implicitly consumed from unapplied state.
    - Ordinary non-admin selfUpdate without admin-only proposals remains allowed.
    - D-05 auto-coupling is included in the same union and authorized before commit construction.
    - WIRE-03 and CONV-01 post-create checks see exactly the normalized union used for authorization.
  </behavior>
  <action>
Add table-driven coverage crossing intent kind (`commit`, `selfUpdate`), actor role, and proposal origin (by value, explicitly selected reference, implicit unapplied reference). Include success controls for ordinary selfUpdate and authorized admin commits, plus an auto-coupled removal case. Refactor remaining duplicated proposal scans so both send cases call the same helper and pass the helper's union into authorization, createCommit inputs, and legality validation without rebuilding it. Preserve D-02 throw semantics and the existing SendResult public shape.
  </action>
  <acceptance_criteria>
    - The test matrix covers both intent kinds and all three proposal origins.
    - No authorization branch scans only `extraProposals` or selected references.
    - `pnpm vitest run src/engine/__tests__/send-commit-legality.test.ts src/engine/__tests__/commit-legality-seams.test.ts src/core/components/__tests__/integrity.test.ts` exits 0.
    - `pnpm build` exits 0.
    - `sha256sum -c /tmp/marmot-ts-phase-03-pnpm-lock.sha256` exits 0 against Plan 03-08's pre-change baseline.
  </acceptance_criteria>
  <verify>
    <automated>test -s /tmp/marmot-ts-phase-03-pnpm-lock.sha256 &amp;&amp; pnpm vitest run src/engine/__tests__/send-commit-legality.test.ts src/engine/__tests__/commit-legality-seams.test.ts src/core/components/__tests__/integrity.test.ts &amp;&amp; pnpm build &amp;&amp; sha256sum -c /tmp/marmot-ts-phase-03-pnpm-lock.sha256</automated>
  </verify>
  <done>Both outbound commit-producing intents authorize and validate the exact proposal union that is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| public send intent → MLS commit builder | Caller role and proposals determine whether a commit may be authored. |
| stored proposal pool → implicit commit union | By-reference proposals can enter a commit without appearing in explicit options. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03G-12 | Elevation of Privilege | selfUpdate | high | mitigate | Apply shared actor authorization over all implicit and explicit committed proposals before createCommit. |
| T-03G-13 | Tampering | proposal normalization | high | mitigate | Resolve once with stable identity/deduplication and reuse for authorization and legality. |
| T-03G-14 | Denial of Service | duplicate references | medium | mitigate | Deduplicate by proposal reference/source identity and retain existing missing-reference errors. |
| T-03G-15 | Repudiation | send failure | low | mitigate | Preserve typed domain throw and unchanged state/lifecycle assertions. |
</threat_model>

<verification>
Run the outbound matrix together with inbound/replay and pure legality suites, then `pnpm build`.
</verification>

<success_criteria>
No locally-authored commit can bypass peer-equivalent actor authorization through selfUpdate or an implicit by-reference proposal, and WIRE-03/CONV-01 remain green.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Staged commits and convergence handling match MDK legality/rewind semantics | 03-08..03-11 | COVERED | Environment proof plus four production gap predicates. |
| REQ | WIRE-03 | Component integrity at send/inbound/replay seams | 03-08, 03-11 | COVERED | Behavioral rerun and exact-union regression. |
| REQ | CONV-01 | Admin/member coupling and outbound authorization parity | 03-08, 03-11 | COVERED | All seams rerun; commit/selfUpdate union fixed. |
| REQ | CONV-02 | Consumer-visible durable removal | 03-09 | COVERED | Listener-first public load path. |
| REQ | CONV-03 | Digest attribution and rewind withdrawal | 03-09 | COVERED | Local commit/selfUpdate recording and real withdrawal. |
| REQ | CONV-04 | Own-confirmed convergence protection | 03-10 | COVERED | Structural chain reconstruction plus native MDK-derived regressions. |
| RESEARCH | — | Shared legality adapter at every seam | 03-08, 03-11 | COVERED | Existing seams rerun and shared proposal union retained. |
| RESEARCH | — | Reference implementation is authoritative for convergence | 03-10 | COVERED | Direct MDK source inspection drives implementation and focused native tests. |
| CONTEXT | D-01..D-09 | Pure validators, typed seam outcomes, auto-coupling, account survival | 03-08, 03-11 | COVERED | Existing decisions preserved and behaviorally rerun. |
| CONTEXT | D-10..D-14 | Notifications, ledger, marker, SelfEvicted, outbound block | 03-09 | COVERED | Load and local-confirmation gaps closed without changing locked semantics. |
| CONTEXT | D-15 | No vector fixtures or scenario driver in Phase 3 | 03-10 | COVERED | Focused native Vitest regressions are derived from direct MDK inspection; vector infrastructure remains Phase 4/CONF-01. |
| CONTEXT | D-16 | Two own-commit selection properties | 03-10 | COVERED | Retained and expanded to chained by-reference proposals. |

Deferred multi-device, push, SafeAAD, general CONF-01 vector infrastructure, cross-runtime QA, and unrelated stale-citation cleanup are excluded from these additive gap plans. Existing plans 03-01 through 03-07 remain historical completed plans and are not rewritten.

<output>
Create `.planning/phases/03-commit-integrity-convergence-parity/03-11-SUMMARY.md` when done.
</output>
