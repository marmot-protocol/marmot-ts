---
phase: 10-founding-group-creation-via-welcome
plan: 04
type: execute
wave: 3
depends_on: [10-03]
files_modified:
  - src/client/__tests__/founding-create.test.ts
  - src/__tests__/integration/founding-group-create-join-message.test.ts
  - docs/client/marmot-client.md
  - docs/client/marmot-group.md
autonomous: true
requirements: [FOUND-02, FOUND-04, FOUND-05]
tags: [integration-test, founding-create, welcome-retry, docs, positive-control]

must_haves:
  truths:
    - "FOUND-05: an invitee who receives their founding Welcome joins at epoch 1 and exchanges messages with the creator in both directions — the phase's positive control, run with relays supplied (R-05)"
    - "FOUND-05: every invitee of a multi-invitee founding create sees the other invitees as members, proving one shared founding Add rather than N sequential commits"
    - "FOUND-02: a founding create whose invitee KeyPackage fails the trust boundary is refused through the public create API, with nothing persisted and nothing published"
    - "D-13: a duplicate invitee is refused loudly, before any commit is built and before any Welcome is delivered"
    - "D-12/FOUND-04: a founding create in which one invitee's Welcome cannot be delivered still resolves, still reaches epoch 1 with all members, and reports only that invitee as pending"
    - "FOUND-04: retryWelcome re-delivers exactly the failed invitee's Welcome and clears it from pendingWelcomes on success"
    - "R-04: the delivery report is non-durable — a group reloaded from the store reports no pending Welcomes even though an invitee was never reached, and the documented recovery is re-invite with a fresh KeyPackage"
    - "D-09/R-05: a relay-less founding create still delivers Welcomes to invitees who published NIP-65 inbox relays, yet the resulting group cannot carry ordinary group traffic — both halves asserted, and documented for downstream apps"
    - "the founding commit's fork-tree node is persisted when a rewind store is configured, so the supplied history tree is actually bound"
    - "D-10: the per-invitee report is discoverable state on the group and is documented as such for downstream apps, including the risk that it can be ignored"
  artifacts:
    - path: src/__tests__/integration/founding-group-create-join-message.test.ts
      provides: "the FOUND-05 end-to-end positive control: founding create with relays, per-invitee Welcome, join at epoch 1, bidirectional messaging"
      contains: "joinGroupFromWelcome"
    - path: src/client/__tests__/founding-create.test.ts
      provides: "the behavioural matrix — D-13 duplicate refusal, FOUND-02 refusal through the public API, D-12 partial failure, FOUND-04 retry, R-04 non-durability, D-09 relay-less delivery, fork-tree persistence"
      contains: "retryWelcome"
    - path: docs/client/marmot-client.md
      provides: "documented founding creation via options.invitees, with the relay-less (R-05/D-09) and non-durable-report (R-04) caveats"
      contains: "invitees"
    - path: docs/client/marmot-group.md
      provides: "documented pendingWelcomes / retryWelcome surface and its recovery path (D-10/R-04)"
      contains: "retryWelcome"
  key_links:
    - from: src/__tests__/integration/founding-group-create-join-message.test.ts
      to: src/client/group-factory.ts
      via: "the integration test drives founding creation through the public client API with options.invitees"
      pattern: 'invitees'
    - from: docs/client/marmot-client.md
      to: docs/client/marmot-group.md
      via: "the create-time caveat links to the per-invitee retry surface"
      pattern: 'marmot-group'
---

<objective>
Prove the phase goal end to end and document the two consequences the locked decisions deliberately accepted:
the FOUND-05 positive control (invitee joins at epoch 1 from a founding Welcome and exchanges messages with the
creator, relays supplied per R-05), the behavioural matrix around partial failure and retry, and the downstream
notes for the non-durable delivery report (R-04) and the relay-less group (D-09/R-05).

Purpose: Phase 9 shipped a Critical defect precisely because it lacked a positive control. This phase's positive
control is an honest all-Welcomes-succeed founding create producing a working group whose invitees can message
the creator — so it is planned explicitly rather than assumed. Test-and-docs only; no production code changes.
Output: the extended behavioural matrix, the new integration suite, and two updated docs pages.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-founding-group-creation-via-welcome/10-CONTEXT.md
@.planning/phases/10-founding-group-creation-via-welcome/10-RESEARCH.md
@.planning/phases/10-founding-group-creation-via-welcome/10-PATTERNS.md
@.planning/phases/10-founding-group-creation-via-welcome/10-03-SUMMARY.md
@CLAUDE.md
@src/__tests__/integration/end-to-end-invite-join-message.test.ts

<interfaces>
Signatures this plan consumes (created by plans 10-01..10-03; this plan adds no production code):

```typescript
// src/client/group-factory.ts
export type CreateGroupOptions = SimpleGroupOptions & { ciphersuite?: CiphersuiteName; invitees?: NostrEvent[] };
// src/client/group/marmot-group.ts
get welcomeDeliveries(): readonly WelcomeDeliveryOutcome[];
get pendingWelcomes(): readonly WelcomeDeliveryOutcome[];
async retryWelcome(pubkey: string): Promise<WelcomeDeliveryOutcome>;
// src/client/transport/nostr/welcome-delivery.ts
export type WelcomeDeliveryOutcome =
  | { kind: "succeeded"; recipient: WelcomeRecipient; response: Record<string, PublishResponse> }
  | { kind: "failed"; recipient: WelcomeRecipient; error: string };
```

Existing test-surface signatures this plan uses (unchanged):

```typescript
// src/client/marmot-client.ts
new MarmotClient({ groupStateStore, keyPackageStore, signer, network, clientId? });
client.groups.create(name, options); client.groups.send(id, intent); client.keyPackages.create({ relays });
client.joinGroupFromWelcome({ welcomeRumor }): Promise<{ group: MarmotGroup }>;
// src/client/groups-manager.ts — GroupsManagerOptions accepts store, signer, network (required) plus optional
// ingestStateStore, lifecycleStore, rewindStore, removedMarkerStore, convergencePolicy, ingestionPool,
// audit/auditContext, cryptoProvider, historyFactory, mediaFactory, verifyEvent.
// src/__tests__/helpers/mock-network.ts — MockNetwork: events[], publish, request, subscription,
// getUserInboxRelays (resolves ["wss://mock-inbox.test"] for any pubkey)
// applesauce-common/helpers/gift-wrap — unlockGiftWrap(event, signer)
// src/core/protocol.ts — GROUP_EVENT_KIND, ADDRESSABLE_KEY_PACKAGE_KIND, WELCOME_EVENT_KIND
```

Pinned expectations for this plan (derived from the locked decisions — assert these, do not soften them):
- Relay-less founding create is **not** an error and **does** deliver to invitees with NIP-65 inbox relays; what
  it cannot do is carry ordinary group traffic afterwards, because `createSimpleGroup` only seeds
  `transport.nostr.routing` when relays are supplied (D-09, RESEARCH "The D-09 footgun, confirmed precisely").
- FOUND-05 is only satisfiable with relays supplied, so the integration test supplies them (R-05).
- A reloaded group reporting no pending Welcomes is the **expected, documented** behaviour of D-04, not a bug to
  fix here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Behavioural matrix — refusals, partial failure, retry, non-durability, relay-less, tree persistence</name>
  <files>src/client/__tests__/founding-create.test.ts</files>
  <read_first>
    - src/client/__tests__/founding-create.test.ts (as created by plan 10-03 — reuse its `beforeEach`, its admin/invitee client setup and its KeyPackage-publishing helper; do not duplicate them)
    - src/client/group/marmot-group.ts as modified by plan 10-03 (`welcomeDeliveries`, `pendingWelcomes`, `deliverFoundingWelcomes`, `retryWelcome` and their documented semantics: unknown pubkey throws, already-succeeded returns unchanged without re-delivering)
    - src/client/group-factory.ts as modified by plan 10-03 (the duplicate-invitee refusal, the admission loop, and the fanout call with an empty group-relay list)
    - src/client/transport/nostr/welcome-delivery.ts (`deliver`'s relay resolution: it tries `getUserInboxRelays(recipient.pubkey)` and falls back to `groupRelays` only when that lookup throws, then throws when the resulting list is empty — the only way to force a per-recipient failure)
    - src/__tests__/helpers/mock-network.ts (`getUserInboxRelays` resolves a mock inbox for every pubkey by default, so a failure must be forced by overriding or spying)
    - src/client/groups-manager.ts lines 100-166 (`GroupsManagerOptions`, including `rewindStore`) and src/__tests__/groups-manager.test.ts lines 50-70 (the proven direct `new GroupsManager({ store, signer, network })` construction to copy)
    - src/extra/in-memory-key-value-store.ts (`InMemoryKeyValueStore`, including `keys()`)
    - .planning/phases/10-founding-group-creation-via-welcome/10-CONTEXT.md decisions D-04, D-09, D-10, D-12, D-13 and risks R-04, R-05
    - refs/marmot/protocol-core/publish-lifecycle.md lines 66-78 (independent per-invitee delivery; a Welcome "succeeds or fails independently and does not affect canonical group state"; consumed KeyPackage material is not restorable and the creator MAY re-invite)
  </read_first>
  <behavior>
    - Test 1 (D-13): creating with the same invitee's KeyPackage event listed twice rejects, and afterwards the mock network holds zero gift wraps and zero kind-445 events — the refusal happened before any KeyPackage material was consumed
    - Test 2 (FOUND-02): creating with an invitee whose KeyPackage event signature has been tampered with rejects, and the group state store holds no keys
    - Test 3 (D-12/FOUND-04): with two invitees where the second's inbox-relay lookup fails and no group relays are supplied, `create()` resolves rather than throwing; the group is at epoch 1 with both invitees as members; `welcomeDeliveries` reports two entries and `pendingWelcomes` reports exactly the second invitee
    - Test 4 (FOUND-04): after the second invitee becomes reachable, `retryWelcome(secondPubkey)` resolves to a succeeded outcome, `pendingWelcomes` becomes empty, and exactly one additional gift wrap was published
    - Test 5 (FOUND-04): `retryWelcome` on a pubkey that was never an invitee rejects, and `retryWelcome` on an already-succeeded invitee publishes no further gift wrap
    - Test 6 (R-04): after a founding create with one undeliverable invitee, a group instance freshly loaded from the same store reports an empty `pendingWelcomes` while its canonical state still holds every member at epoch 1 — the accepted silent-loss window, whose only recovery is a fresh-KeyPackage re-invite
    - Test 7 (D-09/R-05): a founding create with invitees and no `relays` publishes one gift wrap per invitee (the NIP-65 inbox path works with an empty group-relay list), yet the resulting group exposes no relays and an attempt to send an application message through it is refused
    - Test 8: a founding create driven through a `GroupsManager` configured with a rewind store completes without error and leaves at least one entry in that rewind store, pinning that the supplied history tree was bound before the single save
  </behavior>
  <action>
Extend `src/client/__tests__/founding-create.test.ts` with a new describe block per concern group, reusing the
setup plan 10-03 established. Do not restate that setup and do not weaken any assertion plan 10-03 wrote.

For Test 1, pass the same published KeyPackage event twice in `invitees`. Assert the rejection, then assert the
absence of both artifact kinds in the mock network's event log — the point of D-13's pre-send placement is that
nothing was burned, which is only observable as an absence.

For Test 2, take a legitimately published KeyPackage event and produce a tampered copy by altering its `sig`
(keep every other field), then pass the tampered copy as the only invitee. This exercises the inherited SEC-01
signature gate through the public `create()` API, which is the FOUND-02 claim at the client boundary. Assert the
store has no keys afterwards, so a refused founding create leaves no local group.

For Tests 3-6, force the second invitee's delivery to fail by making the network's `getUserInboxRelays` reject
for that pubkey only (spy on or subclass `MockNetwork`) and by creating the group **without** `relays`, so
`deliver`'s group-relay fallback is empty and that one recipient throws while the first still succeeds. Assert
membership at epoch 1 from the group's own state rather than from the delivery outcomes — the whole point of
D-12 is that canonical state is independent of Welcome delivery. For Test 4, restore the lookup before calling
`retryWelcome` and count gift wraps before and after to prove exactly one more was published. For Test 6, load a
second group instance from the same store (a fresh `GroupsManager`/`MarmotClient` over the same store, or the
manager's load path) and assert the empty `pendingWelcomes`; add a comment naming this as R-04's accepted,
documented window and pointing at the re-invite recovery path, so a future reader does not "fix" it.

For Test 7, use the default `MockNetwork` behaviour (inbox relays resolve for everyone) with no `relays` option.
Assert one gift wrap per invitee, that the group reports no relays, and that sending an application message
through the group is refused. Both halves matter: the R-05 documented expectation is that a relay-less founding
create can still deliver Welcomes to invitees who published inbox relays but can never support group messaging.

For Test 8, construct a `GroupsManager` directly with the store, a real signer, the mock network and a fresh
`InMemoryKeyValueStore` as the rewind store, following the construction style already proven in
`src/__tests__/groups-manager.test.ts`. Perform a founding create and assert it resolves and that the rewind
store's keys are non-empty. This is the regression pin for the bound-tree requirement: an unbound supplied tree
would make the single save throw when it flushes.
  </action>
  <verify>
    <automated>pnpm vitest run src/client/__tests__/founding-create.test.ts && pnpm compile</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/client/__tests__/founding-create.test.ts` passes with at least 14 tests (plan 10-03's 6 plus these 8), and no plan 10-03 test is deleted or loosened
    - `grep -c 'retryWelcome' src/client/__tests__/founding-create.test.ts` outputs at least 3
    - `grep -c 'pendingWelcomes' src/client/__tests__/founding-create.test.ts` outputs at least 4
    - `grep -c 'rewindStore' src/client/__tests__/founding-create.test.ts` outputs at least 1
    - `grep -c 'R-04' src/client/__tests__/founding-create.test.ts` outputs at least 1 and `grep -c 'R-05' src/client/__tests__/founding-create.test.ts` outputs at least 1
    - `pnpm vitest run src/client/__tests__/founding-create.test.ts -t "duplicate"` passes
    - `pnpm vitest run src/client/__tests__/founding-create.test.ts -t "retry"` passes
    - `pnpm compile` exits 0
  </acceptance_criteria>
  <done>Every accepted-cost and refusal path of the locked decisions is pinned by an executable assertion, including the two that are assertions about absence and the one that documents a deliberate silent-loss window.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FOUND-05 end-to-end positive control</name>
  <files>src/__tests__/integration/founding-group-create-join-message.test.ts</files>
  <read_first>
    - src/__tests__/integration/end-to-end-invite-join-message.test.ts (full file — the exact scaffolding to copy: account generation, ciphersuite init, `MockNetwork`, two `MarmotClient`s over `InMemoryKeyValueStore`s, `keyPackages.create({ relays })`, recovering the published KeyPackage event from `mockNetwork.events`, the gift-wrap fetch via `mockNetwork.request(["wss://mock-inbox.test"], { kinds: [1059], "#p": [pubkey] })` and `unlockGiftWrap`, `joinGroupFromWelcome`, the consumed-KeyPackage assertions, `getNostrGroupIdHex` + manual `group.ingest(groupEvents)` catch-up, and the application-message send/receive block with `createApplicationMessageIntent` + `deserializeApplicationData`)
    - src/client/group-factory.ts as modified by plan 10-03 (the `invitees` option)
    - .planning/phases/10-founding-group-creation-via-welcome/10-RESEARCH.md "Priority Finding #3" (the concrete 7-step shape for this test)
    - .planning/phases/10-founding-group-creation-via-welcome/10-CONTEXT.md `<specifics>` (the positive control this phase needs, and why Phase 9's missing positive control let a Critical ship green) and risk R-05
    - refs/marmot/protocol-core/joining.md lines 30-33 (every Marmot Welcome's GroupInfo MUST include the `ratchet_tree` extension; a joiner MUST reject a Welcome without it) and lines 44-80 (the Welcome receiving flow, steps 1-14, including step 13's catch-up on outstanding Commits)
  </read_first>
  <behavior>
    - Test 1: an admin founding-creates a group with two invitees and relays; the mock network holds zero kind-445 events immediately afterwards and exactly one gift wrap per invitee, each unwrappable only by its intended recipient
    - Test 2: each invitee independently joins from its own Welcome and lands at epoch 1, matching the creator's epoch
    - Test 3: each invitee sees the creator **and the other invitee** as members, proving a single shared founding Add rather than sequential per-invitee commits
    - Test 4: the first invitee sends an application message the creator receives and decrypts, and the creator sends one the second invitee receives and decrypts — bidirectional exchange relative to the creator
    - Test 5: each invitee's consumed KeyPackage is marked used on its own client
  </behavior>
  <action>
Create `src/__tests__/integration/founding-group-create-join-message.test.ts` modelled directly on
`end-to-end-invite-join-message.test.ts`. Reuse its scaffolding verbatim where it applies; the differences are
that there is no separate invite step, there are two invitees, and the founding commit must be absent from the
transport log.

Open with a module doc comment naming this as the FOUND-05 positive control for phase 10 and citing
`refs/marmot/protocol-core/joining.md` lines 21-30 (the founding-creation exception) and lines 44-80 (the
receiving flow). State that relays are supplied deliberately, per R-05: FOUND-05 is only satisfiable when the
founding group has relays, because a relay-less group has no `transport.nostr.routing` component and can carry
no group traffic.

Set up one admin client and two invitee clients over one shared `MockNetwork`. Have each invitee publish a
KeyPackage and recover both published events. Create the group in a single call with `adminPubkeys`, a relay
list, and both KeyPackage events as `invitees`.

Assert the absence first: filter the mock network's events for the group-event kind and expect none. Then assert
exactly two gift wraps exist and that each unwraps only with its intended recipient's signer, checking each
rumor's kind and that its KeyPackage reference tag matches the event that invitee published.

Have each invitee call `joinGroupFromWelcome` with its own rumor. Assert each joined group's epoch equals the
admin group's epoch and equals 1. Then assert the shared-commit property: read the member pubkeys from each
joined group and expect all three identities present in each. This is the strongest structural proof that one
founding Add carried both invitees.

For messaging, follow the analog's send/ingest pattern in both directions relative to the creator: the first
invitee sends and the admin ingests and decrypts it; the admin sends and the second invitee ingests and decrypts
it. Where the analog performs a manual backlog `ingest` to catch up, keep that step — a joiner must tolerate
re-ingesting the backlog as a no-op.

Do not assert anything about `PendingPublish` or audit events here; that scoping belongs to plans 10-01 and
10-03. Do not add a relay-less variant to this file — the relay-less expectations live in Task 1.
  </action>
  <verify>
    <automated>pnpm vitest run src/__tests__/integration/founding-group-create-join-message.test.ts && pnpm compile</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/__tests__/integration/founding-group-create-join-message.test.ts` passes
    - `grep -c 'joinGroupFromWelcome' src/__tests__/integration/founding-group-create-join-message.test.ts` outputs at least 2 (each invitee joins independently)
    - `grep -c 'GROUP_EVENT_KIND' src/__tests__/integration/founding-group-create-join-message.test.ts` outputs at least 2 (the zero-commit assertion and the later message traffic)
    - `grep -c 'invitees' src/__tests__/integration/founding-group-create-join-message.test.ts` outputs at least 1
    - `grep -c 'joining.md' src/__tests__/integration/founding-group-create-join-message.test.ts` outputs at least 1
    - `grep -c 'R-05' src/__tests__/integration/founding-group-create-join-message.test.ts` outputs at least 1
    - `pnpm vitest run src/__tests__/integration/` passes in full (the new suite does not disturb the existing integration suites)
    - `pnpm compile` exits 0
  </acceptance_criteria>
  <done>An honest all-Welcomes-succeed founding create is proven to produce a working group: both invitees join at epoch 1 from their own Welcome, see each other, and exchange messages with the creator.</done>
</task>

<task type="auto">
  <name>Task 3: Downstream docs for the accepted caveats</name>
  <files>docs/client/marmot-client.md, docs/client/marmot-group.md</files>
  <read_first>
    - docs/client/marmot-client.md lines 51-100 (the "Creating a New Group" section — note its numbered list currently claims creation publishes a kind-445 group event, which is inaccurate even for solo creation since epoch 0 carries an empty publication obligation, and "Joining an Existing Group" immediately after)
    - docs/client/marmot-group.md lines 115-167 ("Proposals and commits", "Events", "Session and runtime", "Next steps" — the heading depth, table style and cross-link style to match)
    - .vitepress/config.ts (the sidebar — confirm both pages are already listed, so no config change is needed for edits to existing pages)
    - src/client/group/marmot-group.ts and src/client/group-factory.ts as modified by plan 10-03 (the exact member names and documented semantics to describe: `welcomeDeliveries`, `pendingWelcomes`, `retryWelcome`, `options.invitees`)
    - .planning/phases/10-founding-group-creation-via-welcome/10-CONTEXT.md decisions D-04, D-09, D-10, D-12 and risks R-04, R-05
    - refs/marmot/protocol-core/publish-lifecycle.md lines 66-78 (the empty publication obligation for epoch 0 and the founding Add; per-invitee retryable delivery; re-invite with a fresh KeyPackage as the recovery path)
  </read_first>
  <action>
**`docs/client/marmot-client.md`, "Creating a New Group".** Correct the numbered list: creating a group generates
the initial MLS state, persists it, and returns a usable `MarmotGroup` — it publishes no group event, because
epoch 0 carries an empty publication obligation. Then document founding creation: passing `invitees` (an array of
published KeyPackage events) merges a single Add commit locally to epoch 1 so every invitee is a member
immediately, publishes **no** group event for that commit, and sends each invitee an independently retryable
Welcome. Add a short example alongside the existing one, passing `invitees` together with `relays`.

Document the two accepted caveats plainly, as caveats a downstream app must handle rather than as footnotes:
- **Relays and founding creation (D-09/R-05).** Invitees without `relays` is supported and does not throw.
  Welcome delivery then relies entirely on each recipient's published NIP-65 inbox relays, so an invitee who
  published none cannot be reached while the others still succeed. More importantly, a group created without
  relays has no Nostr routing component and can never carry ordinary group traffic, so a successful founding
  create can still yield a group that is unusable for messaging. Recommend always supplying `relays` when
  invitees are supplied.
- **The delivery report is discoverable state, not a result (D-10/D-12/R-04).** `create()` never throws on a
  failed Welcome: the group exists at epoch 1 with every member regardless. Failures are readable on the returned
  group and must be checked. The report is in-memory only, so a crash or restart loses it while the invitee
  remains a member who was never told; the only recovery is re-inviting that member with a fresh KeyPackage
  against the now-canonical group. Link to the group page's section for the API.

**`docs/client/marmot-group.md`.** Add a section (same heading depth as "Proposals and commits") covering the
founding-Welcome surface: `welcomeDeliveries` as the full per-invitee report, `pendingWelcomes` as the failed
subset still needing delivery, and `retryWelcome(pubkey)` for re-delivering one invitee. Show a short example that
checks `pendingWelcomes` after creation and retries each entry. State the semantics the implementation documents:
an unknown pubkey throws, an already-succeeded invitee returns its existing outcome without re-publishing, the
report is not persisted, and a late Welcome only lets the joiner catch up if the group has relays. Add the
re-invite recovery sentence with its spec citation. Add the new section to the "Next steps" list if that list
enumerates sections.

Both pages are already in the VitePress sidebar, so `.vitepress/config.ts` must not change — no new page is
created. Match each file's existing prose voice and fenced-example style, and keep `pnpm format` clean.
  </action>
  <verify>
    <automated>pnpm lint && pnpm docs:build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'invitees' docs/client/marmot-client.md` outputs at least 2
    - `grep -c 'retryWelcome' docs/client/marmot-group.md` outputs at least 2
    - `grep -c 'pendingWelcomes' docs/client/marmot-group.md` outputs at least 2
    - `grep -c 'fresh KeyPackage' docs/client/marmot-client.md docs/client/marmot-group.md` reports at least 1 match in each file (the R-04 recovery path is stated where a reader will hit it)
    - `grep -ci 'publication obligation' docs/client/marmot-client.md` outputs at least 1, and the "Creating a New Group" numbered list no longer asserts that creation publishes a group event (the inaccurate claim is corrected in place, not merely supplemented — verify by reading the list)
    - `git diff --stat .vitepress/config.ts` produces no output (no new page added)
    - `pnpm lint` exits 0 (Prettier clean — run `pnpm format` first if needed)
    - `pnpm docs:build` exits 0
  </acceptance_criteria>
  <done>A downstream developer reading the client and group docs learns that founding creation publishes no commit, that relay-less creation yields an unmessageable group, and that the per-invitee Welcome report must be checked because it is neither thrown nor persisted.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files this phase creates (newly-created — not pre-existing drift):

- New files: `src/__tests__/integration/founding-group-create-join-message.test.ts` (this plan), `src/client/__tests__/founding-create.test.ts` (created 10-03, extended here), `src/engine/__tests__/founding-add-send.test.ts` (10-01), `src/client/transport/nostr/__tests__/welcome-delivery.test.ts` (10-02)
- `SendIntent` variant `foundingAdd`, `SendResult` variant `foundingGroupCreated`, audit kinds `"founding_add"` / `"founding_group_created"` — `src/engine/types.ts`, `src/engine/group-engine.ts` (10-01)
- `WelcomeDeliveryOutcome`, `DeliverManyWelcomesOptions`, `NostrWelcomeDelivery.deliverMany()` — `src/client/transport/nostr/welcome-delivery.ts` (10-02)
- `WelcomeFanoutOutcome` — `src/client/session/group-effects.ts` (10-02)
- `CreateGroupOptions.invitees`, `GroupFactoryOptions.verifyEvent` — `src/client/group-factory.ts` (10-03)
- `MarmotGroup.welcomeDeliveries`, `MarmotGroup.pendingWelcomes`, `MarmotGroup.deliverFoundingWelcomes()`, `MarmotGroup.retryWelcome()` — `src/client/group/marmot-group.ts` (10-03)

This plan adds no production symbols. The runtime export snapshot in `src/__tests__/exports.test.ts` is
unaffected by the whole phase (all additions are types or class members); Phase 11's QA-05 owns any snapshot
change, and D-07/D-10's names are the ones it inherits.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| invitee KeyPackage event → founding commit | Exercised here through the public `create()` API with a tampered event |
| Welcome gift wrap → recipient | Each wrap must be openable only by its intended invitee |
| documentation → downstream implementer | An undocumented accepted risk becomes an unhandled production failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-19 | Spoofing | founding admission via public API | high | mitigate | Test 2 drives a tampered-signature KeyPackage through `create()` and asserts refusal plus an empty store, proving the inherited SEC-01 gate is reachable and fail-closed at the client boundary (FOUND-02) |
| T-10-20 | Information disclosure | per-invitee gift wrap | high | mitigate | The integration test asserts exactly one wrap per invitee and that each unwraps only with its intended recipient's signer |
| T-10-21 | Information disclosure | founding commit in the transport log | high | mitigate | Both suites assert zero group-kind events after a founding create — the absence assertion CONTEXT.md requires be made against the publish log rather than the return value |
| T-10-22 | Denial of service | one unreachable invitee blocking the rest | medium | mitigate | Test 3 asserts the reachable invitee still succeeds and canonical state still reaches epoch 1 with all members |
| T-10-23 | Repudiation | silently lost invitee after restart | medium | accept | R-04 is asserted as expected behaviour (Test 6) and documented on both docs pages with the re-invite recovery path; the durable-store alternative was declined in D-04 |
| T-10-24 | Denial of service | relay-less group unusable for messaging | medium | accept | D-09 is locked; Test 7 pins both halves and Task 3 documents the recommendation to always supply relays with invitees |

No package-manager installs occur in this phase, so the `T-{phase}-SC` supply-chain row and its blocking
legitimacy checkpoint are not applicable.
</threat_model>

<verification>
- `pnpm vitest run` is green across the whole suite, including both new/extended test files.
- `pnpm compile` exits 0; `pnpm lint` exits 0; `pnpm docs:build` exits 0.
- `git diff --stat .vitepress/config.ts` is empty.
- No production source file is modified by this plan (`git diff --name-only src/` lists only test files).
</verification>

<success_criteria>
- An invitee of a founding create joins at epoch 1 from their Welcome and exchanges messages with the creator in
  both directions, with relays supplied.
- Every invitee of a multi-invitee founding create sees the others, proving one shared founding Add.
- Duplicate and untrusted invitees are refused before anything is burned, published or persisted.
- Partial Welcome failure is non-fatal, per-invitee retryable, and its non-durability is both asserted and
  documented.
- A relay-less founding create's two documented consequences are each asserted.
- Downstream docs state the founding-create behaviour and both accepted caveats.
</success_criteria>

<output>
Create `.planning/phases/10-founding-group-creation-via-welcome/10-04-SUMMARY.md` when done
</output>
</content>
