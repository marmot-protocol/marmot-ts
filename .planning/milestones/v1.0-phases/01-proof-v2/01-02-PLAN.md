---
phase: 01-proof-v2
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/client/key-package-manager.ts
  - src/client/group-factory.ts
  - src/__tests__/helpers/account-proof.ts
  - src/core/__tests__/darkmatter-invite-compat.test.ts
  - src/core/__tests__/welcome.test.ts
  - src/core/__tests__/capabilities.test.ts
  - src/core/__tests__/key-package.test.ts
  - src/client/group/proposals/__tests__/invite-user.test.ts
  - src/engine/__tests__/group-engine.test.ts
  - src/client/group/__tests__/marmot-group.test.ts
autonomous: true
requirements: [PROOF-01]
must_haves:
  truths:
    - "the widened account-identity-proof signer contract threads through key-package-manager.ts and group-factory.ts with every existing call site still compiling and passing"
    - "a Rust-signed v2 proof is accepted by marmot-ts verification AND marmot-ts reproduces the same kind-450 event id + payload bytes from the same inputs"
    - "no code path publishes or relays the kind-450 proof event"
    - "the full Vitest suite passes with v2 vectors (no v1 proof strings/expectations remain in proof-touching tests)"
  artifacts:
    - "a fresh Rust-signed → TS-verified round-trip fixture test (home Claude's discretion)"
    - "updated proof-touching test files and the account-proof test helper"
  key_links:
    - "accountProofSigner option in KeyPackageManagerOptions / GroupFactoryOptions carries the widened contract"
    - "src/__tests__/helpers/account-proof.ts exposes the signer used by client/integration tests"
---

<objective>
Thread the widened account-identity-proof signer contract (from plan 01-01) through the client
re-exporters, sweep every proof-touching test/fixture from v1 to v2, and prove the migration
interoperates with the MDK Rust reference via a **fresh Rust-signed → TS-verified round-trip
fixture** — and assert the kind-450 event is never published (PROOF-01, ROADMAP criteria 2 and 4).

Purpose: make the v2 core usable end-to-end by downstream client/integration code and prove
byte-for-byte interop with Rust once (the permanent parity-harness wiring is deferred to CONF-01 /
Phase 4 per 01-CONTEXT.md Deferred Ideas).
Output: green cross-runtime suite on v2 vectors plus a pinned Rust round-trip fixture.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-proof-v2/01-CONTEXT.md
@.planning/research/PROOF-V2.md
@src/client/key-package-manager.ts
@src/client/group-factory.ts
@src/__tests__/helpers/account-proof.ts
@.planning/phases/01-proof-v2/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread widened signer contract through client + fix .v1 comment</name>
  <files>src/client/key-package-manager.ts, src/client/group-factory.ts, src/__tests__/helpers/account-proof.ts</files>
  <read_first>
    - src/client/key-package-manager.ts (`accountProofSigner` option + the `.v1` comment at :125-133)
    - src/client/group-factory.ts (`AccountIdentityProofSigner` re-use at :13, :42, :72, :88, :120)
    - src/__tests__/helpers/account-proof.ts (the raw-secret-key signer built for tests)
    - src/core/account-identity-proof.ts (final widened `AccountIdentityProofSigner` shape + new external-signer exports from plan 01-01)
    - .planning/phases/01-proof-v2/01-CONTEXT.md (LOCKED: thread the widened contract through these two consumers; keep a direct raw-secret-key path)
  </read_first>
  <action>
    Update `src/client/key-package-manager.ts` and `src/client/group-factory.ts` to consume the widened `AccountIdentityProofSigner` type from plan 01-01. Wherever the two files reference the signer type or pass `accountProofSigner` through, ensure the new (possibly union) shape flows through without breaking their public option types; keep the existing raw-secret-key usage supported. Update the `accountProofSigner` JSDoc in key-package-manager.ts that names the old v1 proof domain (the `.v1`-suffixed `marmot.account-identity-proof` string, around :127) so it names the `.v2` domain instead.
    In `src/__tests__/helpers/account-proof.ts`, keep the raw-secret-key signer working (it uses `signAccountIdentityProof` — preserved in 01-01). If the widened contract requires a shape change, adapt the helper so both the raw-key path and, if applicable, the event-signing path are available to tests; the existing exported `accountProofSignerFor` must keep its call signature so current test imports compile unchanged.
    No fenced code; name the identifiers and the option fields you touch.
  </action>
  <verify>
    <automated>pnpm compile</automated>
    <automated>test $(grep -c "account-identity-proof.v1" src/client/key-package-manager.ts) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm compile` exits 0.
    - `grep -c "account-identity-proof.v1" src/client/key-package-manager.ts` returns 0.
    - `accountProofSignerFor` in the test helper keeps its existing exported signature (raw-key path preserved).
    - Both client files typecheck against the widened `AccountIdentityProofSigner` with no `any`-casts introduced to paper over the shape.
  </acceptance_criteria>
  <done>Widened signer contract threads through both client re-exporters; `.v1` comment updated to `.v2`; raw-key test helper preserved; `pnpm compile` green.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep proof-touching tests/fixtures from v1 to v2</name>
  <files>src/core/__tests__/darkmatter-invite-compat.test.ts, src/core/__tests__/welcome.test.ts, src/core/__tests__/capabilities.test.ts, src/core/__tests__/key-package.test.ts, src/client/group/proposals/__tests__/invite-user.test.ts, src/engine/__tests__/group-engine.test.ts, src/client/group/__tests__/marmot-group.test.ts</files>
  <read_first>
    - .planning/research/PROOF-V2.md (§Tests — the enumerated proof-touching files that bake in v1 vectors)
    - src/core/__tests__/darkmatter-invite-compat.test.ts (the natural interop home; imports `signAccountIdentityProof`, `verifyLeafAccountIdentityProof`)
    - src/core/account-identity-proof.ts (final v2 exports)
  </read_first>
  <action>
    Grep the repository test tree for the old v1 proof domain string (the `.v1`-suffixed marmot account-identity-proof domain), hard-coded v1 signing-digest/preimage expectations, and version-byte-1 assumptions across the listed files (and any other `*.test.ts` the grep surfaces). For each test that breaks under v2, update it to the v2 expectation — most only need to build proofs through the (now v2) `signAccountIdentityProof`/proof-signer helpers and will pass unchanged once the core is v2, but any test that reconstructs the v1 canonical preimage, pins a v1 digest, or asserts version byte 1 must be updated to the kind-450 event id / version byte 2. Do not weaken assertions to make them pass; fix them to the correct v2 value.
    Run the full suite and drive every listed file to green.
  </action>
  <verify>
    <automated>pnpm vitest run</automated>
    <automated>test $(grep -rc "account-identity-proof.v1" src --include='*.test.ts' | grep -v ':0$' | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run` exits 0 (full suite green on Node).
    - No `*.test.ts` under `src/` contains the string `account-identity-proof.v1`.
    - No test pins a v1 signing digest or asserts proof version byte 1 as valid.
    - No assertion was deleted or loosened solely to pass (updates reflect the real v2 value).
  </acceptance_criteria>
  <done>All proof-touching tests updated to v2; full Vitest suite green; no v1 proof strings remain in test tree.</done>
</task>

<task type="auto">
  <name>Task 3: Rust-signed round-trip fixture + never-published assertion</name>
  <files>src/core/__tests__/darkmatter-invite-compat.test.ts</files>
  <read_first>
    - refs/mdk/crates/cgka-engine/src/account_identity_proof.rs (`proof_event()`, `proof_event_id()`, `signature_from_signed_event()`, in-code tests at the `#[cfg(test)] mod tests` block)
    - src/core/account-identity-proof.ts (v2 digest builder + external-signer exports)
    - .planning/phases/01-proof-v2/01-CONTEXT.md (LOCKED "Round-trip proof" — generate fresh; fixture home is Claude's discretion; permanent parity-harness wiring deferred to Phase 4)
    - .planning/research/PROOF-V2.md (§Test vectors — no shared byte fixture exists; must be generated)
  </read_first>
  <action>
    Generate a fresh Rust-signed proof from the MDK reference: using the local `refs/mdk` crate (cargo is available), sign the canonical kind-450 proof event for a FIXED account secret key and fixed mls signature key + ciphersuite (e.g. via a small throwaway Rust test/example that parses a fixed `nostr::Keys` secret and prints `proof_event_id()` hex and the signed event's 64-byte signature hex). Capture the account-identity pubkey (x-only hex), the mls signature key hex, ciphersuite, the kind-450 event id hex, and the 64-byte Schnorr signature hex.
    Pin those captured values as constants in a round-trip test (home Claude's discretion — darkmatter-invite-compat.test.ts is the natural spot per research, or a dedicated proof-interop test file added to files_modified). The test MUST: (a) rebuild the kind-450 event id from the same inputs via `accountIdentityProofSigningDigest` and assert it equals the pinned Rust event id (proves canonical-serialization parity); (b) assert marmot-ts accepts the pinned Rust signature — either via `schnorr.verify` over the rebuilt digest or by constructing a leaf/proof and calling `verifyLeafAccountIdentityProof`; and (c) reproduce the encoded proof payload bytes and assert the pinned account-identity/mls-key/version fields round-trip through decode.
    Add an assertion (grep-based test or a source check) proving no code path publishes/relays the kind-450 event: confirm `ACCOUNT_IDENTITY_PROOF_EVENT_KIND` (450) is never passed to a publish/network path. Record the captured Rust values and the generation command in the plan SUMMARY so CONF-01/Phase 4 can promote this into the permanent parity harness.
    If cargo cannot run in the execution environment, do not fake a signature: surface the blocker in the SUMMARY and pin the Rust in-code test's own captured output instead, documenting exactly how it was produced.
  </action>
  <verify>
    <automated>pnpm vitest run src/core/__tests__/darkmatter-invite-compat.test.ts</automated>
    <automated>test $(grep -rn "ACCOUNT_IDENTITY_PROOF_EVENT_KIND\|[^0-9]450[^0-9]" src/client src/engine --include='*.ts' | grep -iv test | grep -ci "publish\|relay\|network\|emit(") -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/core/__tests__/darkmatter-invite-compat.test.ts` exits 0.
    - The test pins Rust-produced values and asserts marmot-ts reproduces the identical kind-450 event id from the same inputs.
    - The test asserts marmot-ts accepts (verifies) the pinned Rust 64-byte signature.
    - A check confirms no publish/relay/network path is invoked with kind 450 / `ACCOUNT_IDENTITY_PROOF_EVENT_KIND`.
    - The SUMMARY records the captured Rust fixture values and the exact command used to generate them (for Phase 4 promotion).
  </acceptance_criteria>
  <done>A fresh Rust-signed proof is pinned and verified by marmot-ts with matching event id + payload bytes; kind-450 never-published assertion in place; generation recorded for CONF-01.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                                                                                 |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| external Nostr signer → proof | A NIP-07/NIP-46/hardware signer produces the kind-450 signature; the client must extract exactly the 64 bytes bound to the correct event id |
| Rust reference → TS verifier  | A proof produced by the MDK Rust engine crosses into marmot-ts verification; acceptance proves wire interop                                 |

## STRIDE Threat Register

| Threat ID       | Category               | Component                  | Severity | Disposition | Mitigation Plan                                                                                                                                      |
| --------------- | ---------------------- | -------------------------- | -------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-01-05         | Spoofing               | round-trip fixture         | high     | mitigate    | Fixture is generated by the Rust reference (not self-signed by the same TS code path), so acceptance proves real cross-impl interop, not a tautology |
| T-01-06         | Tampering              | signer contract threading  | medium   | mitigate    | Widened contract keeps the raw-key path and adds the event-signing path without `any`-casts; typecheck gate enforces shape integrity                 |
| T-01-07         | Information disclosure | kind-450 event publication | medium   | mitigate    | Explicit never-published assertion over kind 450 / `ACCOUNT_IDENTITY_PROOF_EVENT_KIND` on network/publish paths                                      |
| </threat_model> |

<verification>
- `pnpm compile` exits 0.
- `pnpm vitest run` exits 0 (full suite on Node).
- No `account-identity-proof.v1` string remains in any `src/**/*.test.ts` or the two client files.
- Rust-signed round-trip fixture verifies and reproduces the kind-450 event id + payload bytes.
- No publish/relay path is invoked with the kind-450 proof event.
</verification>

<success_criteria>

- Widened signer contract threads through client with all call sites compiling (PROOF-01 external-signer parity).
- A Rust-signed v2 proof is accepted by marmot-ts and reproduces the same event id + bytes (ROADMAP criterion 2).
- The kind-450 proof event is never published/relayed (ROADMAP criterion 4, publication half).
- Full Vitest suite green on v2 vectors.
  </success_criteria>

<output>
Create `.planning/phases/01-proof-v2/01-02-SUMMARY.md` when done.
</output>
