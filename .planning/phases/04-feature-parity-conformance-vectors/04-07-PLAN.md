---
phase: 04-feature-parity-conformance-vectors
plan: 07
type: execute
wave: 6
depends_on: [04-06]
files_modified:
  - src/__tests__/conformance/smoke.test.ts
  - src/__tests__/conformance/extended.spec.ts
  - vitest.extended.config.ts
  - src/core/__tests__/darkmatter-invite-compat.test.ts
  - src/__tests__/fixtures/proof-v2-rust.json
  - package.json
autonomous: true
requirements: [CONF-01, WIRE-04]
must_haves:
  truths:
    - "Every portable MDK manifest entry is represented by a passing parity case or an explicit capability result; none disappears by omission."
    - "Normal Vitest runs the locked portable smoke set, including restart and both publish-failure vectors."
    - "Offline-catchup pressure runs deterministically in a separate extended suite for Phase 5 consumption."
    - "The Rust-signed proof-v2 fixture verifies permanently through production code."
    - "No UI, terminal disbanding, full runtime matrix, or Rust-specific storage/performance mechanics are introduced."
  artifacts:
    - "src/__tests__/conformance/smoke.test.ts covers the portable manifest smoke set."
    - "src/__tests__/conformance/extended.spec.ts covers deterministic offline pressure."
    - "vitest.extended.config.ts is the sole discovery path for extended.spec.ts; root Vitest continues to include only src/**/*.test.ts."
    - "A repository-local small proof-v2 fixture is permanently verified."
  key_links:
    - "manifest entries -> capability declarations -> runner -> canonical snapshot assertions"
    - "restart/publish fault actions -> production persistence/rollback -> expected outcomes"
  prohibitions:
    - "Do not run or claim the Phase 5 Node/Deno/Bun release matrix."
    - "Do not create COVERAGE.md or any UI artifact."
---

<objective>Execute the complete portable MDK corpus and offline-pressure family through the reusable adapter, retaining proof-v2 cross-implementation evidence.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@.planning/phases/04-feature-parity-conformance-vectors/04-CONTEXT.md
@.planning/phases/04-feature-parity-conformance-vectors/04-UI-SPEC.md
@refs/mdk/crates/cgka-conformance-simulator/vectors/manifest.v1.json
@src/__tests__/conformance/runner.ts
@src/core/__tests__/darkmatter-invite-compat.test.ts</context>
<assumption_delta_decision>Primary noun: convergence evidence. Decision: no-change. Rationale: a versioned stamp is persisted alongside wire bytes but does not generalize or replace record identity; corpus assertions include the stamped/legacy identity invariant from Plan 04-02.</assumption_delta_decision>

## Artifacts this phase produces

- Default `conformance:smoke` coverage through normal Vitest discovery
- Explicit `conformance:extended` command for deterministic offline pressure
- Permanent `proof-v2-rust` fixture and verification case
- No `COVERAGE.md` because API coverage detection returned false

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Run every portable manifest scenario with stable upstream IDs</name>
  <read_first>refs/mdk/crates/cgka-conformance-simulator/vectors/manifest.v1.json, src/__tests__/conformance/runner.ts, src/__tests__/conformance/subject.ts, src/__tests__/conformance/snapshot.ts</read_first>
  <files>src/__tests__/conformance/smoke.test.ts</files>
  <behavior>
    - Every portable entry has one discovered Vitest case named with its manifest ID.
    - Required smoke cases include three-client exchange, both publish failures, restart-delivery-faults, both convergence selection vectors, admin/group data updates, both semantic forks, queue faults, and delayed input.
    - Unsupported capability is asserted explicitly rather than skipped or absent.
  </behavior>
  <action>Generate tests from the manifest and run supported entries through the production subject. Preserve exact upstream identifiers and compare canonical snapshots, per-input dispositions, outputs, gates, and pending work. Include cross-route own-commit recovery and all related portable entries present at the pinned 93ecfbca manifest. Record intentional spec/MDK divergence in the test expectation with a direct source citation.</action>
  <verify><automated>npx --yes pnpm@10.18.3 vitest run src/__tests__/conformance/smoke.test.ts</automated></verify>
  <acceptance_criteria>The test enumerates the manifest, asserts represented-count equality for every portable entry, and all smoke scenarios including restart/publish failures pass.</acceptance_criteria>
  <done>Roadmap criteria 3 and 7 run as automated cross-implementation parity checks.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add deterministic offline-catchup pressure as an extended suite</name>
  <read_first>src/__tests__/conformance/runner.ts, src/__tests__/helpers/mock-network.ts, refs/mdk/crates/cgka-conformance-simulator/vectors/deferred-tick-catchup.v1.json, .planning/phases/04-feature-parity-conformance-vectors/04-RESEARCH.md</read_first>
  <files>src/__tests__/conformance/extended.spec.ts, vitest.extended.config.ts, package.json</files>
  <behavior>
    - Small pressure remains deterministic and proves continuation, retry after refusal, intermediate anchors, restart, and final snapshot/effect agreement.
    - Larger scales are addressable by one explicit extended command and are not part of default Vitest discovery/runtime matrix claims.
  </behavior>
  <action>Represent the offline-catchup-pressure family in `extended.spec.ts`, which deliberately does not match root `vitest.config.ts`'s sole `src/**/*.test.ts` include. Add `vitest.extended.config.ts` with an exact include for `src/__tests__/conformance/extended.spec.ts`, and add a pnpm script that runs Vitest with that config. Cover MDK d94dc134, 7851a840, 6aba6ef9, 6bf697f9, and the correctness portion of 93ecfbca; exclude campaign CLI, exact thresholds, SQLite/accounting, metrics, telemetry, and audit throttling.</action>
  <verify><automated>test "$(npx --yes pnpm@10.18.3 exec vitest list --config vitest.config.ts | grep -c 'extended.spec.ts')" = "0" &amp;&amp; npx --yes pnpm@10.18.3 exec vitest run --config vitest.extended.config.ts</automated></verify>
  <acceptance_criteria>Root Vitest discovery lists zero extended.spec.ts cases; the dedicated config runs deterministic pressure to green after refusal/retry and restart; no terminal dedup consumes refused input.</acceptance_criteria>
  <done>The locked offline family is represented for later Phase 5 matrix execution.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Permanently verify the Rust-signed proof-v2 fixture and phase boundary</name>
  <read_first>src/core/__tests__/darkmatter-invite-compat.test.ts, src/__tests__/helpers/account-proof.ts, refs/marmot/app-components/account-identity-proof-v2.md, .planning/phases/04-feature-parity-conformance-vectors/04-UI-SPEC.md</read_first>
  <files>src/core/__tests__/darkmatter-invite-compat.test.ts, src/__tests__/fixtures/proof-v2-rust.json</files>
  <behavior>
    - Immutable Rust-produced proof bytes verify through production v2 verification and fail after canonical signed-event mutation.
    - No UI files, disbanding behavior, or Phase 5 runtime-matrix artifacts exist in the phase diff.
  </behavior>
  <action>Retain the small runtime-portable Rust-signed proof fixture in the TypeScript repository and make the previously one-off round-trip a permanent named test. Verify version byte 2, canonical kind-450 event-id signature semantics, and mutation failure with real crypto. Preserve the resolved no-UI contract and do not create COVERAGE.md.</action>
  <verify><automated>npx --yes pnpm@10.18.3 vitest run src/core/__tests__/darkmatter-invite-compat.test.ts src/__tests__/conformance/smoke.test.ts &amp;&amp; npx --yes pnpm@10.18.3 compile</automated></verify>
  <acceptance_criteria>Rust proof verifies, mutation fails, smoke remains green, compile passes, and `git diff --name-only` shows no UI/COVERAGE/disband implementation files.</acceptance_criteria>
  <done>Roadmap criterion 4 is permanent and the Phase 4 scope fence is preserved.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| manifest corpus → test execution | Upstream actions drive production protocol state. |
| Rust fixture → proof verifier | External bytes claim cryptographic authorship. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-19 | Tampering | scenario corpus | high | mitigate | Validated manifest loader and canonical cryptographic snapshots. |
| T-04-20 | Spoofing | proof fixture | high | mitigate | Production Schnorr verification plus signed-event mutation negative test. |
| T-04-21 | Denial of Service | offline pressure | medium | mitigate | Deterministic bounded scale in default work and isolated extended command. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | SafeAAD bytes, MDK vectors, confirm-time own evidence | 01-07 | COVERED | End-to-end wire, engine, client, and harness slices. |
| REQ | WIRE-04 | SafeAAD ID/advertisement/empty entry | 01,07 | COVERED | Production byte tracer and phase verification. |
| REQ | CONF-01 | Routing, scenario, proof-v2 cross-impl vectors | 01,06,07 | COVERED | Manifest-driven reusable harness. |
| RESEARCH | — | Own stamp and legacy compatibility | 02 | COVERED | Costly versioned persistence contract. |
| RESEARCH | — | Missing parent, anchors, refusal/retry | 03 | COVERED | All correctness portions through 93ecfbca. |
| RESEARCH | — | Durable wrapper dedup and re-adoption | 04 | COVERED | Restart-safe client/engine outcomes. |
| RESEARCH | — | Deadline, continuation, admission, fairness | 05 | COVERED | Full updated convergence contract except Phase 04.1. |
| RESEARCH | — | Canonical snapshot and safe loader | 06 | COVERED | Fixture-neutral oracle. |
| CONTEXT | — | Every portable vector + offline family | 07 | COVERED | Count-equality gate prevents omission. |
| CONTEXT | — | Direct immutable MDK fixtures and explicit capabilities | 01,06,07 | COVERED | Small local fixture only for proof portability. |
| CONTEXT | — | Versioned stamp, legacy safety, stable wrapper IDs | 02,04 | COVERED | No unsafe reconstruction. |
| CONTEXT | — | Withdrawal/revalidation and scheduling outcomes | 04,05 | COVERED | Persisted and deterministic. |
| CONTEXT | — | No UI; disbanding/runtime matrix/storage mechanics deferred | all | COVERED | Explicit prohibitions and acceptance gate. |

<verification>
Run focused proof, smoke, extended, all Phase 4 new tests, then `npx --yes pnpm@10.18.3 compile`. Full cross-runtime execution is intentionally reserved for Phase 5.
</verification>
<success_criteria>
All nine ROADMAP criteria are evidenced: 1-2 in 04-01, 5 in 04-02, 6 in 04-03, 9 in 04-05, 8 in 04-06, and 3-4/7 in 04-07; durable dedup and re-adoption locked decisions are covered in 04-04.
</success_criteria>
<output>Create `.planning/phases/04-feature-parity-conformance-vectors/04-07-SUMMARY.md` when done.</output>
