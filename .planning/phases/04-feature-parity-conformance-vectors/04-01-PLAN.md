---
phase: 04-feature-parity-conformance-vectors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/components/ids.ts
  - src/core/components/dictionary.ts
  - src/core/components/__tests__/dictionary.test.ts
  - src/core/components/__tests__/nostr-routing.test.ts
autonomous: true
requirements: [WIRE-04, CONF-01]
must_haves:
  truths:
    - "LeafNode and KeyPackage app-component extensions advertise 0x0001 and carry a separate empty SafeAAD 0x0002 entry byte-for-byte with MDK."
    - "SafeAAD remains LeafNode-only advertisement data and is rejected as group-component state."
    - "All three MDK nostr-routing byte fixtures execute against production codecs, including duplicate-relay rejection."
  artifacts:
    - "src/core/components/ids.ts exports SAFE_AAD_COMPONENT_ID."
    - "src/core/components/dictionary.ts emits the reference leaf dictionary."
    - "Core codec tests consume the pinned MDK byte fixtures."
  key_links:
    - "makeLeafAppComponentsExtension -> all production LeafNode/KeyPackage builders"
    - "MDK JSON bytes -> decode/encodeNostrRouting production functions"
  prohibitions:
    - "Do not admit SafeAAD into group app_data_dictionary state."
    - "Do not add runtime-specific production APIs or UI work."
---

<objective>
Deliver the production-quality tracer for Phase 4: generate one real KeyPackage/LeafNode extension path with the exact MDK SafeAAD bytes, then prove the existing routing codec against upstream fixtures.

Purpose: Close WIRE-04 at the byte boundary and establish that immutable MDK fixtures can drive TypeScript production code.
Output: SafeAAD component registration, reference-shaped leaf dictionary, and automated wire fixture tests.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-feature-parity-conformance-vectors/04-CONTEXT.md
@.planning/phases/04-feature-parity-conformance-vectors/04-RESEARCH.md
@.planning/phases/04-feature-parity-conformance-vectors/04-PATTERNS.md
@refs/marmot/app-components/safe-aad.md
@refs/mdk/crates/cgka-conformance-simulator/vectors/byte-fixtures/
</context>

<assumption_delta_decision>
Primary noun: convergence evidence. Decision: no-change. Rationale: a versioned stamp is persisted alongside wire bytes but does not generalize or replace record identity. The invariant test is owned by Plan 04-02 and proves record identity round-trips across stamped and legacy variants.
</assumption_delta_decision>

## Artifacts this phase produces

- `SAFE_AAD_COMPONENT_ID`
- Reference-compatible output from `makeLeafAppComponentsExtension`
- MDK-backed SafeAAD and `nostr-routing-v1-*` regression tests

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Generate one reference-compatible LeafNode/KeyPackage extension path</name>
  <read_first>src/core/components/ids.ts, src/core/components/dictionary.ts, src/core/components/__tests__/dictionary.test.ts, refs/mdk/crates/cgka-conformance-simulator/vectors/byte-fixtures/AGENTS.md</read_first>
  <files>src/core/components/ids.ts, src/core/components/dictionary.ts, src/core/components/__tests__/dictionary.test.ts</files>
  <behavior>
    - Production builder advertises 0x0001 and emits a distinct 0x0002 entry whose data is an empty Uint8Array.
    - Full TLS extension bytes match the pinned MDK leaf dictionary fixture.
    - Group dictionary validation refuses SafeAAD state while continuing to accept it in the leaf advertisement.
  </behavior>
  <action>Add the named SafeAAD ID and change the shared leaf-extension builder so every production LeafNode and KeyPackage path receives the exact sorted reference dictionary. Keep named exports, Uint8Array values, and NodeNext `.js` imports. Exercise real KeyPackage/LeafNode construction and encoding in the test; do not merely assert an array literal or add SafeAAD to the group-state supported set.</action>
  <verify><automated>corepack pnpm vitest run src/core/components/__tests__/dictionary.test.ts</automated></verify>
  <acceptance_criteria>Encoded production output matches MDK bytes, 0x0002 is empty and leaf-only, group-state SafeAAD is rejected, and the focused real-crypto test exits 0.</acceptance_criteria>
  <done>WIRE-04 is demonstrated end-to-end through the production KeyPackage/LeafNode builder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run MDK nostr-routing byte fixtures against production codecs</name>
  <read_first>src/core/components/nostr-routing.ts, src/core/components/__tests__/nostr-routing.test.ts, refs/mdk/crates/cgka-conformance-simulator/vectors/byte-fixtures/schema.v1.json, refs/mdk/crates/cgka-conformance-simulator/vectors/byte-fixtures/nostr-routing-v1-valid-state.v1.json, refs/mdk/crates/cgka-conformance-simulator/vectors/byte-fixtures/nostr-routing-v1-valid-update.v1.json, refs/mdk/crates/cgka-conformance-simulator/vectors/byte-fixtures/nostr-routing-v1-invalid-duplicate-relay.v1.json</read_first>
  <files>src/core/components/__tests__/nostr-routing.test.ts</files>
  <behavior>
    - Valid state and update bytes decode and re-encode exactly.
    - Duplicate relay fixture is rejected by the production decoder.
  </behavior>
  <action>Load the three immutable upstream fixtures in test-only code, preserve their names in Vitest cases, convert fixture hex with existing noble helpers, and invoke production encode/decode functions. Keep test loading compatible with normal Vitest and avoid copying the corpus or adding dependencies.</action>
  <verify><automated>corepack pnpm vitest run src/core/components/__tests__/nostr-routing.test.ts &amp;&amp; corepack pnpm compile</automated></verify>
  <acceptance_criteria>Both valid fixtures round-trip byte-for-byte, the duplicate-relay case fails for the intended validation reason, and compile exits 0.</acceptance_criteria>
  <done>The first CONF-01 byte family is a permanent automated cross-implementation check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| MDK fixture → test loader | Upstream JSON and hex are untrusted test inputs. |
| Leaf extension bytes → peer | Wire bytes become authenticated MLS capabilities. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Tampering | leaf dictionary | high | mitigate | Pin complete encoded bytes and group-state rejection in production-path tests. |
| T-04-02 | Denial of Service | routing decoder | medium | mitigate | Preserve duplicate/cardinality validation and assert malformed fixture rejection. |
| T-04-03 | Spoofing | fixture identity | low | accept | Fixtures are test-only and pinned by the git submodule commit. |
</threat_model>

<verification>
Run `corepack pnpm vitest run src/core/components/__tests__/dictionary.test.ts src/core/components/__tests__/nostr-routing.test.ts` and `corepack pnpm compile`.
</verification>

<success_criteria>
Roadmap criteria 1 and 2 are true without UI additions, new packages, Buffer, or group-state SafeAAD support.
</success_criteria>

<output>Create `.planning/phases/04-feature-parity-conformance-vectors/04-01-SUMMARY.md` when done.</output>
